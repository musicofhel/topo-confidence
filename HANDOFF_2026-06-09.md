# Handoff — topo-confidence decision-readout (2026-06-09)

## Accomplished this session
- Full diagnostic readout answering 12 decision questions about topo-confidence.
- **RAN the cross-architecture DoM AUROC** (it had never been computed). Wrote
  `pathway11_h100/exp1_cross_model/compute_dom_auroc.py`, result in `dom_auroc_results.json`.
  **F-2 GENERALIZES:** Phi-3-mini **0.8089** (2/3-depth L21) / peak 0.8148 (L17);
  Llama-3.2-1B **0.7458** (L11) / peak 0.7577 (L16); Qwen-1.5B ref 0.7731 (L19).
  → Satisfies F-2's long-pending "cross-architecture prefill test" control.
- `validate_claims.py` internal tier: **202 PASS / 0 FAIL** (clean).
  Regen tier (127 NPZ-recompute checks) was **still running detached** (`python -u validate_claims.py`,
  was PID 18265) → final count lands in `/tmp/vc_full.txt` (prints summary only when all 127 finish).
- **RunPod key swapped** to user's own $20-capped key in `~/.runpod/config.toml`. BOTH fields fixed:
  top-level `apikey` (via `runpodctl config`) AND `[default] api_key` (via sed — runpodctl only sets
  the former; Python `runpod` lib reads the latter). Old (friend's) key fully removed; `runpodctl pod list`
  authenticates clean/empty. **User will BURN this key after the session.**
- Saved memory `topo-confidence-cross-arch-f2.md` + MEMORY.md index pointer.

## Decisions
- **Finish line = ship a TOOL (model improvement), NOT a paper.** Bare prefill-DoM result is SCOOPED
  (Zhu 2509.12886, 2509.10625, 2602.09924 ICLR-26 workshop, 2603.20895 LLM-Router). Unscooped novelty
  = cov-spectrum probe N1 (0.7928) + PC1≈DoM unsupervised identifiability N3 (cos 0.92).
- The program hinges on ONE ~$2 experiment: **is F-2 causal (lever) or correlational (diagnostic)?**

## Current state
- Branch `max-depth-retriage-2026-04-28` is **22+ commits AHEAD of main, 0 behind** → NOT merged, main stale.
- Uncommitted: `tools/streamdeck-status-server.py` + 3 triage briefs dated 2026-06-09.
- Neo4j research-graph live + seeded (14 active findings), container `topo-research-graph` running.
- RunPod ready on user's own key (H100 SXM), no pods running yet.

## Blockers / risks
- **TOP RISK — NO BACKUP:** `pathway11_h100/` (43GB, incl 42GB 7B cache) + `pathway8_layerwise/` (37GB)
  have no off-disk backup (no rclone/restic installed), NPZ caches gitignored, work on unmerged branch.
  Disk death = unrecoverable. **FIX BEFORE BURNING GPU.**
- **FE448 length confound:** residualized AUROC 0.6647 (in-sample upper bound), length-alone 0.7986 >
  raw DoM in-sample. Footnote not fatal but reviewer landmine; demoted F-2 STRONG→MODERATE.
  (F-14: length is Pareto-dominant for compute allocation — length may be the better tool signal.)

## NEXT STEPS (immediate — do both in parallel)
1. **BACK UP** `pathway11_h100/` (43GB) + `pathway8_layerwise/` (37GB) off-disk. No rclone/restic present —
   install one or rsync to external/cloud. Also push branch to remote (currently local-only, unmerged).
2. **LAUNCH FE269 on RunPod H100 SXM** (~$2, ~1hr): directional ablation causal test for F-2.
   Ablate r̂ = unit `prefill_L19_DoM` at every layer+position on Qwen-2.5-1.5B-Instruct, generate MATH-500
   K=1, measure accuracy delta from 48.6% baseline; mirror with activation ADDITION at L19 on the
   K=1-incorrect subset (do any flip to correct?); capability check MMLU+GSM8K. **THE lever-vs-diagnostic
   decider.** Inputs exist but it needs FRESH forward passes (cannot run on cached NPZs alone).
   $20 cap leaves room for FE283 / FE19 afterward.

## NEXT-NEXT STEPS (after FE269 resolves)
- **If CAUSAL → steering path:** H-1 per-position DoM steering (activation addition as model-improvement
  lever; ~$200 / 3 H100-days for the full sweep).
- **If DIAGNOSTIC → routing path:** ship FE19 verification-routing / selective prediction
  (71.6% answered-acc @ 50% coverage vs 48.6% unconditional), now backed by cross-arch generalization.
- **Either way:** wire the new cross-arch result into canonical docs — `research-graph/update_status.py`
  for the cross-arch FE, mark F-2 "cross-architecture prefill test" control SATISFIED in FINDINGS.md,
  add `validate_claims.py` entries (Phi-3 0.8089, Llama 0.7458), regenerate NEXT_EXPERIMENTS.md.
- Commit the uncommitted files; decide whether to merge branch → main.
- Cheap CPU follow-ups queued in `PLAN_cheap_wins.md` (~25 FE, sub-2h each, refutation-first vs F-2).

## Notes
- Compute policy: CLAUDE.md says "local only" but user's global memory says always RunPod H100 for GPU/long
  tasks — user explicitly chose RunPod H100 for the causal test. **Honor RunPod.**
- Query research-graph BEFORE discussing any paper. Goal is model improvement, not papers. Keep
  headline-figure resolution.
- Headline numbers (1024-tok): 1.5B acc 48.6%, prefill L19 DoM 0.7731, cov-spectrum 0.7928,
  selective-pred 71.6% @ 50% coverage.
