# HANDOFF — SPEC v8 "Raise the Ceiling" COMPLETE (2026-06-13)
Project: ~/topo-confidence/pathway11_h100/generalization_edge

## Status: DONE. One user action pending (rotate RunPod key).

## Accomplished (all committed in 560f433 on branch max-depth-retriage-2026-04-28)
SPEC v8 executed end-to-end (EXP-90/91, ~6 H100 hrs, pod removed). Verdict:
**swap the base/target, don't curate data.**
- **H-N CONFIRMED**: escalation target = Qwen2.5-Math-7B-Instruct lifts free-gate
  cascade +4.20pp (0.648->0.690, p=0.0025), rescues 41/121, oracle 0.758->0.800.
- **H-O INCONCLUSIVE**: Mathstral-7B 15/121 disjoint (< 25 confirm bar).
- **H-P CONFIRMED**: off-the-shelf Qwen2.5-Math-1.5B-Instruct 0.740 standalone at
  ~1/4 budget (+9.2pp over budget-matched cascade) -- the real product lever.
  REFUTED for R1-Distill-1.5B (greedy 0.634 / T=0.6 0.678 @ cost 2742; length intrinsic).
- **H-Q CONFIRMED**: free gate generalizes to new bases (OOF AUROC 0.863/0.950/0.936).
- **H-R REFUTED** (centerpiece): zero-label confidence-curated distillation does NOT
  beat unfiltered at matched N=3065 (gate 0.494 < unfiltered 0.500 < skyline 0.504 <
  length-control 0.502; gate = step-length proxy at selection). MATH-only SFT
  catastrophically forgets BBH (all arms < base 0.267). Curation is a free-rider.

## Integration done
- Claims 245->264 internal PASS (+19 edge-v8-*, 0 regressions); verify_anchors
  step5_v8 ALL PASS (rescue triple 257/136/121).
- confgate: README v8 section + pinned_meta v8_frontier anchors (new target,
  base-swap, curation LIMIT); 8/8 pytest green.
- Brief research-graph/briefs/result-2026-06-13-P11-FE-CEILING.md promoted ->
  graph COMPLETED (executes P11-FE5, answers P11-FE1214).
- Commit 560f433: 9 files (SPEC_v8.md, verify_anchors.py, validate_claims.py,
  confgate README + pinned_meta, brief, NEXT_EXPERIMENTS/EXPERIMENT_LOG/STATE).
- Memory: topo-confidence-spec-v8.md + MEMORY.md index line.

## Pending (USER action only)
- **Rotate the RunPod key** `<redacted-runpod-key>`
  ($30-capped, rotation was deferred to end of all GPU work -- now is the time).

## Convention / notes
- Harness v8_*.py + results/v8_*.json + pod_results/ stay LOCAL (not committed),
  per SPEC_v8 Phase-3 convention. They are present on this machine.
- This branch carries prior-session backlog uncommitted (SPEC_v4/v5/v6.md, v7
  harness .py, premises.py edits, autopilot triage briefs). The v8 commit was
  kept scoped to v8 only; that backlog is untouched and awaiting a decision.
- Standing constraints: NO local GPU ever (local = CPU analysis only); H100 SXM only.
- OPEN user decision carried from v7: confgate public release (GitHub/PyPI).
