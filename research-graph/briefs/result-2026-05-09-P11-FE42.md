I found the result JSON at `pathway11_h100/results/fe421_regularized_concat.json`. Two issues to flag:

1. **This experiment is already logged as EXP-71** (2026-05-07) in EXPERIMENT_LOG.md — not EXP-79.
2. **The metadata description (per-head SMITIN probes) doesn't match the actual experiment** (ridge-regularized logistic regression on [prefill, final] concat). The result JSON is for FE421 (regularized concat), not per-head probes.

I'll write the brief based on the actual result JSON, using the FE42 ID as requested and EXP-79 as assigned, but noting the EXP-71 precedent. Here's the brief:

---

# Result brief — P11-FE42 (Ridge-LR on [prefill, final] concat vs single-source)

Ridge-regularized logistic regression on concatenated L19 prefill + final-token
activations (3072-d) reveals the final token carries substantially more
correctness signal than the prefill when properly probed: ridge-LR final-only
AUROC 0.8493 vs prefill-only 0.7844, with concat barely exceeding final-only
at 0.8509.

**Note:** This experiment was previously logged as **EXP-71** (2026-05-07). The
FE42 metadata description (per-head SMITIN probes) does not match the actual
experiment (ridge-regularized concat); this brief documents what was actually
run per `recompute_fe421.py` and the result JSON.

## FE

```yaml
fe_id: P11-FE42
status: COMPLETED
outcome: "Ridge-LR on concat [prefill, final] (3072-d) achieves OOF AUROC 0.8509, but final-token-only (1536-d) nearly matches at 0.8493 — meaning prefill adds only +0.0016. Ridge-LR prefill-only = 0.7844, confirming EXP-58's directional ceiling. DoM concat = 0.7574 (below DoM prefill-only 0.7699), confirming EXP-51's finding that naive mean-diff concat dilutes signal. The final token at L19 carries 12.8pp more signal under regularized probing than DoM reveals (0.8493 vs 0.7210)."
result_json: pathway11_h100/results/fe421_regularized_concat.json
result_json_keys:
  - experiment
  - description
  - n
  - n_correct
  - ridge_lr
  - dom_baselines
  - references
  - cross_check
  - interpretation
  - fold_structure
  - meta
regen_cmd: "python pathway11_h100/regularized_concat/recompute_fe421.py"
```

## Result JSON verification

`pathway11_h100/results/fe421_regularized_concat.json` exists; all declared
keys are top-level. Regen reuses `m15b_prefill.npz` (prefill + final_tok
arrays), deterministic seed=9999, L2-regularized LogisticRegressionCV with
Cs=[0.001, 0.01, 0.1, 1.0, 10.0], 3-fold inner CV for C selection,
5-fold outer OOF evaluation. Cross-check flag `dom_concat_matches_fe254`
= false (DoM concat here 0.7574 vs FE254's 0.6946 — discrepancy likely
due to different DoM implementation details between EXP-51 and this run).

## EXPERIMENT_LOG entry

## EXP-79: P11-FE42 — Ridge-LR on [prefill, final] concat vs single-source
**Date:** 2026-05-09
**Status:** COMPLETE
**Motivated by:** EXP-51/FE254 showed naive DoM concat (0.6946) performs
*worse* than prefill-only DoM (0.7731), but EXP-51 noted this was
probe-family-specific — ridge-LR on concatenated features was an explicit
follow-up to close the FE254 caveat. F-3's cos(prefill_DoM, final_DoM) =
0.046 orthogonality implies the two representations should carry
complementary signal if probed correctly.
**Hypothesis:** Ridge-regularized logistic regression on the concatenated
(500, 3072) [prefill, final] feature space exceeds the F-2 prefill-only
DoM AUROC of 0.7731, because the final token carries complementary
correctness signal that naïve mean-diff projection fails to extract.
**What we actually tested:** L2-regularized LogisticRegressionCV on three
feature sets — concat (3072-d), prefill-only (1536-d), final-only
(1536-d) — plus DoM baselines on each, all on n=500 Qwen-2.5-1.5B
MATH-500 L19 activations with 5-fold stratified OOF (seed=9999). Inner
3-fold CV selects best C from {0.001, 0.01, 0.1, 1.0, 10.0}.
**Key result:** Ridge-LR concat **0.8509** (best C=0.01 all folds);
ridge-LR final-only **0.8493** (C=0.01); ridge-LR prefill-only
**0.7844** (C=0.001). DoM concat 0.7574; DoM prefill 0.7699; DoM final
0.7210. The final token alone, properly regularized, reaches within
+0.0016 of concat — the prefill adds almost nothing to a regularized
final-token probe.
**Verdict:** CONFIRMED — final token carries more correctness signal
than prefill when regularized; naive DoM massively underestimates
final-token information content.
**Changed our understanding of:** F-2's "prefill is special" was
probe-specific. Ridge-LR final-only (0.8493) >> ridge-LR prefill-only
(0.7844) >> DoM prefill (0.7731). The L19 representation space has a
correctness-prediction ceiling of ~0.85, driven primarily by the final
token, not the prefill mean.
**Files:** `pathway11_h100/regularized_concat/recompute_fe421.py` (regen),
`pathway11_h100/results/fe421_regularized_concat.json` (output).
**Depends on:** F-2 (DoM baseline), F-3 (orthogonality motivation),
EXP-51/FE254 (naive concat baseline 0.6946). Previously logged as
EXP-71 (2026-05-07).
**Enables:** Final-token probes as primary correctness signal; re-framing
F-2 from "prefill direction is the probe" to "prefill direction is a
convenient but lossy projection of a richer L19 signal."

## STATE.md last-experiment update

EXP-79 (P11-FE42 Ridge-LR on [prefill, final] concat). Ridge-LR concat
AUROC 0.8509; final-only 0.8493 nearly matches, prefill-only 0.7844
confirms directional ceiling. The final token at L19 carries ~12.8pp
more correctness signal than DoM reveals (0.8493 vs 0.7210).

## FINDINGS.md updates

### F-2: A single L19 prefill direction predicts correctness on Qwen-2.5-1.5B at AUROC 0.7731

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E79]
counterargument: "Ridge-LR on the final token alone (0.8493) exceeds ridge-LR on prefill (0.7844) and the directional ceiling (0.7856). The prefill direction is a convenient but lossy projection — the final token at L19 carries substantially more correctness signal when regularized. F-2's prefill-centric framing understates the L19 representation's true ceiling (~0.85)."
overturned_by: "Final-token-only ridge-LR AUROC ≤ 0.78 on nested 5×5 CV (fold-noise inflated) → final-token advantage evaporates; or final-token signal shown to be purely length/position-driven rather than correctness-driven."
```

**Updated evidence line (append):**
EXP-79 / FE421 (`pathway11_h100/results/fe421_regularized_concat.json`):
ridge-LR concat(3072) = 0.8509, final-only(1536) = 0.8493,
prefill-only(1536) = 0.7844. Confirms directional ceiling (0.7844 ≈
EXP-58's 0.7847); reveals final token carries 12.8pp more signal under
ridge-LR than DoM extracts (0.8493 vs 0.7210).

**Updated controls (append):**
- **Ridge-LR concat/decomposition (EXP-79, FE421)**: final-token-only
  ridge-LR 0.8493 ≈ concat 0.8509 — prefill adds only +0.0016. Ridge-LR
  prefill-only 0.7844 matches EXP-58's 0.7847 ceiling. DoM concat
  0.7574 < DoM prefill 0.7699, confirming EXP-51's naive-concat-dilutes
  finding.

### F-9: CoE-60 trajectory features are redundant with single-layer L19 DoM

```yaml
strength: MODERATE
status: ACTIVE
add_evidence: [P11-E79]
counterargument: "Ridge-LR final-only at L19 (AUROC 0.8493) exceeds CoE-60's within-domain ~0.811, raising the single-layer baseline that trajectory features must beat. CoE redundancy extends beyond DoM to regularized probes."
overturned_by: "CoE ≥ 0.86 at 1024-tok within-domain (H-6), exceeding the new ridge-LR final-only baseline."
```

**Updated evidence line (append):**
EXP-79 / FE421: ridge-LR final-only at L19 = 0.8493, exceeding
CoE-60's within-domain ~0.811 from a *single layer*. This raises the
baseline against which trajectory features must demonstrate value.

## New claims

- fe421-ridge-concat-auroc = 0.8509 (FE421 ridge-LR OOF AUROC on [prefill, final] concat, 3072-d)
- fe421-ridge-prefill-auroc = 0.7844 (FE421 ridge-LR OOF AUROC on prefill-only, 1536-d)
- fe421-ridge-final-auroc = 0.8493 (FE421 ridge-LR OOF AUROC on final-only, 1536-d)
- fe421-dom-concat = 0.7574 (FE421 DoM OOF AUROC on concat, 3072-d)
- fe421-dom-prefill = 0.7699 (FE421 DoM OOF AUROC on prefill-only, 1536-d)
- fe421-dom-final = 0.7210 (FE421 DoM OOF AUROC on final-only, 1536-d)

## Sources

- EXP-51 / FE254: naive DoM concat baseline (AUROC 0.6946) and the caveat that ridge-LR should be tested
- EXP-58 / FE882: full 1536-d L2-reg ceiling (0.7847) confirming directional ceiling saturates at 2-feat
- EXP-55 / FE291: PCA-PC1 ≈ DoM decomposition
- pathway11_h100/results/fe421_regularized_concat.json (result JSON)
- pathway11_h100/regularized_concat/recompute_fe421.py (regen script)