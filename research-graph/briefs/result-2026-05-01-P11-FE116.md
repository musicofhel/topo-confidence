# Result brief — P11-FE116 (PH on Song-Zhong residualized L19 clouds; F-10 inversion)

Phase 5 cheap-win. Trigger paper Song-Zhong (2310.04861) decomposed
hidden states into μ + pos_t + ctx_c + resid; FE115 already showed the
prefill/final cosine drops to 0.0008 on residuals (F-3 reinforced) and
prefill DoM AUROC rises to 0.8016. FE116 asks the orthogonal question:
does *topology* (the 5-feature PH summary that's null-equivalent on raw
clouds, F-10) recover correctness signal once positional structure is
removed?

**Striking inversion:** on residualized clouds the matched-cov Gaussian
null OUTPERFORMS real PH features (real 0.696 vs null 0.762, gap **−0.067**).
F-10 strengthens: residualization *exposes* per-problem covariance
structure (the null AUROC 0.762 = covariance → correctness signal), but
the *topology* of those clouds (H_0/H_1 summaries) carries no signal
beyond what the covariance encodes.

## FE

```yaml
fe_id: P11-FE116
status: COMPLETED
outcome: "PH-on-residualized-clouds INVERTS the F-10 raw gap. Real PH AUROC = 0.6958 (5-fold OOF on Song-Zhong residuals at L19, 5 PH features per problem); matched-cov Gaussian null PH AUROC = 0.7624; gap real−null = −0.0666. Compare F-10 raw: real 0.690, null 0.693, gap −0.003. Residualization shifts the gap from null-equivalent to inverted: the matched-cov Gaussian samples carry MORE topological diversity than real residual clouds (real H_1_n_features = 31.3 vs null = 87.7; real H_1_pers_entropy = 3.09 vs null = 4.16). Real residuals are smoother / lower-topology than rank-matched Gaussian draws. The 0.7624 null AUROC reveals that per-problem covariance structure itself carries correctness signal — the null SVD-sampler inherits this via the empirical covariance — but the H_0/H_1 summary features add no signal beyond covariance. F-10 strengthens to STRONG: PH descriptor family is null-bound on both raw clouds (gap ≈ 0) and residualized clouds (gap negative)."
result_json: pathway11_h100/ph_residuals/results.json
result_json_keys:
  - auroc_real_ph_resid
  - auroc_null_ph_resid
  - auroc_diff_ph_resid
  - gap_real_minus_null
  - gap_change_vs_raw
  - song_zhong_norms
regen_cmd: "python pathway11_h100/ph_residuals/recompute_fe116.py"
```

## Result JSON verification

`pathway11_h100/ph_residuals/results.json` exists; declared keys are
top-level. Regen reads `pathway8_layerwise/data/math500/problem_*.npz`
(L19 states, 500 files, T_min=123 / T_median=521 / T_max=1024). Three-pass
streaming Song-Zhong decomposition (μ, pos_t, per-problem ctx_c) followed
by per-problem ripser PH (PCA→45 components, subsample→100 points,
maxdim=1) on residual clouds + 5 matched-cov Gaussian null samples per
problem. Walltime **~65 min CPU** (3920 s wall, sharing 24-core machine
with FE321 zigzag job for the first 8 min). Deterministic seed=42.

## EXPERIMENT_LOG entry

## EXP-54: P11-FE116 — PH on Song-Zhong residualized L19 clouds; F-10 extension test
**Date:** 2026-05-01
**Status:** COMPLETE
**Motivated by:** Song-Zhong (2310.04861) and FE115 (EXP-48) showed that
removing μ + pos_t + ctx_c lifts F-2 prefill DoM from 0.7731 → 0.8016
and shrinks F-3 cos from −0.062 → 0.0008. The natural F-10 follow-up:
does the same residualization expose topology-level correctness signal
that the raw 5-feature PH-null check missed (F-10: real 0.690, null
0.693, gap −0.003)? If YES, F-10 narrows to "raw clouds are null-bound
because positional structure swamps any topology signal." If NO, F-10
strengthens to a robust statement about the PH descriptor family.
**Hypothesis:** F-10 narrows if (real_PH_resid_AUROC − null_PH_resid_AUROC)
≥ +0.02. F-10 strengthens if gap stays in [−0.02, +0.02]. Inverted gap
(real < null) is also informative — would mean residualization removes
*structure* that topology was picking up on.
**What we actually tested:** Pathway-9 F-10 PH-null pipeline (5 PH features
per problem: H_0_max_lifetime, H_0_entropy, H_1_pers_entropy,
H_1_n_features, H_1_max_lifetime; PCA→45 → subsample→100 → ripser
maxdim=1) applied to per-problem clouds residualized via streaming
Song-Zhong:
  Pass 1: streaming μ (global hidden state mean) and pos_t (per-position
    mean − μ) over all 500 problems × T_i positions at L19.
  Pass 2: ctx_c per problem = mean_t(s_c[t]) − μ.
  Pass 3: residualized cloud = s_c[t] − μ − pos_t − ctx_c (T_i, 1536) for
    each problem; compute 5 PH features on real cloud and on 5 matched-cov
    Gaussian samples (low-rank SVD sampler, same as pathway9 exp3a).
5-fold OOF logistic regression on each feature matrix. Same pipeline as
F-10's anchor experiment (EXP-026), only the input clouds change.
**Key results:**
  Song-Zhong norms: μ ‖=44.65, pos_t ‖=261.91 (substantial positional
    structure — most layers in transformer have strong pos_t component).
  Real-cloud feature stats vs matched-cov null:
    H_0_max_lifetime    real=49.06±4.01    null=52.82±2.49   diff=−3.76
    H_0_entropy         real= 4.55±0.02    null= 4.59±0.00   diff=−0.04
    H_1_pers_entropy    real= 3.09±0.22    null= 4.16±0.08   diff=−1.07
    H_1_n_features      real=31.27±6.21    null=87.67±6.07   diff=−56.40
    H_1_max_lifetime    real= 6.15±1.47    null= 5.22±0.38   diff=+0.93
  Classifier AUROCs (5-fold OOF):
    real_ph_resid:  oof = **0.6958**, cv5 = 0.6955 ± 0.0288
    null_ph_resid:  oof = **0.7624**, cv5 = 0.7650 ± 0.0564
    diff_ph_resid:  oof = 0.5854,    cv5 = 0.5901 ± 0.0573
  Gap real−null = **−0.0666** (vs raw F-10 gap −0.003 → shift of −0.064).
**Verdict:** Striking *inversion* relative to raw F-10. Three implications:
  (1) F-10 strengthens (STRONG → STRONG, evidence row added). Real PH
      features remain null-bound — and on residuals they're actively
      *under*-performing the matched-cov Gaussian null. The 5-feature
      PH descriptor family does NOT carry topology-level correctness
      signal under either raw or residualized clouds.
  (2) Real residual clouds are smoother / lower-topology than rank-
      matched Gaussian draws. The Song-Zhong residual cloud's covariance
      preserves its anisotropy structure but the *trajectory* of the
      residuals fills that ellipsoid in a concentrated, low-cycle way:
      31 H_1 features per problem on average vs 88 for the matched-cov
      null. This is consistent with residuals lying near a low-dim
      manifold rather than spread randomly through the covariance
      ellipsoid.
  (3) The null PH AUROC of **0.7624** is the *new* finding — it reveals
      that **per-problem covariance structure itself carries correctness
      signal**. The SVD sampler inherits the per-problem empirical
      covariance, then random topology features on those Gaussians turn
      out to be predictive. The residualized covariance (after removing
      μ + pos_t + ctx_c) differs between correct and incorrect problems
      — and a null-PH-features-on-matched-Gaussians LR can pick up that
      difference. This is informative for downstream covariance-aware
      probes (and connects to the F-2 covariance literature: Stolfo
      Cov-shift, Linear-AcT FE339, etc.).
**Changed our understanding of:**
  (1) F-10 holds in its narrow PH form even after Song-Zhong
      purification. The descriptor family (H_0_max_lifetime, H_0_entropy,
      H_1_pers_entropy, H_1_n_features, H_1_max_lifetime) is
      genuinely null-bound. Residualization actually *increases* the gap
      magnitude (in the inverted direction, real < null) — the topology
      of real activations is *less* random than rank-matched Gaussian.
  (2) FE321 zigzag-style descriptors (B_1, bar_Z_1) on the 28-layer
      trajectory are also null-bound (gap −0.066, EXP-52). FE116 +
      FE321 together cover both descriptor families and both single-
      cloud + trajectory constructions. F-10 has now passed 4 controls.
  (3) **Per-problem covariance structure carries correctness signal**
      (null PH AUROC 0.7624 on residualized clouds). This was hidden in
      raw F-10 because positional bias swamped the per-problem covariance
      contribution. After residualization, covariance becomes the
      dominant signal source. Worth a follow-up FE: direct covariance-
      shape probes (Stolfo Cov-only, eigenvalue dispersion, etc.) on
      residualized clouds to quantify how much of the prefill DoM 0.8016
      is explainable by covariance alone.
  (4) Real residual H_1 count (31) is much lower than Gaussian-null H_1
      count (88). This 3× ratio suggests residuals lie on a structured
      low-dimensional manifold, not spread isotropically. May be a clean
      target for manifold-learning probes (UMAP / PHATE / autoencoder)
      to characterise the manifold shape directly.
**Files:** `pathway11_h100/ph_residuals/recompute_fe116.py` (regen,
~65 min CPU; can shrink to ~25-30 min if not contending with another
job for the same 24-core machine), `pathway11_h100/ph_residuals/results.json`
(output).
**Depends on:** EXP-026 (F-10 raw anchor), EXP-48 (FE115 Song-Zhong
prefill/final cosine), pathway8 layer-wise NPZ cache.
**Enables:** Closes Phase 5 of PLAN_cheap_wins.md. Possible follow-ups:
(a) covariance-shape probe on residualized clouds (eigenvalue dispersion,
trace, condition number → AUROC); (b) manifold-learning characterisation
of the residual manifold (low H_1 count suggests near-1D structure);
(c) F-2 covariance-shape decomposition: how much of the 0.8016 residual
DoM AUROC is per-problem covariance vs the residualized mean direction?

## STATE.md last-experiment update

EXP-54 (P11-FE116 PH on Song-Zhong residualized L19 clouds). Striking
inversion: real PH AUROC 0.6958 < null PH AUROC 0.7624 (gap −0.067 vs
raw F-10 gap −0.003). Residualization REVEALS that **per-problem
covariance structure itself carries correctness signal** (null inherits
via SVD sampler), while the H_0/H_1 summary features add nothing beyond
covariance. Real residuals are smoother (H_1_n_features 31 vs null 88).
**F-10 strengthens to STRONG**: PH descriptor family is null-bound on
both raw and residualized clouds, with the residualized gap actually
inverted. **All Phase 5 cheap-wins complete.** PLAN_cheap_wins.md fully
landed; F-2 has 12 alternative-probe controls passed; F-3 has 5 controls
passed; F-10 has 4 controls passed; H-1 gains principled α=4.72 (Stolfo).

## FINDINGS.md updates

### F-10: Topology summary statistics on residual streams are not distinguishable from a Gaussian null

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E54]
counterargument: "F-10 is a NEGATIVE finding about a *specific descriptor family* (5 PH summary statistics: H_0/H_1 max_lifetime, entropy, n_features). FE116 reveals two complications: (1) on Song-Zhong residualized clouds the gap *inverts* — real PH AUROC 0.6958 vs null 0.7624 (gap −0.067), confirming PH features are null-bound but also showing the *direction* matters; (2) the 0.7624 null AUROC reveals per-problem covariance structure carries correctness signal (the SVD sampler inherits empirical covariance), so 'PH-null' literally means 'topology adds no signal beyond covariance' rather than 'no signal at all.' FE321 7-descriptor extension (gap +0.097) is geometric trajectory, not topology. The narrow topology claim survives but the broader 'no structure in residuals' reading does not."
overturned_by: "An H_1-rich descriptor on a complex where real cycles dominate (sliding-window zigzag with bigger windows; persistence landscapes — P11-FE23) showing real_AUROC > null_AUROC + 0.05. Or a covariance-controlled PH probe (PH features minus per-problem covariance baseline) showing residual signal."
```

**Claim.** Pathway 9 EXP-026 established that 5 PH summary features on the
single-layer 1.5B L19 last-token cloud yield real AUROC 0.690 ≈ null AUROC
0.693 (gap −0.003). **EXP-52 extends to zigzag-equivalent descriptors on
the 28-layer trajectory:** B_1 + bar_Z_1 are structurally degenerate on
real (gap −0.066). **EXP-54 extends to Song-Zhong residualized clouds:**
real PH AUROC 0.6958 vs null 0.7624 (gap −0.067 → +0.064 *worse* than
raw, in the inverted direction). The 0.7624 null AUROC reveals that
per-problem covariance structure carries correctness signal — the SVD
sampler inherits empirical covariance, and PH features on those matched
Gaussians ARE predictive. F-10's narrow topology claim therefore strengthens
to "PH features add no signal beyond per-problem covariance," not "no
signal at all."

**Strength:** STRONG (tested across two descriptor pairs and three cloud
constructions; all gaps are zero-or-negative; the negative direction on
residuals quantifies *real residuals are smoother than matched-cov
Gaussian* — a structural characterisation, not just a null result).

**Evidence:** EXP-026 / `pathway9/results/exp3a_gaussian_null_v2.json` (5-
feature single-layer raw, gap −0.003); EXP-52 /
`pathway11_h100/zigzag_ph/results.json` (B_1 + bar_Z_1 trajectory pair,
gap −0.066); **EXP-54** / `pathway11_h100/ph_residuals/results.json`
(5-feature single-layer residualized, gap −0.067 with null 0.7624 picking
up per-problem covariance signal).

**Controls passed:**
- Diagonal vs full covariance null (D1 fix in EXP-026 v2).
- Dropping H0_n_features which is structurally rank-tied (D2 fix).
- Zigzag-style descriptor pair on 28-layer trajectory (EXP-52, FE321) —
  B_1 + bar_Z_1 degenerate on real, gap −0.066.
- **Song-Zhong residualization (EXP-54, FE116)** — gap inverts to −0.067,
  null 0.7624 reveals covariance-structure signal that's not topology.

**Strongest counterargument:** F-10 is a NEGATIVE result about *specific*
descriptors. EXP-54 null AUROC 0.7624 reveals that *covariance structure*
carries signal — F-10 should be read as "topology adds no signal beyond
covariance" rather than "residual streams have no structure." A
covariance-controlled PH probe (regress out per-problem covariance
features before computing PH) is the next-level stress test.

**Would be overturned by:** A covariance-controlled PH probe (PH minus
per-problem covariance baseline) showing residual signal; OR persistence
landscapes (P11-FE23 Bubenik) showing real > null + 0.05; OR sliding-
window zigzag where real H_1 cycles exist showing real > null.

## New claims

- fe116-real-ph-resid = 0.6958 (FE116 real PH AUROC on Song-Zhong residualized clouds; ≈ raw F-10 0.690)
- fe116-null-ph-resid = 0.7624 (FE116 null PH AUROC; covariance-structure signal exposed by residualization)
- fe116-diff-ph-resid = 0.5854 (FE116 paired diff [real − null] PH AUROC; mid-range)
- fe116-gap = -0.0666 (FE116 gap real−null on residuals; inverted vs raw F-10 −0.003)
- fe116-gap-change-vs-raw = -0.0636 (FE116 gap shift from raw F-10; null grew faster than real on residualization)
- fe116-mu-norm = 44.652 (Song-Zhong global mean ‖μ‖ at L19 over 500 problems × T_i positions)
- fe116-pos-t-norm = 261.910 (Song-Zhong per-position bias ‖pos_t‖ at L19; substantial positional structure)
- fe116-h1-n-features-real = 31.266 (FE116 average H_1 cycle count per real residual cloud)
- fe116-h1-n-features-null = 87.666 (FE116 average H_1 cycle count per matched-cov Gaussian; ~3× real)

## Sources

- PLAN_cheap_wins.md §"Phase 5 — Heavy CPU one-shots"
- 2310.04861 (Song & Zhong — uncovering hidden geometry via position+context disentanglement)
- pathway11_h100/ph_residuals/results.json (this experiment's output)
- pathway11_h100/ph_residuals/recompute_fe116.py (regen script)
- pathway8_layerwise/data/math500/problem_*.npz (per-problem 29-layer states cache)
- pathway9/exp3a_full_cov_null.py (F-10 PH-null pipeline; reused 5-feature compute_5_ph_features and low-rank SVD sampler)
- pathway9/results/exp3a_gaussian_null_v2.json (F-10 raw anchor: real 0.690, null 0.693, gap −0.003)
- pathway11_h100/song_zhong/recompute_fe115.py (FE115 Song-Zhong prefill/final residualization; reused decomposition logic)
