# Triage brief — 2605.05873 — CITE: Anytime-Valid Statistical Inference in LLM Self-Consistency

Source: https://arxiv.org/abs/2605.05873
Triaged: 2026-05-08

## Refutations

1. **Refutes H-56 (K-sample consistency-rate matches or exceeds prefill-DoM selective prediction at K=8 compute).**
   CITE's anytime-valid e-process framework provides a theoretically principled stopping rule for self-consistency that controls false certification at prescribed level epsilon, independent of category set size. If CITE certifies the modal answer with far fewer samples than K=8 on easy problems while maintaining Type-I control, it would show that *adaptive* self-consistency (with guaranteed error control) beats naive K=8 fixed-sample voting at matched expected compute. This could refute H-56's premise that a fixed K=8 is the right comparison point for DoM selective prediction — the correct comparison should be against CITE's adaptive budget. Falsifiable: run CITE on MATH-500 with Qwen-2.5-1.5B, measure mean stopping time per problem, compare selective-prediction coverage-accuracy curve against F-8's 71.6% at 50% coverage under matched total compute.

2. **Refutes F-8 (selective-prediction via prefill DoM works at 50% coverage).**
   CITE provides a competing abstention signal: problems that CITE *fails* to certify within a compute budget are precisely the ambiguous ones where the model's answer distribution lacks a clear mode. If CITE's non-certification set achieves higher selective-prediction accuracy than prefill-DoM thresholding at the same coverage, F-8's practical value claim (that geometry-based internal signals are the right abstention mechanism) would be substantially weakened. The mechanism: CITE captures distributional ambiguity in the *output* space directly, while DoM captures it indirectly via hidden-state geometry — if the direct signal dominates, the indirect geometry signal is redundant for deployment. Falsifiable: compute CITE certification decisions at coverage 0.5 on MATH-500, compare answered accuracy vs F-8's 71.6%.

3. **Refutes H-670 (confidence-weighted majority vote with prefill-DoM-derived confidence beats threshold-based abstention at matched coverage on MATH-500).**
   CITE's W-CITE extension (Section 5) formalizes confidence-weighted voting with anytime-valid Type-I guarantees, and Theorem 5.4 shows the stopping time bound mirrors the unweighted case with the weighted gap replacing the unweighted gap. If W-CITE with prefill-DoM-derived confidence scores *does not* improve stopping time over unweighted CITE (i.e., DoM-derived weights do not increase the effective weighted gap), H-670's premise — that DoM-derived confidence meaningfully improves majority-vote outcomes — would be contradicted. Falsifiable: run both CITE and W-CITE on MATH-500 with Qwen-2.5-1.5B using DoM scores as confidence weights, compare stopping times and certification rates.

## Direct connections to F-N / H-N

- **F-2 (prefill L19 DoM AUROC 0.7731):** CITE's W-CITE extension (Section 5) takes confidence weights W_i in [0,1]. The DoM score from F-2 can serve as these weights. The direct test: does incorporating DoM-derived confidence into W-CITE reduce the expected stopping time (Theorem 5.4 predicts improvement when weighting increases the effective gap Delta_w without reducing the target weighted mass mu_r)?

- **F-7 (D-bucket collective geometric signature):** CITE certifies the modal answer as the unique mode. D-bucket problems — where the model gets the right answer through a degenerate reasoning path — would produce a clear mode but with a *spurious* certification. CITE's guarantee is about mode identity, not answer correctness (the paper explicitly notes this limitation in Section 7). D-bucket is exactly the gap between mode certification and correctness certification.

- **F-8 (selective-prediction 71.6% at coverage 0.5):** CITE offers a competing selection mechanism. At epsilon=0.05, problems certified within a budget are "confidently modal" — the complement forms a natural abstention set. The coverage-accuracy tradeoff from CITE-based abstention is the direct comparison target for F-8's 71.6%.

- **H-56, H-57, H-58 (K-sample consistency thresholds):** The admission note correctly identifies CITE as a principled replacement for ad-hoc K-sample consistency thresholds in H-56/H-57/H-58. CITE's category-set-size-free stopping time (Theorem 4.3) means the analysis is valid even when MATH-500 produces many distinct answer strings, which is the exact failure mode H-56 doesn't account for.

## Methodologies extracted

- **Pairwise e-process for mode testing** — Sequential martingale-based test comparing a fixed target label against its empirical runner-up using betting-style e-values (Eq. 3). Controls Type-I error uniformly over all stopping times without knowledge of the full category set.
  *Replication cost*: 1h CPU (pure Python implementation of Algorithm 1, no GPU needed).
  *We'd plausibly run this*: yes — direct implementation on MATH-500 answer strings from cached generation outputs.

- **Lower confidence bound on target probability (LCB)** — Time-uniform lower confidence bound on p_r via mixture martingale over a finite grid of betting parameters (Eq. 5, Definition 3.3). Handles hidden competitors that haven't appeared in sample yet.
  *Replication cost*: 30min CPU (straightforward grid computation).
  *We'd plausibly run this*: yes — component of CITE that we'd implement as part of the above.

- **Unseen upper bound** — Probability-weighted union bound giving a time-uniform upper bound on the mass of any unseen category (Definition 3.5). Bypasses the need to estimate aggregate missing mass.
  *Replication cost*: 10min CPU (closed-form computation).
  *We'd plausibly run this*: yes — component of CITE.

- **W-CITE (confidence-weighted extension)** — Extends CITE to weighted observations (X_i, W_i) where W_i is a confidence score in [0,1], certifying the weighted unique mode (Section 5). Stops when the weighted pairwise check passes for all observed competitors and the weighted LCB exceeds the unseen bound.
  *Replication cost*: 2h CPU (extends unweighted implementation; need DoM scores as weights).
  *We'd plausibly run this*: yes — the natural integration point with our prefill DoM signal (F-2).

## Approaches & framings

- **Mode certification vs answer correctness** — The paper explicitly distinguishes certifying that a label is the unique mode of the model's response distribution from certifying that the answer is correct. These "compose and neither subsumes the other" (Section 7). This framing directly maps to the gap between F-2 (correctness prediction via internal geometry) and self-consistency (correctness prediction via output-space voting) — CITE formalizes the latter while our work formalizes the former. The composition question (do they combine?) is left open and maps to H-670.

- **Diffuse-tail regime** — The paper identifies a regime where the modal answer dominates every individual competitor but not the aggregate tail mass. This is exactly the regime relevant to small-model MATH-500 where K=1 accuracy is 48.6% (1.5B) — the model may have a clear best answer but low confidence overall. CITE handles this regime where MMC-style certificates fail (Proposition E.1).

- **Category-set-size-free stopping** — Theorem 4.3's stopping time bound depends on (delta, p_r) but not |A|. For LLM answer distributions where the effective category set grows with sampling (thousands of distinct answer strings), this is essential. This framing connects to the problem F-7 surfaces: D-bucket problems might have deceptively concentrated answer distributions.

## Datasets & benchmarks

- **AIME 2026** (Dekoninck et al.) — Competition math problems. Size not specified in paper. Accessible via MathArena (arxiv 2605.00674). Applicable to us? Possibly — MATH-500 is our primary benchmark, but AIME problems at higher difficulty could test DoM signal in a harder regime.

- **FrontierScience-Olympiad** (Wang et al., 2026) — Expert-level science tasks. ~250 distinct answer values per problem on gpt-oss-20b. License/access via arxiv 2601.21165. Applicable to us? No — requires much larger models than our 1.5B/7B regime.

- **ER-REASON** (Mehandru et al., 2025) — Clinical reasoning benchmark for emergency room triage. Accessible via arxiv 2505.22919. Applicable to us? No — domain mismatch (clinical vs mathematical reasoning).

## Implementation details worth capturing

- Algorithm 1 pseudo-code is complete and self-contained (Section 3.4). Inputs: target label r, parameter grid Lambda_pw with weights, LCB grid Lambda_r with weights, error level epsilon.
- Grid recommendation: Lambda_pw should contain some lambda_pw in [delta/8, delta/4] where delta is the modal gap (Condition 4.2). Lambda_r should contain some lambda_r in [1/32, 1/16].
- Equal error split: alpha_pw = alpha_r = alpha_u = epsilon/3 works for all three components.
- Per-round cost is O(1) for unweighted CITE (runner-up monotonicity, Lemma B.3) but O(|A_t|) for W-CITE (runner-up monotonicity fails under heterogeneous weights).
- The LCB computation requires a grid search over q in (0, p_hat_t(r)] at each round — in practice a coarse grid of ~50-100 values suffices.
- Code: no public repository mentioned in the paper.
- Models used: Qwen3-30B-A3B-Instruct-2507, gpt-oss-20b. Not directly relevant to our 1.5B/7B regime but the algorithm is model-agnostic.

## Replicable intermediates

- We have cached MATH-500 K=1 generation outputs from P11 H100 runs (1024-tok). These include the final answer strings. We could:
  - Run unweighted CITE on the existing answer distribution by re-generating K=8 or K=16 samples per problem and applying Algorithm 1 to the answer label sequence.
  - **Prerequisite**: We need K>1 samples per problem, which are NOT currently cached (P11 only has K=1). However, the generation script exists and could produce K=16 in ~2h on H100.
- If K>1 samples existed, the CITE algorithm itself runs in seconds on CPU — it's pure counting.
- The DoM scores from `pathway11_h100/prefill_gated_compute/results.json` could be used as W-CITE confidence weights once K>1 samples are available.

none — paper claims need fresh data we don't have (K>1 samples per MATH-500 problem are not cached; would need ~2h H100 re-generation).

## Cross-paper signals

- 2510.17472 — NOT in graph; recommend admission. Cordero-Encinar & Duncan (2025), "Certified self-consistency: Statistical guarantees and test-time training for reliable reasoning in LLMs." Closest prior work (MMC); CITE shows strict superiority in diffuse-tail regime (Proposition E.1).
- 2508.15260 — NOT in graph; recommend admission. Fu, Wang, Tian, Zhao (2025), "Deep think with confidence." Confidence-aware self-consistency variant; connects to W-CITE and H-670.
- 2511.02603 — NOT in graph; weak recommend. Aghazadeh et al. (2025), "CGES: Confidence-guided early stopping for efficient and accurate self-consistency." Practical stopping signal without Type-I guarantees; useful as a non-guaranteed baseline.
- 2407.21787 — NOT in graph; weak recommend. Brown et al. (2024), "Large Language Monkeys: Scaling Inference Compute with Repeated Sampling." Empirical scaling of repeated sampling; relevant to H-56/H-57 regime.

## Proposed FutureExperiments

```yaml
- id: P11-FE988
  pathway: P11
  description: "Run CITE (Algorithm 1) on K=16 MATH-500 samples from Qwen-2.5-1.5B at epsilon=0.05. Measure per-problem certification decision, mean stopping time, and derive a CITE-based selective-prediction curve (coverage vs answered accuracy). Compare against F-8's prefill-DoM selective prediction (71.6% at 50% coverage)."
  rationale: "CITE provides a principled output-space competing signal for selective prediction. If CITE-based abstention matches or beats DoM-based abstention at matched coverage, F-8's practical value is weakened. If DoM beats CITE, the internal-geometry signal carries information beyond output-space voting consistency."
  trigger: "Paper 2605.05873 introduces anytime-valid mode certification applicable to our MATH-500 regime"
  status: READY
  priority: HIGH
  roi: 8
  cost: "2h H100 (K=16 generation) + 30min CPU (CITE algorithm)"
  depends-on: [F-2, F-8]
  would-update: [F-8]
  triggered-by: ["2605.05873"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE989
  pathway: P11
  description: "Run W-CITE on same K=16 MATH-500 samples using prefill-L19 DoM scores (from F-2) as confidence weights W_i in [0,1]. Compare W-CITE stopping time and certification rate against unweighted CITE (P11-FE988). Tests whether DoM-derived confidence genuinely improves the effective weighted gap (Theorem 5.4)."
  rationale: "Directly tests H-670's premise: does DoM-derived confidence improve majority-vote selective prediction? If W-CITE stopping time improves over CITE, the DoM signal adds value beyond what output-space consistency captures alone."
  trigger: "P11-FE988 produces K=16 samples and CITE baseline; W-CITE extension from 2605.05873 Section 5 uses exactly our DoM signal"
  status: READY
  priority: HIGH
  roi: 7
  cost: "30min CPU (W-CITE algorithm on existing K=16 samples + DoM scores)"
  depends-on: [F-2, F-8]
  would-update: [F-8]
  triggered-by: ["2605.05873"]
  blocked-by: null
  blocked-by-experiment: [P11-FE988]

- id: P11-FE990
  pathway: P11
  description: "Characterize D-bucket problems (F-7) under CITE: measure whether D-bucket problems are disproportionately certified (false mode certification) or uncertified (CITE correctly abstains). If D-bucket problems have concentrated answer distributions, CITE will certify them — exposing the gap between mode certification and correctness certification."
  rationale: "CITE explicitly distinguishes mode certification from correctness. D-bucket is the exact failure mode: correct answer via degenerate reasoning. If CITE certifies D-bucket problems at high rate, combining CITE certification with DoM-based D-bucket detection (F-7) would create a two-signal abstention system."
  trigger: "P11-FE988 produces CITE certification decisions; D-bucket labels already available in cached data"
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "10min CPU (cross-tabulate CITE certification against D-bucket labels)"
  depends-on: [F-7, F-8]
  would-update: [F-7]
  triggered-by: ["2605.05873"]
  blocked-by: null
  blocked-by-experiment: [P11-FE988]
```

## Proposed HYPOTHESES.md additions

### H-739: CITE anytime-valid mode certification on MATH-500 produces a selective-prediction curve that matches or exceeds F-8's 71.6% at coverage 0.5
**Priority:** HIGH
**Motivated by:** 2605.05873 + F-8 + F-2
**Test:** Generate K=16 samples per MATH-500 problem on Qwen-2.5-1.5B, run CITE Algorithm 1 at epsilon=0.05, define abstention set as non-certified problems at matched coverage, compare answered accuracy. ~2.5h total.
**Requires:** H100 GPU (2h for K=16 generation), CPU (30min for CITE)
**Would change:** Confirm: F-8's geometry-based abstention is redundant for deployment if CITE output-space signal dominates. Reject: F-8's DoM signal carries information beyond what output consistency captures, strengthening the case for internal-geometry methods.
**Blocks:** H-670

### H-740: W-CITE with prefill-DoM-derived confidence weights reduces expected stopping time relative to unweighted CITE on MATH-500
**Priority:** HIGH
**Motivated by:** 2605.05873 + F-2 + H-670
**Test:** Using K=16 samples from H-739, run W-CITE with DoM scores normalized to [0,1] as weights. Compare mean stopping time and certification rate against unweighted CITE. ~30min CPU.
**Requires:** CPU only (depends on H-739 for K=16 samples)
**Would change:** Confirm: DoM signal carries actionable information for output-space aggregation, validating H-670. Reject: DoM weights don't improve the effective weighted gap, suggesting DoM captures something orthogonal to answer-distribution structure.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2605.05873 — CITE: Anytime-Valid Statistical Inference in LLM Self-Consistency (Ota, Iwase, Ichihara, Komiyama, Imaizumi, 2026)

**Relevance:** CITE provides a statistically principled framework for deciding when self-consistency voting has accumulated enough evidence to certify a modal answer, with provable Type-I error control under arbitrary data-driven stopping. For our pipeline, this paper introduces a competing output-space abstention signal that can be directly compared against our geometry-based prefill-DoM selective prediction (F-8). The W-CITE extension (confidence-weighted voting) provides a natural integration point where prefill-DoM scores serve as confidence weights, testing whether internal geometry adds value to output-space voting.

**Key claim we tested:** Anytime-valid mode certification with category-set-size-free stopping time provides efficient and reliable self-consistency certification for LLM reasoning tasks.

**Our result:** **TO TEST** — CITE's framework is directly applicable to our MATH-500 regime but requires K>1 samples per problem (not currently cached). The primary test (P11-FE988) generates K=16 samples and runs CITE vs DoM-based selective prediction. The secondary test (P11-FE989) evaluates whether DoM-derived confidence weights improve W-CITE over unweighted CITE. A third experiment (P11-FE990) characterizes D-bucket behavior under CITE certification.

**Methodologies:**
- Pairwise e-process for sequential mode testing (Eq. 3, Algorithm 1)
- Time-uniform lower confidence bound on target probability (Definition 3.3)
- Unseen upper bound via probability-weighted union bound (Definition 3.5)
- W-CITE confidence-weighted extension for weighted mode certification (Section 5)

**Approaches & framings:**
- Mode certification vs answer correctness distinction (Section 7)
- Diffuse-tail regime identification where MMC fails (Proposition E.1)
- Category-set-size-free stopping time (Theorem 4.3)

**Datasets & benchmarks:**
- AIME 2026, FrontierScience-Olympiad, ER-REASON (evaluated but not directly applicable to our 1.5B/7B MATH-500 regime)

**Implementation details:**
- Algorithm 1 is complete pseudo-code; grid conditions in Condition 4.2; equal error split epsilon/3 per component
- No public code repository

**Replicable intermediates:**
- Requires K>1 generation (not cached); CITE algorithm itself is pure counting, seconds on CPU

**Cross-paper signals:**
- 2510.17472 (MMC, closest prior work; not in graph)
- 2508.15260 (Deep think with confidence; not in graph)

**Related experiments:** P11-FE988, P11-FE989, P11-FE990, H-739, H-740

**Status:** TO TEST

## New claims

none — this brief introduces no new quantitative claims into narrative docs; all numbers cited are from the paper (external) or from the existing headlines table.

## Sources consulted

- /home/musicofhel/topo-confidence/CLAUDE.md lines 142-157 — headlines table (default context)
- /home/musicofhel/topo-confidence/FINDINGS.md — F-N one-liners via grep (default context)
- /home/musicofhel/topo-confidence/HYPOTHESES.md — H-N one-liners via grep, Next ID line (default context)
- /home/musicofhel/topo-confidence/PAPER_INDEX.md — status flags via grep (default context)
- /home/musicofhel/topo-confidence/HYPOTHESES.md lines for H-56, H-57, H-58, H-670 — on-demand read to verify admission note connections
