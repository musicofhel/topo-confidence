# Result brief — P11-FE110 + P10-FE23 + P11-FE455 (GPU bundle, RTX 2060 Super)

Phase 4 cheap-win bundle. One Qwen-2.5-1.5B-Instruct fp16 load on the
local 2060 Super, three head-to-head probes against F-2's prefill L19 DoM
AUROC 0.7731. All three FAIL to displace F-2; results are reported as one
EXP-53 entry covering all three, with separate FE outcome lines and
shared regen script.

## FE

```yaml
fe_id: P11-FE110
status: COMPLETED
outcome: "Phase 4 GPU bundle (Qwen-2.5-1.5B-Instruct fp16 on RTX 2060 Super, ~2.5 min walltime) covers three FEs: P11-FE110 (ActAdd), P10-FE23 (softmax-conf), P11-FE455 (ConCISE). All three FAIL to displace F-2 prefill DoM 0.7731. (1) FE110: 5 contrast pairs through model at L19 — best cos with supervised DoM +0.0651; best projection AUROC 0.6615; supervision adds ~7pp over the strongest generic ActAdd direction. (2) FE23 (P10): softmax-confidence at PANL = 0.4395 — BELOW chance 0.5; the model's local certainty at the moment-before-the-boxed-answer is anti-correlated with correctness on this MATH-500 setting. (3) FE455: ConCISE c_hat = 0.5887, P(' confident') alone 0.5985, P(' sure') alone 0.2927 (anti-predictive). All three FE outcomes promoted under one EXP-53 entry; P10-FE23 and P11-FE455 status updates run manually after this primary FE110 promote."
result_json: pathway11_h100/gpu_bundle/results.json
result_json_keys:
  - fe110.max_cos_with_dom
  - fe110.max_projection_auroc
  - fe110.supervised_dom_auroc_oof
  - fe110.per_pair
  - fe23.auroc_softmax_confidence
  - fe455.auroc_c_hat
  - fe455.auroc_p_confident
  - fe455.auroc_p_sure
regen_cmd: "python pathway11_h100/gpu_bundle/recompute_gpu_bundle.py"
```

**Bundled FE outcomes (manually promoted after primary):**

- **P10-FE23 → COMPLETED:** "Softmax-confidence at PANL position is NOT a calibration signal — it's actively MISCALIBRATED on Qwen-2.5-1.5B-Instruct MATH-500. AUROC predicting correctness = 0.4395 (BELOW chance 0.5). The model's local certainty at the moment-before-the-boxed-answer is anti-correlated with actual correctness — when it's most sure of the next token, it's slightly more likely wrong. F-2's prefill DoM 0.7731 unaffected. 75/500 generations had no `\boxed{` and used end-of-text PANL fallback."

- **P11-FE455 → COMPLETED:** "ConCISE 'So, I'm' confidence detector AUROC = 0.5887 on Qwen-2.5-1.5B-Instruct MATH-500, well below F-2 prefill DoM 0.7731. Forward [chat_template(prompt) + reasoning_up_to_PANL + ' So, I'm'], read next-token logits, compute c_hat = P(' confident') + P(' sure') + P(' pretty') × (P(' confident') + P(' sure')). P(' confident') alone = 0.5985; P(' sure') alone = 0.2927 (anti-predictive). Composite does not displace F-2 — supervised L19 DoM beats ConCISE by 18pp."

## Result JSON verification

`pathway11_h100/gpu_bundle/results.json` exists; keys are nested under
`fe110.*`, `fe23.*`, `fe455.*` (flatten-aware validation in
promote_result.py accepts both nested and bare names). Per-problem NPZ at
`pathway11_h100/gpu_bundle/per_problem.npz` (correct labels, FE23 top-token
probs + token IDs, FE455 c_hat / p_confident / p_sure / p_pretty,
contrast vectors, supervised DoM). Regen reuses MATH-500 problems via
`pathway8_layerwise.extract_math500.load_math500()`, cached generated
solutions from `pathway8_layerwise/data/math500/problem_*.npz` (`text`
field), and the cached supervised DoM source from
`pathway11_h100/prefill_inversion/cache/m15b_prefill.npz`. Walltime ~2.5 min
(model load 4s + FE110 contrast forwards 5s + 500-problem sweep 148s +
LR/AUROC compute). Deterministic seed=42; greedy chat-template forward
passes are reproducible.

## EXPERIMENT_LOG entry

## EXP-53: P11-FE110 + P10-FE23 + P11-FE455 — GPU bundle on RTX 2060 Super
**Date:** 2026-05-01
**Status:** COMPLETE
**Motivated by:** Phase 4 of PLAN_cheap_wins.md required local GPU forward
passes for three head-to-head F-2 alternatives that the cached NPZs
couldn't answer alone. ActAdd (Turner 2308.10248) needs short contrast
prompts forwarded to L19. Softmax-conf and ConCISE (2505.04881) need a
forward pass at PANL position to read next-token logits. Bundling all
three in one model load amortises the ~4s load time.
**Hypothesis:** Each FE displaces F-2 if its AUROC ≥ 0.7731 − 0.02 = 0.7531
on the same 500-problem cache. F-2 holds otherwise.
**What we actually tested:**
  FE110: 5 contrast pairs through Qwen-2.5-1.5B-Instruct fp16. For each
    pair (pos_text, neg_text), forward each through model with NO chat
    template (raw ActAdd convention), grab L19 hidden state at last
    position, compute v = h_pos − h_neg. Compute cos(v, supervised DoM)
    where DoM = mean(prefill_cache[correct]) − mean(prefill_cache[~correct]),
    and AUROC of (m15b_prefill @ v) against `correct` labels.
  FE23: For each of 500 problems, build chat-template prompt, append
    cached generated text up to (but excluding) the last `\\boxed{`,
    forward, take softmax over last-position logits, record top-token
    probability. AUROC against correctness.
  FE455: Same prompt structure as FE23, but append " So, I'm" to the
    PANL prefix. Forward, read softmax probability of " confident",
    " sure", " pretty" tokens (single-token IDs 16506, 2704, 5020).
    c_hat = P(' confident') + P(' sure') + P(' pretty') × (P(' confident') +
    P(' sure')). (We approximate the conditional pair as marginal-
    products since computing the actual P(' confident' | ' pretty')
    requires another forward and the head-to-head AUROC captures the
    same signal.)
**Key results:**
  FE110 per pair (cos_with_DoM / projection_AUROC):
    correct/incorrect math step:    +0.0353 / 0.6218   ‖v‖=28.9
    solve carefully / guess:        +0.0193 / 0.5345   ‖v‖=11103.4 (out-of-distribution norm)
    complete / wrong solution:      +0.0523 / 0.6615   ‖v‖=48.1
    right / wrong answer:           −0.0061 / 0.4901   ‖v‖=31.2
    certain / uncertain:            +0.0651 / 0.6549   ‖v‖=53.3
    Best cos = +0.0651. Best AUROC = 0.6615. Supervised DoM AUROC OOF = 0.7711.
  FE23 softmax confidence:
    AUROC (predict correct via top-prob) = 0.4395
    AUROC (predict incorrect via 1 − top-prob) = 0.4395
    The two are equal because they're the same scoring function with
    flipped sign convention; **0.4395 < 0.5 means top-token confidence
    is anti-correlated with correctness on this dataset**.
  FE455 ConCISE c_hat:
    AUROC (c_hat) = 0.5887
    AUROC (P(' confident') alone) = 0.5985
    AUROC (P(' sure') alone) = 0.2927  (anti-predictive)
    Composite c_hat formula adds noise relative to P(' confident') alone.
  Cache consistency: pathway8 correctness vs m15b_prefill correctness =
    True (500/500 match — same generation source for both caches).
  PANL fallback: 75/500 problems have no `\\boxed{` (15%); for these
    PANL = end-of-text. Affects FE23+FE455 numerics by ~15% but doesn't
    invalidate the AUROC framing.
**Verdict:**
  FE110: F-2 supervised DoM is NOT recoverable from generic ActAdd
    contrast prompts. None of 5 pairs reaches cos > 0.5 (best 0.065)
    or AUROC ≥ 0.75 (best 0.66). The supervision provides ~7pp lift over
    the best contrast-pair direction. **F-2 holds.**
  FE23: Softmax-confidence is anti-calibrated at PANL on this MATH-500
    setting. AUROC 0.44 < 0.5 (chance) << 0.7731 (F-2). **F-2 holds.**
  FE455: ConCISE c_hat 0.59 << 0.7731. The 'So, I'm' detector picks up
    *some* signal (P(' confident') 0.60, P(' pretty') intercept ≈ 0)
    but is dominated by F-2. **F-2 holds.**
**Changed our understanding of:**
  (1) F-2 is robust against three more competing explanations: ActAdd,
      softmax-conf at PANL, ConCISE 'So, I'm'. Cumulative refutation
      record: F-2 has now survived length-partialling (FE448, weakened
      to 0.66), LEACE (FE101, collapses to chance), Song-Zhong
      residualization (FE115, +3pp ceiling), token-prob (FE181, +1pp
      joint), spectral α (FE749, 0.70 < 0.77), Phase 3 corroborators
      (FE319/136/244/331/339/254 — all ≤ 0.77), and now ActAdd +
      softmax + ConCISE.
  (2) ConCISE's 'So, I'm' framing is interesting but Qwen-1.5B does not
      use that surface pattern reliably — P(' sure') is anti-predictive,
      suggesting hesitancy markers correlate with correct reasoning.
  (3) Softmax confidence at PANL is *anti-calibrated* on this dataset
      (AUROC 0.44 < 0.5). When the model is most confident in the next
      token at the moment-before-the-answer, it's slightly more likely
      to be wrong. Worth flagging in any selective-prediction work.
**Files:** `pathway11_h100/gpu_bundle/recompute_gpu_bundle.py` (regen,
~2.5 min on 2060 Super fp16), `pathway11_h100/gpu_bundle/results.json`,
`pathway11_h100/gpu_bundle/per_problem.npz` (per-problem signals saved
for downstream calibration analysis if needed).
**Depends on:** EXP-037 (F-2 supervised DoM anchor), m15b_prefill cache,
pathway8 generated text cache, Qwen-2.5-1.5B-Instruct HF weights.
**Enables:** Closes Phase 4 of PLAN_cheap_wins.md. Possible follow-ups:
(a) FE110 with structured contrast prompts (full chat-templated MATH-500
problems with correct vs incorrect appendices), (b) calibration analysis
on FE23 anti-correlation finding (separate FE).

## STATE.md last-experiment update

EXP-53 (Phase 4 GPU bundle on RTX 2060 Super: P11-FE110 ActAdd contrast
pairs + P10-FE23 softmax-conf + P11-FE455 ConCISE 'So, I'm'). All three
FAIL to displace F-2. FE110 best cos=+0.065 / AUROC=0.66 (supervised DoM
adds ~7pp over best ActAdd direction). FE23 AUROC=0.44 (anti-calibrated
— softmax-conf at PANL is NEGATIVELY correlated with correctness). FE455
c_hat=0.59 (ConCISE detector picks up some signal but dominated by F-2).
**F-2 now has 12 alternative-probe controls passed.** Phase 4 complete;
remaining: Phase 5 FE116 PH-on-residuals (running in background, ETA
~90 min) + future H100 work (FE214 causal noising, FE269 ablation).

## FINDINGS.md updates

### F-2: Prefill L19 DoM is a strong correctness predictor, *better than final-token*

```yaml
strength: MODERATE
status: ACTIVE
add_evidence: [P11-E53]
counterargument: "Length partial-correlation (FE448) remains the dominant counterargument: ~⅔ of AUROC is length-explainable. FE115 reveals an unexploited +3pp ceiling — Song-Zhong residualization (μ + pos_0 + ctx_i subtraction) lifts AUROC to 0.8016. FE181 retires the 'DoM = output entropy' alternative — mean_logp AUROC 0.6721 ≪ DoM 0.7731. FE319 quadratic-SVD probe peaks at 0.7446 (k=10) — F-2 is purely linear at L19 prefill, consistent with FE101 LEACE collapse. FE339 Linear-AcT (variance-aware) ≈ tied at 0.7714 — variance carries no extra signal. EXP-53 closes ActAdd, softmax-conf at PANL (anti-calibrated 0.44), and ConCISE 'So, I'm' (0.59) as alternative explanations."
overturned_by: "OOF residual AUROC ≤ 0.55 after length partialing (would push to INVALIDATED), OR a layer where length-residualized DoM AUROC matches raw within 0.02 (would restore STRONG)."
```

**Claim.** On Qwen-2.5-1.5B-Instruct, a 5-fold OOF logistic probe fit on
mean-pooled L19 activations at position 0 (last prompt token, pre-generation)
achieves **AUROC 0.7731**. Under Song-Zhong residualization (μ + pos_0 +
ctx_i subtraction, EXP-48), AUROC rises to **0.8016** (+3.1pp). Final-token
DoM AUROC is **0.7186** (headline) / 0.6603 raw on per-problem cache /
0.6865 on Song-Zhong residuals. Per-layer sweep (EXP-45): L19 sits on a
3-layer peak plateau (L19=0.7705, L20=0.7706, L21=0.7718). LOCO-CV (EXP-43):
mean 0.7427, worst 0.6032 (Number Theory). Within-topic CV (EXP-46): mean
0.7143, worst 0.6000 (Number Theory). After partialing predicted-length
from prefill (EXP-44), in-sample DoM AUROC drops 0.7834 → **0.6647**.
LEACE strict-null (EXP-47): AUROC collapses 0.7705 → 0.5000 — F-2 IS
purely linear. **Token-prob baseline (EXP-49):** mean log-prob AUROC =
**0.6721 (1.5B) / 0.6336 (7B)**, joint [mean_logp, prefill_DoM_proj] =
0.7836 (+1pp). DoM is not subsumed by sequence-likelihood. **Phase 3
corroborators (EXP-51):** quadratic-SVD probe peak AUROC = **0.7446** at
k=10 (below 0.7731 — F-2 is purely linear); Linear-AcT variance-aware
probe AUROC = **0.7714** (≈ tied; variance-awareness adds 0pp).
**Phase 4 GPU bundle (EXP-53):** ActAdd best contrast-pair AUROC =
**0.6615** (cos with DoM = +0.065 max, well below recovery threshold);
softmax-conf at PANL = **0.4395** (BELOW chance — anti-calibrated);
ConCISE c_hat = **0.5887**. None of the three displaces F-2.

**Strength:** MODERATE (signal real, linear, survives topic and within-topic
controls; ~⅔ of AUROC is length-explainable; FE115 reveals a +3pp ceiling
when context is removed; FE181 retires the "DoM = output entropy"
counterargument; FE319 + FE339 close the "F-2 might be quadratic / variance-
aware" alternatives — DoM at L19 prefill IS purely linear; **EXP-53 closes
ActAdd-recoverable, softmax-confidence, and ConCISE 'So, I'm' as
alternative explanations**).

**Evidence:** EXP-037, EXP-43, EXP-44, EXP-45, EXP-46, EXP-47, EXP-48,
EXP-49, EXP-51; **EXP-53**
(`pathway11_h100/gpu_bundle/results.json`: fe110.max_cos_with_dom 0.0651,
fe110.max_projection_auroc 0.6615, fe23.auroc_softmax_confidence 0.4395,
fe455.auroc_c_hat 0.5887).

**Controls passed:** (existing 5 controls); **Song-Zhong purification
(EXP-48)** — residualization *strengthens* the probe; **token-prob baseline
(EXP-49)** — DoM strictly outperforms mean log-prob, joint adds only +1pp;
**Phase 3 corroborators (EXP-51)** — quadratic-SVD and Linear-AcT both
fail to beat the linear mean-diff probe; **Phase 4 GPU bundle (EXP-53)** —
ActAdd, softmax-conf, ConCISE all <0.67 AUROC, supervised DoM holds with
+7pp margin.

**Controls partially failed:**
- Length partial-correlation (EXP-44, FE448) — DoM AUROC drops 0.7834 →
  0.6647 after partialing predicted-length. Length is a major confound.

**Strongest counterargument:** Sequence length alone (AUROC 0.7986)
predicts correctness *better* than raw DoM at L19. Sum log-prob (EXP-49,
AUROC 0.8478) is similarly length-confounded — `sum_logp = mean_logp ×
seq_len`. Mean log-prob (length-normalized) gives only 0.6721. The
correctness signal has three correlated components: length, mean
likelihood (small), and the geometric DoM direction (dominant within the
length+likelihood-orthogonal subspace).

## New claims

- fe110-best-cos = 0.0651 (FE110 best contrast-pair cosine with supervised DoM; certain/uncertain pair; ≪ 0.5 recovery threshold)
- fe110-best-auroc = 0.6615 (FE110 best contrast-pair projection AUROC; complete/wrong solution pair; well below F-2 0.7731)
- fe110-supervised-dom-oof = 0.7711 (FE110 supervised DoM AUROC OOF on m15b_prefill; matches F-2 0.7731 ± 0.002)
- fe23-softmax-conf = 0.4395 (FE23 softmax-confidence AUROC at PANL; BELOW chance — anti-calibrated)
- fe455-c-hat = 0.5887 (FE455 ConCISE c_hat AUROC; below F-2 0.7731)
- fe455-p-confident = 0.5985 (FE455 P(' confident') alone; weak positive signal)
- fe455-p-sure = 0.2927 (FE455 P(' sure') alone; anti-predictive — hesitancy correlates with correctness)

## Sources

- PLAN_cheap_wins.md §"Phase 4 — Local 2060 GPU runs"
- 2308.10248 (Turner et al. — Activation Addition / ActAdd)
- 2502.06884 (P10-FE23 trigger paper — softmax-confidence sanity check)
- 2505.04881 (P11-FE455 trigger paper — ConCISE 'So, I'm' detector)
- pathway11_h100/gpu_bundle/results.json (this experiment's output)
- pathway11_h100/gpu_bundle/per_problem.npz (per-problem signals)
- pathway11_h100/gpu_bundle/recompute_gpu_bundle.py (regen script)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (cached L19 prefill source for FE110 projection AUROC)
- pathway8_layerwise/data/math500/problem_*.npz (cached generated text for FE23/FE455 PANL extraction)
