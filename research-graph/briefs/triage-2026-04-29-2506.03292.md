# Triage brief — 2506.03292 — HyperSteer: Activation Steering at Scale with Hypernetworks

Source: https://arxiv.org/abs/2506.03292
Triaged: 2026-04-29

## Refutations

1. **Refutes F-3 (prefill/final-token DoM orthogonality, cos = 0.046).** HyperSteer
   trains a single end-to-end hypernetwork that emits *position-dependent* steering
   vectors conditioned on the natural-language prompt and the steered LM's internals.
   If one shared hypernet can produce effective interventions both at prompt-end
   (prefill) and during continuation (final-token regime) using the same
   parameters, the prefill and final-token correctness directions are not
   merely orthogonal in raw cosine — they are reachable from a *common conditioning
   manifold*. F-3 currently treats cos = 0.046 as evidence of two distinct circuits;
   HyperSteer-style architecture would predict a low-rank conditional map between
   them. **Falsifiable test**: train a tiny hypernet (≤ 1M params) on Qwen-2.5-1.5B
   to produce both prefill and final-token DoM-equivalent vectors from the same
   problem text; if AUROC at both positions is within 1 pt of the supervised DoMs,
   F-3's "orthogonal circuits" framing is too strong.

2. **Refutes H-1 (per-position DoM steering bank as the right intervention).**
   H-1 proposes a fixed per-position vector bank extracted via probing. HyperSteer's
   headline claim is that *prompt-conditioned, dynamically generated* steering
   vectors beat static supervised vectors on AxBench, including on
   never-seen-during-training prompts. If HyperSteer outperforms per-position DoM
   on MATH-500 at matched compute, H-1's specific construction is sub-optimal — the
   right object is not "the DoM at L19 prefill" but "f(problem, L19_prefill) → vector".
   **Falsifiable test**: P10-FE1's three-arm steering experiment (probe-weight,
   mass-mean, trained per-layer bias) gains a fourth arm — a HyperSteer-style
   hypernet conditioned on the L19 prefill activation. If arm 4 ≥ arm 1/2 by ≥ 3 pp
   on MATH-500 holdout, H-1 is replaced by a hypernet hypothesis.

3. **Refutes F-7 (D-bucket has a fixed pathological geometric signature).** F-7
   currently frames D-bucket (K=1 right, K=8 wrong) as an inherent collective
   geometry. HyperSteer's promise of *per-prompt* steering vectors implies the
   "right" intervention is problem-specific. If a hypernet trained to push
   D-bucket prefill activations toward the A-bucket DoM region recovers ≥ 30% of
   D-bucket problems on K=1 retry, D-bucket is not a fixed pathological region —
   it is "the prompts whose problem-specific direction the static DoM does not
   capture", which is a much weaker claim. **Falsifiable test**: train a hypernet
   on the 207 A-bucket prefill activations (paired with their problem text) and
   apply at inference to the 36 D-bucket problems; measure flip rate.

4. **Refutes the supervised-DoM regime that backs F-2 (prefill L19 DoM AUROC
   0.7731).** HyperSteer reports its hypernet generalizes to *unseen* steering
   prompts, i.e. the relevant direction is learnable from a meta-distribution.
   If prompt-conditioned direction inference works, then the single supervised
   L19 prefill DoM is leaving signal on the table — a hypernet correctness
   predictor (output: per-problem direction → dot product with L19 activation)
   should beat AUROC 0.7731. **Falsifiable test**: train a hypernet
   text → R^d on MATH-500 problems with cached prefill activations, regress on
   correctness label; OOF 5-fold AUROC. If > 0.80, F-2's supervised single-vector
   probe is a lower bound, not the ceiling.

## Direct connections to F-N / H-N

- **H-1 (per-position DoM steering moves MATH-500 accuracy)** — HyperSteer is the
  most directly competing intervention class. P10-FE1 already plans probe-weight /
  mass-mean / trained-bias arms; HyperSteer adds a learned-conditional fourth arm.
  Headline anchor: AUROC 0.7731 at prefill, cos 0.046 prefill↔final.
- **F-2 (prefill L19 DoM AUROC 0.7731)** — supervised single-vector probe;
  HyperSteer would test whether prompt-conditioned direction inference exceeds it.
- **F-3 (prefill/final orthogonality, cos 0.046)** — challenged if a shared
  conditioning manifold can produce both directions.
- **F-7 (D-bucket collective signature, peak-PR 67/88 K=1 vs K=8 spread)** — a
  per-prompt steering hypernet is a candidate "rescue" intervention.
- **H-13 (head-level attribution of prefill vs final-token circuits)** — HyperSteer
  predicts the two directions share a low-rank conditioning structure; head-level
  attribution would reveal whether the same heads carry both.

## Methodologies extracted

- **Hypernetwork-generated steering vectors** — small neural net consumes
  (prompt-tokens, target-LM-internals) and emits a layer-specific vector that is
  added to residual stream during generation, end-to-end trained with the
  steered-LM behaviour as supervision.
  *Replication cost*: 1–2 H100 days (small hypernet on top of frozen Qwen-2.5-1.5B
  + cached MATH-500 prefills). Add ~$50.
  *We'd plausibly run this*: yes — direct alternative to the static DoM bank in H-1,
  fits Pathway 10's existing steering scaffolding.

- **End-to-end intervention training (ReFT-style)** — train the intervention
  parameters jointly with a behaviour-matching loss over the steered LM's outputs.
  *Replication cost*: 4 h H100 for a small fine-tune loop on top of cached
  prefill NPZs.
  *We'd plausibly run this*: yes for the Pathway 10 steering arms; subsumes the
  trained-per-layer-bias arm of P10-FE1.

- **Generalization-to-unseen-prompts evaluation protocol** — train on AxBench's
  training prompts, measure win rate / behaviour shift on a held-out prompt
  distribution.
  *Replication cost*: trivial once a hypernet is trained — a held-out MATH-500
  split is already cached.
  *We'd plausibly run this*: yes; protocol cleanly slots into our 5-fold OOF.

- **AxBench-style automatic-rater win-rate evaluation** — judges produced per-task
  steering quality vs. a baseline.
  *Replication cost*: 1 day CPU + LLM-judge calls; not directly relevant to MATH-500
  correctness, more for refusal/persona tasks.
  *We'd plausibly run this*: no — orthogonal to our correctness focus, but could
  validate H-12 (semantic-entropy probes for refusal) as a side channel.

## Approaches & framings

- **Steering as a learned function of prompt + activation, not a single vector.**
  Reframes the activation-engineering literature from "which static direction"
  to "which conditional map". Intersects H-1 directly: H-1 currently posits a
  per-position vector bank; HyperSteer would replace this with a learned
  position-and-content-conditional emitter.
- **Bridging unsupervised dictionary learning (SAEs) and supervised vector
  construction.** SAEs scale to many features but lack guarantees per feature;
  supervised vectors are targeted but expensive. The hypernet positions itself
  as a third axis (scalable + targeted). Relevant to P10-FE5 (SAE on L19 prefill):
  the hypernet output could be cosine-compared to SAE features to ask which
  hypernet-emitted directions correspond to interpretable SAE basis vectors.

## Datasets & benchmarks

- **AxBench (Wu et al. 2501.17148)** — steering benchmark, ~thousands of natural
  language steering prompts, HF-accessible.
  *Applicable?* Partly — its tasks (sentiment, refusal, persona) are not
  correctness on math; but the *protocol* (train on subset of prompts, evaluate
  on held-out) is directly portable to MATH-500 splits.
- **GSM8K** — grade-school math word problems, HF-accessible. Applicable as a
  math-reasoning analogue to MATH-500 (smaller / easier; we already use MATH-500
  as primary).
- **AlpacaEval** — instruction-following benchmark, HF-accessible. Applicable?
  No — orthogonal to correctness on reasoning.

## Implementation details worth capturing

- Hypernet trained end-to-end through the steered LM with gradients backpropagating
  through the intervention point — uses the ReFT (Wu et al. ACL 2024) framework
  for differentiable interventions, stable on Llama / Qwen-class decoders.
- Steering vector is generated *per-prompt* (not per-token within the prompt; a
  single conditional vector applied at one or more layer indices during the
  steered run).
- Baseline comparisons reported on AxBench include SAE-based steering and
  difference-of-means (DiffMean / mass-mean) — the latter is exactly our
  P10-FE1 arm (b).
- AxBench's "concept" set is generated from natural-language descriptions (LLM-
  paraphrased), so HyperSteer's training distribution is heavy on instruction-style
  steering, not problem-conditional reasoning steering — porting to MATH-500
  needs a non-trivial design choice on what the conditioning text is (problem
  statement vs. abstract prompt like "solve correctly").
- No public code link confirmed from the abstract page; check the Stanford
  PyVene/AxBench repos before estimating cost (they tend to release).

## Replicable intermediates

- **Cosine of cached L19 prefill DoM (`pathway11_h100/prefill_gated_compute/`)
  against AxBench's published steering vector for "be confident" / "answer
  carefully" concepts.** Tests whether HyperSteer-style supervised steering
  vectors share direction with our supervised correctness DoM. Cost: 30 min
  CPU once vectors are downloaded from AxBench HF.
- **Train a 1-layer MLP hypernet
  text(problem)→R^{d_model} on cached MATH-500 prefill NPZs (1.5B), regress
  with logistic loss on correctness; OOF 5-fold AUROC.** This is the cheapest
  refutation test for F-2 — does a prompt-conditional probe beat 0.7731? Cost:
  4 h CPU on cached prefill activations.
- **Dot-product the per-problem prefill activation with the supervised DoM,
  versus with a trivial text-embedding-of-problem direction.** Lower-bound on
  whether prompt embedding alone can match supervised DoM. Cost: 1 h CPU.

## Cross-paper signals

- **2501.17148** — already in graph (status: pending_triage). Direct precursor
  (AxBench / HyperDAS — same Stanford line). HyperSteer extends AxBench from
  static-vector benchmark to learned-vector method.
- **2306.03341** (Inference-Time Intervention, ITI) — already in graph (status:
  CONTRADICTED). HyperSteer is a successor architecture that addresses ITI's
  static-vector limitation; if HyperSteer beats ITI, it confirms our
  CONTRADICTED status was about the *static* assumption, not steering generally.
- **2408.05147** — NOT in graph; recommend admission. SAE-based steering /
  Gemma-Scope reference, comparison baseline in HyperSteer; relevant to P10-FE5
  (SAE on L19 prefill) which already exists.
- **2024.acl-long.828** (ReFT, Wu et al.) — non-arxiv venue (ACL); skip as a
  graph node, but cite as the differentiable-intervention framework HyperSteer
  builds on. The arxiv version is 2404.03592.
- **2404.03592** (ReFT arxiv preprint) — NOT in graph; recommend admission as
  the methodological backbone of HyperSteer and a candidate for any
  end-to-end-trained intervention work in P10.

## Proposed FutureExperiments

```yaml
- id: P10-FE60
  pathway: P10
  description: "Train a small hypernetwork (problem-text + cached L19 prefill activation → R^{d_model}) on Qwen-2.5-1.5B MATH-500 prefill NPZs with logistic-regression supervision on correctness. Compare OOF 5-fold AUROC against the supervised L19 prefill DoM (0.7731). If hypernet AUROC > 0.80, prompt-conditional direction inference exceeds the single-vector probe and F-2's framing is upgraded."
  rationale: "HyperSteer (2506.03292) shows prompt-conditioned steering vectors generalize to unseen prompts. If correctness directions are similarly prompt-conditional, the single supervised L19 prefill DoM is a lower bound on what residual-stream geometry can predict. This is a cheap CPU-only refutation test for F-2's ceiling."
  trigger: "HyperSteer claims hypernet-generated vectors generalize to held-out steering prompts on AxBench."
  status: READY
  priority: HIGH
  roi: 8
  cost: "4h CPU on cached prefill NPZs"
  depends-on: [F-2, F-3]
  would-update: [F-2]
  triggered-by: ["2506.03292"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P10-FE61
  pathway: P10
  description: "Add a HyperSteer-style fourth arm to P10-FE1's steering sweep. Train a hypernet end-to-end through Qwen-2.5-1.5B with a behaviour-matching loss to emit a per-problem L19 prefill steering vector on MATH-500. Compare alpha-swept accuracy against arms (a) probe-weight, (b) mass-mean, (c) trained per-layer bias. If arm (d) beats the best static arm by ≥ 3 pp on MATH-500 holdout, H-1's per-position DoM bank is replaced by a hypernet steering hypothesis."
  rationale: "HyperSteer is the direct prompt-conditional analogue of H-1's per-position vector bank. Without this fourth arm, P10-FE1 risks declaring a static-vector winner when a learned-conditional alternative exists. Cleanly slots into the existing P10-FE1 sweep."
  trigger: "HyperSteer outperforms supervised difference-of-means on AxBench, including on unseen steering prompts."
  status: BLOCKED
  priority: HIGH
  roi: 8
  cost: "1 day H100 (sits on top of P10-FE1's H100 allocation)"
  depends-on: [F-2, F-3]
  would-update: [F-3]
  triggered-by: ["2506.03292", "2501.17148"]
  blocked-by: "H100 pod for steering interventions; P10-FE1 not yet run"
  blocked-by-experiment: ["P10-FE1"]
```

```yaml
- id: P10-FE62
  pathway: P10
  description: "D-bucket rescue via prompt-conditional steering. Train a hypernet on the 207 A-bucket prefill activations + problem text on Qwen-2.5-1.5B; apply the hypernet's emitted vector at inference to the 36 D-bucket prefills (K=1 right, K=8 wrong). Measure (a) shift in K=1 correctness, (b) reduction in K=8 sampling variance, (c) post-steering peak-PR within the D-bucket. If ≥ 30% of D-bucket flips to A-bucket on K=1 retry, F-7's framing of D-bucket as a fixed pathological collective geometry is weakened to 'static-DoM-blindspot'."
  rationale: "F-7 currently positions D-bucket as inherent fragility. Prompt-conditional steering offers a refutation path: if a hypernet trained on A-bucket geometry can emit a per-D-prompt vector that recovers correctness, D-bucket fragility lives in the supervised-vector blindspot, not in the geometry."
  trigger: "HyperSteer's per-prompt vector inference is a candidate rescue mechanism for problems where the global supervised DoM under-fits."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "1 day H100 + 4h CPU"
  depends-on: [F-7, F-2]
  would-update: [F-7]
  triggered-by: ["2506.03292"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-95: Prompt-conditional steering (HyperSteer-style) beats static DoM on MATH-500
**Priority:** HIGH
**Motivated by:** 2506.03292 + F-2
**Test:** Train a small hypernet text(problem) + L19 prefill activation → R^{d_model} on cached MATH-500 prefill NPZs (1.5B); regress on correctness. Compare OOF 5-fold AUROC and downstream selective-prediction accuracy at coverage 0.5 against the supervised L19 prefill DoM (AUROC 0.7731, sel-pred 71.6%).
**Requires:** CPU, ~4 h, all data cached in `pathway11_h100/prefill_gated_compute/`.
**Would change:** If hypernet AUROC > 0.80 *and* selective-prediction accuracy > 73%, F-2 is reframed: residual-stream correctness lives in a *prompt-conditional* direction, not a single global one. The DoM is a low-rank approximation. If hypernet ≤ DoM, the single-vector framing is on the Pareto frontier and hypernet is unnecessary complexity.
**Blocks:** Decision on whether H-1's steering bank should be a fixed per-position bank or a learned conditional emitter.

### H-96: Prefill ↔ final-token DoMs are reachable from a shared low-rank conditioning manifold
**Priority:** MEDIUM
**Motivated by:** 2506.03292 + F-3
**Test:** Train a single hypernet to emit two outputs (prefill-target, final-token-target) from the same problem-text encoding. If both outputs match their supervised DoM counterparts to AUROC within 1 pt, fit a low-rank linear map from prefill-emitter weights to final-emitter weights and report the singular-value spectrum.
**Requires:** CPU, ~6 h, cached prefill + final-token NPZs.
**Would change:** If a rank-≤ 8 map captures ≥ 95% of the cross-position variation, F-3's "orthogonal circuits" framing is replaced by "low-rank conditional rotation" — orthogonal in raw cosine but parametrically sibling. If no low-rank map fits, F-3's two-circuit interpretation strengthens.
**Blocks:** Interpretation of F-3 in the eventual writeup.

## Proposed PAPER_INDEX.md classification

## 2506.03292 — HyperSteer: Activation Steering at Scale with Hypernetworks (Sun, Wu, Geiger, Goodman et al., 2025)

**Relevance:** HyperSteer trains a hypernetwork end-to-end to emit
prompt-conditional steering vectors at chosen layers of a frozen LM, generalizing
to held-out steering prompts on AxBench. It is the prompt-conditional analogue
of our supervised-DoM intervention plan (H-1, P10-FE1) and the most direct
methodological alternative to a static per-position DoM bank. Its existence
forces a refutation test of F-2's single-vector probe (does a learned conditional
predictor exceed AUROC 0.7731?) and a rescue test for F-7's D-bucket framing
(does per-prompt steering flip pathological problems?).

**Key claim we tested:** Prompt-conditional, end-to-end-trained steering vectors
match or exceed static supervised steering vectors and unsupervised
SAE-feature-derived vectors, including on unseen prompt distributions.

**Our result:** **TO TEST** — directly slots into Pathway 10. Two cheap CPU-only
sanity checks (P10-FE60, H-95) test whether prompt-conditioning improves over the
single supervised L19 DoM on cached prefill NPZs. A heavier H100 arm (P10-FE61)
tests whether hypernet steering improves MATH-500 K=1 accuracy. Until run,
HyperSteer's claims are not in conflict with ours, but they bound the upside of
the static-DoM framing — if prompt-conditional probes win, F-2 is reframed.

**Related experiments:** P10-FE60, P10-FE61, P10-FE62, H-95, H-96, H-1, P10-FE1, P10-FE5

**Status:** TO TEST

## New claims

- None. Brief introduces no new quantitative claims into narrative docs;
  HyperSteer's reported numbers are referenced descriptively only, not asserted
  as project findings. No `validate_claims.py` entry needed.

## Sources consulted

- `~/topo-confidence/CLAUDE.md` (Headline numbers section) — default context.
- `~/topo-confidence/FINDINGS.md`, `~/topo-confidence/HYPOTHESES.md` — F-N / H-N
  one-liners via grep (default context).
- `~/topo-confidence/PAPER_INDEX.md` — status flags via grep (default context).
- `research-graph` `python query.py paper 2506.03292` — prior admission note.
- `research-graph` `python query.py future P10` — checked existing P10 FE IDs to
  pick non-colliding numbers (highest existing P10-FE5; chose 60-62).
- `research-graph` `python query.py future P11` — sanity check on P11 FE IDs (not
  the target pathway).
- `research-graph` `python query.py paper 2024.acl-long.828` /
  `python query.py paper 2408.05147` / `python query.py paper 2501.17148` —
  cross-paper graph membership check.
- `~/topo-confidence/HYPOTHESES.md` (tail, template + last H-22) — H-N
  numbering and template.
- `https://arxiv.org/abs/2506.03292` and `https://arxiv.org/pdf/2506.03292`
  via WebFetch — paper content.
