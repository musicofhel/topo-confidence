# Result brief — P11-FE181 (Token-probability baseline vs F-2 DoM)

Second Phase 2 ROI-10 anchor. Direct head-to-head: does sequence-likelihood
predict correctness as well as F-2's 0.7731 prefill-DoM AUROC? Tests the
"DoM is just restating output entropy" alternative explanation. Cached
scope: each per-problem NPZ stores scalar `mean_logprob`, so we test mean
and sum aggregations; min/product require fresh per-token logprobs and are
deferred (call out in scope_note).

## FE

```yaml
fe_id: P11-FE181
status: COMPLETED
outcome: "Token-probability baseline does NOT subsume F-2 prefill DoM. Mean log-prob AUROC = 0.6721 (1.5B) / 0.6336 (7B) — both materially below DoM 0.7731. Sum log-prob = 0.8478 (1.5B) / 0.8672 (7B) — appears stronger but is length-confounded (FE448 already established length-alone AUROC 0.7986). Joint [mean_logp, prefill_DoM_proj] = 0.7836, only +1pp over DoM alone — DoM carries geometric signal beyond what mean token-likelihood encodes."
result_json: pathway11_h100/token_prob/results.json
result_json_keys:
  - auroc_mean_logp_15b
  - auroc_sum_logp_15b
  - joint_meanlogp_dom_auroc_15b
  - auroc_mean_logp_7b
  - auroc_sum_logp_7b
  - tokenprob_beats_dom_15b
regen_cmd: "python pathway11_h100/token_prob/recompute_fe181.py"
```

## Result JSON verification

`pathway11_h100/token_prob/results.json` exists; declared keys are
top-level. Regen reuses scalar `mean_logprob` from per-problem NPZs at
`pathway8_layerwise/data/math500/problem_*.npz` (1.5B, 500 files) and
`pathway11_h100/data/math500_7b/problem_*.npz` (7B, 500 files); joint
calculation uses prefill cache at
`pathway11_h100/prefill_inversion/cache/m15b_prefill.npz` for the L19 prefill
DoM projection. Deterministic seed=9999. Sum aggregation = mean × seq_len
(scalar arithmetic on cached fields).

## EXPERIMENT_LOG entry

## EXP-49: P11-FE181 — Token-probability baseline (mean / sum log-prob) vs F-2 DoM
**Date:** 2026-05-01
**Status:** COMPLETE
**Motivated by:** A standing alternative explanation for F-2 has been
"prefill DoM is just a fancy way of measuring how confidently the model
generates the answer span — it's restating mean token-probability." If
true, DoM offers no novel geometric content over a free, model-internal
calibration signal. Cheapest direct head-to-head in scope: regenerate the
same OOF AUROC pipeline against `mean_t log P(y_t | y_<t, x)` on the
answer span (already cached as scalar `mean_logprob` in every per-problem
NPZ). Adding the joint test with prefill DoM separates "redundant" from
"complementary."
**Hypothesis:** F-2 is subsumed if `auroc_mean_logp_15b > 0.7731` AND
`joint_meanlogp_dom_auroc_15b ≈ auroc_mean_logp_15b` (DoM adds no
information beyond token-prob).
**What we actually tested:** Loaded scalar `mean_logprob` from 500 1.5B
NPZs and 500 7B NPZs. Computed (1) 5-fold stratified OOF DoM-style 1D
probe with fold-safe orientation on `mean_logprob`; (2) same probe on
`sum_logprob = mean_logprob × seq_len`; (3) joint OOF probe via
`Σ⁻¹(μ_pos − μ_neg)` on `[mean_logprob, prefill_DoM_proj_L19]` aligned with
the m15b_prefill cache. seed=9999.
**Key result (1.5B):** **mean_logp AUROC = 0.6721**; sum_logp AUROC =
**0.8478**; joint [mean_logp, DoM_proj] = **0.7836**. F-2 DoM (0.7731)
beats mean_logp by 10pp; the joint adds only ~1pp over DoM alone. Sum_logp
is structurally length-confounded — FE448 already established length-alone
AUROC 0.7986, and `sum_logp = mean_logp × seq_len`. The 7B numbers track
the 1.5B numbers (mean 0.6336, sum 0.8672), so the conclusion isn't
model-specific.
**Verdict:** F-2 NOT SUBSUMED by token-probability. The DoM direction
encodes correctness signal that mean log-likelihood does not access. Sum
log-prob's higher AUROC is a length artifact, not a calibration win.
**Changed our understanding of:** Two updates.
(1) F-2's "is this just output entropy?" counterargument is RETIRED.
mean_logp gives 0.67 — meaningfully below DoM. The geometry is real.
(2) Joint probe lifts only +1pp, suggesting the prefill DoM and mean
token-likelihood are *partially* redundant but each carries some unique
correctness information. Worth a follow-up that pulls per-token
min/product logprobs (which the cached scalar can't cover) — that's
gated on a fresh forward pass, deferred to next H100 session.
**Phase 2 status:** Second of 3 Phase 2 anchors complete. F-2 retains
geometric content. FE115 already added a Song-Zhong +3pp ceiling for F-2
prefill DoM (0.7705 → 0.8016 on residuals); FE181 confirms that ceiling is
not just "more length" — Song-Zhong residualization removes ctx_i, while
mean_logp is length-normalized — so the +3pp lift is *not* the same signal
that drives sum_logp's 0.8478.
**Files:** `pathway11_h100/token_prob/recompute_fe181.py` (regen, ~1 min
on CPU; scalar I/O dominated),
`pathway11_h100/token_prob/results.json` (output).
**Caveat:** Scope is mean and sum aggregations only — per-token min and
product would require either re-extracting per-token logprob arrays from
HF or running a fresh forward pass with `output_scores=True`. Deferred to
next H100 session per scope_note in results.json. The mean-only test
answers the headline "DoM = entropy?" question; min/product are
follow-ups, not gates.
**Depends on:** EXP-037 (F-2 anchor), EXP-44 (length partialing for the
sum_logp confound interpretation), EXP-48 (FE115 Song-Zhong context).
**Enables:** Closes the "DoM = output entropy" alternative on F-2's
PERSPECTIVES list. Per-token min/product extraction is the next natural
extension if and when fresh forward passes are budgeted.

## STATE.md last-experiment update

EXP-49 (P11-FE181 token-prob baseline — F-2 NOT subsumed by mean log-prob).
mean_logp AUROC 0.6721 (1.5B) / 0.6336 (7B) — both well below DoM 0.7731.
Joint [mean_logp, DoM] = 0.7836 — only +1pp over DoM alone. Sum log-prob
0.8478 is length-confounded. **Phase 2 second anchor complete; F-2 carries
geometric signal beyond token entropy.** Next: FE749 spectral α (running
in background; results pending).

## FINDINGS.md updates

### F-2: Prefill L19 DoM is a strong correctness predictor, *better than final-token*

```yaml
strength: MODERATE
status: ACTIVE
add_evidence: [P11-E49]
counterargument: "Length partial-correlation (FE448) remains the dominant counterargument: ~⅔ of AUROC is length-explainable. FE115 reveals an unexploited +3pp ceiling — Song-Zhong residualization (μ + pos_0 + ctx_i subtraction) lifts AUROC to 0.8016. The per-problem context vector ctx_i carries confound; this is consistent with FE448's length-R²≈1.0 finding. FE181 retires the 'DoM = output entropy' alternative — mean_logp AUROC 0.6721 ≪ DoM 0.7731."
overturned_by: "OOF residual AUROC ≤ 0.55 after length partialing (would push to INVALIDATED), OR a layer where length-residualized DoM AUROC matches raw within 0.02 (would restore STRONG)."
```

**Claim.** On Qwen-2.5-1.5B-Instruct, a 5-fold OOF logistic probe fit on
mean-pooled L19 activations at position 0 (last prompt token, pre-generation)
achieves **AUROC 0.7731**. Under Song-Zhong residualization (μ + pos_0 +
ctx_i subtraction, EXP-48), AUROC rises to **0.8016** (+3.1pp). Final-token
DoM AUROC is **0.7186** (headline) / 0.6603 raw on per-problem cache /
0.6865 on Song-Zhong residuals. Per-layer sweep (EXP-45): L19 sits on a
3-layer peak plateau (L19=0.7705, L20=0.7706, L21=0.7718). LOCO-CV (EXP-43):
mean 0.7427, worst 0.6032 (Number Theory). Within-topic CV (EXP-46): mean
0.7143, worst 0.6000 (Number Theory). After partialing predicted-length
from prefill (EXP-44), in-sample DoM AUROC drops 0.7834 → **0.6647**.
LEACE strict-null (EXP-47): AUROC collapses 0.7705 → 0.5000 — F-2 IS
purely linear. **Token-prob baseline (EXP-49):** mean log-prob AUROC =
**0.6721 (1.5B) / 0.6336 (7B)**, joint [mean_logp, prefill_DoM_proj] =
0.7836 (+1pp). DoM is not subsumed by sequence-likelihood.

**Strength:** MODERATE (signal real, linear, survives topic and within-topic
controls; ~⅔ of AUROC is length-explainable; FE115 reveals a +3pp ceiling
when context is removed; FE181 retires the "DoM = output entropy"
counterargument — DoM beats mean_logp by 10pp on 1.5B and 14pp on 7B).

**Evidence:** EXP-037, EXP-43, EXP-44, EXP-45, EXP-46, EXP-47, EXP-48;
**EXP-49** (`pathway11_h100/token_prob/results.json`: auroc_mean_logp_15b
0.6721, auroc_sum_logp_15b 0.8478, joint_meanlogp_dom_auroc_15b 0.7836,
7B parallels).

**Controls passed:** (existing 5 controls); **Song-Zhong purification
(EXP-48)** — residualization *strengthens* the probe; **token-prob baseline
(EXP-49)** — DoM strictly outperforms mean log-prob, joint adds only +1pp.

**Controls partially failed:**
- Length partial-correlation (EXP-44, FE448) — DoM AUROC drops 0.7834 →
  0.6647 after partialing predicted-length. Length is a major confound.

**Strongest counterargument:** Sequence length alone (AUROC 0.7986)
predicts correctness *better* than raw DoM at L19. Sum log-prob (EXP-49,
AUROC 0.8478) is similarly length-confounded — `sum_logp = mean_logp ×
seq_len`. Mean log-prob (length-normalized) gives only 0.6721. The
correctness signal has three correlated components: length, mean
likelihood (small), and the geometric DoM direction (dominant within the
length+likelihood-orthogonal subspace).

## New claims

- fe181-mean-logp-15b = 0.6721 (mean log-prob AUROC, 1.5B; below F-2 DoM 0.7731)
- fe181-sum-logp-15b = 0.8478 (sum log-prob AUROC, 1.5B; length-confounded, not a clean baseline)
- fe181-joint-meanlogp-dom-15b = 0.7836 (joint [mean_logp, prefill_DoM_proj] AUROC; +1pp over DoM alone)
- fe181-mean-logp-7b = 0.6336 (mean log-prob AUROC, 7B)
- fe181-sum-logp-7b = 0.8672 (sum log-prob AUROC, 7B)

## Sources

- PLAN_cheap_wins.md §"Phase 2 — ROI-10 anchors"
- pathway11_h100/token_prob/results.json (this experiment's output)
- pathway8_layerwise/data/math500/problem_*.npz (1.5B mean_logprob field)
- pathway11_h100/data/math500_7b/problem_*.npz (7B mean_logprob field)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (DoM_proj alignment for joint test)
- pathway11_h100/length_partial/results.json (FE448 length R²≈1.0 anchor)
