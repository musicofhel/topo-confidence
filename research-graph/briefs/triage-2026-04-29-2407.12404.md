# Triage brief — 2407.12404 — Analysing the Generalisation and Reliability of Steering Vectors

Source: https://arxiv.org/abs/2407.12404
Triaged: 2026-04-29

## Refutations

1. **F-2 / F-8 may be partly a spurious-factor predictor, not a correctness representation.** Tan et al. (Sec 5, Fig 4) show that mean-difference contrastive vectors extracted from multiple-choice activations are heavily contaminated by *steerability biases* — token preference (Yes/No) and position preference (A/B) — that survive standard data balancing and explain a large share of per-sample variance. Our prefill L19 DoM is structurally identical: mean(correct activations) − mean(incorrect activations) at a single residual-stream layer/token position. If MATH-500 correct vs incorrect generations covary with template-level spurious axes (answer length, boxed-answer token id, leading-digit prevalence, presence/absence of $\boxed{}$), AUROC 0.7731 may be mostly that spurious signal rather than a correctness representation. **Mechanism:** any contrastive direction inherits whatever feature distinguishes the two classes most linearly — Tan demonstrates that this feature is often template-level, not the named concept. **Falsifiable test:** decompose per-problem DoM scores against template features and report variance-explained à la Tan Fig 4 (FE-74). If >40% of variance is explained by template features, F-2's "L19 DoM = correctness representation" framing weakens to "L19 DoM = template+correctness mixture".

2. **F-3 cos(prefill_DoM, final_DoM) = 0.046 may be a prompt-distribution artifact, not a two-circuit signal.** Tan (Sec 6, Fig 7-8) finds that steering vectors are "mostly a property of the dataset, not the model" — the same model under different prompt distributions yields different effective directions, and SV cosine similarity correlates with prompt-distribution similarity (App E.3). Prefill activations are extracted under the question-only prompt distribution; final-token activations under the question+full-CoT distribution. These are two different prompt distributions in Tan's sense. **Mechanism:** if the residual stream encodes "stuff currently in context" linearly, two different contexts give two different DoM directions even if the underlying "correctness" concept is identical. **Falsifiable test:** re-extract DoM at the *same* token position (e.g. prefill) but under USER_POS vs SYS_POS prompt injections; if cos(prefill_BASE, prefill_SYS_POS) is also near zero, F-3's mechanistic "two distinct verifier circuits" interpretation collapses into "DoM rotates with any prompt change" (FE-76). H-17 (DoM rotation is RoPE-mechanical) becomes the more parsimonious explanation.

3. **H-1 per-position DoM steering on MATH-500 will likely show ≥30% anti-steered problems.** Tan's Fig 1 shows that for many MWE concepts, 30–50% of inputs are *anti-steerable* — adding the SV produces the opposite of the intended effect. If H-1 (per-position DoM steering on Qwen 1.5B at λ > 0 to flip incorrect→correct) inherits this property, the aggregate accuracy delta could be near zero or negative even when the SV "works" on a subset. **Mechanism:** the SV captures a mixture of (a) the intended concept and (b) spurious template factors that interact differently per input; for inputs where the spurious component dominates, sign of effect inverts. **Falsifiable test:** when H-1 runs, supplement aggregate K=1 accuracy at λ ∈ {−1.5,…,+1.5} with per-problem propensity-curve histograms (FE-75). If >30% of MATH-500 problems show negative steering slope at the same λ where the median problem shows positive slope, H-1 is dead-on-arrival as a *reliable* steering intervention even if aggregate accuracy looks neutral or mildly positive.

4. **(Bonus, against F-9 framing)** Tan documents "un-steerable behaviours" — entire concepts where propensity is flat in λ. F-9's claim is that CoE-60 is *redundant with* single-layer L19 DoM (i.e. they carry the same predictive signal). Tan motivates a sharper distinction: a clean linear *probe* (DoM AUROC 0.7731) does not entail a strong linear *intervention*. F-9's redundancy claim is therefore probe-side only, and shouldn't be cited as evidence that DoM "captures the causal correctness direction" — that would require a successful steering experiment, which Tan suggests may fail even when the probe works.

## Direct connections to F-N / H-N

- **F-2 (Prefill L19 DoM AUROC 0.7731, OOF 5-fold, 1.5B)**: Tan's central reliability critique applies directly. The MD direction we extract is methodologically identical to Tan's CAA SV up to label-source (we use correctness, they use behaviour-pair). Same per-input variance question applies. Read FINDINGS.md F-2 to confirm extraction protocol matches.

- **F-3 (cos(prefill_DoM, final_DoM) = 0.046)**: Tan's prompt-distribution-as-source-of-direction-variation (Sec 6) is the most natural alternative explanation to "two distinct circuits". Read FINDINGS.md F-3 for orthogonality interpretation.

- **F-8 (selective-prediction acc 71.6% at coverage 0.5)**: same exposure as F-2. If the underlying probe is partly spurious, the abstention boundary is also partly spurious — high-confidence "answered" set may over-represent template-clean problems, not correctness-decidable ones.

- **H-1 (per-position DoM steering moves MATH-500 accuracy)**: Tan's Fig 1 is the direct prior. Expected effect-size distribution should be reported, not just mean.

- **H-17 (DoM rotation is RoPE-mechanical, not computed)**: Tan's "SV rotates with prompt distribution" finding is corroborating evidence for a mechanical rather than computational interpretation of DoM rotation.

- **H-22 (abstract-CoT shows different breathing)**: Tan's "steerability is a dataset property" implies that abstract-CoT may show very different DoM directions even from the same model, providing a sharper test of representation universality than F-1 alone.

## Methodologies extracted

- **Per-sample steerability metric** — slope of mean logit-difference vs steering multiplier λ, computed *per input* rather than aggregate. *Replication cost*: 30 min CPU on cached `pathway11_h100/prefill_gated_compute/oof_scores.json` (no λ sweep needed for the analog — use per-problem decision margin in place of slope). *We'd plausibly run this*: yes — directly enables refutation test of F-2 by exposing per-problem variance hidden inside the AUROC 0.7731 number.

- **Anti-steerable fraction** — fraction of inputs where per-sample steering slope has opposite sign to median. *Replication cost*: trivial CPU once per-sample slopes exist. *We'd plausibly run this*: yes — drop-in metric for any future H-1 steering experiment.

- **Steerability-bias decomposition (Tan Fig 4)** — variance of per-sample steerability decomposed against template factors (token-id, position-id, balanced 4-cell). Reported as "variance explained by spurious factors" plus "marginal variance explained by Yes/No after removing A/B". *Replication cost*: 1 h CPU. *We'd plausibly run this*: yes — clean falsifier for the "DoM = correctness representation" framing.

- **Propensity-curve λ sweep** — evaluate propensity metric at λ ∈ {−1.5, −1.0, −0.5, 0, 0.5, 1.0, 1.5} (7 points) per input, fit OLS slope. *Replication cost*: 30 min H100 per (model, dataset, prompt-variant) cell once steering hook is wired. *We'd plausibly run this*: yes — required infrastructure for any DoM-based intervention experiment in pathway 11.

- **Logit-difference propensity (vs softmax probability)** — propensity = logit(pos) − logit(neg), no softmax. Argued more linear in activations. *Replication cost*: trivial. *We'd plausibly run this*: yes — better calibration target for selective prediction than current probability-based confidence.

- **Validation-split layer sweep** — choose steering layer by argmax aggregate steerability on a 10-sample val split. *Replication cost*: 1 h H100 per model. *We'd plausibly run this*: yes — provides a cheap sanity check on whether L19 is genuinely optimal for steering on Qwen 1.5B vs being inherited from probe-side optima.

- **Targeted prompt-injection distribution shift (BASE / SYS-POS / SYS-NEG / USER-POS / USER-NEG)** — controlled OOD via prepending instruction in either system or user role. Five cells defined in Tan's Table 1. *Replication cost*: trivial (just regenerate prompts; reuse cached extractor). *We'd plausibly run this*: yes — directly enables F-3 robustness test (FE-76).

- **Relative steerability** — `s_rel(v_A, D_B, Λ) = s(v_A, D_B, Λ) / s(v_B, D_B, Λ)`. Generalisation metric: ratio of cross-distribution to within-distribution steering slopes. *Replication cost*: trivial. *We'd plausibly run this*: yes once prompt-injection variants exist.

- **Cross-model dataset-property test (Llama vs Qwen Spearman ρ)** — Tan reports ρ=0.769 ID, ρ=0.586 OOD. *Replication cost*: ~2 h CPU once we have per-problem DoM scores from at least two models on overlapping problems. *We'd plausibly run this*: yes — Qwen 1.5B + 7B + Phi-3 + Llama cached data per F-1 makes this immediate.

## Approaches & framings

- **"Steerability is a dataset property, not a model property"** — reframes F-1 (universality of dimensional breathing across architectures). The cross-architecture universality may be a dataset/task universality (MATH-500 is a fixed benchmark) rather than a representation-substrate universality. Sharper test would compare breathing across *different* math benchmarks at fixed model.

- **Local-linearity vs global-linearity distinction (App B.1)** — Tan explicitly contrasts ID linearity (consistent with any continuous function locally) vs OOD linearity (the strong claim of LRH). All of F-2, F-3, F-9 are ID-only; none license the global-linearity claim. F-3's orthogonality in particular shouldn't be read as a global-geometric statement about the residual stream.

- **Aggregate-vs-per-input disaggregation as standard practice** — Tan's central methodological move is reporting per-sample slopes alongside aggregate steerability. Any future findings claim derived from a single mean (AUROC, accuracy delta, cos similarity) should now be paired with the per-problem distribution; the aggregate may be misleading.

- **Probe ≠ intervention** — Tan reinforces a distinction this project has not been crisp about: a clean linear probe (high AUROC for DoM) does not imply a working linear intervention. F-2/F-9 are probe results; H-1 is an intervention prediction. The two should be argued separately.

- **Steerability bias as a new bias type** — distinct from standard token/position bias on logits. Models can be more *steerable* toward outputs in particular positions even when their unsteered logit distribution is balanced. Implication for our extraction: the prefill-time DoM may bake in a "favoured-answer-position" bias from the prompt template even if the test-time accuracy is balanced.

## Datasets & benchmarks

- **Model-Written Evaluations (MWE) — 40 selected datasets** — 1000 samples per category, multiple-choice (A/B or Yes/No), 40-10-50 train-val-test split. HF-accessible (Anthropic release, Perez et al. 2212.09251). Applicable? No — multiple-choice persona/behaviour, not math correctness; but the *protocol* (40/10/50 split, contrastive prompts, λ sweep) is directly portable.
- **TruthfulQA** — 817 questions on truthfulness. Public. Applicable? No — Q/A truthfulness ≠ math correctness; cited only as canonical CAA benchmark.
- **Sycophancy + AI-risk subsets** (Anthropic release) — covered above.

## Implementation details worth capturing

- **Steering layer (fixed across all 40 datasets after val sweep)**: Llama-2-7b-Chat → layer 13; Qwen-1.5-14b-Chat → layer 21. Tan reports this is "remarkably consistent across many datasets" (matches Rimsky CAA). Sanity-check for our own setup: validate L19 on Qwen 1.5B is genuinely optimal *for steering* (not just for probing).
- **Steering applied at last-token position only**, at the chosen layer, in residual stream.
- **λ sweep**: 7 points, {−1.5, −1.0, −0.5, 0, 0.5, 1.0, 1.5}.
- **Aggregation**: mean-difference (MD), not PCA or LR. Tan notes PCA gives near-identical direction (cf. Tigges et al. 2310.15154 in their refs).
- **Propensity metric**: `logit(positive_token) − logit(negative_token)`, no softmax (more linear).
- **Steerability metric**: OLS slope of mean propensity vs λ over the 7-point sweep.
- **Random A/B + Yes/No assignment per example during extraction**, to avoid token-bias leakage. Note: still doesn't remove the steerability bias.
- **Train/val/test = 40/10/50 per dataset** — small training set, deliberate, focuses test on per-input variance.
- **Hardware**: A100 40GB.
- **Code**: https://github.com/dtch1997/steering-bench
- **OOD prompt injections**: see Tan Table 1 — system-vs-user prompt placement matters; both reported.

## Replicable intermediates

- **Per-problem decomposition of cached prefill L19 DoM scores against template features.** Inputs: `pathway11_h100/prefill_gated_compute/oof_scores.json` (per-problem OOF scores) × MATH-500 metadata (`data/math500.json` or equivalent). Compute per-problem template features: answer length (chars), boxed-token id (or absence), leading-digit-of-answer, problem-domain tag. Regress score on features; report variance explained by features alone vs by features + correctness. Produces the Tan-Fig-4 analog for our project. ~30 min CPU.

- **Fraction anti-classified** as proxy for "fraction anti-steerable". For each problem in OOF set, compute decision margin sign (= sign(score − threshold)). Compare to global majority sign per (correct, incorrect) class. Report fraction inverted. ~5 min CPU.

- **F-3 prompt-stability sanity check** at small N. Pick 50 cached MATH-500 problems for which we have full Stage 2 NPZs; compute prefill L19 mean activation under (a) BASE prompt, (b) BASE + USER_POS injection ("Be confident in your answer"), (c) BASE + SYS_POS variant. Compute cos(prefill_DoM_BASE, prefill_DoM_USER_POS) and (BASE, SYS_POS). If both ≪ 1 even within the same token position, F-3's two-circuits interpretation needs rephrasing. ~1 h H100 (or shorter if cached extractor reusable on 50 problems).

- **Cross-model per-problem score Spearman ρ** for Qwen 1.5B vs Qwen 7B on overlapping MATH-500 problems. Tan predicts ρ ≈ 0.7+ if "predictability is a dataset property". Trivial CPU once OOF scores from both models are in hand.

## Cross-paper signals

- **2306.03341** (Inference-Time Intervention, Li et al.) — already in graph (status: graphed; PAPER_INDEX status CONTRADICTED). Tan cites this as a foundational SV reference and extends the reliability critique. Strengthens the existing CONTRADICTED stance.
- **2308.10248** (Activation Addition / ActAdd, Turner et al.) — already in graph (graphed). Foundational SV method Tan builds on.
- **2310.06824** (Geometry of Truth, Marks & Tegmark) — already in graph (graphed). Tan cites as ID validation of LRH; their work pushes to OOD.
- **2311.03658** (LRH, Park et al.) — already in graph (graphed). Tan's App B.1 directly engages the local-vs-global linearity distinction.
- **2405.07987** (Platonic Representation Hypothesis, Huh et al.) — already in graph (TO TEST). Tan cites as motivation for "different models converge to similar ontologies" and uses cross-model steerability as evidence.
- **2312.06681** (CAA, Rimsky et al.) — already in graph (graphed; promotion rejected as duplicate of ActAdd). The exact protocol Tan uses.
- **2212.09251** (Model-Written Evaluations, Perez et al.) — NOT in graph; recommend admission. Underlies all 40 datasets in this paper and is the Anthropic-canonical persona-evaluation harness; if H-1 ever explores non-math behaviours, MWE is the obvious benchmark.
- **2310.01405** (Representation Engineering / RepE, Zou et al.) — NOT in graph; recommend admission. Foundational top-down representation-engineering paper Tan cites as parent framework alongside ActAdd. Should be admitted as canonical RepE+SV reference.
- **2311.06668** (In-context Vectors, Liu et al.) — NOT in graph; recommend admission. Alternative steering paradigm (latent-space steering for ICL); relevant to H-1 as a methodological alternative if straight CAA-style DoM steering fails per Tan's mechanism.
- **2404.03592** (ReFT, Wu et al.) — NOT in graph; recommend admission. Representation finetuning — the trainable analogue if inference-time SVs prove unreliable per Tan.
- **2310.15154** (Linear Representations of Sentiment, Tigges et al.) — NOT in graph; weaker recommendation. Cited by Tan as evidence that PCA ≈ MD for SV extraction.

## Proposed FutureExperiments

```yaml
- id: P11-FE74
  pathway: P11
  description: "Steerability-bias decomposition of prefill L19 DoM. Take cached oof_scores.json from pathway11_h100/prefill_gated_compute/, extract per-problem template features (answer length, boxed-token id, leading-digit of boxed answer, presence/absence of $\\boxed{}$ environment, prompt-template id), regress per-problem score against features, report variance-explained-by-template-features vs variance-explained-by-correctness. Compare to Tan Fig 4 — if template features explain >40% of per-problem score variance, the AUROC 0.7731 framing as 'correctness representation' weakens to 'template+correctness mixture'."
  rationale: "Tan demonstrates that mean-difference contrastive vectors over multiple-choice activations are heavily contaminated by spurious template biases that explain a large share of per-sample variance even after data balancing. Our prefill L19 DoM is structurally identical (mean(correct) − mean(incorrect)) and inherits the same risk. Decomposing now is the cheapest available falsifier of F-2's representation interpretation."
  trigger: "Tan et al. 2407.12404 Fig 4 variance decomposition; explicit prediction that any contrastive direction inherits template biases."
  status: READY
  priority: HIGH
  roi: 8
  cost: "30min CPU on cached pathway11_h100 OOF scores + MATH-500 metadata"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2407.12404"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE75
  pathway: P11
  description: "Per-problem propensity-curve disaggregation for any future H-1 steering run. When/if H-1 (per-position DoM steering on Qwen 1.5B for MATH-500) executes at λ ∈ {−1.5, −1.0, −0.5, 0, +0.5, +1.0, +1.5}, supplement aggregate K=1 accuracy delta with per-problem propensity histograms (Tan Fig 1 analog) and report 'fraction anti-steered' as a primary metric. Pre-register: if >30% of MATH-500 problems show steering slope of opposite sign to median, declare H-1 unreliable regardless of aggregate accuracy delta."
  rationale: "Tan Fig 1 shows 30–50% anti-steerable fractions for many MWE concepts despite balanced aggregate slopes. H-1 will inherit this risk; reporting only aggregate accuracy will obscure the failure mode. Pre-registering the per-problem distribution makes the experiment falsifiable rather than confirmatory."
  trigger: "Tan et al. 2407.12404 Sec 5 anti-steerable phenomenon; pre-registration is needed before H-1 launches to prevent post-hoc reframing."
  status: BLOCKED
  priority: HIGH
  roi: 7
  cost: "1h CPU post-hoc on H-1 outputs (incremental on top of H-1 GPU cost)"
  depends-on: [F-2, F-3]
  would-update: [F-2]
  triggered-by: ["2407.12404"]
  blocked-by: "H-1 not yet executed"
  blocked-by-experiment: ["P11-FE-H1-base"]

- id: P11-FE76
  pathway: P11
  description: "Prompt-injection robustness check on F-3 orthogonality. For ~50 cached MATH-500 problems, re-extract prefill L19 mean activations under three prompt variants: (a) BASE (current), (b) USER_POS (prepend 'Be confident in your reasoning' to user prompt), (c) SYS_POS (replace system prompt with same instruction). Compute cos(DoM_BASE, DoM_USER_POS) and cos(DoM_BASE, DoM_SYS_POS). Then re-do for final-token DoM. If cos(prefill_BASE, prefill_SYS_POS) is also near-zero (matching the current 0.046 prefill↔final number), F-3's 'two-distinct-verifier-circuits' interpretation collapses into 'DoM rotates with any prompt change' — corroborates H-17 (RoPE-mechanical rotation) and demotes F-3 from a mechanism finding to a prompt-distribution observation."
  rationale: "Tan Sec 6 + App E.3 finds SV cosine similarity correlates with prompt-distribution similarity; the same model under different prompt distributions yields different DoM directions even when the underlying concept is identical. Prefill-vs-final-token activations live under different prompt distributions (question-only vs question+full-CoT), so cos=0.046 may be Tan's distribution-shift effect rather than two distinct circuits."
  trigger: "Tan et al. 2407.12404 Sec 6 dataset-property finding + App E.3 cosine similarity result."
  status: READY
  priority: HIGH
  roi: 8
  cost: "1h H100 (50 problems × 3 prompt variants × 2 token positions, reuses Stage 2 extractor)"
  depends-on: [F-3]
  would-update: [F-3]
  triggered-by: ["2407.12404"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-72: Prefill L19 DoM AUROC has substantial spurious-template-factor share
**Priority:** HIGH
**Motivated by:** Tan et al. 2407.12404 (Sec 5, Fig 4) + F-2
**Test:** Decompose per-problem OOF scores from `pathway11_h100/prefill_gated_compute/oof_scores.json` against template features (answer length, boxed-token id, leading-digit of answer, $\boxed{}$ presence, prompt-template id). Report fraction of per-problem score variance explained by template features alone vs jointly with correctness label. ~30 min CPU.
**Requires:** CPU only, cached OOF scores + MATH-500 metadata.
**Would change:** If template features explain ≥40% of per-problem score variance, F-2's framing shifts from "L19 DoM = correctness representation" to "L19 DoM = template+correctness mixture", and downstream F-8 selective-prediction interpretation needs rephrasing. If <20%, F-2 strengthens significantly.
**Blocks:** H-1 framing — should not promote DoM steering as a "correctness intervention" until this decomposition is in.

### H-73: F-3 prefill⊥final-token orthogonality is prompt-distribution-driven, not two-circuit
**Priority:** HIGH
**Motivated by:** Tan et al. 2407.12404 (Sec 6 + App E.3) + F-3 + H-17
**Test:** Re-extract prefill and final-token L19 mean activations on 50 cached MATH-500 problems under three prompt variants (BASE, USER_POS, SYS_POS). Compute pairwise cos similarities. Predict: cos(prefill_BASE, prefill_SYS_POS) ≈ cos(prefill_BASE, final_BASE) ≈ 0.05 if prompt-distribution effect dominates; ≫ 0 if there really are two distinct circuits.
**Requires:** ~1 h H100, reuses Stage 2 extractor on 50 problems.
**Would change:** Confirm → F-3 demoted from "two-circuit" to "prompt-distribution-rotation" finding; H-17 (RoPE-mechanical) becomes the parsimonious explanation. Reject → F-3 strengthens substantially as a genuine two-circuit signal.
**Blocks:** nothing critical, but should run before any paper-write-up cites F-3 as a circuits result.

### H-74: H-1 per-position DoM steering on MATH-500 will produce ≥30% anti-steered problems
**Priority:** MEDIUM (pre-registration only — not a separate experiment)
**Motivated by:** Tan et al. 2407.12404 (Fig 1) + H-1
**Test:** Pre-registration. When H-1 runs, classify each MATH-500 problem as steered, neutral, or anti-steered based on sign of per-problem accuracy delta at the maximum-aggregate-effect λ. Predict ≥30% anti-steered (Tan-equivalent rate on MWE personas).
**Requires:** No additional GPU; piggy-backs on H-1 outputs.
**Would change:** Confirm (≥30% anti-steered) → H-1 cannot be claimed as a reliable intervention even if aggregate accuracy lifts; reframe as "DoM steering helps a subset, harms another subset". Reject (<10% anti-steered) → MATH-500 + correctness is a regime where SV reliability is much better than MWE personas, and Tan's critique doesn't transfer at full strength.
**Blocks:** premature claims of "DoM controls correctness".

## Proposed PAPER_INDEX.md classification

## 2407.12404 — Analysing the Generalisation and Reliability of Steering Vectors (Tan, Chanin, Lynch, Paige, Kanoulas, Garriga-Alonso, Kirk; NeurIPS 2024)

**Relevance:** Direct methodological critique of any contrastive-mean-difference (CAA-style) direction extracted from residual-stream activations — exactly the structure of our prefill L19 DoM. Reports per-input variance, anti-steerability, and dataset-vs-model decomposition that all transfer to F-2 / F-3 / F-8 / H-1. The most important non-replication-target paper in our SV neighbourhood: it doesn't tell us a number to match, it tells us a class of confound to test for.

**Key claim we tested:** Mean-difference contrastive directions extracted from MC-prompt activations exhibit (a) high per-input variance with substantial anti-steerable fractions, (b) spurious template-bias contamination that explains a large share of per-input variance, (c) generalisation that's mostly a dataset property rather than a model property, and (d) brittleness to OOD prompt injections. Our terms: any DoM-style probe/intervention extracted from a single (layer, token, prompt-distribution) cell may be a template+target mixture rather than a target representation, with the spurious component dominating per-input variance.

**Our result:** **TO TEST.** Three specific tests proposed: P11-FE74 (variance decomposition of prefill L19 DoM against template features — CPU-only, immediate), P11-FE76 (prompt-injection robustness check on F-3 cos=0.046 orthogonality — 1 h H100), and P11-FE75 + H-74 (pre-registration of anti-steered fraction for H-1 if/when run). Until FE-74 lands, F-2's "correctness representation" framing is provisional.

**Related experiments:** P11-FE74, P11-FE75, P11-FE76, H-1, H-17, H-72, H-73, H-74. Indirect: H-22 (abstract-CoT may give a different DoM at the same model — direct test of Tan's "dataset property" claim).

**Status:** TO TEST

## New claims

- **Quantitative:** Tan reports Spearman ρ = 0.891 (Llama) / 0.694 (Qwen) for ID↔OOD steerability, ρ = 0.769 (ID) / 0.586 (OOD) for cross-model (Llama↔Qwen) steerability, ρ = −0.26 (Llama) / −0.46 (Qwen) for propensity-similarity↔relative-steerability. These are *their* numbers, not ours. Promoting this brief into PAPER_INDEX.md introduces them into the narrative for context but does not require a `validate_claims.py` entry — they are not back-checkable against our committed JSONs.
- **No new project-side quantitative claims introduced.** All proposed FE / H entries are tests, not results. `validate_claims.py` does not need an update for promotion.

## Sources consulted

- `~/topo-confidence/CLAUDE.md` (headlines section only) — default context.
- `~/topo-confidence/FINDINGS.md` (one-line headers via grep only) — default context.
- `~/topo-confidence/HYPOTHESES.md` (one-line headers + template footer) — default context + template lookup.
- `~/topo-confidence/PAPER_INDEX.md` (status flags only via grep) — default context.
- `cd ~/topo-confidence/research-graph && python query.py paper 2407.12404` — prior admission note.
- `cd ~/topo-confidence/research-graph && python query.py paper {2310.01405, 2311.06668, 2404.03592, 2402.09236, 2308.10248, 2306.03341, 2310.06824, 2311.03658, 2405.07987, 2312.06681, 2212.09251}` — graph membership of cited papers.
- `cd ~/topo-confidence/research-graph && python query.py future P11` — next available P11-FE id.
- `~/topo-confidence/HYPOTHESES.md` (final ~80 lines) — to lift the H-N template and find next H-id (current max H-22).
- arXiv full-text of 2407.12404 via `mcp__arxiv__read_paper` — paper content (abstract, Sec 1–7, References, App A–D.4, Tables 1–3, Figs 1–9).
