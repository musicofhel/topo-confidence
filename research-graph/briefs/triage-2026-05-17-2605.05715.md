# Triage brief — 2605.05715 — Decodable but Not Corrected by Fixed Residual-Stream Linear Steering: Evidence from Medical LLM Failure Regimes

Source: https://arxiv.org/abs/2605.05715
Triaged: 2026-05-17

## Refutations

1. **Refutes H-1 (Per-position DoM steering moves MATH-500 accuracy)**: This paper demonstrates that a direction decodable at 71.6% accuracy (comparable to our DoM AUROC 0.7731) carries only 11.9% mode-specific variance — 88% is entangled with task-critical computation. Their 29 steering configurations across 5 families all produce Δ≈0pp. If our L19 DoM direction suffers similar entanglement (testable by computing specificity ratio on our cached activations), H-1 is likely dead: the DoM direction *predicts* but cannot *steer* because perturbing it damages task computation. Falsifiable by: compute specificity ratio of our DoM vs. a shared "incorrect-vs-correct" direction; if ratio < 0.2, H-1 is structurally blocked.

2. **Challenges F-2 (L19 prefill DoM predicts correctness at AUROC 0.7731) — specifically the *causal interpretation*)**: Their concept erasure experiment shows that erasing the OT direction (P=I−d̂d̂ᵀ) *damages* accuracy by 3.6pp (177 damages vs 131 corrections), demonstrating that decodability ≠ causal control. Our DoM direction achieves high AUROC but this paper provides a concrete mechanism (representational entanglement, within-class variance 2–4× inter-centroid gap) by which the same direction could be epiphenomenal — a statistical signature co-occurring with task computation rather than causally mediating correctness. Falsifiable by: apply concept erasure to our L19 DoM direction on cached activations and measure whether downstream logits degrade (would require fresh forward passes, so H100 experiment).

3. **Refutes H-1's implicit assumption that cos(DoM, PC1)=0.9216 makes DoM a viable steering target**: The paper shows refusal direction has specificity 0.999 and *successfully* steers (p=0.008), while OT direction has specificity 0.119 and fails despite comparable decode accuracy. The critical variable is specificity, not decode AUROC. Our DoM ≈ PC1 (cos=0.9216) means our DoM is the *dominant principal component* of the activation space — maximally entangled with task computation by construction. This is the worst possible profile for a steering vector. Falsifiable by: compute specificity ratio of our L19 DoM direction relative to Qwen-2.5-1.5B's overall correct/incorrect mean difference; predict ratio < 0.15 given cos(DoM, PC1) = 0.9216.

4. **Constrains F-8 (selective-prediction via prefill DoM at 50% coverage)**: Their selective abstention achieves AUROC 0.610 on held-out test (down from 0.721 in-sample 5-fold CV). Their 71.6% in-domain decode accuracy drops to 61.4% on cross-domain zero-shot transfer. This raises the question of whether our F-8 selective-prediction (71.6% answered accuracy at 50% coverage) is train-domain-overfit. Their GroupKFold shows <1pp drop, but cross-dataset transfer shows large decay. Our 5-fold OOF design partially addresses this, but cross-benchmark generalization (H-3, H-5) becomes more urgent. Falsifiable by: test our DoM-based selective predictor on a held-out benchmark (e.g. GSM8K) without refitting.

## Direct connections to F-N / H-N

- **F-2 (AUROC 0.7731)**: Their OT binary classification achieves 71.6% accuracy (AUROC 0.672 for binary OT detection). Our prefill DoM at 0.7731 is in the same performance class. The key connection: both demonstrate that a single linear direction in mid-layer residual streams captures correctness-relevant signal — but this paper adds the critical insight that decodability alone does not imply causal control.

- **F-3 (cos(prefill_DoM, final_DoM) = 0.046)**: Their prompt-end probing achieves only 54.4% vs last-token's 71.6% (cosine between prompt-end and last-token vectors ≤ 0.17). This mirrors our finding of near-orthogonality between prefill and final-token directions, but from a different angle: the failure signal is *not accessible* at the prefill position, only after generation completes. Our prefill DoM measures something fundamentally different — problem-geometry recognition before generation begins.

- **F-8 (selective-prediction at 50% coverage, 71.6% answered accuracy)**: Their selective abstention using hidden-state probes outperforms five uncertainty baselines (entropy, max-prob, semantic uncertainty, verbalized confidence, position-weighted max-prob) with AUROC 0.610 on held-out test. This independently validates F-8's approach (probe-based abstention > standard uncertainty). Their coverage-accuracy curve (+5.5pp at 60% coverage on held-out) is comparable in structure to our selective-prediction stack.

- **F-10 (PH null beyond Gaussian)**: Not directly connected, but their specificity analysis framework provides a mechanistic explanation for *why* adding topological summaries fails: if the predictive direction is already PC1 (maximally shared with task computation), any higher-order geometric feature is either redundant or orthogonal to the decodable signal.

- **H-1 (DoM steering)**: Direct negative evidence. Their 29-configuration exhaustive steering null is the strongest extant evidence against fixed-direction linear steering when the decodable direction has low specificity. The refusal control (specificity=0.999, successful steering) provides the critical contrast: only directions that are *not* entangled with task computation can steer.

## Methodologies extracted

- **Specificity ratio decomposition** — Decomposes a contrastive vector into mode-specific and shared components. Computes proportion of variance that is specific to the target failure mode vs. shared with all-class-incorrect-vs-correct. Provides a scalar diagnostic for whether a given direction is steerability-viable.
  *Replication cost*: 10min CPU (requires only cached activation vectors and correct/incorrect labels).
  *We'd plausibly run this*: yes — directly applicable to our DoM direction on cached NPZs. This is the key diagnostic for H-1 viability.

- **Within-class variance / inter-centroid ratio** — Measures structural limits of linear separability: if within-class scatter exceeds between-class centroid gap by N×, linear intervention displaces only 1/N of instances to the correct side.
  *Replication cost*: 5min CPU (standard numpy operations on cached activations).
  *We'd plausibly run this*: yes — immediately reveals whether our 0.7731 AUROC is operating near the linear ceiling or has headroom.

- **Linear Accessibility Profile (LAP)** — Measures gap between probe accuracy and logit-lens classification accuracy (here: 81.5% vs 58.6% = 0.23 gap). A large gap indicates the signal is representationally present but not output-aligned, predicting steering failure.
  *Replication cost*: 30min CPU for cached activations + unembedding matrix access (needs model weights loaded).
  *We'd plausibly run this*: no — requires fresh forward passes with logit lens hooks, not just cached L19 activations.

- **Shared-specific decomposition with permutation null** — 10,000 label shuffles to establish null specificity distribution, then tests whether observed specificity is significantly below null.
  *Replication cost*: 15min CPU.
  *We'd plausibly run this*: yes — standard permutation test on cached vectors.

- **TOST equivalence testing for steering nulls** — Two One-Sided Tests to formally bound steering effects within a pre-specified margin (they use ±3pp). Better statistical practice than simply reporting p>0.05 for null steering.
  *Replication cost*: trivial (2min CPU).
  *We'd plausibly run this*: yes — useful for formally establishing H-1 null if we ever run steering experiments.

- **Concept erasure (mean-diff projection)** — P=I−d̂d̂ᵀ applied to residual stream to remove a direction, measuring whether accuracy drops (indicating entanglement) or improves (indicating causal correction).
  *Replication cost*: 2h H100 (requires fresh forward passes through the model with modified activations).
  *We'd plausibly run this*: yes (H100 tier) — this is the definitive causal test for whether DoM is merely correlated or causally mediating.

## Approaches & framings

- **Observation-intervention asymmetry**: Probes extract *any* statistical regularity; steering requires *causally effective* perturbation. This is a fundamental distinction the field has conflated. Intersects F-2/H-1: our DoM may be on the "decodable but not steerable" side of this divide.

- **Specificity as the key variable for steering viability**: Not decode AUROC, not probe accuracy, but the proportion of direction variance that is target-specific vs. shared with task computation. This reframes H-1: before testing steering, compute specificity first as a go/no-go gate.

- **Dynamic vs. static mismatch**: Failure mode identity determined pre-generation but occurrence accumulates during reasoning. Static vectors cannot address dynamic phenomena. Partially relevant to our prefill/final orthogonality (F-3) — prefill DoM captures static pre-generation geometry, but correctness may emerge dynamically.

- **Three-tier intervention landscape** (from their cited Billa 2026): Linear steering works only for directions with high specificity (tier 1: refusal-like). Medium-specificity directions need learned non-linear adapters (tier 2: +2.8pp from their MLP). Low-specificity directions (tier 3: the bulk of task-entangled signals) are unreachable by any single-step intervention.

## Datasets & benchmarks

- **MedQA** — 10,178 medical exam questions, public (Jin et al. 2021). 34,532 traces with failure-mode annotations (KD/RCB/OT). Applicable? no — domain-specific, not directly usable for MATH-500 pipeline.

- **MMLU-STEM** — 300 questions, 18 subjects (Hendrycks et al. 2021), public. Applicable? partially — demonstrates cross-domain transfer of failure-mode probes. Could be used to test our DoM's cross-benchmark generalization (H-3, H-5).

## Implementation details worth capturing

- Contrastive vectors: mean(correct) − mean(failure_mode), L2-normalized. Applied additively via forward hooks.
- Specificity ratio computed as: project mode-specific contrastive vector onto shared incorrect-vs-correct direction; ratio = 1 − cos²(v_mode, v_shared).
- PCA-50 before logistic regression for probing (prevents overfitting in high-d space).
- GroupKFold by question prevents same-question traces from appearing in both train and test.
- Temperature 0.1 for evaluation, 0.8 for annotation — temperature mismatch ruled out as confound.
- Learned MLP adapter: 4096→64→4096 bottleneck with residual connection achieves +2.8pp (modest but only non-null intervention).
- Peak detection layer varies by failure mode (L17 for OT on Llama, L5 for OT on Qwen) — layer choice matters.

## Replicable intermediates

- **Specificity ratio of our DoM**: Compute cos(DoM, mean_incorrect − mean_correct) on cached L19 prefill activations. If > 0.85, confirms entanglement prediction. Script: load `pathway11_h100/prefill_gated_compute/` NPZs, compute mean correct/incorrect hidden states, project DoM onto that axis.
- **Within-class variance / inter-centroid ratio**: Compute Var(correct class) and Var(incorrect class) relative to ||mean_correct − mean_incorrect||². If ratio > 2, confirms structural limit applies to our AUROC 0.7731. Uses same cached NPZs.
- **PCA-50 probing comparison**: They report PCA-50 + logistic regression. We can compare against our full-1536-d L2-reg ceiling (0.7847) vs. PCA-50 version on same data to see if dimensionality reduction helps or hurts.

## Cross-paper signals

- 2306.03341 — already in graph (status: graphed). Their ITI method is one of the steering approaches this paper shows fails when specificity is low. Connection: strengthens CONTRADICTED status for ITI on long-CoT.
- 2306.03819 — already in graph (status: pending_triage). LEACE concept erasure applied here; their whitened variant shows −2.1pp (not significant).
- 2308.10248 — already in graph (status: pending_triage). Turner ActAdd is their "contrastive steering" family 1. Paper shows it produces Δ≈0 when specificity < 0.15.
- 2604.22271 — already in graph (status: pending_triage). Kumaran self-correction paper — complementary angle (post-hoc confidence signals vs. this paper's pre-generation probe).
- 2509.12886 — already in graph (status: graphed). Zhu "LLM Already Knows" — independent corroboration that initial hidden states predict correctness. This paper adds the critical negative: decodable ≠ steerable.

## Proposed FutureExperiments

### Tier 1 — Cheap local (5min–2hr, CPU only, ROI 5–10)

```yaml
- id: P11-FE130380
  pathway: P11
  description: "Compute specificity ratio of L19 prefill DoM direction: project DoM onto the shared incorrect-vs-correct mean-difference direction and measure proportion of DoM variance that is mode-specific vs shared with task computation"
  rationale: "Paper shows specificity ratio is the key diagnostic for steering viability. Our cos(DoM, PC1)=0.9216 predicts specificity < 0.15, which would formally close H-1 without requiring GPU steering experiments"
  trigger: "2605.05715 demonstrates 88% entanglement for comparable-AUROC probe direction"
  status: READY
  priority: CRITICAL
  roi: 9
  cost: "10min CPU"
  depends-on: [F-2, F-8]
  would-update: [F-2]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130381
  pathway: P11
  description: "Compute within-class variance to inter-centroid distance ratio for correct/incorrect L19 prefill activations. If ratio > 2, linear interventions are structurally limited regardless of decode AUROC"
  rationale: "Paper shows 2-4x ratio structurally caps linear steering. Combined with specificity < 0.15, this would fully explain the predict-but-not-steer regime our DoM likely occupies"
  trigger: "2605.05715 reports 2-4x within-class variance exceeding inter-centroid gap"
  status: READY
  priority: HIGH
  roi: 7
  cost: "5min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130382
  pathway: P11
  description: "PCA-50 + logistic regression probe on L19 prefill activations for correctness prediction, comparing against full-1536-d L2-reg ceiling (0.7847) and DoM AUROC (0.7731). Tests whether dimensionality reduction changes the picture"
  rationale: "Paper uses PCA-50 as standard preprocessing; if our AUROC barely changes with 50 components, confirms signal is low-rank and dominated by PC1 (as cos(DoM,PC1)=0.9216 already suggests)"
  trigger: "2605.05715 uses PCA-50 + logistic regression as standard probing pipeline"
  status: READY
  priority: MEDIUM
  roi: 5
  cost: "10min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130383
  pathway: P11
  description: "Permutation-null specificity test (10,000 shuffles): establish null distribution of specificity ratios under random correct/incorrect label assignment, test whether observed DoM specificity is significantly below null"
  rationale: "Paper uses permutation test to establish that low specificity is not expected by chance (their null mean=0.370, observed=0.119, p<0.0001). Same test on our DoM would establish statistical significance of the entanglement finding"
  trigger: "2605.05715 permutation framework directly applicable to cached data"
  status: READY
  priority: HIGH
  roi: 6
  cost: "15min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required (hours–days, high ROI only)

```yaml
- id: P11-FE130384
  pathway: P11
  description: "Concept erasure causal test: apply P=I-DoM*DoM^T to L19 residual stream during Qwen-2.5-1.5B forward passes on MATH-500, measure accuracy delta. If negative delta, confirms DoM is entangled with task computation (replicating their result). Compare against 10 random-direction erasures as control"
  rationale: "This is the definitive causal test for whether our DoM direction is merely correlated or causally mediating correctness computation. Paper shows -3.6pp for their direction. A negative result would close the causal interpretation of F-2 while preserving the predictive utility (F-8)"
  trigger: "2605.05715 concept erasure methodology with clear positive control"
  status: READY
  priority: HIGH
  roi: 8
  cost: "2h H100"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130385
  pathway: P11
  description: "Full steering null replication on Qwen-2.5-1.5B MATH-500: apply contrastive DoM steering at L19 with alpha in {0.5, 1.0, 1.5, 2.0, 3.0}, measure accuracy delta with TOST equivalence within +/-2.5pp. Positive control: use refusal direction (expect p<0.01)"
  rationale: "Direct test of H-1 using the paper's exhaustive methodology. With specificity ratio from P11-FE130380 as prior, this becomes confirmatory rather than exploratory. TOST framework properly tests the null rather than failing to reject"
  trigger: "2605.05715 provides the full steering protocol with TOST equivalence"
  status: READY
  priority: HIGH
  roi: 7
  cost: "4h H100"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.05715"]
  blocked-by: null
  blocked-by-experiment: [P11-FE130380]
```

## Proposed HYPOTHESES.md additions

### H-860: L19 prefill DoM direction has specificity ratio < 0.20 (>80% variance shared with task-critical computation)
**Priority:** CRITICAL
**Motivated by:** 2605.05715 + F-2
**Test:** Compute specificity ratio from cached NPZs: project DoM onto mean(incorrect) − mean(correct) direction, measure residual. 10min CPU.
**Requires:** CPU, cached L19 prefill activations (existing NPZs)
**Would change:** On confirm: H-1 (steering) is structurally blocked; F-2 interpretation shifts from "causal direction" to "statistical signature co-occurring with task computation." On reject (ratio > 0.5): H-1 remains viable and paper's result is domain/architecture-specific.
**Blocks:** H-1

### H-861: Fixed linear steering of the L19 DoM direction produces Δ ≈ 0pp on MATH-500 accuracy (TOST-equivalent within ±2.5pp)
**Priority:** HIGH
**Motivated by:** 2605.05715 + F-2 + H-1
**Test:** Contrastive steering at L19 with α ∈ {0.5, 1.0, 1.5, 3.0}, TOST equivalence. 4h H100.
**Requires:** H100, Qwen-2.5-1.5B, MATH-500 500-sample set
**Would change:** On confirm: H-1 formally closed. On reject (Δ > 2.5pp): paper's result is domain-specific, DoM steering viable.
**Blocks:** H-1

### H-862: Learned non-linear adapter (bottleneck MLP on L19 residual stream) achieves Δ > +2pp on MATH-500 accuracy where linear steering fails
**Priority:** MEDIUM
**Motivated by:** 2605.05715 (their MLP achieves +2.8pp, the only non-null intervention) + F-2
**Test:** Train 1536→64→1536 residual adapter on correct/incorrect prefill activations, evaluate downstream accuracy. 6h H100.
**Requires:** H100, training pipeline for adapter
**Would change:** On confirm: establishes that correctness signal IS causally accessible via non-linear intervention (Billa tier-2 regime). On reject: signal may be purely epiphenomenal.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2605.05715 — Decodable but Not Corrected by Fixed Residual-Stream Linear Steering: Evidence from Medical LLM Failure Regimes (Liu, 2026)

**Relevance:** Directly tests the decodability-steerability gap for residual-stream directions that predict LLM failure modes. Their "Overthinking" probe achieves 71.6% accuracy (comparable to our F-2 AUROC 0.7731), but all 29 linear steering configurations across 5 families produce Δ≈0pp. They identify representational entanglement (88% shared variance with task computation, specificity ratio 0.119) as the mechanism. Their selective-abstention result (AUROC 0.610 on held-out, beating 5 uncertainty baselines) independently validates F-8's approach. The specificity framework provides the missing diagnostic for H-1 steering viability.

**Key claim we tested:** Hidden-state probes that decode failure modes at ~70% accuracy cannot correct those failures via fixed linear steering when the decodable direction's specificity ratio is < 0.15.

**Our result:** **TO TEST** — Their framework predicts our DoM direction (cos(DoM, PC1)=0.9216) has specificity < 0.15, which would structurally block H-1. FE130380 (CPU, 10min) computes this directly. Their selective-abstention result independently validates F-8.

**Related experiments:** P11-FE130380, P11-FE130381, P11-FE130382, P11-FE130383, P11-FE130384, P11-FE130385, H-860, H-861, H-862

**Status:** TO TEST

## New claims

None — this brief introduces no new quantitative claims into narrative docs. All numbers cited are from the paper itself or existing headlines table values.

## Sources consulted

- ~/topo-confidence/CLAUDE.md (## Headline numbers section) — default context
- ~/topo-confidence/FINDINGS.md (grep only, F-N one-liners) — default context
- ~/topo-confidence/HYPOTHESES.md (grep only, H-N one-liners + Next ID) — default context
- ~/topo-confidence/PAPER_INDEX.md (status flags grep) — default context
- research-graph query.py paper 2605.05715 — admission note
- research-graph query.py future P11 — FE ID numbering
- research-graph query.py paper 2306.03341 — cross-paper check (ITI)
- research-graph query.py paper 2306.03819 — cross-paper check (LEACE)
- research-graph query.py paper 2308.10248 — cross-paper check (Turner ActAdd)
- research-graph query.py paper 2604.22271 — cross-paper check (Kumaran)
- research-graph query.py paper 2509.12886 — cross-paper check (Zhu)
