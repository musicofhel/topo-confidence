# Triage brief — 2501.09929 — Interpretable Steering of Large Language Models with Feature Guided Activation Additions

Source: https://arxiv.org/abs/2501.09929
Triaged: 2026-04-29

## Refutations

1. **CAA-is-weak refutes the implicit premise of H-1 / P10-FE1.** The project's planned per-position DoM steering experiment (H-1, P10-FE1) uses contrastive-pair difference-of-means at L19 as the steering vector — which FGAA classifies as CAA. FGAA reports CAA Behavioral-Coherence Score = 0.2201 on Gemma-2-2B vs FGAA = 0.4702 (≈2× gap), and CAA is the *worst* of the four methods tested on Gemma-2-9B as well (0.2729 vs FGAA 0.3979). Mechanism: dense polysemantic directions are dominated by features that fire on irrelevant tokens (FGAA's BOS-removal step is critical), so mass-mean steering bleeds into BOS/format features. **Falsifiable test**: when P10-FE1 runs the L19 mass-mean steering arm on MATH-500, if accuracy moves <1pp at any α before coherence collapses, this paper's CAA result predicted it. The project should add an FGAA-style arm to P10-FE1 rather than running CAA alone.

2. **Density filtering refutes the "L19 DoM is the right operator" framing of F-2.** F-2 reports prefill L19 DoM AUROC = 0.7731 OOF on Qwen-2.5-1.5B. FGAA shows that the *raw* SAE difference vector underperforms a density-and-BOS-filtered version by ≈2× BCS, with optimal n₁ ∈ [1,8] features (extreme sparsity). Mechanism: a small number of high-density features (ρ > 0.01) and BOS-firing features dominate the L1 norm of any difference vector, including DoM. If we ever train an SAE on L19 prefill (P10-FE5), the same filtering applied to a feature-decomposed correctness direction should move AUROC up — and if it does, the published 0.7731 is a *floor*, not the ceiling. **Falsifiable test**: after P10-FE5, recompute correctness AUROC using only features with ρ < 0.01 and non-BOS dominance; compare against 0.7731. If filtered AUROC > 0.79, raw DoM is mis-specified.

3. **Layer-12 steering finding refutes the F-9 generalization that "single mid-layer suffices".** F-9 says CoE-60 (multi-layer trajectory features) is redundant with single-layer L19 DoM on Qwen-2.5-1.5B at the *correctness* readout. FGAA finds layer 12 sufficient on Gemma-2-2B (26-layer model) for *behavioral* steering. But FGAA does not establish that *every* signal collapses to one layer — and Gemma-2-9B (42 layers) still uses layer 12, suggesting absolute-index rather than relative-depth localization. Mechanism: if behavioral and correctness signals live at *different* absolute layers (not relative depths), the F-9 "single-layer suffices" generalization is architecture-specific to Qwen-2.5-1.5B's 28-layer geometry. **Falsifiable test**: when P11-FE5 (DeepSeek-R1-Distill-Qwen-1.5B) and any Gemma run land, repeat the L19 vs CoE-60 comparison at multiple layers; if optimal layer shifts by >2 between architectures, F-9 is a per-model claim.

4. **FGAA's "compositional steering via OR" weakly refutes the F-3 orthogonality reading.** F-3 reports cos(prefill DoM, final-token DoM) = 0.046 on Qwen-2.5-1.5B and frames this as "prefill and final live in different subspaces". FGAA shows steering vectors for *different behaviors* (anger, conspiracy, French) can be composed in SAE feature space (paper's Section 5 on conjunctive steering) — i.e. orthogonal directions in feature space combine additively. If two behaviorally orthogonal directions still steer additively, the project's orthogonality interpretation (different *subspaces*) may collapse to "different sparse-feature supports within the same subspace". **Falsifiable test**: project both prefill DoM and final-token DoM into a hypothetical L19 SAE basis (P10-FE5); if their non-zero feature supports are disjoint but live in the same dense subspace, F-3's "different subspaces" framing needs softening.

## Direct connections to F-N / H-N

- **H-1 (per-position DoM steering moves MATH-500 accuracy)**: FGAA *is* the canonical interpretable-steering benchmark we should compare CAA-with-DoM against. Headline anchor: AUROC 0.7731 doesn't transfer to "steering moves accuracy" — FGAA's CAA BCS = 0.22 is the warning. Recommended: add FGAA arm to P10-FE1.
- **P10-FE5 (train SAE on L19 prefill)**: FGAA's filtering recipe (density + BOS + top-k) is the exact post-processing step P10-FE5 needs after SAE training. This brief upgrades P10-FE5 from "compute cosines" to "apply FGAA filter, then compute cosines and AUROC".
- **F-2 (prefill L19 DoM AUROC 0.7731)**: a density+BOS-filtered version may lift AUROC; or it may not, in which case the dense direction is genuinely the right operator (which would be a non-trivial finding given FGAA's behavior-task results).
- **P2-FE1 (re-derive steering vector at 1024-tok labels)**: FGAA argues the L1-normalized target outperforms L2; P2-FE1 should test both norms.
- **F-3 (cos = 0.046)**: FGAA shows behaviorally-distinct directions can still be feature-additive. Soften the "different subspaces" interpretation pending SAE basis test.

## Methodologies extracted

- **FGAA (Feature Guided Activation Additions)** — Compute SAE-encoded difference vector → filter (density ρ > 0.01 zeroed; BOS-dominant features removed; top n₁ positive + n₂ negative kept) → optimize a linear approximator `v_opt = (W·v_target)/||W·v_target||` against pre-trained Chalnev approximator.
  *Replication cost*: needs a trained SAE on Qwen-2.5-1.5B L19 (we don't have one); ≈4h H100 to train + 1h CPU to apply filter. P10-FE5 is the unblocker.
  *We'd plausibly run this*: yes — once P10-FE5 lands, FGAA filtering becomes a 1-hour CPU add-on.
- **Density filtering of SAE features (θ = 0.01)** — Zero out features whose activation density exceeds 1%.
  *Replication cost*: 10min CPU on a trained SAE.
  *We'd plausibly run this*: yes — useful even for the SAE we'd train under P10-FE5.
- **BOS-token feature removal** — Eliminate features whose strongest activation is on `<bos>`. Critical step; without it CAA-style methods fail.
  *Replication cost*: trivial post-processing.
  *We'd plausibly run this*: yes — and worth checking whether L19 DoM is contaminated by BOS-firing features even before SAE training (project DoM is computed on prefill but token positions weren't separately checked).
- **Behavioral-Coherence Score (BCS = B × C)** — Multiply per-completion behavior strength by coherence (each ∈ [0,1] from a judge model). Single scalar that penalizes capability collapse.
  *Replication cost*: 1h CPU (Claude/GPT judge of generated completions).
  *We'd plausibly run this*: yes — directly applies to evaluating the steering arm of P10-FE1; better metric than raw accuracy because it captures coherence collapse.
- **Linear approximator for steering** — Pre-trained `W` matrix that maps steering direction to expected effect; optimize `v_opt` to maximize cosine with desired effect.
  *Replication cost*: needs Chalnev's approximator to be trained for our model; ≈1d H100.
  *We'd plausibly run this*: no — only if FGAA arm shows huge gains over filtered-only.
- **L1 normalization of target vector** — Empirically beats L2 for SAE-feature steering targets.
  *Replication cost*: trivial.
  *We'd plausibly run this*: yes — cheap A/B for any DoM steering.
- **Steering-scale sweep (α ∈ [0, 300]), inflection at ~40** — Capability collapse curve characterization on MMLU/MMLU-Pro.
  *Replication cost*: 4h H100 to run MMLU at 8 alphas.
  *We'd plausibly run this*: yes — establishes our own collapse curve for L19 DoM and pins down whether 1.5B has a comparable inflection.

## Approaches & framings

- **Steering signals live in sparse SAE features, not dense residual directions**: reframes activation steering from "find the right linear direction" to "find the right *features* and recompose them". Intersects F-2/F-3: our DoM directions may be sparse-feature mixtures whose individual features have different correctness/temporal roles.
- **Behavior × coherence is the right metric, not behavior alone**: any steering-success claim that doesn't bound coherence is suspect. Applies to P10-FE1 evaluation design.
- **CAA as a strawman**: FGAA explicitly positions CAA (== mass-mean DoM steering) as the weakest of four steering families. If H-1 is framed as "CAA-style steering moves MATH-500", H-1 is asking the wrong question — should be reframed as "any steering technique up to FGAA moves MATH-500".

## Datasets & benchmarks

- **9 semantic steering tasks (anger, evangelist, conspiracy, French, London, love, praise, want-to-die, wedding)** — small open-set, behavioral. Not applicable to MATH-500/correctness regime (no correctness signal).
- **MMLU / MMLU-Pro** — used for capability-degradation curve. Applicable: yes — could establish project's own L19-DoM steering capability-degradation curve. License: open. Accessibility: HF.
- **GemmaScope SAEs (16,384 features, residual stream layer 12)** — pre-trained, open. Applicable: no — wrong base model (Gemma vs Qwen-2.5). We'd need our own SAE on Qwen-2.5-1.5B L19 (P10-FE5).

## Implementation details worth capturing

- Layer 12 residual stream for both Gemma-2-2B (26 layers) and Gemma-2-9B (42 layers) — absolute index, not relative depth.
- GemmaScope 16,384-feature SAE used directly.
- Density threshold θ = 0.01 (1% activation density cutoff).
- Optimal feature counts: n₁ ∈ [1, 8] positive, n₂ ≈ 0 (negative features degraded performance).
- Generation: 100 completions × 33 tokens each, prompt `<bos>I think`.
- α tested 0–300, capability inflection ~40, sharp collapse 50–150.
- L1 normalization of `v_target` before approximator step.
- Pre-trained effect approximators from Chalnev et al. 2411.02193.
- 9 main pages + 13 appendix; v3 dated April 2025; no code link in arxiv HTML.

## Replicable intermediates

- **BOS-feature contamination check on L19 DoM**: split prefill positions by token type (BOS, system, user, content) and recompute DoM separately per type. If BOS-position DoM accounts for most of the AUROC, F-2 is BOS-contaminated. Cached NPZs sufficient (`pathway11_h100/prefill_gated_compute/`); ≈1h CPU.
- **L1 vs L2 normalization of L19 DoM probe**: re-fit prefill probe with L1-normalized DoM target and compare AUROC vs the existing L2-normalized 0.7731. Cached NPZs sufficient; 30min CPU.
- **Capability-degradation envelope for L19 DoM**: when P10-FE1 runs, sweep α over [0, 300] and measure MMLU at each — should produce a curve to compare against FGAA's α=40 inflection point. Needs H100; ≈4h.

## Cross-paper signals

- 2411.02193 — NOT in graph; recommend admission. Chalnev et al. SAE-TS, FGAA's direct precursor, defines the linear approximator that FGAA optimizes against. Required reference for any FGAA replication.
- 2308.10248 — already in graph (status: graphed). Turner ActAdd, foundational.
- 2312.06681 — already in graph (CAA, Rimsky). FGAA's main baseline; FGAA shows CAA underperforms.
- 2306.03341 — already in graph (ITI). Sibling steering paper; project already cites as CONTRADICTED.
- 2509.18116 — already in graph (ALS). Cross-checks: ALS is layer-adaptive, FGAA is single-layer; both relevant to where steering should live.

## Proposed FutureExperiments

```yaml
- id: P10-FE60
  pathway: P10
  description: "Apply FGAA-style filtering recipe to L19 SAE features once P10-FE5 trains an SAE: zero density>0.01, remove BOS-dominant features, top n₁∈[1,8] positive features, L1-normalize. Recompute correctness AUROC on prefill probe and compare to raw L19 DoM AUROC=0.7731."
  rationale: "FGAA shows raw difference vectors (== CAA == DoM) underperform filtered SAE-feature directions by ≈2× BCS on behavioral tasks (CAA 0.22 vs FGAA 0.47 on Gemma-2-2B). If the same filtering lifts correctness AUROC, then 0.7731 is a floor not a ceiling, and our DoM direction is contaminated by high-density / BOS-firing features."
  trigger: "FGAA paper 2501.09929 + density+BOS filtering recipe"
  status: BLOCKED
  priority: HIGH
  roi: 7
  cost: "1h CPU after P10-FE5 lands"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2501.09929"]
  blocked-by: "P10-FE5 (SAE trained on L19 prefill)"
  blocked-by-experiment: ["P10-FE5"]
```

```yaml
- id: P10-FE61
  pathway: P10
  description: "Run BOS-feature contamination check on cached L19 prefill NPZs: split prefill positions by token type (BOS, system, user, content), compute DoM separately per type, evaluate AUROC of each. If BOS-position DoM dominates AUROC of the full prefill DoM, then F-2's headline number is BOS-contaminated and FGAA's BOS-removal step is needed even pre-SAE."
  rationale: "FGAA explicitly identifies BOS-token-firing features as a critical confound that CAA fails to remove; their BOS-removal step is what separates FGAA from CAA. Project hasn't checked whether L19 prefill DoM is dominated by BOS-firing positions. Cheap sanity check; if positive, would force re-statement of F-2."
  trigger: "FGAA paper 2501.09929 + BOS-removal step necessity"
  status: READY
  priority: HIGH
  roi: 8
  cost: "1h CPU on cached pathway11_h100/ NPZs"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2501.09929"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P10-FE62
  pathway: P10
  description: "Add FGAA arm to P10-FE1 steering experiment: alongside (a) probe-weight, (b) mass-mean DoM (== CAA), (c) Sinii bias, add (d) FGAA-filtered DoM (density-filtered, BOS-removed top-k features). Sweep α∈[0,300], measure MATH-500 accuracy + Behavioral-Coherence Score (BCS) per arm."
  rationale: "FGAA reports CAA underperforms FGAA by ≈2× BCS on Gemma. P10-FE1 currently runs CAA-style mass-mean as one arm; if FGAA result transfers, CAA arm will move accuracy <1pp before coherence collapse and we'll wrongly conclude steering doesn't work. Adding FGAA arm hedges against the false-negative."
  trigger: "FGAA paper 2501.09929 + CAA-vs-FGAA gap"
  status: BLOCKED
  priority: HIGH
  roi: 8
  cost: "1d H100 for steering pass + 4h CPU for BCS judge"
  depends-on: [F-2, F-3]
  would-update: [F-3]
  triggered-by: ["2501.09929"]
  blocked-by: "P10-FE5 (SAE) and H100 pod for steering generation"
  blocked-by-experiment: ["P10-FE5", "P10-FE1"]
```

## Proposed HYPOTHESES.md additions

### H-95: BOS-token-position prefill activations dominate L19 DoM AUROC
**Priority:** HIGH
**Motivated by:** 2501.09929 + F-2
**Test:** Split MATH-500 prefill positions by token type (BOS, system header, user content). Compute DoM separately per token-type subset and evaluate correctness AUROC of each subset's DoM. If BOS-only subset achieves ≥ 0.70 AUROC and content-only subset is much lower, F-2's 0.7731 is a BOS-contamination artifact. ≈1h CPU on cached `pathway11_h100/prefill_gated_compute/` NPZs.
**Requires:** CPU only; cached NPZs.
**Would change:** On confirm, F-2 narrative needs "AUROC is partly carried by `<bos>` token activations, which is a known SAE-steering confound (FGAA 2501.09929)". On reject, F-2 strengthens (DoM is *not* a BOS artifact).
**Blocks:** P10-FE60, P10-FE62

### H-96: FGAA-filtered SAE features at L19 lift correctness AUROC above 0.7731
**Priority:** MEDIUM
**Motivated by:** 2501.09929 + P10-FE5 + F-2
**Test:** After P10-FE5 trains an SAE on L19 prefill, apply FGAA recipe: zero density>0.01, remove BOS-dominant features, retain top n₁∈[1,8] positive features, L1-normalize. Recompute correctness probe with filtered SAE-feature direction. AUROC > 0.79 confirms; AUROC ≤ 0.78 rejects.
**Requires:** SAE trained on L19 (P10-FE5 unblocker, ≈4h H100), then 1h CPU.
**Would change:** On confirm, F-2's 0.7731 is a floor; project should switch to FGAA-filtered direction as canonical correctness probe. On reject, raw DoM is genuinely the right operator (non-trivial result given FGAA's behavior-task gains).
**Blocks:** any future selective-prediction headline replaces 0.7731 with filtered AUROC.

## Proposed PAPER_INDEX.md classification

## 2501.09929 — Interpretable Steering of Large Language Models with Feature Guided Activation Additions (Soo, Guang, Teng, Balaganesh, Guoxian, Ming, 2025)

**Relevance:** FGAA is the strongest interpretable-steering baseline against which the project's planned per-position DoM steering experiments (H-1, P10-FE1) need to be evaluated. The paper's most actionable contribution for our regime is its decomposition of "why CAA-style mass-mean steering underperforms": dense directions are contaminated by high-density polysemantic features and BOS-firing features. Two of those failure modes are testable on our cached L19 prefill NPZs without any new generation, and the third (SAE-feature filtering) becomes a 1-hour add-on once P10-FE5 trains a Qwen-2.5-1.5B L19 SAE.

**Key claim we tested:** Filtered SAE-feature steering vectors outperform raw mass-mean (CAA == DoM) by ≈2× BCS on Gemma-2-2B/9B; BOS-token-firing features are a critical confound CAA fails to remove; layer-12 single-layer steering suffices for behavioral tasks on both Gemma sizes.

**Our result:** **TO TEST.** No replication run yet. Three experiments queued: P10-FE61 (BOS-contamination check on cached NPZs, 1h CPU, ready now), P10-FE60 (FGAA filtering on L19 SAE, blocked on P10-FE5), P10-FE62 (FGAA arm added to P10-FE1 steering experiment, blocked on H100 pod + P10-FE5). The paper's CAA-underperforms-FGAA result is a strong prior that H-1 with naive CAA steering will fail, and P10-FE1 needs an FGAA arm to avoid a false-negative on H-1.

**Related experiments:** P10-FE60, P10-FE61, P10-FE62, P10-FE1, P10-FE5, P2-FE1, H-1, H-95, H-96

**Status:** TO TEST

## New claims

- "FGAA CAA Behavioral-Coherence Score = 0.2201 vs FGAA = 0.4702 on Gemma-2-2B (≈2× gap), CAA = 0.2729 vs FGAA = 0.3979 on Gemma-2-9B." — cited from 2501.09929; if used in narrative docs, add Claim entry to validate_claims.py with source = paper.
- "FGAA capability inflection at α≈40, sharp collapse 50–150 on MMLU/MMLU-Pro." — cited from 2501.09929; if invoked when reporting our own L19 DoM degradation envelope, add Claim entry.

(Brief introduces no project-internal quantitative claims; only quotes from the paper. No `validate_claims.py` update strictly required unless these numbers move into FINDINGS.md / HYPOTHESES.md narrative text.)

## Sources consulted

- `~/topo-confidence/CLAUDE.md` (headlines section only) — default context
- `~/topo-confidence/FINDINGS.md` (F-N one-liners only via grep) — default context
- `~/topo-confidence/HYPOTHESES.md` (H-N one-liners only via grep) — default context
- `~/topo-confidence/PAPER_INDEX.md` (status flags only via grep) — default context
- `query.py paper 2501.09929` — prior admission note
- `query.py future P11 / P10 / P2` — current FE numbering for collision-avoidance
- `query.py paper 2411.02193 / 2308.10248 / 2312.06681 / 2306.03341` — cross-paper signals
- arxiv abstract page + HTML rendering of v3 — paper content (PDF binary fetch failed, fell back to HTML)
- No on-demand reads of F-N or H-N bodies were necessary — refutations and connections derivable from one-liners + headlines table.
