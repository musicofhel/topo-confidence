# Result brief — P11-FE448 (Length partial-correlation on F-2)

Direct refutation experiment for F-2: if prefill L19 carries length information
that drives the AUROC, residualizing prefill against predicted-length should
collapse DoM AUROC toward 0.5. Thresholds (per FE448 spec): residual ≤ 0.55 ⇒
length artifact; > 0.65 ⇒ survives partialing.

## FE

```yaml
fe_id: P11-FE448
status: COMPLETED
outcome: "Length R² from L19 prefill = ~1.0 (in-sample, n<d overfit). Seq-length alone AUROC 0.7986 — STRONGER than raw DoM (in-sample 0.7834, OOF 0.7731). DoM AUROC after partialing out predicted-length collapses from 0.7834 to 0.6647 (12pp drop). Result is in the gray zone — neither cleanly refuted (≤ 0.55) nor cleanly survives (> 0.65). F-2 demoted from STRONG to MODERATE: signal is real but ~⅔ of the AUROC is length-explainable."
result_json: pathway11_h100/length_partial/results.json
result_json_keys:
  - length_r2_from_prefill
  - auroc_raw_in_sample
  - auroc_residualized
  - auroc_drop
  - auroc_length_alone
  - auroc_predicted_length_alone
  - cos_dom_raw_vs_resid
  - f2_length_artifact
  - f2_survives_partialing
regen_cmd: "python pathway11_h100/length_partial/recompute_fe448.py"
```

## Result JSON verification

`pathway11_h100/length_partial/results.json` exists; declared keys are
top-level. Regen recomputes from the same `m15b_prefill.npz` cache as
FE719 — deterministic, n=500 d=1536, ridge α=1e-3.

## EXPERIMENT_LOG entry

## EXP-44: P11-FE448 — Length partial-correlation control on F-2 prefill L19 DoM
**Date:** 2026-05-01
**Status:** COMPLETE
**Motivated by:** Su et al. 2505.00127 (Between Underthinking and
Overthinking) plus the concern that prefill = question-only tokens may
carry a length-prior. F-9 already showed the 44-feature ABC pipeline has
length confounds; FE448 tests whether the *raw L19 activations* themselves
encode length to a degree that explains F-2.
**Hypothesis:** If L19 prefill encodes mostly "predicted output length"
and length predicts correctness, the headline DoM result is doubly
explained by a non-geometric variable. Refutation: residual AUROC after
partialing predicted-length ≤ 0.55. Survives: > 0.65.
**What we actually tested:** (1) Ridge regression of `seq_len` on L19
prefill activations (n=500, d=1536, α=1e-3), getting predicted length
ŝ. (2) Computed R² of (s, ŝ). (3) Per-feature Frisch-Waugh
residualization: X_resid[:,j] = X[:,j] − αⱼ ŝ_centered. (4) DoM AUROC
on X_resid vs raw X, both in-sample on n=500.
**Key result:** length R² ≈ 1.0 (in-sample, n<d ridge near-interpolates).
Sequence length alone has AUROC 0.7986 vs `correct` — *higher* than raw
DoM in-sample (0.7834) and the headline 5-fold OOF (0.7731). Predicted
length alone has AUROC 0.7994. After partialing predicted-length out of
prefill, DoM AUROC drops 0.7834 → 0.6647 (12pp drop). cos(DoM_raw,
DoM_resid) = 0.785 — the residualized direction still aligns ~79% with
raw, so the projection isn't degenerate.
**Verdict:** F-2 GRAY ZONE — neither refuted (residual 0.6647 > 0.55)
nor cleanly surviving (residual 0.6647 ≤ 0.65). Demote F-2 from STRONG
→ MODERATE. The length confound is large enough that ≈⅔ of the original
AUROC is length-explainable.
**Changed our understanding of:** F-2's strength claim. The prefill DoM
is partly a correctness direction *and* partly a length-predictive
direction; the two share a near-perfect linear subspace at L19. Pure
"correctness geometry" framing is too strong. Combined with FE719 (LOCO
mean 0.7427), F-2 is showing consistent erosion under cheap controls but
hasn't fallen below 0.5 — a real signal contaminated by length.
**Phase 1 decision branch status:** 1 of 5 cheap-controls below AUROC
0.70 (FE448 residualized). Need ≥3 to trigger demotion + Phase-3 skip.
Continue Phase 1 (FE145, FE299, FE101) before pivoting.
**Files:** `pathway11_h100/length_partial/recompute_fe448.py` (recompute,
deterministic), `pathway11_h100/length_partial/results.json` (output).
**Caveat:** Length R² ≈ 1.0 is partly a ridge-overfit artifact at n<d.
A proper OOF length-prediction R² would be lower; the in-sample residual
AUROC 0.6647 is therefore an *upper bound* on the contamination — true
OOF residual would likely be lower still.
**Depends on:** EXP-037, EXP-43.
**Enables:** Cleaner FE145 interpretation (per-layer DoM may pick up
length-predictive layers); motivates FE299 (topic-stratified AUROC) and
a possible follow-up FE on per-layer length-predictability.

## STATE.md last-experiment update

EXP-44 (P11-FE448 length partial-correlation — F-2 demoted STRONG →
MODERATE). L19 prefill near-perfectly encodes seq_len (R² ≈ 1.0,
in-sample); seq_len alone AUROC 0.7986 *exceeds* raw DoM 0.7834.
After partialing predicted-length, DoM AUROC drops to 0.6647 — gray
zone (refutation threshold 0.55, survival 0.65). Phase 1 decision-branch
counter: 1 of 5 below 0.70. Continue Phase 1.

## FINDINGS.md updates

### F-2: Prefill L19 DoM is a strong correctness predictor, *better than final-token*

```yaml
strength: MODERATE
status: ACTIVE
add_evidence: [P11-E44]
counterargument: "FE448 (EXP-44): L19 prefill encodes seq_len near-perfectly (R² ≈ 1.0 in-sample); seq_len alone AUROC 0.7986 exceeds raw DoM 0.7834. After partialing predicted-length, DoM AUROC drops to 0.6647 — in the gray zone between length-artifact (≤0.55) and survives-partialing (>0.65). ~⅔ of the original AUROC is length-explainable."
overturned_by: "OOF residual AUROC ≤ 0.55, OR a layer where length-residualized DoM AUROC matches raw within 0.02 (would restore the 'pure correctness direction' framing)."
```

**Claim.** On Qwen-2.5-1.5B-Instruct, a 5-fold OOF logistic probe fit on
mean-pooled L19 activations at position 0 (last prompt token, pre-generation)
achieves **AUROC 0.7731** for K=1-correctness. Final-token DoM AUROC is
**0.7186**. Under leave-one-subject-out CV across the 7 MATH-500 subjects
(EXP-43), mean AUROC is **0.7427**. After partialing out predicted-length
from prefill (EXP-44, FE448), in-sample DoM AUROC drops from 0.7834 to
**0.6647** — gray zone between length-artifact (≤0.55) and survives-
partialing (>0.65). The 7B shows the same prefill-pattern with a larger
gap to final-token (~0.876 vs ~0.77).

**Strength:** MODERATE (signal is real but ~⅔ of the AUROC is
length-explainable; demoted from STRONG after FE448).

**Evidence:** EXP-037
(`pathway11_h100/prefill_gated_compute/results.json` fields
`prefill_DoM_auroc_oof` = 0.7731, `final_token_DoM_auroc_oof` = 0.7186).
EXP-43 (`pathway11_h100/loco_subject/results.json`: LOCO mean 0.7427,
worst 0.6032). EXP-44
(`pathway11_h100/length_partial/results.json`: length R² 1.0,
auroc_length_alone 0.7986, auroc_residualized 0.6647).

**Controls passed:**
- 5-fold stratified OOF (seed 9999) — not a test/train leak.
- Leave-One-Subject-Out CV (EXP-43) — mean AUROC 0.7427, worst 0.6032
  (Number Theory). Subject-stratified leakage is small (~3pp).

**Controls partially failed:**
- Length partial-correlation (EXP-44, FE448) — DoM AUROC drops 0.7834 →
  0.6647 after partialing predicted-length. Not refuted (0.6647 > 0.55)
  but does not survive partialing (0.6647 ≤ 0.65). Length is a major
  confound at L19, not just at the engineered-feature level (F-9).

**Strongest counterargument:** Sequence length alone (AUROC 0.7986)
predicts correctness *better* than raw DoM at L19. The DoM direction may
be largely re-discovering "this prompt will get a short / long
generation" — which itself proxies for problem familiarity. Pure
"correctness geometry" framing is too strong.

## New claims

- fe448-length-r2 = 1.0 (FE448 length R² from L19 prefill, in-sample, n<d ridge)
- fe448-auroc-resid = 0.6647 (FE448 DoM AUROC after partialing predicted-length)
- fe448-auroc-length-alone = 0.7986 (FE448 seq_len-alone AUROC, exceeds raw DoM)
- fe448-auroc-drop = 0.1187 (FE448 in-sample AUROC drop from raw to residualized)

## Sources

- PLAN_cheap_wins.md §"Phase 1: Sub-1h CPU sanity battery"
- 2505.00127 (Su et al., Between Underthinking and Overthinking — length-prior trigger)
- pathway11_h100/length_partial/results.json (this experiment's output)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (input cache)
