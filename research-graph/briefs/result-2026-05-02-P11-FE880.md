# Result brief — P11-FE880 (PH features on PC1-residualized L19 clouds vs matched-cov Gaussian null)

FE291 follow-up #1, paired with FE881 (cov-spectrum) and FE882
(decomposition triangle). FE291 (EXP-55) grounded F-2's covariance
pathway as a single direction: supervised DoM ≈ unsupervised PC1
(cosine 0.922, 85% DoM-energy in PC1, 14.7% of L19 prefill variance).
F-10's "PH = matched-cov Gaussian null" then has an obvious sharper
test: project out PC1 from each per-problem L19 trajectory cloud
(`x' = x − (x·pc1)·pc1`), recompute the 5 V-R PH features on the
residualized clouds, and compare to a 100-sample matched-cov Gaussian
null on the *same* residualized covariance.

**Result.** On the PC1-residualized L19 trajectory clouds (500
problems), 5-fold OOF logistic AUROC: real PH = **0.6884** (cv5
0.6945 ± 0.021), matched-cov Gaussian null PH = **0.7628** (cv5
0.7658 ± 0.057), gap = real − null = **−0.074**. The matched-cov
Gaussian null on the residualized covariance beats real PH by 7.4 pp.

F-10's "would be overturned by" clause was: "PH features computed on
the [PC2..PC1536] subspace (PC1-residualized clouds) showing
real_AUROC ≥ null + 0.05." FE880 tests exactly that — and the gap
is *more negative* than the raw-cloud baseline (FE026 gap −0.003;
FE116 zigzag-residualized gap −0.067). F-10 *strengthens further*:
not only do PH summaries fail to beat a Gaussian null on raw L19
clouds, they fail *worse* on the PC1-residualized covariance
ellipsoid where the null retains second-order structure that PH
descriptors integrate over.

## FE

```yaml
fe_id: P11-FE880
status: COMPLETED
outcome: "On 500 MATH-500 PC1-residualized L19 trajectory clouds (PC1 from FE291 m15b_prefill SVD, var_share 0.1474), 5-fold OOF logistic AUROC for the 5 V-R PH features: real = 0.6884 (cv5 0.6945 ± 0.021), matched-cov Gaussian null = 0.7628 (cv5 0.7658 ± 0.057), gap = real − null = −0.074. The matched-cov Gaussian null on the residualized covariance beats real PH features by 7.4 pp — a tighter, more falsifiable form of F-10 than the raw-cloud −0.003 gap. Confirms F-10's 'topology adds no signal beyond covariance': PH summaries layered on a covariance ellipsoid carry NEGATIVE signal once the dominant correctness direction is removed. Per-feature comparison shows H1_max_lifetime real (8.53) > null (5.30) — so PH does pick up *something* second-order — but the rest of the feature vector (H0/H1 entropies, H1 count) is dominated by null structure, and the joint OOF logistic ranks the null vector higher. The signal that survives PC1 residualization lives in the *full eigenvalue spectrum* of the residualized cov (FE881: top-20 log-eigvals OOF AUROC = 0.7928), not in the PH descriptor family."
result_json: pathway11_h100/ph_residuals/pc1_resid_results.json
result_json_keys:
  - classifiers.real_ph_pc1resid.auroc_oof
  - classifiers.null_ph_pc1resid.auroc_oof
  - pc1_var_share
  - feature_comparison
regen_cmd: "python pathway11_h100/ph_residuals/recompute_pc1_resid_ph.py"
```

## Result JSON verification

`pathway11_h100/ph_residuals/pc1_resid_results.json` exists; declared
keys (`classifiers.real_ph_pc1resid.auroc_oof`,
`classifiers.null_ph_pc1resid.auroc_oof`, `pc1_var_share`,
`feature_comparison`) are present. Regen reads
`pathway11_h100/prefill_inversion/cache/m15b_prefill.npz` (500 × 1536
fp16 prefill at L19, deriving FE291 PC1 in sub-second SVD) and the 500
cached pathway8 trajectory NPZs at
`pathway8_layerwise/data/math500/problem_*.npz`. Walltime **~13 min**
cold disk (PH compute on 500 residualized clouds × 100 null subsamples
each × 5 features). Deterministic seed=42.

## EXPERIMENT_LOG entry

## EXP-56: P11-FE880 — PH features on PC1-residualized L19 clouds vs matched-cov Gaussian null
**Date:** 2026-05-02
**Status:** COMPLETE
**Motivated by:** EXP-55 (FE291) grounded F-2's covariance pathway as
a single direction: supervised DoM ≈ unsupervised PC1 (cosine 0.922,
14.7% of variance, 85% of DoM-energy in PC1). FINDINGS.md F-10's
"would be overturned by" clause read: "PH features computed on the
[PC2..PC1536] subspace (PC1-residualized clouds) showing real_AUROC
≥ null + 0.05." This experiment tests that condition directly.
**Hypothesis pre-registered:** F-10's narrow form ("PH = matched-cov
Gaussian null") would be overturned if PC1-residualized PH gives
real ≥ null + 0.05. F-10 strengthens if real < null on the same
residualized covariance.
**What we actually tested:** for each of 500 MATH-500 problems, load
the cached L19 trajectory cloud from
`pathway8_layerwise/data/math500/problem_*.npz`. Project out the
FE291 PC1 direction: `x' = x − (x·pc1)·pc1`. Compute 5 V-R PH
features on `x'` (subsampled to 100 points): H0_max_lifetime,
H0_entropy, H1_pers_entropy, H1_n_features, H1_max_lifetime. Sample
5 matched-cov Gaussian null clouds (same per-problem mean, full cov
of `x'`, same n=100 subsample) and compute the same 5 PH features
on each null draw, average. 5-fold StratifiedKFold OOF logistic
regression (with PCA-45 + standardize pipeline) on real PH and null
PH, separately.
**Key results:**
  | Probe                                 | OOF AUROC | cv5 mean ± std |
  |---------------------------------------|-----------|----------------|
  | real PH on PC1-residualized cloud     | **0.6884** | 0.6945 ± 0.021 |
  | matched-cov Gaussian null on residual | **0.7628** | 0.7658 ± 0.057 |
  | difference (real − null)              | **−0.074** | −0.071 ± 0.074 |

  Per-feature real vs null mean (Δ = real − null):
  - H0_max_lifetime: 50.29 vs 53.55 (Δ −3.26)
  - H0_entropy: 4.548 vs 4.592 (Δ −0.044)
  - H1_pers_entropy: 3.814 vs 4.157 (Δ −0.343)
  - H1_n_features: 63.7 vs 87.4 (Δ −23.7)
  - H1_max_lifetime: **8.53 vs 5.30 (Δ +3.23)** — the only feature
    where real > null, suggesting PH picks up some second-order
    *outlier* structure that a Gaussian null does not. But this
    single-feature edge is dominated by the other four where the null
    Gaussianizes the residualized cov and PH summarizes more
    consistently.

  Anchors (pre-existing):
  - F-10 raw 5-feature gap (FE026): −0.003
  - F-10 zigzag-residualized gap (FE116, EXP-54): −0.067
  - F-10 PC1-residualized gap (THIS EXPERIMENT, EXP-56): **−0.074**
**Verdict:** F-10 *strengthens further*. The null on the
PC1-residualized covariance beats real PH by 7.4 pp — more negative
than the raw-cloud −0.003 and the zigzag-residualized −0.067. PH
summaries do not improve on Gaussian-null structure even when the
dominant correctness direction has been projected out. The covariance
pathway carries the residual signal (FE881: top-20 log-eigvals OOF
AUROC 0.7928), not the PH descriptor family.
**Changed our understanding of:**
  (1) F-10 sharpens to "PH features layered on a covariance ellipsoid
      carry NEGATIVE signal once PC1 is removed." Three independent
      cloud constructions (raw, zigzag-residualized, PC1-residualized)
      now agree: real PH ≤ matched-cov Gaussian null on the *same*
      cloud's covariance.
  (2) The single-feature signal in H1_max_lifetime (real 8.53 vs null
      5.30) is intriguing — suggests there's *some* outlier structure
      in residualized clouds that Gaussianization erases. But it's not
      enough to overcome the other 4 features at the joint-feature
      level. Open follow-up: H1_max_lifetime alone single-feature
      OOF AUROC (next move below).
  (3) The FE881 cov-spectrum result (top-20 log-eigvals, OOF AUROC
      0.7928, BEATS supervised DoM by 2.5 pp) is the right place to
      look for the PC1-orthogonal correctness signal — not PH.
**Open follow-ups suggested by this result:**
  (a) Single-feature H1_max_lifetime AUROC on PC1-residualized clouds
      — the only feature where real beats null (Δ +3.23). If single-
      feature AUROC ≥ 0.6, there *is* topological residual signal,
      it's just dominated by Gaussianized other features in the joint
      probe.
  (b) Per-problem effective rank / participation ratio as a *single*
      scalar feature — does any standard scalar of the residualized
      cov match the FE881 top-20 0.7928?
  (c) Generalize PC1 → top-K residualization. If residualizing PC1..PC9
      (covering the 2-feat directional ceiling) keeps the cov-spectrum
      AUROC at 0.79, the second-order signal is in PC10+ low-eigenvalue
      tail. If it collapses, F-2 is fully captured by directional+spectral.
**Files:** `pathway11_h100/ph_residuals/recompute_pc1_resid_ph.py`
(regen, ~13min CPU cold disk), `pathway11_h100/ph_residuals/pc1_resid_results.json`
(output), `pathway11_h100/prefill_inversion/cache/m15b_prefill.npz`
(FE291 PC1 source), `pathway8_layerwise/data/math500/problem_*.npz`
(per-problem L19 trajectory clouds).
**Depends on:** EXP-55 (FE291 PC1 grounding); EXP-026 (F-10 raw PH
null baseline); EXP-54 (F-10 residualized PH baseline).
**Enables:** FE881 (cov-spectrum: the spectral signal that survives
PC1 residualization, top-20 log-eigvals OOF 0.7928, EXP-57); FE882
(decomposition triangle: full 1536-d L2-reg ceiling = 2-feat 0.7856,
EXP-58). The three FE291 follow-ups land as a paired narrative.

## STATE.md last-experiment update

EXP-56 (P11-FE880 PH features on PC1-residualized L19 clouds). On 500
MATH-500 PC1-residualized L19 trajectory clouds (PC1 from FE291,
14.7% of variance), 5-fold OOF logistic AUROC: real PH = 0.6884,
matched-cov Gaussian null PH = 0.7628, gap = −0.074. The null beats
real by 7.4 pp on the same residualized covariance. F-10 *strengthens
further*: three independent cloud constructions (raw, zigzag-residualized,
PC1-residualized) all show real ≤ null. F-10's "would be overturned by"
clause (real ≥ null + 0.05 on PC1-residualized clouds) was NOT met —
the gap went the other direction. H1_max_lifetime is the only feature
where real beats null (Δ +3.23) — open follow-up. The PC1-orthogonal
correctness signal lives in the full residualized covariance spectrum
(FE881 top-20 log-eigvals OOF AUROC 0.7928, EXP-57), not in PH.

## FINDINGS.md updates

### F-10: Topology summary statistics on residual streams are not distinguishable from a Gaussian null

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E56]
counterargument: "F-10's narrow form held under three orthogonal cloud constructions (raw FE026, zigzag-residualized FE116, PC1-residualized FE880); residualizing the dominant FE291 PC1 direction *increased* the negative gap (−0.003 → −0.067 → −0.074). The pre-registered overturning clause ('real ≥ null + 0.05 on PC1-residualized clouds') was tested directly and failed — the gap is more negative than the baseline. The only single-feature crack is H1_max_lifetime (real 8.53 vs null 5.30, Δ +3.23) — worth a single-feature follow-up — but four other features Gaussianize on residualization and dominate the joint logistic. The covariance pathway carries the post-PC1 correctness signal (FE881 top-20 log-eigvals OOF AUROC 0.7928, beats DoM 0.7679 by 2.5pp), and that signal is purely spectral, not topological."
overturned_by: "(a) A single-feature PH probe (H1_max_lifetime alone, or another descriptor) showing real_AUROC ≥ 0.65 on PC1-residualized clouds while matched-cov Gaussian null sits at chance — would say PH does have a real residual signal, just buried in the joint feature vector. (b) A higher-order PH instrument (zigzag, FTS-PH, persistence landscapes) computed on PC1-residualized clouds showing real ≥ null + 0.05 — would say *some* topology is recoverable post-residualization, just not V-R-H0/H1. (c) A non-MATH-500 benchmark where raw L19 clouds give real ≥ null + 0.05 — would say the F-10 null result is dataset-specific."
```

**Claim.** F-10's narrow form (5 V-R PH summary features ≈ matched-cov
Gaussian null on the same cloud's covariance) now holds under **three**
orthogonal cloud constructions — raw L19 (FE026 gap −0.003),
zigzag-residualized (FE116 gap −0.067), and **PC1-residualized
(FE880 gap −0.074)**. The PC1-residualized test was the
pre-registered overturning condition for F-10 (FINDINGS.md "would be
overturned by: PH features computed on the [PC2..PC1536] subspace
showing real ≥ null + 0.05"). The gap went *more negative*, not
positive. Per-feature inspection: H1_max_lifetime has real 8.53 vs
null 5.30 (Δ +3.23) — the single feature where PH outperforms
Gaussianized matched cov — but the other four features Gaussianize
on residualization (H0_max_lifetime Δ −3.26, H1_pers_entropy Δ −0.34,
H1_n_features Δ −23.7, H0_entropy Δ −0.04) and dominate the joint
OOF logistic. The post-PC1 correctness signal lives in the full
covariance eigenvalue spectrum (FE881 top-20 log-eigvals OOF AUROC
0.7928), not the PH descriptor family.

**Strength:** STRONG (PH-null gap holds under raw, zigzag-residualized,
PC1-residualized cloud constructions — the pre-registered overturning
condition was tested directly and failed by 12.4 pp in the wrong
direction; covariance pathway grounded in PC1 + residual eigenvalue
spectrum, not topology).

**Evidence:** EXP-026 (raw 5-feature PH gap −0.003); EXP-52 (FE321
zigzag gap −0.066 + 7-descriptor +0.097, no topology component);
EXP-54 (FE116 residualized gap −0.067); **EXP-55** (PCA-PC1 = DoM
direction, covariance pathway grounded as PC1); **EXP-56** /
`pathway11_h100/ph_residuals/pc1_resid_results.json` (PC1-residualized
real PH 0.6884 vs null 0.7628, gap −0.074 — pre-registered
overturning condition tested and failed).

**Controls passed:**
- Diagonal vs full covariance null (D1 fix in EXP-026 v2).
- Drop H0_n_features rank-tie (D2 fix).
- Zigzag descriptor pair on 28-layer trajectory (FE321).
- Song-Zhong residualization (FE116).
- PCA-PC1 = supervised DoM (EXP-55, FE291) — covariance pathway is
  a single direction with 14.7% var share and cos 0.922 to DoM.
- **PC1-residualization (EXP-56, FE880)** — pre-registered overturning
  condition for F-10 tested directly; gap went MORE negative
  (−0.074 vs raw −0.003), not positive.

**Strongest counterargument:** F-10's narrow form held under three
orthogonal cloud constructions (raw, zigzag-residualized,
PC1-residualized); residualizing the dominant FE291 PC1 direction
*increased* the negative gap. The pre-registered overturning clause
("real ≥ null + 0.05 on PC1-residualized clouds") was tested
directly and failed. The only single-feature crack is H1_max_lifetime
(real 8.53 vs null 5.30) — worth a single-feature follow-up — but
four other features Gaussianize on residualization. The covariance
pathway carries the post-PC1 correctness signal (FE881 top-20
log-eigvals 0.7928), and that signal is purely spectral, not
topological.

**Would be overturned by:** (a) Single-feature H1_max_lifetime PH
probe on PC1-residualized clouds with real_AUROC ≥ 0.65 and matched
null at chance. (b) A higher-order PH instrument (zigzag, FTS-PH,
persistence landscapes) on PC1-residualized clouds with real ≥ null
+ 0.05. (c) Non-MATH-500 benchmark where raw L19 clouds give real
≥ null + 0.05.

## New claims

- pca-pc1resid-ph-real-auroc = 0.6884 (FE880 5 V-R PH features on PC1-residualized L19 clouds, 5-fold OOF logistic AUROC)
- pca-pc1resid-ph-null-auroc = 0.7628 (FE880 5 V-R PH features on 100-sample matched-cov Gaussian null on same residualized cov, 5-fold OOF logistic AUROC)
- pca-pc1resid-ph-gap = -0.0744 (FE880 real − null gap on PC1-residualized clouds; pre-registered overturning condition for F-10 tested directly and failed by 12.4 pp in the wrong direction)

## Sources

- pathway11_h100/ph_residuals/recompute_pc1_resid_ph.py (this experiment's regen)
- pathway11_h100/ph_residuals/pc1_resid_results.json (output)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (FE291 PC1 source: 500 × 1536 fp16 L19 prefill)
- pathway8_layerwise/data/math500/problem_*.npz (per-problem L19 trajectory clouds, 500 NPZs)
- FINDINGS.md F-10 "would be overturned by" clause (pre-registered overturning condition)
- briefs/result-2026-05-02-P11-FE291.md (FE291 PC1 grounding)
- handoff/2026-05-02-pca-followups-session4.md (paired-promote narrative for FE880/881/882)
