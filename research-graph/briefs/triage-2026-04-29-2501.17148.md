# Triage brief — 2501.17148 — AxBench: Steering LLMs? Even Simple Baselines Outperform Sparse Autoencoders

Source: https://arxiv.org/abs/2501.17148
Triaged: 2026-04-29

## Refutations

1. **AxBench predicts H-1 fails on plain activation addition.** AxBench's *Supervised Steering Vector* (SSV — gradient-trained activation-add at a residual layer) reaches only **0.026 / 2.0** mean overall steering score on Gemma-2-2B/9B; their *DiffMean* steering (which is exactly the construction underlying our prefill L19 DoM in F-2) reaches **0.239 / 2.0** — even though SSV gets concept-detection AUROC 0.950 at L20. This is a direct refutation of **H-1** ("Per-position DoM steering moves MATH-500 accuracy"). Mechanism: a residual-stream direction can be a strong correctness/concept *detector* without being a viable *causal* steering vector under simple `h ← h + α w`. Falsifiable test: take the cached L19 prefill DoM from `pathway11_h100/prefill_gated_compute/results.json`, re-extract on Qwen-2.5-1.5B with a `pyvene`-style hook, sweep α ∈ {−2, −1, 0, 1, 2} × max-activation on MATH-500 (50 problems × 14 factors, two-halves protocol from §3.3), and check whether K=1 accuracy moves ≥ 2 pp away from the 48.6 % baseline. If accuracy is flat or only loses fluency, H-1 is refuted in the same way DiffMean steering fails on Gemma-2.

2. **F-2's 0.7731 AUROC ceiling is task-induced, not DoM-induced.** AxBench DiffMean — the *same construction* as our DoM — reaches mean AUROC **0.942** across four Gemma residual sites (Table 1; range 0.921–0.955). Our prefill L19 DoM hits 0.7731 on MATH correctness. The 0.17-AUROC gap refutes the implicit framing of **F-2** that 0.7731 is "what DoM can do" — it is specifically what DoM can do for *MATH correctness on Qwen-1.5B*. Mechanism: AxBench concepts (e.g. "Golden Gate Bridge") are encoded as a near-linear feature with clean labels; correctness on MATH is multi-cause (skill, length, rare-token contamination) and labels are noisy. Falsifiable test: synthesize an AxBench-style binary "calculation-error present" concept on the same MATH-500 generations using gpt-4o-mini, refit a DoM probe at L19, and report AUROC. If ≥ 0.90, F-2's headline number reflects label structure rather than a fundamental signal-quality limit, and the project's correctness ceiling stops being "the DoM ceiling" and becomes "the labeling ceiling".

3. **Layer specificity (L19) in F-2 is not robust.** AxBench Gemma-2-2B DiffMean detection AUROC: L10 = 0.948, L20 = 0.946 — statistically indistinguishable. Gemma-2-9B: L20 = 0.955, L31 = 0.921. Their probing reveals a *wide* layer band carries the signal, contradicting any implicit reading of **F-2** that "L19 of Qwen-1.5B is uniquely the best". Mechanism: in DiffMean-style supervised probing, the concept signal is replicated across mid-to-late layers; the specific layer-19 selection in our work may be local-optimum noise on a flat AUROC-vs-layer curve. Falsifiable test: refit our DoM at L8, L13, L19, L23, L25 of Qwen-2.5-1.5B from cached prefill activations and report 5-fold OOF AUROC for each. If the L19 advantage over the next-best layer is < 0.01 AUROC, F-2's "L19" framing should weaken to "any deep-middle layer". This also reframes H-13 (head-level attribution): if many layers carry the signal equally, head-attribution should be done at multiple layers, not just L19.

## Direct connections to F-N / H-N

- **F-2** (prefill L19 DoM AUROC 0.7731, 1.5B): AxBench's DiffMean is mathematically identical to our DoM (Eq. 4 = mean(positive activations) − mean(negative activations) at a single layer). Their Gemma-2-2B L20 result of 0.946 sets a "what DiffMean can do under clean labels" benchmark that our 0.7731 is far below — see Refutation 2.

- **F-3** (cos(prefill_DoM, final_DoM) = 0.046, near-orthogonal): AxBench *corroborates* F-3 indirectly. Their DiffMean direction has detection AUROC 0.946 at L20 but only 0.178 steering score at L20 (Table 2) — i.e., the detection-direction and the causal-control-direction diverge sharply within the same layer. Our F-3 says prefill-DoM and final-DoM are orthogonal at L19 (cos 0.046); AxBench says even within prefill, the "what reads out" direction and the "what intervenes" direction are not the same. (Read: `~/topo-confidence/FINDINGS.md` F-3 body for exact orthogonality construction.)

- **F-8** (selective prediction at coverage 0.5 = 71.6 %): AxBench has no selective-prediction analog (their setup is concept-presence on long-form generations, not abstain-or-answer). No direct connection.

- **H-1** (per-position DoM steering moves MATH-500 accuracy): direct refutation predicted — see Refutation 1.

- **H-12** (semantic-entropy probes label-free replace supervised DoM): AxBench's *unsupervised* SAEs are dominated by the *supervised* DiffMean on both axes (concept detection: SAE 0.695 vs DiffMean 0.942; steering: SAE 0.165 vs DiffMean 0.239) — this is *evidence against* the broader "label-free replaces supervised" thesis that motivates H-12. Lowers H-12 priority.

- **H-13** (head-level attribution of prefill vs final-token circuits): AxBench operates at the residual stream, not heads — but the wide-layer-band evidence (Refutation 3) suggests head attribution should be performed across multiple layers, not pinned to L19.

## Methodologies extracted

- **DiffMean (supervised steering vector, Eq. 4)** — `w = mean(H+) − mean(H−)`; identical to our DoM construction.
  *Replication cost*: already replicated by F-2 (cached at `pathway11_h100/prefill_gated_compute/results.json`).
  *We'd plausibly run this*: yes — already do; useful as a sanity-check for our DoM matches AxBench's DiffMean numerics.

- **SSV — Supervised Steering Vector (Eq. 6–7)** — single learnable vector `w` added to every token's residual; trained by backprop through LM loss on positive responses only.
  *Replication cost*: 4 h H100 to train + sweep 14 steering factors on MATH-500.
  *We'd plausibly run this*: yes — directly operationalizes H-1 (gradient-trained DoM-style steering for correctness).

- **ReFT-r1 — Rank-1 Representation Finetuning (Eq. 8–10)** — joint detection (sigmoid probe) + steering (TopK-gated activation add) with L1 regularization on non-top-k latents.
  *Replication cost*: 1 d H100 per concept; ~ 1 day for a single MATH-correctness concept on Qwen-1.5B.
  *We'd plausibly run this*: yes — strongest candidate for an H-1 successor, since it is the only AxBench method that approaches prompting on steering (0.543 / 2.0).

- **Linear Probe (Eq. 5)** — sigmoid + BCE on token-level positive/negative labels; unit-norm constrained.
  *Replication cost*: cached (we already use logistic regression on DoM features).
  *We'd plausibly run this*: yes — already do.

- **LAT — Linear Artificial Tomography** — PCA over normalized pairwise activation differences δ = (h_i − h_j) / ‖h_i − h_j‖ across positive set.
  *Replication cost*: 30 min CPU on cached activations.
  *We'd plausibly run this*: yes — useful comparison for H-12 / H-13 (LAT's poor 0.712 mean AUROC on AxBench is informative about RepEng's actual ceiling).

- **SAE-A — pretrained-SAE feature selection by AUROC** — for each concept, max-pool each SAE feature's activations, compute AUROC against labels, take the best-scoring feature.
  *Replication cost*: blocked — no public Qwen-2.5-1.5B SAEs as of 2026-04 (GemmaScope is Gemma-only).
  *We'd plausibly run this*: no — gated by Qwen SAE availability.

- **Two-halves steering-factor protocol** — partition Alpaca-Eval instructions into halves; pick best α on first half, evaluate on second half. Defends against α-overfitting per concept.
  *Replication cost*: free; protocol-only.
  *We'd plausibly run this*: yes — required for any honest H-1 evaluation.

- **Harmonic-mean LLM-judge scoring (concept, instruct, fluency ∈ {0,1,2})** — heavily penalizes failure on any axis; final overall score ∈ [0, 2].
  *Replication cost*: gpt-4o-mini API calls (≪ $5 per 500-concept run).
  *We'd plausibly run this*: yes for any steering-effect evaluation we run; better than AUROC for steering quality.

- **Hard-negative mining via polysemy** — generate semantically-related but distinct-sense distractors from the LLM (Step 4 of §3.1).
  *Replication cost*: gpt-4o-mini API.
  *We'd plausibly run this*: maybe — useful if we ever build a "is the response wrong-for-the-right-reason" dataset.

## Approaches & framings

- **Detection/steering decoupling**: AxBench frames model-control along *two* utility axes — concept-detection **C** and model-steering **S** — and shows methods can be strong on one and weak on the other (DiffMean: 0.942 C, 0.239 S; ReFT-r1: top on both). This is a useful lens for our project: F-2/F-8 live entirely on the **C** axis (DoM as a *predictor*), while H-1 lives on the **S** axis (DoM as a *steering vector*). The paper's main empirical claim — that even the best **C** method (DiffMean) is weak on **S** without joint-training tricks (ReFT-r1) — is the strongest case yet for treating F-2 as a *detection* result and refusing to read it as evidence about steerability.

- **SDL framing (supervised dictionary learning) as the SAE alternative**: AxBench reframes the SAE-vs-supervised debate as a dictionary-construction question — both are dictionaries; one is unlabeled (SAE) and one is labeled (DiffMean / Probe / ReFT-r1). This bears on whether our DoM is "just a feature" (SDL view) or a structural geometric property (the "topological" framing already overturned in F-10). The SDL view is more parsimonious.

## Datasets & benchmarks

- **CONCEPT500** — 500 concepts; 144 training + ≈ 72 eval examples per concept; synthesized via `gpt-4o-mini-2024-07-18`; concepts sampled from Neuronpedia GemmaScope SAE labels. License Apache-2.0. Released at `huggingface.co/pyvene`. Applicable? **partial** — relevant for H-12 / SAE-comparison work; not directly applicable to MATH correctness because their concepts are open-vocabulary noun-phrases, not "math-step-correct".

- **CONCEPT16K** — 16 000-concept training/eval suite; same generation pipeline; same release path. Applicable? **no** for MATH-500 directly; **yes** if we ever benchmark our DoM against an SDL baseline at scale.

- **Alpaca-Eval (Li et al. 2023)** — 805 instructions used for steering evaluation; 10 sampled per concept × 14 steering factors. Applicable? **yes** — drop-in for any future H-1 steering evaluation; lets us reuse AxBench's two-halves α-selection protocol.

- **GemmaScope SAEs (Lieberum et al. 2024)** — pretrained SAEs at Gemma-2-2B L10/L20, Gemma-2-9B L20/L31. Applicable? **no** — Qwen-only project; no Qwen GemmaScope-equivalent.

## Implementation details worth capturing

- `pyvene` (Wu et al. 2024b) — open-source intervention library used for all in-place residual-stream hooks. Drop-in alternative to writing forward-hooks ourselves. Github: `github.com/stanfordnlp/pyvene`.
- AxBench code: `github.com/stanfordnlp/axbench`. Apache-2.0.
- Layer choice convention: 2 layers per model — early-mid (L10 / L20) and late (L20 / L31). For Qwen-2.5-1.5B (28 layers), the analogous sites would be ~ L13 and ~ L25.
- Steering magnitude: `α = steering_factor × max_activation_over_eval_set_per_concept`. 14 factors. `max_activation` is critical to prevent units from drifting between concepts.
- Unit-norm constraint on all learned subspaces *except* SSV — SSV's magnitude is also learned. AxBench notes SSV is "hard to use for steering" with unit-norm, which is why they relax.
- TopK + L1 regularizer in ReFT-r1: `min ‖a_i‖₁ for a_i ∉ TopK(Ψ(h))` — encourages activations outside the top-k positions to be sparse, but allows the top-k to grow freely. This is a key technical move and would be central if we adapted ReFT-r1 to MATH-correctness steering.
- LLM judge: gpt-4o-mini, three sub-scores 0/1/2, harmonic-mean aggregation. Harmonic mean is load-bearing — if any sub-score is 0, overall = 0.

## Replicable intermediates

- **AxBench-DiffMean reproduction on cached Qwen-1.5B activations**: re-fit our DoM exactly per Eq. 4 (mean(H⁺) − mean(H⁻), unit-normalized) using the binary-correctness labels and `pathway11_h100/prefill_gated_compute/` cached prefill activations; report L19 OOF AUROC and verify it matches our 0.7731 (sanity check that our pipeline matches AxBench's recipe).
- **Layer-band ablation for F-2**: reuse the same cached prefill activations to refit DoM at L8, L13, L23, L25 (in addition to the existing L19) and report AUROC at each. Tests Refutation 3.
- **DiffMean steering null-test on MATH-500**: requires fresh forward passes with `pyvene` hooks, so not "right now" — but the protocol is template-ready.
- **Probe vs DiffMean parity check**: refit a logistic probe (Eq. 5) at L19 prefill on the same cached activations and compare AUROC to DoM. AxBench finds Probe ≈ DiffMean within 0.002 AUROC; if our Probe-AUROC also matches DoM-AUROC at L19, that's a third-party corroboration of F-2's robustness to method choice.

## Cross-paper signals

- **2306.03341** — Inference-Time Intervention (Li et al.) — already in graph (status: graphed; CONTRADICTED in PAPER_INDEX). AxBench cites this as a canonical static-vector steering baseline; AxBench's SSV-fails result (0.026) is an independent corroboration of our F-3 orthogonality argument against ITI.
- **2310.06824** — Geometry of Truth (Marks & Tegmark) — already in graph (status: pending_triage). DiffMean is their Eq. 4. AxBench is essentially a horse-race against DiffMean; this paper is the upstream method paper.
- **2310.01405** — Representation Engineering / LAT (Zou et al. 2023) — NOT in graph; recommend admission. AxBench's LAT implementation (Eq. for δ, pairwise PCA) is from this paper; LAT's poor 0.712 mean AUROC is informative about RepEng's actual concept-detection ceiling.
- **2404.03592** — ReFT (Wu et al. 2024a) — NOT in graph; recommend admission. ReFT-r1 is the rank-1 specialization of this method. If we ever try the ReFT-r1 approach on MATH correctness (P11-FE71 below), the upstream method paper is required reading.
- **2403.07403** — Templeton et al. SAE scaling (Anthropic) — NOT in graph; recommend admission for H-12 context (the canonical "SAE works at scale" paper that AxBench partially refutes for the steering axis).

## Proposed FutureExperiments

```yaml
- id: P11-FE71
  pathway: P11
  description: "Test H-1 (DoM steering moves MATH-500 accuracy) using AxBench's DiffMean steering protocol: hook prefill L19 with `h ← h + α·w_DoM` for α∈{−2,−1,−0.5,0,0.5,1,2} × max-activation, sweep on 50 MATH-500 problems, two-halves α-selection (best α on half-A, evaluate on half-B), report K=1 accuracy delta from 48.6% baseline and harmonic-mean (correctness, instruct, fluency) judge score per AxBench §3.3."
  rationale: "AxBench shows DiffMean steering is weak (0.239/2.0) and SSV (gradient-trained activation-add) is essentially zero (0.026/2.0) on Gemma-2 despite strong detection AUROC. If our prefill L19 DoM behaves similarly under the same activation-addition protocol, H-1 is refuted by direct experiment rather than by analogy. Critical because H-1 currently sits at HIGH priority on the assumption that DoM is causally as well as predictively useful."
  trigger: "AxBench (2501.17148) Tables 2 and 3 — DiffMean steering 0.239 mean overall, SSV 0.026 mean overall, despite SSV detection AUROC 0.950 at L20."
  status: READY
  priority: HIGH
  roi: 8
  cost: "4h H100 for re-extraction with pyvene hooks + 1 day CPU for judge scoring"
  depends-on: [F-2, F-3]
  would-update: [F-2]
  triggered-by: ["2501.17148"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE72
  pathway: P11
  description: "Layer-band ablation for F-2: refit DoM at L8, L13, L19, L23, L25 of Qwen-2.5-1.5B from cached `pathway11_h100/` prefill activations; report 5-fold OOF AUROC at each layer; check whether L19's 0.7731 is statistically separable from neighbors. If max(other-layer-AUROC) is within 0.01 of L19, reframe F-2 from 'L19 is best' to 'a deep-middle layer band carries the signal'."
  rationale: "AxBench (2501.17148) Table 1 shows DiffMean detection AUROC differs by ≤ 0.002 between Gemma-2-2B L10 (0.948) and L20 (0.946) — a wide layer band carries concept signal. Our pipeline's L19 selection was made early in P11 and never ablated against neighbors; this is cheap (CPU-only on cached NPZs) and either firms up the L19 anchor or relaxes F-2's framing."
  trigger: "AxBench (2501.17148) Table 1 — Gemma-2-2B L10/L20 AUROCs essentially tied."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "2h CPU on cached prefill activations"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2501.17148"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE73
  pathway: P11
  description: "Synthetic concept-detection bound on F-2's AUROC ceiling. Build an AxBench-style binary 'calculation-error present' synthetic concept on the same MATH-500 generations using gpt-4o-mini (per AxBench §3.1 prompt template), refit DiffMean / linear probe at L19 prefill, report AUROC. If ≥ 0.92, F-2's 0.7731 ceiling is bounded by correctness-label structure, not by DoM as a method."
  rationale: "AxBench DiffMean reaches 0.942 mean AUROC on clean concept labels — far above our 0.7731 on MATH correctness. Either (a) DoM's ceiling on math-correctness labels is fundamentally lower because correctness is multi-cause, or (b) our labels are noisy. This experiment isolates label-quality from method-quality and tells us where the ceiling lives."
  trigger: "AxBench (2501.17148) Table 1 — DiffMean mean AUROC 0.942 vs our F-2 prefill DoM 0.7731."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "$5 gpt-4o-mini + 30min CPU on cached activations"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2501.17148"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE74
  pathway: P11
  description: "ReFT-r1 alternative for H-1: implement Eq. 8–10 from AxBench (joint TopK-gated detection + LM-loss steering with L1 regularizer on non-top-k latents) on Qwen-2.5-1.5B at L19, train a single concept = 'response-is-correct' on 200 MATH problems, evaluate K=1 accuracy on remaining 300 problems vs the prefill-DoM-gated baseline (F-8 at 71.6% on answered)."
  rationale: "AxBench's only steering method that closes the gap to prompting is ReFT-r1 (0.543/2.0 vs DiffMean 0.239 vs SSV 0.026). If H-1 fails under plain DiffMean activation-add (P11-FE71), ReFT-r1 is the obvious next step before declaring H-1 dead — it is *also* a DoM-style direction but trained jointly with TopK-gated insertion."
  trigger: "AxBench (2501.17148) Table 2 — ReFT-r1 dominates all activation-addition steering methods by ≥ 0.3 overall score."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "1d H100 for ReFT-r1 training + 4h H100 for evaluation"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2501.17148"]
  blocked-by: null
  blocked-by-experiment: ["P11-FE71"]
```

## Proposed HYPOTHESES.md additions

### H-150: Prefill L19 DoM detects correctness but does not steer it
**Priority:** HIGH
**Motivated by:** 2501.17148 + F-2 + F-3
**Test:** Run AxBench DiffMean steering protocol (Eq. 4, two-halves α-selection, harmonic-mean LLM judge) on Qwen-2.5-1.5B at L19 prefill, sweeping 7 steering factors over 50 MATH-500 problems. Report K=1 accuracy delta from 48.6% baseline and judge-scored quality. (See P11-FE71.)
**Requires:** 4h H100 with `pyvene` hooks; cached L19 DoM direction.
**Would change:** Confirm → H-1 stays HIGH and we move to scale-up. Reject → H-1 becomes PARKED, and the project's H-1 rationale ("DoM is a causal handle, not just a probe") is dropped from the narrative; F-2 becomes a strict *detection* finding.
**Blocks:** H-1 (treats this as a precondition test before any further DoM-steering work).

### H-151: ReFT-r1-style joint training closes the H-1 gap when DiffMean fails
**Priority:** MEDIUM
**Motivated by:** 2501.17148 + H-150
**Test:** Train ReFT-r1 (TopK-gated detection + LM-loss steering with L1 regularizer on non-top-k latents) at Qwen-2.5-1.5B L19 with concept = "response-is-correct" on 200 MATH-500 problems; evaluate K=1 accuracy on the remaining 300 vs the prefill-DoM-gated baseline (F-8). (See P11-FE74.)
**Requires:** 1d H100 for training + 4h H100 evaluation; `pyvene` + ReFT codebase.
**Would change:** Confirm → restores H-1 as "DoM is causal *with* the right training procedure" and reframes F-2 as the detection half of a two-axis story. Reject → strong evidence that residual-stream steering is fundamentally hard for correctness, regardless of method.
**Blocks:** nothing (downstream of H-150).

### H-152: F-2 AUROC ceiling is label-induced, not DoM-induced
**Priority:** MEDIUM
**Motivated by:** 2501.17148 (AxBench DiffMean 0.942 mean AUROC) + F-2
**Test:** Synthesize a binary concept "response contains a calculation error" on the same MATH-500 generations using gpt-4o-mini per AxBench §3.1; refit DiffMean / Probe at L19 prefill; report AUROC. (See P11-FE73.)
**Requires:** $5 gpt-4o-mini + 30min CPU on cached activations.
**Would change:** Confirm (AUROC ≥ 0.92) → F-2's 0.7731 reflects correctness-label noise; the ceiling argument shifts to "improve labels, not methods". Reject (AUROC < 0.85) → F-2's 0.7731 is genuinely close to the DoM ceiling for math-style residual-stream encoding.
**Blocks:** nothing.

## Proposed PAPER_INDEX.md classification

## 2501.17148 — AxBench: Steering LLMs? Even Simple Baselines Outperform Sparse Autoencoders (Wu et al., 2025)

**Relevance:** Direct method-level peer to F-2. AxBench's DiffMean (their Eq. 4) is mathematically identical to our prefill L19 DoM construction; their results across 4 Gemma residual sites bound what DiffMean can do as a *concept detector* (mean AUROC 0.942) and as an *activation-addition steering vector* (mean overall steering score 0.239 / 2.0). Their SSV result (0.026 / 2.0) — gradient-trained DoM-style steering — is the strongest available signal that H-1 will fail under plain activation addition. Their ReFT-r1 result (0.543 / 2.0) defines what's possible if we abandon static activation addition for joint TopK-gated training.

**Key claim we tested:** Supervised steering vector methods (DiffMean, SSV) are strong concept detectors but weak causal steerers; only joint-trained methods (ReFT-r1) close the gap to prompting / finetuning baselines.

**Our result:** **TO TEST.** We have not yet evaluated DoM as a *steering vector* on MATH-500; F-2 and F-8 only test it as a *detector*. P11-FE71 directly replicates AxBench's DiffMean steering protocol on prefill L19 DoM; P11-FE74 follows up with the ReFT-r1 alternative if FE71 fails. The detection-vs-steering decoupling AxBench documents is consistent with our F-3 (prefill / final DoM orthogonality), suggesting the result will replicate.

**Related experiments:** P11-FE71, P11-FE72, P11-FE73, P11-FE74; H-1, H-150, H-151, H-152.

**Status:** TO TEST

## New claims

- AxBench DiffMean mean AUROC = 0.942 over 4 Gemma residual sites (Table 1, mean of L10/L20/L20/L31 column average).
- AxBench DiffMean mean overall steering score = 0.239 / 2.0 (Table 2, average column).
- AxBench SSV mean overall steering score = 0.026 / 2.0 (Table 2).
- AxBench ReFT-r1 mean overall steering score = 0.543 / 2.0 (Table 2).
- AxBench ReFT-r1 winrate vs SAEs = 81.8 % (Table 3, average column).
- AxBench Gemma-2-2B DiffMean L10 / L20 detection AUROC = 0.948 / 0.946 (Table 1).

These are claims about the cited paper, not about our own measurements, so they do not require `validate_claims.py` entries unless reproduced into our narrative docs as comparison numbers in F-2 / F-3 commentary.

## Sources consulted

- `/tmp/axbench_paper.md` (full converted PDF text via `mcp__arxiv__read_paper`) — paper body, Tables 1–3, §3.1, §3.3, §4 method definitions.
- `~/topo-confidence/CLAUDE.md` (headlines table only) — anchor numbers.
- `~/topo-confidence/FINDINGS.md` and `~/topo-confidence/HYPOTHESES.md` (one-liners only via grep) — F-N / H-N IDs.
- `~/topo-confidence/PAPER_INDEX.md` (status flags only via grep) — existing classifications.
- `research-graph/query.py paper {2501.17148, 2310.01405, 2404.03592, 2306.03341, 2310.06824, 2502.16002, 2403.07403}` — cross-paper presence check.
- `research-graph/query.py future P11`, `P10`, `P9` — next-FE-ID anchoring.
- WebFetch of `https://github.com/stanfordnlp/axbench` — layer-choice convention (Gemma-2-2B L10/L20, Gemma-2-9B L20/L31), license (Apache-2.0), data sources.
- WebFetch of `https://arxiv.org/abs/2501.17148` — abstract sanity-check.
- Semantic Scholar paper details endpoint — citation count (139), venue (ICML), author list.

I did **not** read F-N body text beyond the one-liner grep, per the per-paper-isolation protocol.
