# Triage brief — 2502.18862 — One-shot Optimized Steering Vectors Mediate Safety-relevant Behaviors in LLMs

Source: https://arxiv.org/abs/2502.18862
Triaged: 2026-04-29

Authors: Jacob Dunefsky & Arman Cohan (Yale)
Venue: COLM 2025 (v2: 2025-08-12)
Code: https://github.com/jacobdunefsky/one-shot-steering-repro and https://github.com/jacobdunefsky/one-shot-steering-misalignment

## Refutations

1. **Predict-control discrepancy refutes the *causal* reading of F-2 (prefill L19 DoM AUROC 0.7731).** The paper documents a "predict-control discrepancy" (after Wattenberg & Viégas 2024): the five norm-15 SVs that flip Poser-model behavior on >80% of benign prompts have max ROC-AUC 0.66 and median 0.56 as classifiers — i.e. directions effective at *steering* a behavior are unrelated to directions effective at *classifying* it. F-2 currently treats 0.7731 prefill AUROC as evidence that the L19 prefill direction *mediates* correctness. If the discrepancy holds in our setting, the prefill DoM is a predictive correlation only; the mediating direction lives elsewhere. Falsifiable: optimize a one-shot SV at L19 prefill to flip a single MATH-500 problem's correctness, then evaluate (a) cos with our prefill DoM, (b) AUROC of `mean(activation · optimized_SV)` vs the 0.7731 baseline, (c) accuracy lift on held-out 499 problems. If the optimized SV transfers and our DoM does not, F-2's mediation reading dies.

2. **Single-prompt SV optimization refutes H-1 if it works (and refutes H-1 if it scales-fails).** H-1 ("per-position DoM steering moves MATH-500 accuracy") presupposes that the contrastive-mean direction is the right control vector. The paper's central result is that one-shot gradient-descent SVs achieve 96.9% Harmbench ASR vs CAA's lower (and strongly per-input variable) numbers. Two-sided refutation: (a) if a one-shot SV optimized on one MATH-500 problem reliably steers correctness on the other 499, H-1's "DoM is the right form of steering vector" is wrong by *construction* — the right SV is the optimization solution, not the contrastive mean; (b) the Turner et al. 2025 result quoted in §1.1 ("on the more powerful Gemini 1.5 v2, optimizing SVs no longer beat baselines such as multi-shot prompting") suggests SV-based steering may not scale to larger models, which would refute H-1 outright on the 7B Qwen target. Falsifiable: run one-shot promotion-steering on a single MATH-500 problem at L19 of Qwen-2.5-1.5B; measure transfer-set accuracy lift. If both 1.5B fails *and* the contrastive DoM fails, H-1 is dead. If 1.5B works but 7B doesn't, H-1 is bounded by scale.

3. **High-variance one-shot results refute the implicit assumption (under F-2/F-7/H-1) that there exists a single privileged correctness direction at L19.** §3.3 explicitly notes "high variability in the efficacy of SVs trained on different examples"; only 3-of-5 norm-15 SVs flip benign behavior on >80% of prompts and only 3-of-24 Harmbench SVs achieve ASR > 70%. The implicit project picture is "L19 has *the* DoM direction that mediates correctness" (F-2, F-9 collapse to L19). If correctness-steering SVs at L19 from different MATH-500 problems are mutually orthogonal yet all transfer, "the L19 direction" is a misnomer — the right object is a low-dim subspace (matches P11-FE7 reframing). Falsifiable: optimize 5 one-shot correctness-steering SVs on 5 different MATH-500 problems at L19; compute the pairwise cosine matrix and the SVD of the 5×d stack. If pairwise cos < 0.5 yet each SV transfers individually, F-2 needs reframing as "low-dim manifold, not single direction."

## Direct connections to F-N / H-N

- **H-1 (per-position DoM steering moves MATH-500 accuracy)** — paper's promotion/suppression/mixed-steering loss is the direct comparison method to per-position DoM. Numerical anchor: 96.9% Harmbench ASR for the best one-shot SV vs CAA's lower ASR (Fig 2). Implication: H-1 should explicitly compare DoM-steering against one-shot-optimized-steering rather than just measuring DoM efficacy in isolation.
- **F-2 (prefill L19 DoM AUROC 0.7731)** — the "predict-control discrepancy" (max ROC-AUC 0.66 for steering-effective SVs, vs CAA-skyline highest classifier) directly threatens the implicit causal reading of F-2's 0.7731. Read F-2's body to confirm whether F-2 ever claims mediation vs only correlation. (See "Sources consulted".)
- **F-3 (cos(prefill, final) ≈ 0.046)** — the paper provides a *mechanism* for orthogonality of two predictive directions: predict-control discrepancy says many directions can carry similar predictive content via different geometric paths; orthogonality is then unsurprising rather than a deep geometric fact. Anchor: cos 0.046 from `scratch/pathway10_temporal_and_verifier_results.json`.
- **F-9 (CoE-60 redundant with single-layer L19 DoM)** — predict-control discrepancy is consistent with F-9's interpretation: features that reach the same predictive content via different geometric paths (CoE trajectory features vs single-layer DoM) carry the same information. Anchor: F-9's redundancy claim.
- **H-12 (semantic-entropy probes label-free for refusal)** — paper's one-shot optimization is itself a label-free-ish technique: it needs a *single* labeled (x,y) pair, not a contrastive dataset. This is a distinct point in the "label-light steering" design space worth noting alongside H-12.

## Methodologies extracted

- **Promotion steering (single-input gradient descent on log P(y|x;v))** — minimize `−Σ log P_model(y_{k+1} | y_k…y_1, x; v)` w.r.t. SV `v` added at one fixed layer. Trains a single residual-stream additive vector to push the model toward target output `y` on prompt `x`. *Replication cost*: 30min CPU per SV (single forward+backward graph, ~1k–10k steps Adam). *We'd plausibly run this*: yes — direct alternative to DoM for H-1; cheap on 1.5B at L19.
- **Suppression steering (one-shot)** — minimize `−Σ log(1 − P_model(...))` to push the model away from `y`. Useful for the *negative* half of correctness-steering (force a correct problem to fail). *Replication cost*: 30min CPU. *We'd plausibly run this*: yes — gives a complementary refutation experiment for F-2.
- **Mixed steering** — sum of promotion and suppression losses across two prompts (one to flip up, one to leave). *Replication cost*: 30min CPU. *We'd plausibly run this*: yes — useful for "force B-bucket → A-bucket" (recoverable problems, F-7).
- **Reentrant steering** (App A) — more invasive variant; details in appendix. *Replication cost*: unknown until App A read. *We'd plausibly run this*: no, not on first pass; only if vanilla promotion underperforms.
- **Dot-product classifier evaluation** — for any candidate SV `v`, compute `mean(activation · v)` over a prompt's tokens, then ROC-AUC for binary label. This is the "predict-control" probe that exposes the discrepancy. *Replication cost*: <1min on cached NPZs. *We'd plausibly run this*: yes — *highest-leverage cheap experiment* — apply to F-2's prefill DoM and to any optimized SV.
- **Surprisal-per-token vs base model evaluation** (Fig 5 right) — measure naturalness of steered generations under the base (non-finetuned) model's distribution as a sanity check that steering doesn't push activations into nonsense regions. *Replication cost*: 1h GPU per layer. *We'd plausibly run this*: maybe, downstream of a successful one-shot correctness SV.
- **Greedy beam search width 5 to construct optimization targets** (used in §6) — when the "right answer" target completion is uncertain, use beam-5 of the unsteered model on the *real* prompt as the target for steering on the *fake* prompt. *Replication cost*: trivial. *We'd plausibly run this*: yes — gives a label-free proxy for "what the model would say if it were correct."
- **Norm sweeping** — paper sweeps SV norm ∈ {2.5, 5, 7.5, 10, 15} (Fig 1) to expose the steering-strength tradeoff. *Replication cost*: trivial multiplier on optimization cost. *We'd plausibly run this*: yes — important for fair head-to-head with DoM (which has its own norm).

## Approaches & framings

- **Predict-control discrepancy (Wattenberg & Viégas 2024 frame)** — directions effective at *predicting* a behavior are not necessarily directions effective at *causing* a behavior. This is the most important conceptual import for our project: it explicitly separates the "AUROC story" (F-2, F-9) from the "steering story" (H-1) at a level our current docs do not. Recommend folding this term into FINDINGS.md F-2's caveats section.
- **One-shot ≠ unsupervised** — the paper distinguishes its *targeted* method (single labeled (x,y)) from Mack & Turner's unsupervised single-prompt method. This is a useful lens for our H-12 (label-free probes) work — there is a continuum from contrastive-dataset (CAA, our DoM) → single-labeled-example (this paper) → single-prompt-unsupervised (Mack & Turner) → fully unsupervised.
- **"Promotion" vs "CAA skyline"** — paper treats CAA (= our DoM construction) as a *data-intensive baseline / skyline* and shows that single-example optimization can match or exceed it for steering. Inverts the usual hierarchy where DoM is the simpler/cheaper choice.
- **Emergent misalignment as evidence for a "harm" subspace** — §5 finding that an SV optimized to write vulnerable code also induces unrelated misaligned outputs is framed as evidence for a *shared activation direction* mediating broad classes of behavior. The cross-pathway analogue for us: an SV optimized to flip *one* MATH-500 problem might steer correctness on *unrelated* MATH-500 problems — i.e. a correctness subspace.

## Datasets & benchmarks

- **Harmbench (Mazeika et al. 2024)** — safety eval pipeline; 364 benign + 151 harmful prompts. HF-accessible. Applicable? no — orthogonal task domain (safety, not math correctness).
- **Poser testbed (Clymer et al. 2024)** — 36 alignment-faking models on Llama-13B. Source code on github. Applicable? no — alignment-faking domain, not correctness.
- **Custom public-figure-occupation dataset (§6)** — names with one of six occupations, 76–97 names per occupation. Custom-built per paper. Applicable? no — but the *FIR (fictitious information retraction)* paradigm is interesting for an analogue: prefill the model with an incorrect MATH-500 final answer and study whether it self-corrects.

## Implementation details worth capturing

- Layer choices in the paper: **L10 (Llama-13B Poser)**, **L12 (Gemma-2-2B-it refusal)**, **L16 (Qwen-2.5-Coder-14B vulnerable code)**, **L20 (Qwen anti-refusal)**. Compares to our **L19 (Qwen-2.5-1.5B prefill DoM)**. Worth checking whether L19 in our 1.5B is the analogue of L20 in their Qwen-7B-anti-refusal sweep.
- Optimizer: Adam (Kingma 2017). Norm constraint enforced by sweeping target norms ∈ {2.5, 5, 7.5, 10, 15}.
- Optimization is at a *single* fixed layer — no per-position, no per-token; the SV is broadcast across all token positions of the residual stream.
- Optimization stops when steered-prob on fake prompt ≥ unsteered-prob of completion on real prompt (§6 setup) — a surprisal-equality stopping rule.
- Llama-3.1-8B-Instruct prefill-attack baseline only achieves 30.0% ASR vs 93.1% for the optimized SV — useful sanity baseline for any "the model was already going to be steered" objection.
- Two GitHub repos with full reproduction code (one-shot-steering-repro, one-shot-steering-misalignment) — substantially derisks any replication.

## Replicable intermediates

- **Predict-control discrepancy probe on F-2's DoM** — for *any* steering-vector candidate `v`, compute `mean(prefill_activation · v)` over the 500 cached MATH-500 prefill activations and produce ROC-AUC. Apply to (a) our prefill DoM (should give 0.7731 by construction), (b) a randomly-initialized vector (sanity), (c) a vector optimized to flip a single problem (paper's prediction: low AUROC despite steering effectiveness). Cached artifact: `pathway11_h100/prefill_gated_compute/results.json` + the underlying L19 prefill activations NPZ. <30min CPU.
- **One-shot SV via promotion loss on a single MATH-500 problem at L19** — pick one B-bucket problem (K=1 wrong, K=8 right per F-7), optimize an additive SV at L19 that maximizes log P(correct_answer | prompt). All math is on cached activations + a small attached optimizer; we have everything needed except the gradient pass (needs the model loaded — 1.5B fits on a 4090, so CPU-or-modest-GPU). Compare cos(optimized_SV, our_DoM). 1–2h on a 4090.
- **Mixed-steering correctness flip on a (correct, incorrect) pair** — promotion of the correct answer on the incorrect prompt + suppression of the incorrect answer on the correct prompt. Tests for the existence of a single direction that bidirectionally moves correctness. Same compute as above.
- **Norm-sweep dose-response** — once we have a one-shot correctness SV, sweep its norm ∈ {2.5…15} as the paper does (Fig 1) and plot accuracy lift vs steering-side-effect on a held-out 100 problems. Trivial multiplier.

## Cross-paper signals

- **2406.11717 (Arditi et al. — Refusal Direction)** — already in graph (status: pending_triage). Paper's §1.1 cites as the canonical "single direction mediates X" precedent for refusal; layer-12 Gemma sweep here directly extends Arditi.
- **2406.00045 (Cao et al. — BiPO)** — already in graph (status: pending_triage). Paper's §1.1 cites Turner et al. 2025 using BiPO and finding that on Gemini 1.5 v2 SV optimization no longer beats prompting baselines — a key caveat for any project that wants to scale steering to larger models.
- **2502.17424 (Betley et al. — Emergent Misalignment)** — NOT in graph; recommend admission. Direct precedent for paper's §5; if we ever pursue "correctness has a shared subspace" claims, this is the citation.
- **2509.12886 (Zhu — "The LLM Already Knows")** — already in graph (status: graphed, REPLICATED). The Dunefsky paper's §6 "FIR" question — does the model's correctness signal exist *before* the explicit verbalization — is directly the same question Zhu's "the LLM already knows" asks at a different level. Worth cross-citing.
- **Wattenberg & Viégas 2024 (predict-control discrepancy)** — likely a blog/short-form piece; would need to check arxiv. NOT in graph; recommend admission as the citation for the "predict-control" framing.
- **Belrose 2024 (diff-in-means worst-case optimal)** — blog post, not arxiv. CITED-ONLY worthy footnote on F-2.
- **Mack & Turner 2024 (unsupervised one-prompt SV)** — referenced as the unsupervised analogue; arxiv ID not extracted from §1.1. Worth searching and admitting if relevant to H-12.

## Proposed FutureExperiments

```yaml
- id: P11-FE80
  pathway: P11
  description: "Predict-control discrepancy probe on F-2's prefill L19 DoM. Compute ROC-AUC of `mean(prefill_activation · v)` over cached 500 MATH-500 prefill activations for v ∈ {our prefill DoM (sanity = 0.7731), random unit vector (null), one-shot optimized SV from P11-FE81}. Tests Wattenberg-Viégas predict-control discrepancy on our setting: if optimized SV has high steering effect but ROC-AUC < 0.65 as a classifier, F-2's mediation interpretation is overturned."
  rationale: "Cheapest possible test of whether F-2's 0.7731 AUROC reflects a causal/control direction or merely a predictive correlation. Predict-control discrepancy is the central conceptual import of Dunefsky 2025."
  trigger: "Paper §3.3 dot-product-classifier result: ROC-AUC max 0.66 for the five norm-15 SVs that flip behavior on >80% of benign prompts."
  status: READY
  priority: HIGH
  roi: 9
  cost: "30min CPU on cached NPZs"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2502.18862"]
  blocked-by: null
  blocked-by-experiment: ["P11-FE81"]
```

```yaml
- id: P11-FE81
  pathway: P11
  description: "One-shot promotion-steering for MATH-500 correctness at L19 of Qwen-2.5-1.5B. Pick one B-bucket problem (K=1 wrong, K=8 right). Optimize an additive SV `v` at L19 to maximize log P(correct_completion | prompt; v) via Adam, sweep norms {2.5, 5, 7.5, 10, 15} per paper Fig 1. Evaluate transfer: accuracy lift on held-out 499 problems for each norm. Compare against (a) F-2's prefill DoM as steering vector, (b) random unit vector at matched norm. Headline metric: best transfer accuracy lift, and cos(optimized_SV, our_DoM)."
  rationale: "Direct H-1 test using the paper's targeted optimization rather than DoM. If one-shot SV transfers but DoM doesn't, H-1's 'DoM is the right steering form' is overturned. Also produces the optimized SV input to P11-FE80."
  trigger: "Paper main result: 96.9% Harmbench ASR with single-example one-shot optimization; CAA-skyline as data-intensive baseline."
  status: READY
  priority: HIGH
  roi: 9
  cost: "2h on 4090 (1.5B fits) or 1h H100"
  depends-on: [F-2, F-7]
  would-update: [F-2]
  triggered-by: ["2502.18862"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE82
  pathway: P11
  description: "Subspace-vs-direction test for L19 correctness signal. Optimize 5 one-shot correctness-steering SVs at L19 on 5 different B-bucket MATH-500 problems (1 per difficulty bucket). Compute the pairwise cosine matrix and the SVD spectrum of the stacked 5×d matrix. If pairwise cos < 0.5 yet each SV transfers (≥+5pp on held-out 100 problems), F-2's 'single direction' is wrong — the right object is a low-dim subspace. Cross-validates against P11-FE7 (SVD of per-problem prefill activations)."
  rationale: "Paper §3.3 documents 'high variability' across training examples — different examples yield SVs that all work but differ in direction. This is the strongest evidence that 'L19 mediates correctness via a subspace, not a single vector.'"
  trigger: "Paper §3.3 high-variance result + §1.1 framing of 'many directions can mediate the same behavior'."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "10h on 4090 or 5h H100 (5× the FE81 cost)"
  depends-on: [F-2, F-7]
  would-update: [F-2]
  triggered-by: ["2502.18862"]
  blocked-by: null
  blocked-by-experiment: ["P11-FE81"]
```

## Proposed HYPOTHESES.md additions

### H-72: Predict-control discrepancy holds for L19 prefill DoM
**Priority:** HIGH
**Motivated by:** 2502.18862 + F-2 (prefill DoM AUROC 0.7731) + EXP-037
**Test:** Compute ROC-AUC of `mean(prefill_activation · v)` on cached 500 MATH-500 prefill activations for v ∈ {prefill DoM, random unit, one-shot optimized SV from H-73}. <30min CPU on cached NPZs. Confirm: optimized SV with strong steering effect should have classifier ROC-AUC well below 0.7731 if the discrepancy holds.
**Requires:** cached `pathway11_h100/prefill_gated_compute/` activations + the optimized SV from H-73; CPU only.
**Would change:** confirm → F-2's mediation reading is downgraded to "predictive correlation only" and the 0.7731 number is reframed as a probe AUROC, not evidence for a causal direction. reject → F-2's causal reading survives a non-trivial challenge from the steering literature.
**Blocks:** H-1 framing — if confirmed, H-1 must be reformulated as "the *optimized* per-position SV moves accuracy" rather than "the *DoM* per-position SV moves accuracy."

### H-73: One-shot optimized L19 SV transfers correctness across MATH-500
**Priority:** HIGH
**Motivated by:** 2502.18862 §3 + §4 (96.9% Harmbench ASR from single example)
**Test:** Pick one B-bucket MATH-500 problem (K=1 wrong, K=8 right). Run Adam on additive L19 SV maximizing log P(correct_completion | prompt; v) at norms {2.5, 5, 7.5, 10, 15}. Evaluate accuracy lift on held-out 499. 2h on 4090 / 1h H100.
**Requires:** Qwen-2.5-1.5B loaded + autograd through L19 residual; 4090 sufficient; no new data.
**Would change:** confirm → H-1 has a working steering construction (not the DoM but a one-shot optimized vector); the project gains a per-pathway tool for "make this problem correct" interventions. reject → either L19 doesn't mediate correctness causally or the one-shot method doesn't generalize from refusal/safety to math correctness — both are interesting, the latter is consistent with Turner-et-al.-2025 scale-failure on Gemini 1.5v2.
**Blocks:** H-72, H-74; provides the optimized-SV input to both.

### H-74: L19 correctness signal is a low-dim subspace, not a single direction
**Priority:** MEDIUM
**Motivated by:** 2502.18862 §3.3 (high variance across training examples) + P11-FE7 (SVD of prefill activations)
**Test:** Optimize 5 one-shot correctness-steering SVs on 5 different B-bucket problems (1 per difficulty level if possible). Compute pairwise cos matrix + SVD spectrum. If pairwise cos < 0.5 *and* each SV transfers (≥+5pp on held-out 100), the underlying object is a subspace.
**Requires:** ~5× cost of H-73; same hardware.
**Would change:** confirm → F-2 reframed from "single direction" to "low-dim manifold" — consistent with P11-FE7's framing. reject → "single direction at L19" survives.
**Blocks:** F-2 and F-9 reframing.

## Proposed PAPER_INDEX.md classification

## 2502.18862 — One-shot Optimized Steering Vectors Mediate Safety-relevant Behaviors in LLMs (Dunefsky & Cohan, 2025)

**Relevance:** Methodological alternative to contrastive-mean (CAA / our DoM) steering, plus the Wattenberg-Viégas "predict-control discrepancy" frame. The paper shows (a) single-example gradient-descent optimization of a residual-stream additive vector can match or exceed CAA for steering, and (b) directions effective at *steering* a behavior have ROC-AUC ≤ 0.66 as *classifiers* of that behavior — separating the predictive and causal stories. Both points bear directly on F-2's 0.7731 prefill AUROC: if the discrepancy holds in our setting, F-2 is a predictive correlation rather than a mediating direction, and H-1 needs a different steering construction than the DoM.

**Key claim we tested:** Single-example optimized SVs at a fixed layer transfer across inputs to mediate a target behavior, while being uncorrelated with classifier-effective directions for the same behavior.

**Our result:** **TO TEST** Predict-control discrepancy and one-shot transfer have not yet been evaluated on MATH-500 correctness at L19. P11-FE80–82 (READY) and H-72/H-73/H-74 (HIGH/HIGH/MEDIUM) test exactly this.

### Methodologies

- Promotion steering (single-input gradient descent on log P(y|x;v))
- Suppression steering (single-input minimize log(1−P))
- Mixed steering (sum of promotion and suppression)
- One-shot optimization (single (x,y) pair at a fixed layer; norm-constrained)
- Reentrant steering (more invasive; App A)
- Dot-product classifier evaluation (ROC-AUC of mean(activation·v) for any candidate v)
- Surprisal-per-token vs base model as naturalness sanity check
- Greedy beam search width 5 to construct optimization targets when ground truth is uncertain
- Norm sweeping ∈ {2.5, 5, 7.5, 10, 15} for steering-strength tradeoff curve

### Approaches & framings

- Predict-control discrepancy (Wattenberg & Viégas 2024) — directions effective at *predicting* a behavior need not be effective at *causing* it
- Targeted single-labeled-example SVs distinct from unsupervised single-prompt SVs (Mack & Turner)
- CAA (= our DoM) as data-intensive *skyline*, not floor
- Emergent misalignment as evidence for a shared activation subspace mediating broad behavior classes

### Datasets

- Harmbench (Mazeika 2024) — 364 benign + 151 harmful; HF; not applicable to MATH-500
- Poser testbed (Clymer 2024) — 36 alignment-faking Llama-13B models; not applicable to MATH-500
- Custom public-figure-occupation dataset (§6) — 76–97 names per occupation; not applicable
- (No correctness/math benchmarks)

### Implementation details

- Layers: L10 (Llama-13B), L12 (Gemma-2-2B-it), L16 (Qwen-2.5-Coder-14B), L20 (Qwen anti-refusal)
- Optimizer: Adam; norm sweep ∈ {2.5, 5, 7.5, 10, 15}
- SV is layer-broadcast across all token positions
- Stopping rule (§6): steered-prob on fake prompt ≥ unsteered-prob on real prompt
- Code: github.com/jacobdunefsky/one-shot-steering-repro and one-shot-steering-misalignment

### Replicable intermediates

- Dot-product classifier ROC-AUC on cached prefill activations vs F-2's DoM (<30min CPU)
- One-shot promotion SV at L19 on a single B-bucket MATH-500 problem (2h on 4090)
- 5-SV subspace test on different MATH-500 problems (~10h on 4090)

### Cross-paper signals

- Arditi et al. 2406.11717 — already in graph; the canonical "single direction mediates X" precedent extended here
- Cao et al. 2406.00045 (BiPO) — already in graph; cited via Turner et al. 2025's negative scale result on Gemini 1.5v2
- Betley et al. 2502.17424 (Emergent Misalignment) — NOT in graph; recommend admission
- Zhu 2509.12886 — already in graph; same "model already knows" intuition as paper's §6 FIR
- Mack & Turner 2024 (unsupervised single-prompt SV) — likely worth admitting; relates to H-12

**Related experiments:** P11-FE80, P11-FE81, P11-FE82, H-72, H-73, H-74

**Status:** TO TEST

## New claims

- "Predict-control discrepancy on F-2's prefill DoM" — will become a quantitative claim once P11-FE80 runs and produces a number (e.g. "optimized SV ROC-AUC = X.XXX vs prefill DoM 0.7731 at matched steering effectiveness"). No new claims to register *yet*; register on completion of FE80.
- One-shot transfer-set accuracy lift from H-73 will be a new quantitative claim once measured. No claim to register yet.

## Sources consulted

- `~/topo-confidence/CLAUDE.md` (Headlines section only) — for the AUROC 0.7731, cos 0.046, and 71.6% selective-prediction anchors
- `~/topo-confidence/FINDINGS.md` and `~/topo-confidence/HYPOTHESES.md` (one-liner grep only, not full bodies) — for F-N / H-N enumeration
- `~/topo-confidence/PAPER_INDEX.md` (status-flag grep only) — for current-paper classifications
- `cd ~/topo-confidence/research-graph && python query.py paper 2502.18862` — prior LLM admission note
- `cd ~/topo-confidence/research-graph && python query.py future P11` — to find next available P11-FE ID
- `cd ~/topo-confidence/research-graph && python query.py paper {2406.11717, 2402.13212, 2406.00045, 2502.17424, 2509.12886}` — to check whether cited papers are already in the graph
- arxiv abs page + full PDF (10 pages) of 2502.18862 — paper read
