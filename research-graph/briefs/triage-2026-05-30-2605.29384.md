# Triage brief — 2605.29384 — Latent Terms: Dense Retrievers Contain Trivially Extractable BM25-ready Zipfian Vocabularies

Source: https://arxiv.org/abs/2605.29384
Triaged: 2026-05-30

## Refutations

**R1 — Could refute the sufficiency of a single direction (F-2: L19 DoM AUROC 0.7731).**
The paper decomposes frozen model activations via SAE into 32,768 sparse latent features and finds three qualitatively distinct feature types (lexical ~33%, narrow semantic ~10%, broad topical ~57%). If an analogous SAE decomposition of L19 prefill activations reveals that correctness-discriminative information is distributed across many sparse features of heterogeneous type — and that no single latent feature achieves AUROC > 0.65 while the top-K ensemble exceeds 0.7731 — this would refute the claim that a "single direction" captures the correctness signal. The DoM/PC1 cosine of 0.9216 would then be reinterpreted as a low-rank compression artifact of the linear probe rather than evidence that the signal is intrinsically one-dimensional.
*Falsifiable experiment:* Train top-K SAE (K=16, 32K latents) on L19 prefill activations with reconstruction-only loss. Measure per-feature AUROC and compare max single-feature AUROC to ensemble AUROC. If max_single < 0.65 and ensemble > 0.7731, refutation fires.

**R2 — Could refute the linear-scoring ceiling (F-2 + cov-spectrum AUROC 0.7928).**
The paper demonstrates a dramatic scoring-method asymmetry: SAE features scored with dot product achieve 0.381 nDCG@10 but with BM25 achieve 0.582 — a 53% relative gap. Our entire probe pipeline (logistic regression, ridge, PCA projections) is inner-product/linear. If SAE features from L19 scored with BM25-style sublinear weighting (sqrt-tf × IDF) substantially beat 0.7928, this would show the current "ceiling" is an artifact of linear scoring applied to features with inherently nonlinear distributional structure. The mechanism: reconstruction-trained SAE features have Zipfian collection statistics that reward saturation-aware scoring, which dot product / linear probes cannot express.
*Falsifiable experiment:* Extract SAE features from L19 prefill, compute BM25-weighted sparse representations per problem, train a classifier on BM25 scores vs. correctness labels. If AUROC > 0.82 (>3% above current ceiling), refutation fires.

**R3 — Could refute CoE-60 redundancy with single-layer DoM (F-9).**
The paper's feature taxonomy shows that different layers and positions in the same model contribute qualitatively different feature types. If SAE decomposition across multiple layers (as in CoE-60) reveals that intermediate layers contribute narrow-semantic features while L19 contributes broad-topical features — and combining them provides non-redundant signal — this would refute F-9's claim that CoE-60 is redundant with L19 DoM. The mechanism: single-layer DoM can only capture the dominant variance direction, while multi-layer SAE features can capture distributed, type-heterogeneous structure.
*Falsifiable experiment:* Train separate SAEs on L5, L10, L15, L19. Measure feature-type distribution overlap. If L19-only features have <50% type overlap with the union and the multi-layer ensemble AUROC exceeds single-layer by ≥0.03, refutation fires.

## Direct connections to F-N / H-N

- **F-2 (L19 DoM AUROC 0.7731):** The paper's SAE methodology provides a concrete, unsupervised alternative to the supervised DoM probe. Their reconstruction-only training on unlabeled text requires no correctness labels, which would test whether the correctness signal emerges from the activation geometry alone or requires supervision. Directly tests whether the 0.7731 can be matched or beaten by unsupervised feature extraction.

- **F-10 (PH null — topology not distinguishable from Gaussian):** The paper's Zipfian analysis provides a non-topological probe of distributional structure. If L19 SAE features exhibit Zipfian statistics (non-Gaussian heavy tails), this would identify non-Gaussian structure that PH misses — consistent with F-10 (PH is null) but suggesting the geometry is non-trivial in ways PH can't detect.

- **H-391 (top-K≈20 SAE features match DoM AUROC 0.7731):** Directly addressed. This paper provides a concrete SAE architecture (top-K, 32K latents, reconstruction loss on unlabeled data) that could be applied to test H-391 without any retrieval-specific training.

- **H-539 (TopK SAE recovers sparse correctness feature with AUROC > 0.7731):** The paper's methodology is exactly what H-539 calls for, minus the correctness supervision. If reconstruction-only SAE features predict correctness, H-539 is confirmed in a stronger form (no supervision needed).

- **H-795 (SAE decomposition reveals multiple distinct correctness-predictive latents):** The paper's three-type taxonomy (lexical/narrow-semantic/broad-topical) provides a concrete framework for categorizing whatever features SAE extracts from L19. If analogous heterogeneity appears, H-795 is supported.

## Methodologies extracted

- **Top-K SAE with reconstruction-only training** — Sparse autoencoder with fixed top-K sparsity (K=16) and 32,768 latent dimensions, trained purely on reconstruction loss against unlabeled text (FineWeb-Edu). No task-specific supervision.
  *Replication cost*: 2h on single A100 for full training; ~30min CPU to apply a pre-trained SAE to cached activations.
  *We'd plausibly run this*: yes — can train SAE on L19 prefill activations (500×1536) using reconstruction loss only, then score latents against correctness labels post-hoc.

- **Sum-pooling + sublinear activation transform** — Aggregates per-token SAE codes via sum-pooling, then applies sqrt transform to match Zipfian statistics for BM25 scoring.
  *Replication cost*: 10min CPU once SAE features exist.
  *We'd plausibly run this*: yes — applicable to any per-token SAE output aggregated across sequence positions.

- **BM25 scoring over latent features** — Standard Okapi BM25 formula (k₁=8, b=0.7) applied to SAE latent activations rather than term frequencies. IDF computed from feature collection statistics.
  *Replication cost*: 20min CPU.
  *We'd plausibly run this*: yes — BM25 is a nonlinear scoring function we've never tried on our activation features; directly tests whether nonlinear reweighting beats linear probing.

- **Zipfian distribution analysis of latent features** — Fitting rank-frequency distributions to SAE feature activation statistics to check for power-law / Zipfian structure.
  *Replication cost*: 10min CPU on cached features.
  *We'd plausibly run this*: yes — trivial to compute from cached L19 activations or SAE features.

- **LLM-assisted feature taxonomy** — Using an LLM (Gemini 3 Pro in their case) to categorize SAE features into lexical / narrow-semantic / broad-topical types, with human verification.
  *Replication cost*: 1h LLM API + manual review.
  *We'd plausibly run this*: maybe — useful for interpretability of SAE correctness features if H-795 fires, but not a priority until SAE training is done.

## Approaches & framings

- **Scoring function ≠ representation quality.** The paper decouples the scoring operator from the learned representation, showing that the same features can perform dramatically differently under different scoring regimes (dot product vs BM25: 0.381 vs 0.582). This intersects with F-2 and the cov-spectrum ceiling (0.7928): our entire evaluation pipeline uses linear scoring (logistic regression, ridge). A nonlinear scoring regime might unlock signal already present in cached features.

- **Reconstruction-only SAE as unsupervised feature extractor.** Training SAEs with no task labels recovers task-relevant structure. This reframes H-391/H-539: we don't need correctness labels to extract correctness-predictive features; reconstruction-only SAE might find them because the features that explain activation variance may naturally separate correct from incorrect.

- **Zipfian distributional structure as a diagnostic.** The presence or absence of Zipfian statistics in SAE-extracted features is a quick check for whether the features have "term-like" structure. Applicable as a diagnostic for our L19 activations — non-Zipfian distribution would suggest the representation space has fundamentally different structure from retrieval models.

## Datasets & benchmarks

- **BEIR** — 15-dataset retrieval benchmark. Not applicable to MATH-500 / hidden-state pipeline.
- **LIMIT** — Benchmark designed to expose single-vector retrieval failures. Not applicable.
- **FineWeb-Edu** — Unlabeled web text used for SAE training. Accessible (HF). Applicable: yes — could be used as SAE training corpus for L19 activations if we want an external text distribution, though our own 500 MATH-500 activations may suffice for the initial test.

## Implementation details worth capturing

- SAE architecture: top-K with K=16, 32,768 latents, AdamW optimizer, lr=0.001 with 5% linear warmup + cosine decay
- Training: 30B tokens, 3 epochs, batch size 4096, single A100 under 2h
- Activation transform: square-root as default sublinear function (better than log, linear)
- BM25 defaults: k₁=8, b=0.7 — much higher k₁ than typical text BM25 (usually 1.2–2.0), reflecting the different activation magnitude distribution
- Pruning: removing top 1% most-activated features loses only 1% performance; top 10% loses 8.3%
- 5 random seeds used for all SAE training runs
- No code link provided in the paper

## Replicable intermediates

- **Zipfian distribution check on raw L19 features.** We have cached 500×1536 L19 prefill activations. Compute rank-frequency distribution of feature activations across the 500 samples. Fit power-law vs. exponential. Script: `pathway11_h100/pca_covariance/` has PCA infrastructure; rank-frequency is a one-liner on the existing eigenvalue spectrum.
- **BM25-style reweighting of existing PCA features.** Take the top-20 PC projections (FE881 cov-spectrum features), apply sqrt transform + IDF weighting (treating each PC coefficient as a "term frequency" across the 500 problems), then logistic regression. Compare to the existing 0.7928 AUROC. This tests the scoring-regime hypothesis without training an SAE.
- **Feature activation distribution shape.** Check whether L19 prefill activations per-dimension follow Zipfian, Gaussian, or other distributions. Kolmogorov-Smirnov test against each. Uses only cached NPZs.

## Cross-paper signals

- 2406.04093 — already in graph (status: graphed). SAE interpretability work that motivated H-795; this paper provides a concrete SAE training recipe.
- 2505.23556 — already in graph (status: graphed). SAE-based mechanistic interpretability that motivated H-391; this paper's unsupervised training approach is a key methodological alternative.
- 2602.11910 — already in graph (status: graphed). SAE + activation steering; this paper's reconstruction-only approach contrasts with supervised SAE methods.
- 2309.08600 — NOT in graph; recommend admission. Gao et al. "Scaling and Evaluating Sparse Autoencoders" — foundational SAE scaling work this paper builds on; relevant to H-391/H-539 SAE methodology choices.
- 2407.14435 — NOT in graph; recommend admission. Rajamanoharan et al. "Jumping Ahead: Improving Reconstruction Fidelity with JumpReLU Sparse Autoencoders" — alternative SAE architecture this paper explicitly notes as unexplored; could matter for H-391.

## Proposed FutureExperiments

### Tier 1 — Cheap local (5min–2hr, CPU only, ROI 5–10)

```yaml
- id: P11-FE130450
  pathway: P11
  description: "Zipfian distribution analysis of L19 prefill activation features: fit rank-frequency distribution of per-dimension activation magnitudes across 500 MATH-500 samples. Test power-law vs exponential vs Gaussian. Compare correct vs incorrect subgroups."
  rationale: "Paper shows retrieval model SAE features follow Zipfian distribution. If raw L19 activations (pre-SAE) already show Zipfian structure, this motivates BM25-style scoring; if Gaussian, it explains why linear probes are near-optimal."
  trigger: "2605.29384 demonstrates Zipfian distributional structure in frozen-model activations"
  status: READY
  priority: HIGH
  roi: 7
  cost: "15min CPU"
  depends-on: [F-2, F-10]
  would-update: [F-2, F-10]
  triggered-by: ["2605.29384"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130451
  pathway: P11
  description: "BM25-style nonlinear reweighting of existing PCA/cov-spectrum features: apply sqrt transform + IDF weighting to top-20 PC projections (FE881 features), then 5-fold logistic regression. Compare AUROC to current 0.7928 linear ceiling."
  rationale: "Paper shows 53% relative performance gap between dot-product and BM25 scoring on same SAE features. If analogous gap exists for our cov-spectrum features, the current 0.7928 ceiling is a scoring artifact, not a signal ceiling."
  trigger: "2605.29384 Table 6 shows dot-product vs BM25 scoring asymmetry"
  status: READY
  priority: HIGH
  roi: 8
  cost: "20min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.29384"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE130452
  pathway: P11
  description: "Feature activation distribution shape test on L19 prefill: per-dimension Kolmogorov-Smirnov test of each of the 1536 L19 feature dimensions against Zipfian, Gaussian, and Laplacian nulls. Compute fraction of dimensions rejecting each null at p<0.01. Separate correct vs incorrect."
  rationale: "F-10 says PH can't distinguish from Gaussian null. But PH tests topology, not marginal distributions. Per-dimension distributional tests may reveal non-Gaussian structure PH misses."
  trigger: "2605.29384 Zipfian analysis methodology"
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "10min CPU"
  depends-on: [F-10]
  would-update: [F-10]
  triggered-by: ["2605.29384"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required (hours–days, high ROI only)

```yaml
- id: P11-FE130453
  pathway: P11
  description: "Train top-K SAE (K=16, 32K latents) on L19 prefill activations with reconstruction-only loss on unlabeled text activations. Then score each latent against correctness labels. Measure: (a) per-feature max AUROC, (b) top-K ensemble AUROC vs 0.7731 DoM baseline, (c) feature-type heterogeneity."
  rationale: "Tests H-391/H-539/H-795 simultaneously using the exact methodology from this paper. Reconstruction-only training means no correctness supervision — if it still predicts correctness, the signal is structural."
  trigger: "2605.29384 demonstrates unsupervised SAE extraction recovers task-relevant features"
  status: READY
  priority: HIGH
  roi: 9
  cost: "2h H100"
  depends-on: [F-2, F-9]
  would-update: [F-2, F-9]
  triggered-by: ["2605.29384"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-930: BM25-style nonlinear scoring of L19 cov-spectrum features beats the 0.7928 linear AUROC ceiling
**Priority:** HIGH
**Motivated by:** 2605.29384 + F-2
**Test:** Apply sqrt-tf × IDF reweighting to top-20 PC projections from FE881, 5-fold logistic regression on reweighted features. 20min CPU.
**Requires:** CPU, cached cov-spectrum features from FE881
**Would change:** If confirmed (AUROC > 0.82), the "ceiling" shifts from 0.7928 to a higher value and the bottleneck is scoring regime, not feature quality. If rejected (AUROC ≈ 0.79), linear scoring is near-optimal for these features.
**Blocks:** nothing

### H-931: Reconstruction-only SAE on L19 prefill activations recovers correctness-predictive features without supervision
**Priority:** HIGH
**Motivated by:** 2605.29384 + H-391 + H-539
**Test:** Train top-K SAE (K=16, 32K latents) on L19 activations with reconstruction loss only (no correctness labels). Post-hoc correlate latent activations with correctness. 2h H100.
**Requires:** H100, L19 prefill activations (cached), unlabeled text activations for training distribution
**Would change:** If confirmed, correctness information is structural (emerges from reconstruction), not supervisory. Reframes F-2 from "a direction we find with labels" to "a direction the model's own geometry exposes." If rejected, supervision is necessary and H-391/H-539 remain the path.
**Blocks:** H-795

## Proposed PAPER_INDEX.md classification

## 2605.29384 — Latent Terms: Dense Retrievers Contain Trivially Extractable BM25-ready Zipfian Vocabularies (Clavié, Lee, Shakir, Kato, 2026)

**Relevance:** Methodology transfer paper. Demonstrates that SAEs trained with reconstruction-only loss on frozen model activations extract sparse latent features with Zipfian statistics, and that BM25 scoring dramatically outperforms dot-product scoring on such features. Directly applicable to H-391/H-539/H-795 (SAE decomposition of L19 prefill activations) and provides a concrete recipe. The scoring-regime finding (dot product vs BM25 gap of 53% relative) challenges our exclusive reliance on linear scoring, potentially relevant to the cov-spectrum ceiling of 0.7928.

**Key claim we tested:** Dense model activations decompose via SAE into sparse features with Zipfian statistics that reward nonlinear (BM25) scoring over linear (dot product) scoring.

**Our result:** **TO TEST** — proposed FE130450–FE130453 would test whether L19 prefill activations exhibit Zipfian structure and whether BM25-style scoring lifts the linear ceiling.

**Related experiments:** P11-FE130450, P11-FE130451, P11-FE130452, P11-FE130453, H-930, H-931, H-391, H-539, H-795

**Status:** TO TEST

## New claims

None. This brief quotes only existing headline numbers (AUROC 0.7731, 0.7928, cos 0.9216) and numbers from the paper itself. No new quantitative claims are introduced into narrative docs.

## Sources consulted

- ~/topo-confidence/CLAUDE.md §Headline numbers — default context
- ~/topo-confidence/FINDINGS.md (grep only, one-liners) — default context
- ~/topo-confidence/HYPOTHESES.md (grep only, one-liners + H-391, H-539, H-795 bodies) — default context + on-demand for admission-note cross-references
- ~/topo-confidence/PAPER_INDEX.md (grep only, status flags) — default context
- research-graph query.py paper lookups for 2605.29384, 2406.04093, 2505.23556, 2602.11910, 2309.08600, 2407.14435, 2312.03078, 2407.20151, 2506.06609, 2309.10305 — cross-paper signal checks
