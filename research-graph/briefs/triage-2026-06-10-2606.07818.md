# Triage brief — 2606.07818 — Representational Similarity and Model Behavior in Multi-Agent Interaction

Source: https://arxiv.org/abs/2606.07818
Triaged: 2026-06-10

> Authors: Yujin Potter, Seun Eisape, Shiyang Lai, Alexander Huth, James Evans, Been Kim, Jacob Eisenstein, Dawn Song, Alane Suhr (2026).
> One-line: 276 LLM **pairs** play 8 cooperation/novelty games; pairs with more **similar representation spaces** cooperate more but produce **less novelty**, robust to controls for performance disparity and model size; **early-layer** similarity is the strongest predictor.

**Topical honesty up front.** This is a multi-agent social-science paper about *pairwise* representational similarity driving *cooperation vs. creativity* in games. It is not about single-model correctness prediction. Its relevance to this program is almost entirely **methodological transfer**: the RSA/CKA cross-model similarity machinery and the *early-layer-is-most-predictive* result map directly onto our cached cross-scale (1.5B vs 7B) L19-prefill activations and onto H-896 / F-6. The refutations below are real but fire *indirectly*, through that transfer — none is a head-on contradiction of a correctness number.

## Refutations

1. **Refutes the L19-is-special framing behind F-2 / F-6 (layer-of-measurement confound).**
   Mechanism: the paper's headline secondary result is that **early-layer** representational similarity carries the strongest behavior-predictive signal, attributed to shared lexical/semantic grounding; middle/late layers are weaker. Our whole stack privileges **L19** (a *middle* layer, ~19/28). If a layer-swept *cross-model* similarity-vs-correctness analysis on our cached NPZs shows early layers matching or beating L19, then L19's apparent specialness is partly a *measurement-layer* artifact rather than an emergent confidence locus.
   Falsifiable: compute RSA(h_1.5B^ℓ, h_7B^ℓ) per layer ℓ over the 500 MATH-500 problems, regress against per-problem 1.5B correctness; if early-layer RSA AUROC ≥ L19 RSA AUROC, F-2/F-6's L19-centric reading is weakened.

2. **Refutes F-2's "a single direction is the carrier" by offering a relational alternative.**
   Mechanism: the paper's entire predictive object is a **pairwise/relational** quantity (similarity between two representation spaces), not a fixed direction inside one space. If a purely *relational* per-problem signal — cross-model agreement cos(h_7B, h_1.5B) at L19 prefill — predicts 1.5B correctness at AUROC comparable to the supervised DoM's **0.7731**, then the DoM direction is not privileged; the correctness signal could be relational geometry that a single hyperplane only approximates.
   Falsifiable: this is exactly H-896. Compute per-problem cross-model cos at L19 prefill, AUROC vs correctness; ≥ ~0.77 (or material lift when stacked on DoM) refutes the single-direction sufficiency claim.

3. **Refutes the pure-correctness interpretation of F-4 / F-8 via a correctness↔novelty trade-off.**
   Mechanism: the paper establishes (within its regime) that **more-similar / lower-diversity representations → less novelty**. F-4 says *correct* trajectories collapse harder (lower-dimensional, more "similar to each other"). Chaining these: the very collapse that F-4/F-8 read as a *correctness* signal may partly be an *exploration/diversity-suppression* axis. If high-DoM (predicted-correct, high-collapse) problems also show lower solution diversity across K samples, then the DoM/selective-prediction signal is entangled with a creativity axis, not pure correctness.
   Falsifiable: on MATH-500 problems with multiple valid solution paths, bin by L19 prefill DoM projection and measure inter-sample solution-path diversity across K draws; a monotone DoM↑ → diversity↓ relationship would contaminate the F-8 correctness story.

## Direct connections to F-N / H-N

- **H-896** (cross-model activation agreement cos(h_7B, h_1.5B) at L19 prefill predicts 1.5B correctness independently of DoM) — this paper is the literature anchor for that hypothesis; its RSA/CKA toolkit is the exact method H-896 needs. Numerical target: beat or complement DoM AUROC **0.7731**.
- **F-6** (1.5B/7B prefill PR inversion driven by incorrect-group concentration) — the paper's layer-wise cross-model similarity sweep is the natural way to ask *at which layer* the two scales diverge; the inversion may coincide with a similarity breakpoint.
- **F-1** (dimensional breathing universal across scales) — "similar representation spaces" is operationally a low-effective-dimensionality statement; the breathing/participation-ratio curve is our per-layer dimensionality profile. Early-layer-high-similarity ↔ early-layer breathing-curve shape is testable on cached PR curves.
- **H-20 / H-214 / H-394** (cross-model kernel alignment & cross-scale DoM transfer) — RSA/CKA gives a principled similarity kernel to operationalize "transfer," replacing ad-hoc cosine.
- **F-2** (single L19 DoM at AUROC **0.7731**) — challenged by the relational-vs-directional framing (Refutation 2).

## Methodologies extracted

- **Representational Similarity Analysis (RSA)** — build a problem×problem representational dissimilarity matrix (RDM) per model from activations, then compare two models via second-order (Spearman) correlation of their RDMs.
  *Replication cost*: ~20min CPU on cached 500×1536 NPZs (two models).
  *We'd plausibly run this*: **yes** — directly feeds H-896 and gives a layer-agnostic cross-model similarity not dependent on the DoM direction.
- **CKA (linear & kernel, Kornblith 2019)** — bias-corrected normalized similarity between two activation matrices; rotation-invariant, the standard cross-model metric.
  *Replication cost*: ~15min CPU on cached NPZs.
  *We'd plausibly run this*: **yes** — cleaner than raw cosine for cross-scale (1536-d vs 3584-d) comparison.
- **Layer-wise similarity sweep** — recompute the similarity metric at every layer and locate where it is most behavior-predictive.
  *Replication cost*: ~30min CPU if multi-layer activations are cached; else needs re-extraction.
  *We'd plausibly run this*: **yes** if cross-layer NPZs exist — it is the test for Refutation 1.
- **Procrustes alignment** — orthogonal map between the two representation spaces; alignment residual as a (dis)similarity score.
  *Replication cost*: ~10min CPU.
  *We'd plausibly run this*: **maybe** — useful as a third similarity view; lower priority than CKA/RSA.
- **Mixed-effects regression with covariate controls (model size, performance disparity)** — the statistical core: predict behavior from similarity while partialling out confounds via random effects.
  *Replication cost*: ~10min CPU (statsmodels/lme4-style).
  *We'd plausibly run this*: **yes, as a control pattern** — when we relate cross-model similarity to correctness we should partial out per-problem difficulty and accuracy-gap, exactly as the paper partials out performance disparity.

## Approaches & framings

- **Relational > directional.** Behavior is predicted by *similarity between two spaces*, not by a coordinate inside one space. Intersects F-2/H-896: invites a relational rival to the DoM hyperplane.
- **Cooperation↔novelty trade-off as a representational consequence.** Reframes low-dimensionality not as "good" but as "less creative." Intersects F-4/F-8: a caution that collapse may not be a pure correctness virtue.
- **Early-layer grounding hypothesis.** Shared lexical/semantic substrate in early layers does the predictive work. Intersects F-1/F-6: predicts the cross-scale signal should be *strongest before* L19, not at it.

## Datasets & benchmarks

- **8 multi-agent cooperation/novelty games (276 model pairs)** — custom interactive game suite; not publicly characterized in the abstract. Applicable? **no** — social-game interaction data, orthogonal to MATH-500 single-model correctness.

## Implementation details worth capturing

- Predictor is *pairwise* representational similarity; outcomes are cooperation and novelty/creativity scores from game transcripts.
- Controls explicitly include **performance disparity** and **model size** — adopt these as covariates in any similarity→correctness regression to avoid the obvious "the 7B is just better" confound that already bit us in the 256-tok cross-scale story.
- Multiple open model families compared (Gemma-3, Llama, OLMo, Falcon, GPT-family references) — confirms RSA/CKA is the accepted cross-family similarity tooling.
- No code link surfaced from the PDF extraction; RSA/CKA are standard and trivially reimplementable.

## Replicable intermediates

- **CKA(1.5B L19 prefill, 7B L19 prefill)** over the 500 MATH-500 problems, if both scales' L19-prefill activation matrices are cached (the 7B prefill PR analysis behind F-6 implies a 7B activation cache exists — verify in `pathway11_h100/` before claiming). One-shot CKA + per-problem cross-model cos → AUROC vs the committed correctness labels in `pathway11_h100/prefill_gated_compute/results.json`. This is the smallest H-896 sanity check.
- **Layer-wise RSA** if multi-layer NPZs exist; otherwise this drops to Tier 2 (needs re-extraction).

(If only L19 is cached, the cross-layer refutation test (#1) needs fresh forward passes — see Tier 2.)

## Cross-paper signals

- **2405.07987** — already in graph (status: TO TEST). The Platonic Representation Hypothesis is the theoretical backbone for "models converge to similar representations"; this paper is its multi-agent behavioral instantiation. Strengthens the H-180/H-181/H-182 cluster.
- **Kornblith et al. 2019 (CKA, "Similarity of Neural Network Representations Revisited", arXiv 1905.00414)** — NOT in graph; recommend admission. Foundational cross-model similarity metric we would actually run for H-896 / H-20.
- **Kriegeskorte et al. 2008 (RSA)** — NOT in graph; foundational but a Nature Neuroscience methods paper, not arXiv — fold in as a method citation rather than a graphed Paper node.
- **Raghu et al. 2017 (SVCCA)** / **Morcos et al. 2018 (PWCCA)** — NOT in graph; admit only if we adopt CCA-family similarity (lower priority than CKA).

## Proposed FutureExperiments

### Tier 1 — Cheap local

```yaml
- id: P11-FE960
  pathway: P11
  description: "Compute CKA (linear + RBF) and per-problem cross-model cosine between cached Qwen-2.5-1.5B and 7B L19-prefill activation matrices over the 500 MATH-500 problems; AUROC the per-problem cross-model agreement against committed correctness labels and compare to DoM 0.7731."
  rationale: "Paper's relational similarity framing predicts a pairwise quantity may predict behavior/correctness without any supervised direction. Directly executes H-896; tests Refutation 2 against F-2."
  trigger: "Representational Similarity and Model Behavior in Multi-Agent Interaction (2606.07818): similarity between representation spaces predicts behavior, robust to size/performance controls."
  status: READY
  priority: HIGH
  roi: 8
  cost: "30min CPU"
  depends-on: [F-2, F-6]
  would-update: [F-2, F-6]
  triggered-by: ["2606.07818"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE961
  pathway: P11
  description: "RSA cross-check: build problem×problem RDMs from cached 1.5B and 7B L19-prefill activations, correlate them (Spearman), and use the per-problem leave-one-out RSA neighborhood agreement as a correctness predictor; compare AUROC to CKA (FE960) and DoM."
  rationale: "RSA is the paper's primary metric and is rotation/scale invariant across the 1536-d vs 3584-d mismatch, unlike raw cosine. Provides a second relational predictor to triangulate the H-896 result."
  trigger: "Paper uses RSA (Kriegeskorte 2008) as primary cross-model similarity metric."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "25min CPU"
  depends-on: [F-2, H-896]
  would-update: [F-2]
  triggered-by: ["2606.07818"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE962
  pathway: P11
  description: "Diversity-vs-DoM contamination check: bin the 500 problems by L19-prefill DoM projection and, where K>1 samples exist, measure inter-sample solution-path diversity (answer-set entropy / token-level dispersion); test for a monotone DoM↑ → diversity↓ relation."
  rationale: "Paper establishes similar/low-diversity representations → less novelty. Tests whether F-4/F-8's collapse signal is partly a creativity-suppression axis rather than pure correctness (Refutation 3)."
  trigger: "Paper: more-similar representation spaces yield reduced novelty/creativity."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "40min CPU"
  depends-on: [F-4, F-8]
  would-update: [F-8]
  triggered-by: ["2606.07818"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required

```yaml
- id: P11-FE963
  pathway: P11
  description: "Layer-swept cross-model similarity-vs-correctness: re-extract per-layer prefill activations for 1.5B and 7B across all layers, compute CKA/RSA at each layer, and locate the layer where cross-model similarity is most correctness-predictive; test the paper's early-layer-is-most-predictive claim against our L19 prior."
  rationale: "Directly tests Refutation 1 — whether L19's apparent specialness is a measurement-layer artifact. Needs full-layer activations we likely do not cache for both scales."
  trigger: "Paper finds early-layer representational similarity most behavior-predictive; our stack privileges middle-layer L19."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "1 H100 day"
  depends-on: [F-2, F-6, F-1]
  would-update: [F-2, F-6]
  triggered-by: ["2606.07818"]
  blocked-by: null
  blocked-by-experiment: [P11-FE960]
```

## Proposed HYPOTHESES.md additions

### H-960: A relational cross-model similarity signal predicts 1.5B correctness without a supervised direction
**Priority:** HIGH
**Motivated by:** 2606.07818 + F-2 + H-896
**Test:** Compute CKA and per-problem cross-model cosine between cached 1.5B and 7B L19-prefill activations; AUROC vs committed correctness labels; compare to DoM 0.7731 and to DoM+similarity stack. ~30min CPU (P11-FE960).
**Requires:** CPU; cached 1.5B and 7B L19-prefill NPZs (verify 7B cache exists).
**Would change:** Confirm → DoM is one approximation of a relational signal, demoting "single direction" framing of F-2. Reject → DoM's directional sufficiency strengthened.
**Blocks:** nothing

### H-961: Cross-model representational similarity is most correctness-predictive at early layers, not L19
**Priority:** MEDIUM
**Motivated by:** 2606.07818 + F-6 + F-1
**Test:** Layer-swept CKA/RSA(1.5B^ℓ, 7B^ℓ) vs per-problem correctness across all layers; identify peak-predictive layer. Needs full-layer re-extraction for both scales (~1 H100 day, P11-FE963).
**Requires:** GPU re-extraction (per-layer prefill activations, both scales); MATH-500.
**Would change:** Confirm → L19 specialness is partly a measurement-layer choice; reframes F-2/F-6. Reject → middle-layer L19 is genuinely the cross-scale information locus.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2606.07818 — Representational Similarity and Model Behavior in Multi-Agent Interaction (Potter, Eisape, Lai, Huth, Evans, Kim, Eisenstein, Song, Suhr, 2026)

**Relevance:** A multi-agent social-science study: 276 LLM pairs play 8 cooperation/novelty games, and pairwise representational similarity predicts higher cooperation but lower novelty, robust to performance-disparity and model-size controls, with early-layer similarity most predictive. Topically off-axis from single-model correctness, but its RSA/CKA cross-model similarity toolkit and layer-wise framing map cleanly onto our cached cross-scale (1.5B vs 7B) L19-prefill activations and onto the open H-896 cross-model-agreement hypothesis. The relational (similarity-between-spaces) predictor is also a conceptual rival to our directional DoM (F-2).

**Key claim we tested:** Representational similarity between two models is a behavior-predictive quantity in its own right, strongest at early layers — implying (in our terms) a relational, layer-distributed signal rather than a single middle-layer direction.

**Our result:** **TO TEST** — Three cheap CPU experiments queued (P11-FE960 CKA/cosine cross-model AUROC vs DoM 0.7731; P11-FE961 RSA cross-check; P11-FE962 DoM↔diversity contamination), plus one H100 layer-sweep (P11-FE963) to test the early-layer claim against our L19 prior. Nothing run yet.

**Related experiments:** P11-FE960, P11-FE961, P11-FE962, P11-FE963, H-896, H-960, H-961

**Status:** TO TEST

## New claims

None — this brief introduces no new quantitative numbers into the narrative docs; it only references existing anchors (DoM AUROC 0.7731, F-6, H-896). New numbers would arrive only when P11-FE960–963 produce result JSONs, at which point `validate_claims.py` entries should be added.

## Sources consulted

- WebFetch https://arxiv.org/abs/2606.07818 — abstract, authors, contributions (MCP arxiv/paper-search readers returned empty/not-stored).
- WebFetch https://arxiv.org/pdf/2606.07818 — methods (RSA/CKA/Procrustes, mixed-effects), models, cited similarity literature.
- `query.py paper 2606.07818` (via .venv) — prior admission/relevance note.
- CLAUDE.md `## Headline numbers` section — DoM 0.7731, cos 0.046, cross-scale anchors.
- grep of FINDINGS.md / HYPOTHESES.md — F-1…F-10 and H-N one-liners; confirmed next H-ID 914 (chose 960/961 to avoid concurrent-brief collisions).
- grep of PAPER_INDEX.md — confirmed 2405.07987 (Platonic) already graphed (TO TEST); no Kornblith/CKA node present.
