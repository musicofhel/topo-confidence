# Triage brief — 2602.15438 — Logit Distance Bounds Representational Similarity

Source: https://arxiv.org/abs/2602.15438
Triaged: 2026-05-06

## Refutations

1. **Refutes the implicit assumption behind F-2 (linear DoM probe ≈ unique best direction).**
   F-2 establishes that a single L19 prefill direction (DoM) predicts correctness at AUROC 0.7731. The paper proves (Thm. 3.4) that mCCA between embedding spaces is bounded by logit distance, NOT by KL or cross-entropy loss used to train models. If two Qwen-2.5-1.5B checkpoints (e.g., different seeds or GRPO stages) have low logit distance but different internal representations, our DoM direction is just one member of a linear equivalence class — a *different* invertible linear transform of the same subspace could score identically. The refutation fires if: train two Qwen-2.5-1.5B from different seeds on the same data, extract L19 DoM from each, and show cos(DoM_A, DoM_B) ≪ 1 despite identical AUROC. This would mean F-2's direction is seed-contingent, not a universal geometric feature.

2. **Refutes F-3 (prefill/final DoM orthogonality as structural, not artefactual).**
   F-3 reports cos(prefill_DoM, final_DoM) = 0.046. The paper's framework (Eq. 1) shows that for autoregressive LMs, the embedding function f(x) and unembedding function g(y) are coupled: closeness in logit distance guarantees linear alignment of BOTH embedding and unembedding spaces jointly. If prefill-token and final-token "models" (same weights, different input positions) produce similar logit distributions (small d_logit), Thm. 3.4 guarantees their representations should be mCCA ≈ 1, i.e., linearly related. The observed cos ≈ 0 between prefill and final DoM could then be an artefact of fitting separate linear probes without accounting for the full-rank linear transform A that mediates their equivalence class. Refutation test: compute mCCA(L19_prefill_activations, L19_final_activations) across the 500 MATH-500 samples; if mCCA is high, the orthogonality in F-3 is a probe-fitting artefact within a single linear equivalence class.

3. **Challenges the meaningfulness of F-9 (CoE-60 redundancy with single-layer DoM).**
   F-9 says CoE-60 trajectory features are redundant with L19 DoM. The paper proves (Thm. 4.3) that logit-distance-preserving distillation conserves linearly encoded concepts, but KL-based training does NOT. If the 60-layer CoE trajectory encodes correctness "concepts" that are linearly recoverable from the teacher (full trajectory) but the single-layer L19 extraction corresponds to a KL-like projection (discarding non-top-1 logit structure), then F-9's redundancy finding may hold only because our probe is too weak to detect the extra linear structure in CoE-60. Refutation test: apply the paper's d_rep dissimilarity (Def. 3.7) between CoE-60 embeddings and L19-only embeddings; if d_rep is large despite similar AUROC, the "redundancy" is an artefact of limited probe expressivity.

## Direct connections to F-N / H-N

- **F-2 (L19 DoM AUROC 0.7731):** The paper's identifiability theory (Thm. 2.2) directly characterizes what "a single direction predicts correctness" means in terms of linear equivalence classes. Our DoM is one representative of its ∼_L equivalence class; the AUROC is invariant to the choice of representative within that class (linear probes are closed under composition with invertible linear maps). This means our 0.7731 is robust to reparametrization, which is reassuring — but it also means no linear probe can exceed the information in the logit-distance sense.

- **F-1 (breathing is universal across architectures):** The paper's Thm. 3.4 implies that if two architectures have small logit distance on the same inputs, their internal representations are linearly related (high mCCA). This gives a *theoretical mechanism* for why breathing patterns transfer cross-architecture: if both models approximate the same conditional distribution (as measured by d_logit), their hidden states must be linearly aligned, making "breathing" (an expansion/contraction pattern in residual-stream norms) a linear invariant that transfers.

- **cos(DoM, PC1) = 0.9216:** The paper's equivalence class structure (Thm. 2.2) explains why the supervised DoM direction aligns so closely with unsupervised PC1 — both are members of the same identifiability class, differing at most by an invertible linear map with near-unit singular values when the model is well-conditioned.

- **H-5 (prefill signal encodes problem familiarity vs decomposability):** Thm. 4.3 guarantees that if a "familiarity" concept is linearly encoded in embeddings, and logit distance between model variants is small, then the concept transfers. This provides a theoretical path to test H-5 cross-model.

## Methodologies extracted

- **mCCA (mean Canonical Correlation Analysis)** — Measures linear representational similarity between two sets of embeddings by computing canonical correlations and averaging. Standard metric in representational similarity literature.
  *Replication cost*: 5min CPU on cached NPZs (500 × 1536 matrices).
  *We'd plausibly run this*: yes — directly applicable to compare L19 prefill vs final-token activations, or 1.5B vs 7B activations.

- **d_rep (linear identifiability dissimilarity, Def. 3.7)** — Averages the reconstruction error f(x) − Ã_J f'(x) over all pivot/label choices, measuring how far two representations are from the same linear equivalence class. Zero iff representations are linearly equivalent.
  *Replication cost*: 30min CPU (requires shifted-unembedding matrices from model weights + activations).
  *We'd plausibly run this*: yes — we have cached L19 activations AND model weights for Qwen-2.5-1.5B.

- **d_logit (logit distance, Def. 3.1)** — Square root of mean squared logit difference E_x ||u(x) - u'(x)||². A metric on conditional distributions that, unlike KL, guarantees representational similarity.
  *Replication cost*: 20min CPU if logits are cached; 2h H100 if fresh forward passes needed.
  *We'd plausibly run this*: yes for comparing two model checkpoints; needs forward passes we may not have cached.

- **L1-logit distillation loss (Eq. 19)** — L1 variant of logit distance for training stability. Minimizing this preserves teacher's linear structure better than KL.
  *Replication cost*: N/A (training method, not applicable to cached activations).
  *We'd plausibly run this*: no — we're not training models.

- **Aitchison distance (§D.2)** — Compositional data analysis distance on probability simplices. Shown equivalent to logit distance (Prop. D.6). Proper metric on probability distributions that can be computed from softmax outputs.
  *Replication cost*: 10min CPU if softmax outputs cached.
  *We'd plausibly run this*: yes — if we have token-level probability vectors from MATH-500 generations.

## Approaches & framings

- **Approximate identifiability under distributional closeness:** Instead of asking "are these representations the same?", the paper asks "given these distributions are ε-close, how close are the representations up to their intrinsic symmetry?" This reframes cross-model comparison (relevant to F-1 universality) from pointwise direction comparison to equivalence-class distance.

- **Logit distance as the "correct" metric for representational guarantees:** KL divergence does NOT bound representational similarity (their negative result); only logit distance does. This reframes our approach to cross-model DoM transfer (H-5, F-1): we should measure d_logit between models, not KL or accuracy gap, when asking whether representations will be linearly aligned.

- **Embedding/unembedding coupling:** The model family p_{f,g}(y|x) = softmax(f(x)^T g(y)) explicitly couples encoder f with the unembedding matrix g. Linear identifiability is about the JOINT (f, g) structure, not f alone. This lens suggests our DoM direction (extracted from f only) should be understood relative to the unembedding g — the direction might mean nothing without its paired unembedding row.

## Datasets & benchmarks

- **Synth** — 2D synthetic classification dataset, 7 classes, designed for non-linear-separability testing. Not released as a standalone benchmark. Applicable? no — too simple, no LLM connection.

- **CIFAR-100** — 100-class image classification, 60K images. Public, torchvision. Applicable? no — vision domain, not language.

- **SUB (Synthetic Unique Birds)** — 33-class bird images with concept annotations, from Bader et al. 2025. HF availability unknown. Applicable? no — vision domain.

## Implementation details worth capturing

- mCCA computation uses Eq. (26): Σ_XX^{-1/2} Σ_XY Σ_YY^{-1/2}, taking singular values. Standard scipy implementation.
- d_rep requires access to the shifted unembedding matrix L̃_J from model weights (the token-embedding/unembedding layer). For Qwen-2.5-1.5B this is the lm_head weight matrix (vocab_size × 1536).
- τ-lower-bound assumption (Assumption 3.2): their KL → logit bound requires min probability ≥ τ > 0 across all tokens. For LMs with 150K+ vocab, τ is astronomically small, making their KL bound vacuous in practice. This is actually key context for us — it means KL-based comparisons between our 1.5B and 7B models give NO representational similarity guarantees.
- Code not publicly released as of paper date (Feb 2026).

## Replicable intermediates

- **mCCA between L19 prefill activations (correct vs incorrect groups):** We have the 500×1536 activation matrix cached. Split by correctness label and compute mCCA between the two groups to test whether correct/incorrect representations live in the same linear equivalence class. This cross-checks against F-2 (if mCCA is near 1, the groups ARE linearly related and DoM is just the separating hyperplane within that class).
- **mCCA between L19 prefill and L19 final-token activations:** Directly tests whether F-3's cos=0.046 orthogonality survives when measured with the paper's proper invariant metric. Cached in `pathway11_h100/prefill_gated_compute/`.
- **Singular value spectrum of Qwen-2.5-1.5B lm_head:** The paper's bound (Thm. 3.9) depends on σ_min of the unembedding matrix. Computing this tells us how tight any logit-distance bound would be for our model. Requires loading model weights (10min, CPU).

## Cross-paper signals

- 2405.07987 — already in graph (status: graphed). Platonic Representation Hypothesis directly motivates the universality question; this paper provides the formal apparatus (d_logit bounds mCCA) that could make "convergent representations" testable.
- 2311.03658 — already in graph (status: graphed). Linear Representation Hypothesis is the theoretical foundation for this paper's identifiability results.
- 2506.03784 — NOT in graph; recommend admission. Nielsen et al. 2025 "When does closeness in distribution imply representational similarity?" — the direct predecessor to 2602.15438, proves KL does NOT guarantee linear similarity (negative result our paper builds on).
- 2502.18710 — NOT in graph; recommend admission. Kapoor et al. 2025 "Bridging critical gaps in convergent learning" — studies how representational similarity evolves across layers and training, directly relevant to F-1 breathing universality and cross-layer DoM analysis.

## Proposed FutureExperiments

### Tier 1 — Cheap local (5min–2hr, CPU only, ROI 5–10)

```yaml
- id: P11-FE111438
  pathway: P11
  description: "Compute mCCA between L19 prefill and L19 final-token activations to test whether F-3 orthogonality is a probe artefact or genuine structural separation"
  rationale: "Paper proves mCCA is the proper invariant measure of linear representational similarity. If mCCA(prefill, final) ≈ 1 despite cos(DoM_prefill, DoM_final) = 0.046, then the orthogonality is within-class (artefact of probe direction choice) not between-class (genuine structural separation)."
  trigger: "2602.15438 Thm. 3.4 shows mCCA bounds representational similarity; our F-3 uses cosine between 1D projections, which is NOT invariant to the equivalence class."
  status: READY
  priority: HIGH
  roi: 8
  cost: "10min CPU"
  depends-on: [F-3, F-2]
  would-update: [F-3]
  triggered-by: ["2602.15438"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE111439
  pathway: P11
  description: "Compute mCCA between correct-group and incorrect-group L19 prefill activations to characterize whether DoM is a separating direction within a shared linear equivalence class"
  rationale: "If correct and incorrect groups have mCCA ≈ 1, they share the same linear subspace and DoM is simply the optimal separating hyperplane within that class. If mCCA < 1, the groups genuinely occupy different representation geometries."
  trigger: "2602.15438 Def. 3.7 shows representations can look different pointwise (low cosine) while being linearly equivalent (high mCCA). Need to check this for F-2's DoM."
  status: READY
  priority: HIGH
  roi: 7
  cost: "10min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2602.15438"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE111440
  pathway: P11
  description: "Compute σ_min of Qwen-2.5-1.5B lm_head (unembedding) matrix to assess tightness of logit-distance representational bounds for our model"
  rationale: "Thm. 3.9 bound scales as d_logit / σ_min. If σ_min is very small (ill-conditioned unembedding), logit-distance guarantees are weak for our model. If well-conditioned, cross-model DoM transfer gets theoretical backing."
  trigger: "2602.15438 Thm. 3.9 — bound tightness depends on σ_min of unembedding."
  status: READY
  priority: MEDIUM
  roi: 5
  cost: "15min CPU"
  depends-on: [F-1, F-2]
  would-update: [F-1]
  triggered-by: ["2602.15438"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE111441
  pathway: P11
  description: "Compute Aitchison distance between logit distributions of correct vs incorrect MATH-500 problems at the final generated token to test whether correctness creates distributional separation in the paper's metric"
  rationale: "Paper proves Aitchison distance = logit distance (Prop. D.6), and logit distance bounds representational similarity. If Aitchison distance is large between correct/incorrect, this provides a distributional-level explanation for why L19 DoM works (the model's output distribution is genuinely far in the 'right' metric)."
  trigger: "2602.15438 §D.2 — Aitchison distance on token probabilities is equivalent to logit distance."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "30min CPU"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2602.15438"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required (hours–days, high ROI only)

```yaml
- id: P11-FE111442
  pathway: P11
  description: "Compute d_logit between Qwen-2.5-1.5B and Qwen-2.5-7B on MATH-500 prefill tokens, then use Thm. 3.4 to derive a lower bound on mCCA(1.5B_L19, 7B_L19) for cross-scale DoM transfer"
  rationale: "F-1 claims breathing universality across scales. Paper gives the first formal tool to bound cross-scale representational similarity from observable logit differences. If d_logit is small and mCCA bound is ≥ 0.8, we get theoretical backing for cross-scale DoM transfer."
  trigger: "2602.15438 Thm. 3.4 — d_logit between two models bounds mCCA between their embeddings."
  status: READY
  priority: HIGH
  roi: 7
  cost: "2h H100"
  depends-on: [F-1, F-2]
  would-update: [F-1]
  triggered-by: ["2602.15438"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-750: mCCA between prefill and final-token L19 activations is high (≥0.9), implying F-3 orthogonality is within-equivalence-class
**Priority:** HIGH
**Motivated by:** 2602.15438 + F-3
**Test:** Compute mCCA(L19_prefill_500×1536, L19_final_500×1536) using canonical correlation analysis on cached NPZ activations. Compare against cos(DoM_prefill, DoM_final) = 0.046.
**Requires:** CPU, cached pathway11_h100 NPZs
**Would change:** If confirmed (mCCA ≥ 0.9): F-3 needs reinterpretation — prefill/final are linearly equivalent, the cos=0.046 orthogonality is a 1D projection artefact. If rejected (mCCA ≪ 0.9): F-3 strengthened — genuine structural separation, not just a rotated basis.
**Blocks:** nothing

### H-751: Logit distance between 1.5B and 7B on MATH-500 is small enough to guarantee high mCCA, providing theoretical backing for cross-scale DoM transfer
**Priority:** MEDIUM
**Motivated by:** 2602.15438 + F-1
**Test:** Forward-pass both models on 500 MATH-500 prefill prompts, compute d_logit (Def. 3.1), apply Thm. 3.4 bound using μ_m from logit covariance. Need both models' full vocab logits.
**Requires:** H100 (two forward passes), Qwen-2.5-{1.5B, 7B}
**Would change:** If confirmed (bound implies mCCA > 0.8): strong theoretical support for F-1 universality. If rejected (bound vacuous due to large d_logit): models are too distributionally different for this theory to apply cross-scale.
**Blocks:** H-750

## Proposed PAPER_INDEX.md classification

## 2602.15438 — Logit Distance Bounds Representational Similarity (Nielsen, Marconato, Gresele, Dittadi, Buchholz, 2026)

**Relevance:** Proves that logit distance (not KL divergence) is the correct metric for guaranteeing linear representational similarity between models. Directly applicable to our cross-scale DoM transfer question (F-1), the interpretation of prefill/final orthogonality (F-3), and the theoretical grounding of linear probes on residual streams (F-2). Provides formal tools (mCCA bounds, d_rep dissimilarity) that can be computed on our cached activations.

**Key claim we tested:** Small logit distance between two models' conditional distributions implies their internal representations are related by an invertible linear transformation (high mCCA), with explicit quantitative bounds (Thm. 3.4, Thm. 3.9). KL divergence does NOT provide such guarantees.

**Our result:** **TO TEST** — mCCA computation on cached L19 activations (P11-FE111438, P11-FE111439) will validate whether our prefill/final-token and correct/incorrect group representations satisfy the paper's linear equivalence predictions. Cross-scale d_logit measurement (P11-FE111442) will test whether the theory's bounds are informative for our 1.5B/7B comparison.

**Related experiments:** P11-FE111438, P11-FE111439, P11-FE111440, P11-FE111441, P11-FE111442, H-750, H-751

**Status:** TO TEST

## New claims

None — this brief introduces no new quantitative claims into narrative docs. All proposed numbers are future experimental targets, not established results.

## Sources consulted

- ~/topo-confidence/CLAUDE.md (## Headline numbers section) — default context
- ~/topo-confidence/FINDINGS.md (grep for F-N one-liners) — default context
- ~/topo-confidence/HYPOTHESES.md (grep for H-N one-liners + Next ID) — default context
- ~/topo-confidence/PAPER_INDEX.md (status flags grep) — default context
- research-graph query.py paper 2602.15438 — admission note
- research-graph query.py paper 2405.07987 — cross-paper check (Platonic Rep Hypothesis)
- research-graph query.py paper 2311.03658 — cross-paper check (Linear Rep Hypothesis)
- research-graph query.py paper 2506.03784 — cross-paper check (Nielsen et al. 2025, not in graph)
- research-graph query.py future P11 — FE ID allocation
