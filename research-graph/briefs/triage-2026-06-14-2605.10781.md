# Triage brief — 2605.10781 — Rebellious Student: Reversing Teacher Signals for Reasoning Exploration with Self-Distilled RLVR

Source: https://arxiv.org/abs/2605.10781
Triaged: 2026-06-14

## Refutations

1. **Refutes the *robustness* of F-2 (single L19 prefill DoM, AUROC 0.7731) to current post-training.**
   F-2 treats the L19 prefill→correctness direction as a stable, model-intrinsic readout. RLRT is a *token-level* RLVR intervention that, by construction, reshapes which reasoning tokens the policy reinforces ("self-driven" student-divergent tokens on correct rollouts). **Mechanism:** if RLRT fine-tuning materially moves the L19 prefill activation geometry — the DoM direction realigns, or the OOF AUROC drifts away from 0.7731 — then F-2 is a *checkpoint-specific* property, not a portable correctness law, because the dominant modern post-training recipe (RLVR-with-exploration) rewrites it. **Falsifiable:** fine-tune Qwen-2.5-1.5B with RLRT vs vanilla GRPO, re-extract L19 prefill, re-measure DoM AUROC; a material shift (≥0.03) on either arm refutes durability.

2. **Refutes the *interpretation* of F-4 (asymmetric collapse: correct trajectories collapse harder than incorrect at the final token).**
   RLRT's central thesis is that *valuable* correct rollouts are precisely the ones that **diverge** from the teacher's predicted path — productive exploration, not convergence. **Mechanism:** if good correct reasoning is characterized by self-driven divergence, then the best correct trajectories should exhibit *more* off-axis dispersion, not harder collapse. F-4's "correct collapses harder" then describes only an average over a mixture that hides an exploratory high-quality sub-population. **Falsifiable:** on cached L19 activations, split correct rollouts by a divergence/surprise proxy and measure off-DoM effective dimensionality; if high-divergence correct trajectories show *higher* off-DoM variance than incorrect ones, F-4's collapse framing is incomplete.

3. **Refutes the completeness of F-9 (CoE-60 trajectory features redundant with single-layer L19 DoM).**
   RLRT reinforces a *sparse, positionally-distributed* set of high-asymmetry tokens scattered along the trajectory. **Mechanism:** if the correctness-relevant signal lives in those specific token positions, a trajectory-aware aggregation that up-weights high-divergence positions could carry signal *not* captured by a single pooled L19 DoM projection — exactly the redundancy F-9 asserts. **Falsifiable:** weight cached per-token L19 activations by a token-level surprise/asymmetry proxy and test whether the trajectory-weighted readout beats the single-position DoM (0.7731); a beat refutes "single-layer DoM subsumes trajectory structure."

## Direct connections to F-N / H-N

- **F-2** (L19 prefill DoM, **AUROC 0.7731**, OOF 5-fold) — RLRT is a clean stress test of whether this number survives RLVR post-training; the relevance note already flags this as the headline durability check.
- **F-4** (asymmetric collapse) — RLRT's "exploration grounded in success" framing directly contests whether correct = collapsed; touches the **cos(prefill_DoM, final_DoM)=0.046** structural-orthogonality picture too (F-3) if training reorients either direction.
- **F-9** (CoE-60 redundant with single-layer DoM, the 0.811-vs-DoM equivalence) — RLRT's positional token reweighting is a candidate source of *non-redundant* trajectory signal.
- **H-2 / H-3** (breathing on short-CoT / code) and the RL-fragility hypothesis cluster cited in the admission note (H-8 etc.) — RLRT supplies the concrete training intervention those hypotheses need to be tested against.

## Methodologies extracted

- **RLRT (RLVR with Reversed Teacher)** — augments GRPO by reinforcing, on *correct* rollouts, the tokens where the student succeeded along a path the (information-advantaged) teacher would not have predicted.
  *Replication cost*: H100 day(s) — requires RL fine-tuning of Qwen.
  *We'd plausibly run this*: yes, but only as a Tier-2 durability probe of F-2/F-4, not as a method we adopt.
- **Reversed self-distillation signal (information asymmetry)** — teacher = same model conditioned on extra info; per-token weight = gap between teacher and student token probabilities, *read in reverse* (reward divergence on success instead of suppressing it).
  *Replication cost*: needs fresh forward passes (teacher conditioned on answer/hint) to get per-token logit gaps — we don't have these cached.
  *We'd plausibly run this*: no, unless we re-run extraction with a hinted-teacher pass.
- **Full-trajectory token-reinforcement heatmaps** (Appendix H) — visualize which token positions receive the reversed signal across a trajectory.
  *Replication cost*: 30min CPU *if* we had per-token L19 traces; our cache is prefill + final-token aggregates.
  *We'd plausibly run this*: no, blocked on per-token cache.
- **GRPO baseline** — standard group-relative policy optimization, used as the comparison arm.
  *Replication cost*: H100 day(s).
  *We'd plausibly run this*: only as the control arm of the RLRT durability experiment.

## Approaches & framings

- **"Information asymmetry as a design axis for RLVR."** Reframes exploration from uniform entropy/diversity bonuses to *targeted* reinforcement of student-vs-teacher divergence. Intersects our framings by offering a mechanistic candidate for *why* the prefill geometry might or might not move under training — exploration is concentrated on specific tokens, not spread uniformly.
- **"Read the self-distillation signal in reverse."** Conceptual inversion: the same teacher-student gap that overwrites the student on success can instead mark self-driven reasoning. Intersects F-3/F-4 — if "good" reasoning is divergence rather than collapse, our collapse-asymmetry story needs a quality-stratified retelling.

## Datasets & benchmarks

- **MATH** — competition math; directly comparable to our MATH-500 1024-tok regime. Applicable? yes — same task family, lets us re-measure F-2 on a post-RLRT checkpoint.
- **AIME** — harder competition math, small N. Applicable? partially — useful for cross-difficulty but not our cached regime.
- **GSM8K** — grade-school math. Applicable? partially — easier; weak overlap with our hard-MATH cache.
- **Qwen3 (base / instruction-tuned / thinking-tuned)** — model family, not a dataset, but note: our cache is Qwen-2.5-1.5B/7B, so an exact-checkpoint replication would need Qwen3 or a Qwen-2.5 RLRT run. Applicable? yes with a fresh fine-tune.

## Implementation details worth capturing

- Three checkpoint regimes evaluated: Qwen3 **base, instruction-tuned, thinking-tuned** — RLRT reportedly beats self-distillation and exploration baselines on all three.
- Algorithm = GRPO + a token-level reweighting term keyed on teacher-vs-student token-probability gap on correct rollouts.
- Theoretical scaffolding: Theorems 1–2 (Appendix C) on reasoning-boundary / exploration constraints; hyperparameters in Appendices F–G (learning rates, rollout counts — not legible in the PDF metadata extract, would need the appendix tables).
- No public code link surfaced in the abstract/intro; verify before assuming a repo exists.
- **Gotcha:** "teacher" and "student" are the *same* model — the teacher just gets extra context. There is no separate model to load; the asymmetry is entirely from conditioning.

## Replicable intermediates

- **Off-DoM dispersion check (correct vs incorrect), cached now.** Using `pathway11_h100/...` L19 prefill NPZ (500×1536), residualize out the DoM direction and compare effective dimensionality / participation ratio of the correct vs incorrect groups. This is the smallest cross-check of RLRT's "correct = exploratory divergence" premise against our F-4 "correct collapses harder" finding — no training required.
- Everything tied to RLRT's *headline* (benchmark-accuracy gains from training) is **not** replicable without an H100 RL run.

## Cross-paper signals

- 2203.11171 — already in graph (status: graphed; REPLICATED + EXTENDED). RLRT positions itself against self-consistency-style exploration; our self-consistency entry is the natural comparison anchor.
- GRPO / DeepSeekMath (RLVR base method) — likely NOT in graph as a triaged node; recommend admission if we pursue the Tier-2 RLRT experiment, since GRPO is the control arm and the reward-baseline reference. (Verify exact arxiv id before admitting — not confirmed present.)

## Proposed FutureExperiments

### Tier 1 — Cheap local

```yaml
- id: P11-FE10781
  pathway: P11
  description: "On cached L19 prefill NPZ, residualize out the DoM direction and compare off-DoM effective dimensionality / participation ratio of correct vs incorrect rollouts, stratified by a token-surprise (logprob) proxy, to test RLRT's claim that valuable correct reasoning is exploratory divergence rather than collapse."
  rationale: "RLRT asserts good correct rollouts diverge from the predicted path; F-4 asserts correct trajectories collapse harder. A pure cached-activation dispersion test adjudicates whether the high-quality correct sub-population is exploratory (high off-DoM variance) — refining or contradicting F-4 with zero training."
  trigger: "RLRT (2605.10781) reframes correct reasoning as productive divergence, not convergence."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "45min CPU"
  depends-on: [F-4, F-2]
  would-update: [F-4]
  triggered-by: ["2605.10781"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

### Tier 2 — H100 required

```yaml
- id: P11-FE10782
  pathway: P11
  description: "Fine-tune Qwen-2.5-1.5B with RLRT (reversed-teacher GRPO) vs vanilla GRPO on MATH, then re-extract L19 prefill activations and re-measure the DoM correctness AUROC and the F-4 final-token collapse asymmetry on each post-training checkpoint."
  rationale: "F-2 (AUROC 0.7731) and F-4 are measured on a fixed pre-RL checkpoint. RLRT is a token-level intervention that could rewrite the prefill geometry. This is the decisive durability test: does the single-direction correctness readout survive the dominant modern RLVR recipe, and does correct/incorrect collapse asymmetry flip under exploration-targeted training?"
  trigger: "RLRT (2605.10781) provides a concrete reversed-teacher RLVR recipe whose exploration is concentrated on specific tokens, a candidate to move L19 geometry."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "1 H100 day"
  depends-on: [F-2, F-4]
  would-update: [F-2, F-4]
  triggered-by: ["2605.10781"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

## Proposed HYPOTHESES.md additions

### H-10781: The L19 prefill DoM correctness signal (AUROC 0.7731) is invariant to reversed-teacher RLVR fine-tuning
**Priority:** MEDIUM
**Motivated by:** 2605.10781 + F-2
**Test:** RLRT vs GRPO fine-tune of Qwen-2.5-1.5B on MATH, re-extract L19 prefill, re-fit OOF 5-fold DoM probe, compare AUROC and cos(DoM_pre, DoM_post). ~1 H100 day.
**Requires:** H100; Qwen-2.5-1.5B; RLRT/GRPO training loop; fresh extraction of L19 prefill.
**Would change:** On confirm (AUROC within ±0.02, cos≥0.9), F-2 is elevated from a checkpoint property to a training-robust readout. On reject, F-2 is reframed as recipe-dependent geometry, weakening any "intrinsic correctness direction" claim.
**Blocks:** nothing

### H-10782: High-divergence correct rollouts show higher off-DoM dispersion than incorrect ones, contradicting uniform "correct collapses harder"
**Priority:** MEDIUM
**Motivated by:** 2605.10781 + F-4
**Test:** Residualize DoM from cached L19 NPZ; compute participation ratio of correct vs incorrect groups, then stratify correct by token-logprob-surprise and re-compare. ~45 min CPU.
**Requires:** CPU; cached L19 prefill + final-token NPZ; per-rollout mean logprob (already cached for the free baseline).
**Would change:** On confirm, F-4's collapse asymmetry is a mixture artifact masking an exploratory high-quality sub-population. On reject, collapse asymmetry holds even for divergent correct rollouts and RLRT's exploration framing does not map onto our geometry.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2605.10781 — Rebellious Student: Reversing Teacher Signals for Reasoning Exploration with Self-Distilled RLVR (Anonymous/Qwen-based authors, 2026)

**Relevance:** A training-time RLVR method (RLRT) that augments GRPO by reinforcing, on correct rollouts, the tokens where a student diverges from an information-advantaged teacher — "information asymmetry" as an exploration axis. It does not study hidden-state correctness readouts, so it is not a direct competitor to F-2/F-4/F-9; its value to us is as a *stress test*. It is the cleanest available intervention to ask whether our fixed-checkpoint findings (the L19 prefill DoM AUROC 0.7731, the asymmetric-collapse F-4) survive the dominant modern post-training recipe, and whether RLRT's "correct = productive divergence" thesis contradicts our "correct = harder collapse" geometry.

**Key claim we tested:** In our terms — valuable correct reasoning is exploratory divergence (sparse, positionally-distributed high-asymmetry tokens), and RLVR exploration targeted at those tokens improves reasoning across base/instruct/thinking checkpoints.

**Our result:** **TO TEST** No experiment run yet. Two tracks queued: a cheap cached-activation dispersion check (P11-FE10781 / H-10782) adjudicating divergence-vs-collapse against F-4, and an H100 RLRT-vs-GRPO durability run (P11-FE10782 / H-10781) re-measuring F-2 and F-4 on post-training checkpoints.

**Related experiments:** P11-FE10781, P11-FE10782, H-10781, H-10782

**Status:** TO TEST

## New claims

None — this brief introduces no committed quantitative numbers; it only quotes existing headline values (AUROC 0.7731, cos 0.046) and proposes thresholds inside forward-looking H-N/FE blocks (which become live `PENDING_FE` only when their result JSONs land).

## Sources consulted

- https://arxiv.org/abs/2605.10781 — abstract + method framing (primary fetch).
- https://arxiv.org/pdf/2605.10781 — full-text PDF for algorithm, models, benchmarks, appendices (partial extraction; appendix hyperparameter tables not fully legible).
- ~/topo-confidence/research-graph query.py paper 2605.10781 — prior admission/relevance note.
- ~/topo-confidence/CLAUDE.md (Headline numbers section only) — anchor values.
- ~/topo-confidence/FINDINGS.md + HYPOTHESES.md (grep of F-/H- one-liners only) — finding IDs.
- ~/topo-confidence/PAPER_INDEX.md (status-flag grep) — existing classifications + confirm 2203.11171 graphed.
- premises.py list — premise vocabulary (confirmed no applicable non-refuted premise; relies-on left []).
