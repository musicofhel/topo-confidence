# Triage brief — 2505.24850 — Harnessing Negative Signals: Reinforcement Distillation from Teacher Data for LLM Reasoning (REDI)

Source: https://arxiv.org/abs/2505.24850
Triaged: 2026-06-12

> **One-line:** Two-stage distillation recipe (SFT on correct traces → asymmetrically-weighted REINFORCE-style refinement on correct+incorrect preference pairs). Base = Qwen2.5-Math-1.5B; trained on 131k Open-R1 traces; Qwen-REDI-1.5B hits **83.1% MATH-500**, matching DeepSeek-R1-Distill-Qwen-1.5B (800k proprietary data). Code: github.com/Tim-Siu/reinforcement-distillation.
>
> **Project-state caveat that governs this whole brief:** this is a *training-recipe* paper with **no hidden-state / residual / probe / geometry analysis** (verified by keyword scan: `residual`=0, `hidden state`=0, `probe`=0, `geometr`=0). It touches our program only at three seams: (a) it shares our base model family + MATH-500, (b) it is the canonical SFT→REINFORCE pipeline that H-912/H-913 ask about, and (c) its core scalar is a *length-normalized sequence logprob margin* — which lands squarely on our surviving `free-baseline-strongest-readout` premise (CONFIRMED 2026-06-12). Note that `geometry-portable-signal` and `dom-causal-lever` are both **REFUTED** (2026-06-11), so any FE that assumes DoM is a meaningful lever is born MOOTED by design — flagged where it occurs.

## Refutations

1. **H-912 (DoM is an SFT→RL distribution-sharpening artifact, not intrinsic).** REDI is precisely a two-stage SFT→REINFORCE-style refinement, and its Figure 9 shows average token entropy collapsing sharply in the first 400–600 refinement steps (more so on training sequences). *Mechanism:* if the L19 prefill DoM correctness signal (F-2, AUROC 0.7731) is a by-product of how the SFT→RL pipeline sharpens the residual-stream distribution, then a model refined with REDI's negative-trace objective should show a *different* DoM AUROC (collapsed, inflated, or rotated) than the same checkpoint pre-refinement. *Falsifiable:* take a 1.5B SFT checkpoint, run REDI Stage-2 refinement, re-extract L19 prefill activations on MATH-500, recompute OOF DoM AUROC. If it moves materially (Δ > 0.05) the "intrinsic property" reading of F-2 is refuted in favor of H-912.

2. **F-2 prefill-primacy / F-3 prefill⊥final (cos 0.046).** REDI's *only* confidence-dynamics evidence (Section 4.7, Fig 9) is at the **final/generated-token logit** level — entropy of the generated sequence sharpens, and accuracy rises, with no prefill manipulation whatsoever. *Mechanism:* if all of REDI's accuracy gain (SFT ≈ 80% → REDI 83.1%) is carried by final-token logit sharpening while the prefill representation is untouched, that is evidence the *actionable* correctness signal lives at the final token — inverting our headline ranking (prefill DoM 0.7731 > final DoM 0.7186). *Falsifiable:* before/after REDI, compare prefill-DoM AUROC vs final-token-DoM AUROC; if final overtakes prefill post-refinement, F-2's prefill-primacy is recipe-dependent, not fundamental.

3. **F-8 selective prediction — the "discard the low-confidence half" premise.** Our selective stack *abstains on* (throws away) the rejected ~50% coverage (71.6% answered acc). REDI's central, empirically-backed claim is the opposite: the *incorrect/discarded* traces "contain vital insights into common pitfalls" and recover 6× data efficiency when recycled as negatives. *Mechanism:* REDI shows the low-confidence/incorrect population is not noise to be dropped but signal to be down-weighted (α∈[0,1]) and trained on. *Falsifiable:* take the traces our gate rejects, label them as DPref negatives, run REDI refinement, and measure whether (a) overall accuracy rises and (b) the post-refinement DoM/free-baseline separability of the *remaining* answered set degrades — which would mean our gate was discarding learnable signal, weakening F-8's framing of rejection as the terminal action. (This is exactly the v9 "gate-REJECTED traces become negatives" slot the admission note pre-registered.)

## Direct connections to F-N / H-N

- **H-912 / H-913** (SFT→RL distribution sharpening vs expansion; DoM-as-artifact). REDI is the concrete SFT→REINFORCE pipeline these two hypotheses are *about*. H-912 would be directly tested by re-extracting DoM (AUROC **0.7731**, F-2) on a REDI-refined checkpoint; H-913 (distribution expansion → higher-dim geometry) is the contrast case (REDI is sharpening, not expansion).
- **F-2** (single L19 prefill direction, AUROC **0.7731**) and **`free-baseline-strongest-readout`** (CONFIRMED). REDI/SimPO's core scalar — length-normalized sequence logprob, log π(y|x)/|y| — is a *specific normalization of our free length+logprob readout*. The headline free baseline already bundles length+logprob; REDI motivates testing the SimPO normalization form against it.
- **F-4** (asymmetric collapse: correct trajectories collapse harder at final token). REDI Fig 9's "entropy decreases more for training sequences, model becomes more confident" is a training-time analogue of our inference-time asymmetric-collapse observation — both are sharpening signals tied to correctness, worth cross-referencing for the H-4 EoS/sharpness framing.
- **F-6** (7B prefill PR inversion driven by *incorrect-group concentration*). REDI's α-asymmetric weighting exists precisely because the negative class behaves differently (penalizing negatives can suppress similar positives → collapse). This is a training-side echo of F-6's "the incorrect group is the geometrically distinctive one."
- **Numerical anchor / regime gap:** our 1.5B MATH-500 K=1 = **48.6%** (base instruct, 1024-tok budget); REDI's 83.1% is a *long-CoT reasoning* model at 32,768-tok generation. Not comparable — different regime — but it is the ceiling our base model reaches once distilled, useful for framing.

## Methodologies extracted

- **REDI objective** — asymmetrically-weighted, reference-free, REINFORCE-style loss: `L = E[ −logπ(y_w|x)/|y_w| + α·logπ(y_l|x)/|y_l| ]`, α∈[0,1] down-weights the negative-trace gradient (α=0 → SFT; α=1 → symmetric; best ≈ **α=0.8**). *Replication cost:* needs GPU refinement of a checkpoint (H100 hours). *We'd plausibly run this:* yes (Tier 2, v9 slot) — it's the pre-registered recycle-rejected-traces idea.
- **β→0 limit derivation (DPO/SimPO → REINFORCE)** — shows the KL penalty β trades peak performance for stability; dropping it recovers a simple REINFORCE-style loss (Appendix B). *Replication cost:* theory, no run. *We'd plausibly run this:* no — analytical, but informs why a reference-free probe-training objective might behave differently.
- **Asymmetric α-weighting to prevent training collapse** — down-weight negative gradient to stop the "penalize-negative suppresses-similar-positive → degenerate" failure (cites off-policy-gradient work Razin 2025, Ren & Sutherland 2025, Yan 2025b). *Replication cost:* trivial as a probe-side class-weight. *We'd plausibly run this:* yes (Tier 1) — port α to our logistic DoM/free-baseline probe as class weighting.
- **Token-entropy dynamics analysis across checkpoints × sample type** (Fig 9) — average per-token entropy of train_chosen / train_rejected / test_generated logits over refinement steps. *Replication cost:* cheap *if* per-token logits are cached; we have L19 activations, not logits, so this needs a fresh forward pass. *We'd plausibly run this:* partial — only if we already log per-example logprob/length (the free baseline suggests we do).
- **Length-normalized sequence logprob (SimPO form, log π/|y|)** — the correctness margin lives in length-normalized logprob space. *Replication cost:* ~10min CPU *if* sequence logprob + length are cached. *We'd plausibly run this:* yes (Tier 1) — directly tests `free-baseline-strongest-readout`.
- **Dual-judge correctness labeling** (Llama judge AND Math-Verify both "True"). *Replication cost:* labeling-side only. *We'd plausibly run this:* no — we already have MATH-500 correctness labels.

## Approaches & framings

- **"Negative traces are underutilized signal, not noise."** Reframes the discarded-incorrect population as a learnable resource. Intersects F-8 / our selective-prediction stack directly — we *discard* the population REDI *mines*. This is the most generative framing for us (v9 slot).
- **Performance–stability trade-off governed by a single regularizer (β/KL).** Lens: the same penalty that stabilizes training caps peak accuracy. Loosely analogous to our regularization sweeps on the L2 probe (full 1536-d ceiling 0.7847 at C=0.001) — strong regularization caps the geometry probe too. Reusable framing for "why the free baseline generalizes but the over-regularized geometric probe does not."
- **Sharpening as the mechanism of distillation gain.** Frames distillation improvement as distribution sharpening (entropy collapse), which is exactly the mechanism H-912 posits behind the DoM signal — making REDI the cleanest available probe of that hypothesis.

## Datasets & benchmarks

- **OpenR1-Math-Raw (Open-R1)** — 78k positive (x, y_w) pairs + 53k preference triplets (x, y_w, y_l); cn_k12 subset excluded. HF, open license. Applicable? **yes** — it is the only readily-available source of *paired correct/incorrect CoT traces on math* we could use to test whether the residual geometry of incorrect traces is distinct/multimodal (our MATH-500 cache is K=1, so has no within-problem correct/incorrect pairs).
- **MATH-500** — 500 problems. HF/open. Applicable? **yes** — our canonical benchmark; our cache is built on it.
- **AIME24 / AMC23 / Minerva / OlympiadBench** — small eval sets; open. Applicable? **partial** — OlympiadBench is their OOD STEM check; could serve as an out-of-family generalization probe for the free-baseline readout, but no cached activations exist for them.

## Implementation details worth capturing

- Base model: **Qwen2.5-Math-1.5B** (note: *Math* variant; our project uses Qwen2.5-1.5B). Best final model starts from a 5-epoch SFT checkpoint (Qwen-SFT-1.5B-5ep).
- Stage 1 (SFT): AdamW, batch 128, linear 10% warmup → linear decay to 0; 3 epochs (comparative) / 5 epochs (final). SFT MATH-500 plateaus ≈ 5 epochs.
- Stage 2 (REDI): 1 epoch over DPref, batch 32, same LR schedule; **α≈0.8** best, LR≈1e-6. DPO peak ≈82.3% but collapses at low β; REDI matches without the collapse.
- Eval: temperature 0.6, top-p 0.95, max-gen **32,768 tokens**; pass@1 averaged over 16 samples (DeepScaleR/rllm); intermediate tuning via LightEval pass@1 over 4 samples.
- Collapse signature: simultaneous drop in chosen *and* rejected logp + accuracy + gradient-step surge — useful diagnostic vocabulary.
- Code: https://github.com/Tim-Siu/reinforcement-distillation

## Replicable intermediates

- **Length-normalized logprob readout (SimPO/REDI form) vs our free baseline.** *If* `pathway11_h100/prefill_gated_compute/results.json` (or the feature cache feeding it) stores per-example total sequence logprob and token length, compute logπ/|y| and its MATH-500 AUROC; compare to raw-logprob and length-alone AUROC. Smallest cross-check against REDI's central scalar. (Cached artifact: the free-baseline feature columns; if absent, this drops to "needs fresh data.")
- **α-as-class-weight diagnostic on the DoM/free-baseline logistic probe.** Refit the existing 5-fold OOF logistic readout on cached L19 features with incorrect-class weight swept α∈{0.2,0.5,0.8,1.0}; does AUROC/calibration shift? Tests whether incorrect-group concentration (F-6) drives the readout. Cached artifact: `pathway11_h100/pca_covariance/results.json` feature matrix (500×1536) + labels.

## Cross-paper signals

- **2501.12948** — DeepSeek-R1 — already in graph (status: **graphed**). REDI's teacher/baseline (DeepSeek-R1-Distill-Qwen-1.5B is the 83.2% comparison point); REDI matches it with 6× less data.
- **2305.18290** — DPO (Rafailov et al.) — **NOT in graph**. Optional admission: it is the preference-optimization baseline REDI's β→0 derivation starts from; relevant only if we ever build a preference-style probe-training objective. Low priority for a geometry-focused graph.
- **2405.14734** — SimPO (Meng et al.) — **NOT in graph**. Optional admission: source of the *length-normalized logprob* form that touches `free-baseline-strongest-readout`. Marginally higher value than DPO for us because of the length-normalization connection, but still a training-method paper.
- (Razin 2025, Ren & Sutherland 2025, Yan 2025b — off-policy-gradient collapse mechanism — cited but not on-topic for our graph; not flagging for admission.)

## Proposed FutureExperiments

### Tier 1 — Cheap local

```yaml
- id: P11-FE1850
  pathway: P11
  description: "Length-normalized sequence logprob (SimPO/REDI form, log-pi(y|x)/|y|) as a correctness readout on cached MATH-500 features, benchmarked against raw sequence logprob and token-length alone, by OOF AUROC."
  rationale: "REDI/SimPO's central scalar is length-normalized logprob. Our surviving signal is the free length+logprob readout (free-baseline-strongest-readout, CONFIRMED). This tests whether REDI's specific normalization beats our current free-baseline form, at near-zero cost if logprob+length are cached."
  trigger: "Cached per-example sequence logprob and token length exist in the prefill_gated_compute / free-baseline feature set."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "15min CPU"
  depends-on: [F-8, F-2]
  would-update: [F-8]
  triggered-by: ["2505.24850"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: [free-baseline-strongest-readout]

- id: P11-FE1851
  pathway: P11
  description: "Port REDI's asymmetric alpha-weighting to the L19 logistic correctness probe as incorrect-class weighting; sweep alpha in {0.2,0.5,0.8,1.0} and measure OOF AUROC + calibration shift to diagnose whether incorrect-group concentration (F-6) drives the readout."
  rationale: "REDI needs alpha<1 because the negative class behaves differently (penalizing negatives destabilizes positives). The probe-side analogue is class-weighting. If down-weighting the incorrect class collapses AUROC, the correctness signal is carried by incorrect-group structure (F-6), not a symmetric direction."
  trigger: "Cached 500x1536 L19 prefill feature matrix + correctness labels available (pca_covariance/results.json feature set)."
  status: READY
  priority: MEDIUM
  roi: 5
  cost: "20min CPU"
  depends-on: [F-6, F-2]
  would-update: [F-6]
  triggered-by: ["2505.24850"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

### Tier 2 — H100 required

```yaml
- id: P11-FE1852
  pathway: P11
  description: "v9 slot: recycle gate-REJECTED MATH-500 traces as REDI DPref negatives, run one epoch of REDI Stage-2 asymmetric refinement (alpha~0.8) on a 1.5B SFT checkpoint, and measure (a) MATH-500 accuracy delta vs SFT-only and vs escalation, and (b) post-refinement free-baseline correctness separability on the answered set."
  rationale: "Admission note pre-registers this as the v9 upgrade. REDI's core claim refutes the 'discard the rejected half' premise behind F-8 selective prediction: rejected traces may be learnable signal. Tests whether recycling beats / complements escalation (escalation-beats-introspection, CONFIRMED) at matched data budget."
  trigger: "SPEC v8 H-R confirms the gate; an SFT checkpoint + gate-rejected trace store exist; H100 available."
  status: READY
  priority: HIGH
  roi: 7
  cost: "1 H100 day"
  depends-on: [F-8]
  would-update: [F-8]
  triggered-by: ["2505.24850"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

> **Born-MOOTED note (intentional, not proposed as a live FE):** the cleanest test of H-912 — "REDI-refine a checkpoint, re-extract L19 prefill activations, recompute DoM AUROC; if it moves, DoM is a recipe artifact" — necessarily assumes `geometry-portable-signal` (REFUTED 2026-06-11). Promoting it would correctly admit it as MOOTED. It is logged here for resurrection if geometry-portable-signal is ever revived, but is deliberately **not** emitted as a READY YAML block.

## Proposed HYPOTHESES.md additions

### H-1485: REDI-style asymmetric negative-trace refinement reshapes residual-stream correctness separability, demonstrating DoM is recipe-dependent
**Priority:** MEDIUM
**Motivated by:** 2505.24850 + H-912 + F-2
**Test:** Re-extract L19 prefill activations on MATH-500 from a checkpoint before vs after REDI Stage-2 refinement (α≈0.8); recompute OOF DoM AUROC and final-token-DoM AUROC. Δ AUROC > 0.05 (or a prefill↔final crossover) confirms recipe-dependence. ~1 H100 day for refinement + minutes for the probe.
**Requires:** H100 for REDI refinement; 1.5B SFT checkpoint + Open-R1 DPref negatives; existing DoM probe.
**Would change:** Confirm → F-2's "intrinsic single direction" reading downgrades to "SFT→RL sharpening artifact" (H-912); reject → strengthens F-2 as recipe-invariant. NOTE: relies on the REFUTED geometry-portable-signal premise, so any FE form is born MOOTED.
**Blocks:** H-912, H-913

### H-1486: Length-normalized sequence logprob (SimPO/REDI form) is a stronger free-baseline correctness readout than raw sequence logprob
**Priority:** MEDIUM
**Motivated by:** 2505.24850 + F-8 + free-baseline-strongest-readout
**Test:** On cached MATH-500 features compute logπ(y|x)/|y| and compare OOF AUROC against raw logprob and length alone; if the normalization lifts AUROC out-of-family it becomes the new free-baseline form. ~15min CPU (P11-FE1850).
**Requires:** CPU; cached per-example sequence logprob + token length.
**Would change:** Confirm → updates the canonical free-baseline definition used across the selective stack (F-8); reject → confirms raw length+logprob is already the right free readout.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2505.24850 — Harnessing Negative Signals: Reinforcement Distillation from Teacher Data for LLM Reasoning (Xu, Peng, Long, Xu, Chu, Qi; 2025)

**Relevance:** A training-recipe paper (REDI: SFT on correct traces → asymmetrically-weighted REINFORCE-style refinement on correct+incorrect preference pairs) on our exact base-model family (Qwen2.5-Math-1.5B) and benchmark (MATH-500, 83.1%). It contains **no hidden-state, probe, or geometry analysis**, so it does not directly test the residual-stream findings; it connects to our program through (a) the SFT→RL distribution-sharpening mechanism that H-912/H-913 hypothesize underlies the DoM signal, (b) its length-normalized logprob scalar landing on the CONFIRMED `free-baseline-strongest-readout` premise, and (c) the admission-note v9 slot: recycle gate-rejected traces as REDI negatives instead of discarding them.

**Key claim we tested:** Incorrect/discarded reasoning traces are learnable signal — down-weighting (not dropping) the negative class yields 6× data efficiency and matches an 800k-data model with 131k traces.

**Our result:** **TO TEST** — no experiment run yet. Two Tier-1 CPU cross-checks (length-normalized logprob readout P11-FE1850; α-class-weight diagnostic P11-FE1851) and one Tier-2 v9 experiment (recycle gate-rejected traces as REDI negatives, P11-FE1852) are queued. The H-912 DoM-artifact test is logged but born-MOOTED under the REFUTED geometry-portable-signal premise.

**Related experiments:** P11-FE1850, P11-FE1851, P11-FE1852, H-1485, H-1486, H-912, H-913

**Status:** TO TEST

## New claims

None — this brief introduces no new quantitative claims about our system. All cited numbers are either external (REDI's 83.1% MATH-500, 78k/53k/131k trace counts, α≈0.8) or already-tracked internal anchors (48.6%, 0.7731, 0.7186, 0.7847). External REDI numbers should enter `validate_claims.py` as `kind="external"` only if/when a PAPER_INDEX entry quotes them in a comparative claim; the brief itself asserts no new internal number.

## Sources consulted

- arxiv full text 2505.24850 (via mcp__arxiv__read_paper → cached tool-result file) — methodology §2, experimental setup §3, results §4 incl. Fig 9 token-entropy analysis; keyword-scanned for residual/probe/geometry (all 0).
- ~/topo-confidence/CLAUDE.md (## Headline numbers section only) — anchors 48.6%, 0.7731, 0.7186, 0.7847.
- grep of FINDINGS.md/HYPOTHESES.md F-N/H-N one-liners — F-2/F-3/F-4/F-6/F-8, H-911/H-912/H-913 (next H-ID H-914).
- `premises.py list` (via ~/topo-confidence/.venv) — confirmed geometry-portable-signal + dom-causal-lever REFUTED, free-baseline-strongest-readout + escalation-beats-introspection CONFIRMED.
- `query.py future P11` — highest graphed FE = P11-FE999 (chose 1850+ block to avoid concurrent collision).
- `query.py paper` for 2505.24850 (admission note: v9 slot), 2501.12948 (DeepSeek-R1, graphed), 2305.18290 / 2405.14734 (DPO/SimPO, not in graph).
