# Triage brief — 2602.02710 — Maximum Likelihood Reinforcement Learning

Source: https://arxiv.org/abs/2602.02710
Triaged: 2026-05-06

## Refutations

1. **F-2 (prefill L19 DoM AUROC 0.7731) and F-8 (selective-prediction at 50% coverage) may be GRPO training artifacts, not intrinsic geometric properties.** MaxRL's key theoretical result is that GRPO induces a population-level weighting function w(p) = 1/√(p(1−p)), which disproportionately upweights medium-difficulty prompts and *inverts* for very easy prompts (Section 5, Table 2, Figure 1). Our probed model (Qwen2.5-1.5B-Instruct) was trained with GRPO. The DoM direction at L19 may be a supervised readout of the difficulty-weighted gradient structure GRPO carved into the residual stream — not a universal correctness signal. A MaxRL-trained Qwen checkpoint, which uses w(p) = (1−(1−p)^T)/p ≈ 1/p (monotonically emphasizing hard problems), would reshape the geometry of L19 activations around a different difficulty-difficulty weighting. If the DoM direction rotates substantially or AUROC drops below 0.70 on a MaxRL-trained model, F-2's claim of a single predictive direction and F-8's selective-prediction stack both collapse. **Falsifiable test:** Train Qwen2.5-1.5B with MaxRL on same MATH data (or use the publicly released MaxRL Qwen3-1.7B checkpoint), extract L19 prefill activations on MATH-500, refit DoM, report AUROC and cos(GRPO-DoM, MaxRL-DoM).

2. **F-7 (D-bucket's distinctive collective geometric signature) may vanish under MaxRL training.** The D-bucket contains problems where the model is decisively wrong. GRPO's w(p) = 1/√(p(1−p)) weighting delivers *vanishing gradient* for prompts with p→0 (the hardest problems), as explicitly shown in MaxRL's Figure 6 gradient-norm analysis on Qwen2.5-1.5B-Instruct + MATH-500. Under MaxRL, gradient signal concentrates on these hard problems (w(p) → ∞ as p→0), and Figure 7 shows MaxRL models generate at least one correct rollout for a larger fraction of training prompts. If MaxRL "fills in" the D-bucket by successfully training on exactly those prompts GRPO gave up on, the D-bucket's distinctive geometric signature would disappear — it was a scar from GRPO's gradient void, not a genuine competence boundary. **Falsifiable test:** Extract L19 activations from a MaxRL-trained checkpoint, classify MATH-500 problems into ABCD difficulty buckets using the same procedure as F-7, test whether D-bucket still separates geometrically from ABC in PCA/covariance space.

3. **F-4 (asymmetric collapse — correct trajectories collapse harder) may be reweighting-dependent.** MaxRL's gradient norm analysis (Figure 6) shows that MaxRL produces dramatically different gradient distributions across pass rates compared to GRPO and RLOO on the same Qwen2.5-1.5B-Instruct + MATH-500 setup we used. If the asymmetric collapse pattern in F-4 exists because GRPO's symmetric-around-0.5 weighting selectively sharpens the model's representation of medium-difficulty correct solutions (where gradient is strongest) while leaving hard-correct representations diffuse, then MaxRL's hard-problem emphasis would either (a) equalize the collapse ratio between correct and incorrect, or (b) reverse it (incorrect collapse harder, because MaxRL's large gradients on hard problems would force-learn even wrong-answer representations). **Falsifiable test:** Compare participation ratio trajectories for correct vs incorrect samples on a MaxRL-trained checkpoint; check whether the asymmetric collapse ratio (correct PR / incorrect PR = 0.509 at 1.5B) changes sign or shrinks to ~1.0.

## Direct connections to F-N / H-N

- **F-2 (AUROC 0.7731):** MaxRL's Figure 6 gradient norm analysis is performed on *exactly* our model (Qwen2.5-1.5B-Instruct) and benchmark (MATH-500), providing the first external study of how the RL training objective shapes the per-prompt gradient landscape on the identical model+data we probe. The w(p) difference between GRPO (1/√(p(1-p))) and MaxRL (≈1/p) predicts different geometry at the layer where our DoM lives.

- **F-8 (71.6% selective accuracy at coverage 0.5):** MaxRL's pass@k preservation (Figure 5: no pass@k degradation for MaxRL vs severe degradation for GRPO) means a MaxRL-trained model would have higher output diversity. Selective prediction exploits the gap between easy (high-DoM) and hard (low-DoM) problems — if MaxRL closes that gap by training harder problems successfully, the selective-prediction frontier shifts.

- **H-282 (F-8 as GRPO variance-prioritization artifact):** MaxRL directly tests this. GRPO prioritizes high-variance (medium-difficulty) prompts; MaxRL prioritizes low-pass-rate (hard) prompts. Comparing DoM-gated selective prediction on both training regimes disambiguates whether the signal is GRPO-specific or RL-general.

- **H-682 (F-8 RL-fragility under GRPO):** MaxRL provides a cleaner test than Dr.GRPO ablations. Rather than modifying GRPO's normalization post-hoc, MaxRL proposes an entirely different objective. If F-8 survives on a MaxRL checkpoint, H-682 is rejected (the signal is robust to RL objective choice).

- **F-1 (dimensional breathing is universal):** MaxRL's Figure 13 shows MaxRL-trained models retain higher actor entropy during training. If breathing is linked to how the model distributes probability mass across tokens (F-5: content-dependent), then MaxRL's entropy preservation could yield a different breathing pattern — potentially broader and more uniform rather than the punctuated expansion-contraction cycle.

- **F-9 (CoE-60 redundant with L19 DoM):** If the DoM direction itself is GRPO-dependent, the CoE-60 vs L19-DoM comparison in F-9 may also be GRPO-contingent. Under MaxRL's different gradient landscape, CoE features might decorrelate from single-layer DoM.

## Methodologies extracted

- **Population-level weight function analysis** — Express any RL objective's gradient as w(p)·∇p, where w(p) is a scalar weight depending only on pass rate p. Different objectives (GRPO, RLOO, MaxRL, ML) correspond to different w(p) curves (Table 2, Section 5).
  *Replication cost*: 20min CPU (compute w(p) curves analytically; no model needed).
  *We'd plausibly run this*: yes — apply to our per-problem pass-rate estimates from multi-sample generation to predict how each objective would weight gradient across MATH-500 difficulty buckets.

- **Gradient norm vs pass-rate scatter analysis** — For each prompt in a batch, record per-prompt gradient L2 norm against per-prompt pass rate (Figure 6). Reveals how different objectives allocate learning signal across difficulty.
  *Replication cost*: 2-4h H100 (requires backward passes on the model; 131,072 rollouts per example in their full analysis, but coarse estimates at 128 rollouts would suffice).
  *We'd plausibly run this*: yes — but only if we train a MaxRL checkpoint; a gradient analysis on the existing GRPO-trained model is a useful control but not new relative to the paper's own Figure 6.

- **Maclaurin expansion truncation of log-likelihood** — Express log-probability as infinite series of fail@k terms (Eq. 4), truncate to order T. Provides a compute-indexed family of objectives interpolating between expected reward (T=1) and maximum likelihood (T→∞).
  *Replication cost*: 5min CPU (pure math, no code needed).
  *We'd plausibly run this*: no — this is a training objective, not a post-hoc analysis we'd apply to cached activations.

- **Success-conditioned gradient estimator** — Normalize REINFORCE gradient by number of successful samples K instead of total samples N (Algorithm 1). Single-line modification to advantage calculation.
  *Replication cost*: 1h implementation + H100 training run.
  *We'd plausibly run this*: yes if training a checkpoint — trivial to implement in verl.

## Approaches & framings

- **RL-as-approximate-ML framing.** The paper reframes all existing RL objectives (REINFORCE, GRPO, RLOO) as varying-quality approximations to maximum likelihood, differentiated only by their w(p) weighting. This provides a unified lens for understanding *why* GRPO-trained models have the specific geometric signatures we observe: GRPO's w(p) = 1/√(p(1−p)) creates a particular difficulty-dependent gradient landscape that plausibly shapes F-2's DoM direction, F-7's D-bucket signature, and F-4's asymmetric collapse.

- **Compute-indexed objective interpolation.** The truncation level T in MaxRL controls how much the objective resembles ML vs RL. This intersects H-282: different T values should produce different amounts of the GRPO-like variance-prioritization artifact, providing a continuous knob to test the "training artifact" hypothesis.

- **Pass@k degradation as objective-level failure, not optimization-level.** MaxRL attributes GRPO's pass@k collapse to the objective (not the optimizer), which reframes our F-8 selective-prediction result: the ability to "refuse and spend" may be parasitic on GRPO-induced distribution sharpening rather than being a robust model property.

## Datasets & benchmarks

- **MATH-500** — 500 problems, used in their Section 6.5 gradient analysis on Qwen2.5-1.5B-Instruct (our exact setup). Open, accessible via HF. Applicable? yes — identical to our evaluation set.

- **GSM8K** — 7,473 training / 1,319 test problems, used for SmolLM2-360M training experiments. Open, HF. Applicable? yes — could extend our pipeline to a simpler reasoning benchmark for sanity checks.

- **POLARIS-53K** — ~50K mathematical reasoning prompts used for Qwen3 training. Available via HKU NLP. Applicable? yes — if training a MaxRL checkpoint, this is the dataset they used.

- **BeyondAIME** — Hard math competition problems. HF. Applicable? medium — could test DoM generalization on harder problems, but we'd need new extractions.

## Implementation details worth capturing

- MaxRL is a single-line change to GRPO/RLOO advantage calculation: divide by mean reward instead of standard deviation (Algorithm 1, line 7-8). Implemented in verl framework.
- Code and checkpoints available at https://zanette-labs.github.io/MaxRL/
- They train Qwen3-1.7B-Base and Qwen3-4B-Base (not Instruct variants) with MaxRL on POLARIS-53K, 256 prompts/batch, 16 rollouts/prompt, 1000 RL steps on 32×H200.
- Fully on-policy: one gradient update per batch of rollouts, no importance ratio or clipping.
- Gradient analysis in Figure 6 uses 131,072 rollouts per example on Qwen2.5-1.5B-Instruct + MATH-500.

## Replicable intermediates

- **Weight-function difficulty reweighting analysis on cached per-problem pass rates.** If we have per-problem pass@1 estimates from multi-sample generation (K=2.5 avg in F-8), we can analytically compute w_GRPO(p), w_MaxRL(p), and w_ML(p) for each MATH-500 problem, then correlate with the L19 DoM projection. This checks whether the DoM direction's predictive power is concentrated on the problems that GRPO upweights. Script: any cached per-problem accuracy + `pathway11_h100/prefill_gated_compute/` data.
- **DoM projection vs difficulty bucket overlap.** From cached L19 prefill activations (500 samples × 1536 dims) and per-problem accuracy labels, compute DoM projections, bin by GRPO w(p) quantiles, and test whether DoM discrimination tracks GRPO's gradient-weighting profile. Script: `pathway11_h100/pca_covariance/` cached data.

## Cross-paper signals

- 2501.12948 — already in graph (status: graphed). DeepSeek-R1: MaxRL positions itself as improving on GRPO, which DeepSeek-R1 uses. Direct lineage.
- 2504.13837 — NOT in graph; recommend admission. Yue et al. "Does RL really incentivize reasoning capacity beyond the base model?" — MaxRL cites extensively; directly relevant to whether F-2/F-8 are pre-training artifacts amplified by RL.
- 2507.14843 — NOT in graph; recommend admission. Wu et al. "The invisible leash: Why RLVR may or may not escape its origin" — studies whether RL moves beyond pretraining distribution, directly relevant to H-282.
- 2510.04996 — NOT in graph; recommend admission. Xiong et al. "REINFORCE-ADA: adaptive sampling under non-linear RL objectives" — closely related to MaxRL, alternative adaptive-sampling approach to the same 1/p weighting problem.
- 2503.20783 — NOT in graph; recommend admission. Liu et al. "Understanding R1-zero-like training: A critical perspective" — studies RLVR dynamics, directly relevant to understanding what RL training carved into the hidden states we probe.
- 2505.15201 — NOT in graph; recommend admission. Walder & Karkhanis "Pass@k Policy Optimization (PKPO)" — optimizes pass@k directly; MaxRL shows it's a special case of their framework. Relevant to pass@k diversity and F-8.

## Proposed FutureExperiments

### Tier 1 — Cheap local (5min–2hr, CPU only, ROI 5–10)

```yaml
- id: P11-FE110700
  pathway: P11
  description: "Weight-function difficulty reweighting analysis: compute GRPO w(p), MaxRL w(p), and ML w(p) for each MATH-500 problem using cached per-problem accuracy, then correlate each weighting profile with L19 DoM projection to test whether DoM discrimination tracks GRPO's gradient-weighting."
  rationale: "If DoM projection correlates strongly with GRPO's w(p) profile but not ML's or MaxRL's, it implies the correctness signal is partly a GRPO training artifact (H-282). Conversely, if DoM correlates equally with all weighting functions, the signal is objective-agnostic."
  trigger: "MaxRL paper Section 5 derives closed-form w(p) for all objectives"
  status: READY
  priority: HIGH
  roi: 8
  cost: "30min CPU"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2602.02710"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE110701
  pathway: P11
  description: "D-bucket gradient void analysis: for each MATH-500 problem, compute estimated pass rate from cached K-sample accuracy, apply GRPO/MaxRL/ML weighting functions, and test whether D-bucket problems (F-7) fall in GRPO's gradient-void region (low w(p) at low p)."
  rationale: "If D-bucket problems cluster where GRPO's w(p) is minimal but MaxRL's w(p) is large, the D-bucket geometric signature is predicted to be a GRPO training artifact — the model never received gradient on those problems."
  trigger: "MaxRL Figure 6 shows GRPO has low gradient norm at low pass rates on Qwen2.5-1.5B-Instruct + MATH-500"
  status: READY
  priority: HIGH
  roi: 7
  cost: "20min CPU"
  depends-on: [F-7, F-2]
  would-update: [F-7]
  triggered-by: ["2602.02710"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE110702
  pathway: P11
  description: "Correlate L19 covariance spectrum eigenvalues (F-2's cov-spectrum probe, AUROC 0.7928) with GRPO weighting profile: compute per-problem w_GRPO(p) and test whether the top spectral features the cov-spectrum probe uses align with GRPO's difficulty emphasis pattern."
  rationale: "The cov-spectrum probe beats single-direction DoM (0.7928 vs 0.7731). If the extra spectral features track GRPO's w(p) rather than task-intrinsic structure, the cov-spectrum advantage is also GRPO-dependent."
  trigger: "MaxRL provides closed-form w(p) enabling analytical decomposition of spectral features by difficulty"
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "45min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2602.02710"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required (hours–days, high ROI only)

```yaml
- id: P11-FE110703
  pathway: P11
  description: "Train Qwen2.5-1.5B with MaxRL on MATH-train (or download MaxRL Qwen3-1.7B checkpoint if released), extract L19 prefill activations on MATH-500 at 1024-tok, refit DoM, report AUROC and cos(GRPO-DoM, MaxRL-DoM). Directly tests H-282."
  rationale: "This is the definitive test of whether F-2 and F-8 are GRPO artifacts. If AUROC holds and cos(DoM_GRPO, DoM_MaxRL) > 0.8, the signal is RL-objective-agnostic. If AUROC drops or cos < 0.5, F-2/F-8 are partly GRPO artifacts."
  trigger: "MaxRL paper provides code, training recipe, and checkpoints for same-family model"
  status: READY
  priority: CRITICAL
  roi: 9
  cost: "1-2 H100 day(s)"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8, F-7, F-4]
  triggered-by: ["2602.02710"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE110704
  pathway: P11
  description: "On MaxRL-trained checkpoint (from FE110703), repeat F-4 asymmetric-collapse analysis and F-7 D-bucket geometric signature test. Check whether GRPO-specific gradient void at low pass rates is the cause of both phenomena."
  rationale: "F-4 and F-7 may share a common cause: GRPO under-training hard problems. MaxRL's emphasis on hard problems (w(p)≈1/p) would fill in the geometric void. If asymmetric collapse disappears and D-bucket loses its distinctive signature, both findings are RL-objective-dependent."
  trigger: "Depends on FE110703 producing a MaxRL checkpoint"
  status: BLOCKED
  priority: HIGH
  roi: 8
  cost: "2h H100"
  depends-on: [F-4, F-7]
  would-update: [F-4, F-7]
  triggered-by: ["2602.02710"]
  blocked-by: null
  blocked-by-experiment: [P11-FE110703]
```

## Proposed HYPOTHESES.md additions

### H-750: Prefill DoM direction at L19 rotates under MaxRL training vs GRPO training
**Priority:** HIGH
**Motivated by:** 2602.02710 §5 (weight function analysis) + F-2 (AUROC 0.7731 on GRPO-trained Qwen2.5-1.5B-Instruct)
**Test:** Train Qwen2.5-1.5B with MaxRL (single-line change to advantage normalization in verl), or use released Qwen3-1.7B-MaxRL checkpoint. Extract L19 prefill activations on MATH-500 at 1024 tok. Fit DoM direction. Report cos(DoM_GRPO, DoM_MaxRL). 1-2 days H100.
**Requires:** H100, MATH-train for RL training (or released checkpoint), extraction pipeline from P11
**Would change:** On confirm (cos < 0.5): F-2 and F-8 are partly GRPO artifacts; on reject (cos > 0.8): DoM is robust to RL objective, strengthening F-2 and effectively closing H-282.
**Blocks:** H-282

### H-751: D-bucket geometric signature (F-7) is caused by GRPO gradient void at low pass rates
**Priority:** HIGH
**Motivated by:** 2602.02710 Figure 6 (GRPO gradient norm → 0 at low pass rate, on our exact model+data) + F-7
**Test:** Compute GRPO w(p) for each MATH-500 problem using cached per-problem accuracy estimates. Check whether D-bucket problems cluster in the w(p) → 0 region. Cheap CPU analysis (FE110701) gives indirect evidence; direct confirmation requires MaxRL checkpoint (FE110704). 20min CPU + optional 2h H100.
**Requires:** Cached per-problem accuracy from K-sample generation, cached L19 activations
**Would change:** On confirm: F-7 is reframed as "GRPO gradient void signature" not "competence boundary signature." On reject: F-7 is robust and reflects genuine competence structure.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2602.02710 — Maximum Likelihood Reinforcement Learning (Tajwar, Zeng, Zhou, Song, Arora, Jiang, Schneider, Salakhutdinov, Feng, Zanette, 2026)

**Relevance:** MaxRL proposes a principled alternative to GRPO for RL training of reasoning LLMs, showing that GRPO optimizes only a first-order approximation of the ML objective and induces a specific difficulty-dependent gradient weighting (w(p) = 1/√(p(1−p))). Their gradient norm analysis is performed on exactly our model (Qwen2.5-1.5B-Instruct) and benchmark (MATH-500), providing the first external characterization of how the RL training objective shapes per-prompt gradient landscape on the model we probe. This directly addresses whether our findings (F-2 DoM direction, F-7 D-bucket signature, F-8 selective prediction) are intrinsic geometric properties of the model or artifacts of GRPO's specific difficulty-weighting.

**Key claim we tested:** GRPO concentrates gradient on medium-difficulty prompts and delivers vanishing gradient for hard (low pass-rate) prompts, while MaxRL approximates maximum likelihood by upweighting hard prompts with w(p) ≈ 1/p.

**Our result:** **TO TEST** — The weight-function analysis (FE110700, FE110701) can be run immediately on cached data to check whether our DoM/D-bucket signals correlate with GRPO's w(p) profile. The definitive test (FE110703) requires training or obtaining a MaxRL checkpoint.

**Related experiments:** P11-FE110700, P11-FE110701, P11-FE110702, P11-FE110703, P11-FE110704, H-750, H-751, H-282, H-682

**Status:** TO TEST

## New claims

None — this brief quotes only existing headline numbers (AUROC 0.7731, cos 0.046, 71.6% selective accuracy at 50% coverage, PR ratio 0.509) and the paper's own w(p) formulas, which are mathematical identities not empirical claims requiring validation.

## Sources consulted

- ~/topo-confidence/CLAUDE.md §Headline numbers — default context
- ~/topo-confidence/FINDINGS.md (grep for F-N one-liners) — default context
- ~/topo-confidence/HYPOTHESES.md (grep for H-N one-liners + Next ID) — default context
- ~/topo-confidence/PAPER_INDEX.md (grep for status flags) — default context
- ~/topo-confidence/DATA_MANIFEST.md — confirmed model is Qwen2.5-1.5B-Instruct (GRPO-trained)
- ~/topo-confidence/HYPOTHESES.md §H-282, §H-682 (grep) — confirmed existing hypotheses about GRPO artifact
