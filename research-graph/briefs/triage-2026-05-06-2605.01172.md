# Triage brief — 2605.01172 — A Theory of Generalization in Deep Learning

Source: https://arxiv.org/abs/2605.01172
Triaged: 2026-05-06

## Refutations

1. **Refutes F-2 (DoM predicts correctness at AUROC 0.7731) via signal-channel rank saturation.** The paper's Theorem 5.1 shows that test error decomposes exactly into signal-channel variance + bias, and only the signal channel (range of cumulative dissipation W_S) carries test-predictive information. If our DoM direction (≈ PC1 of L19 prefill activations) aligns with the top eNTK eigenmode, then the AUROC ceiling of 0.7731 (and the cov-spectrum ceiling of 0.7928) is mechanistically explained as the rank of the signal channel at layer 19 — a fixed property of the trained model, not a probe engineering problem. The refutation fires if we show that the signal-channel dimensionality at L19 matches the effective rank of our predictive features, implying no further probe can exceed this ceiling. **Falsifiable by:** computing the NTK eigenspectrum at L19 for a subset of MATH-500 problems and checking whether the number of eigenvalues above the reservoir threshold matches the effective dimensionality of our cov-spectrum probe (≈20).

2. **Refutes F-1 (dimensional breathing is universal) via implicit-bias spectral schedule.** The paper's Corollary H.2 ("implicit bias") shows the signal channel fills from the largest eNTK eigenvalue down during training. If generation-time token-by-token processing is analogous to a spectral schedule filling (more directions become "active" as the sequence progresses then collapse at the final token), then what we observe as "breathing" (participation-ratio expansion and collapse) is simply the autoregressive process visiting different spectral modes of the frozen per-position NTK — a mechanical consequence of the architecture, not a learned geometric signal about correctness. The refutation fires if breathing amplitude is fully predicted by the per-position NTK eigenspectrum shape. **Falsifiable by:** computing per-position effective NTK rank on cached activations and correlating with observed PR trajectory; if correlation > 0.9, breathing is an NTK artifact not a learned feature.

3. **Refutes H-5 (prefill signal encodes familiarity vs decomposability) via noise-channel semantics.** The paper's Proposition 3.2 proves that reservoir directions are unconditionally test-invisible — any information in those directions is memorization, not generalization. If the "familiarity" signal we hypothesize in H-5 lives primarily in the reservoir (high-dim noise directions), it would be a training artifact, not a generalizable difficulty signal. The refutation fires if a substantial portion of the DoM's variance (say >30%) projects onto eNTK eigenvectors with near-zero eigenvalues. **Falsifiable by:** projecting cached L19 DoM vector onto the NTK eigenspectrum and measuring what fraction of DoM variance lives in sub-threshold (reservoir) eigenvalues.

4. **Challenges F-8 (selective-prediction via DoM works at 50% coverage) via population-risk label-free alternative.** The paper's Section 6 derives a validation-free population-risk objective that can identify which parameters (and by extension, which data points via self-influence) contribute to generalization versus memorization. If this objective produces a correctness signal competitive with DoM (AUROC ≥ 0.77) without requiring any labeled correct/incorrect supervision, it would render the supervised DoM approach unnecessary for the selective-prediction application. **Falsifiable by:** implementing the self-influence computation on our 500 MATH-500 problems and checking whether per-sample self-influence score achieves AUROC ≥ 0.77 for correctness.

## Direct connections to F-N / H-N

- **F-2 (AUROC 0.7731):** The cov-spectrum probe (AUROC 0.7928, top-20 log-eigvals) is essentially measuring eigenvalue structure of the activation covariance. The paper's theory predicts that only eigenmodes in the "signal channel" carry test-visible information. The 20 features that achieve 0.7928 may correspond precisely to the signal-channel rank at L19, explaining both why it works and why it plateaus.

- **F-10 (PH null):** The paper's reservoir concept provides a direct mechanistic explanation. High-dimensional residuals sitting in the reservoir (ker W_S) are provably test-invisible, meaning their geometry *should* be indistinguishable from Gaussian noise — exactly what F-10 observes. The topology of reservoir directions is information-less by theorem, not by empirical coincidence.

- **F-4 (asymmetric collapse):** The "noise channels die at rate 1/√n" while "signal channels accumulate linearly" (Theorem 4.1) maps directly to our observation that correct trajectories collapse harder (signal concentration) while incorrect ones remain dispersed (stuck in noise channels).

- **H-12 (label-free DoM replacement):** The population-risk objective computes an unbiased estimate of generalization from a single batch with no validation data. This is precisely the kind of "label-free correctness signal" we need for H-12, operating at the optimizer level rather than the probe level.

- **H-16 (Marchenko-Pastur bias correction):** The paper's spectral-decay framework for characterizing signal vs noise eigenvalues provides a more principled alternative to MP correction — instead of removing "bulk" eigenvalues as MP noise, one identifies the signal channel directly via the NTK dissipation operator.

## Methodologies extracted

- **Signal-channel / reservoir decomposition via cumulative dissipation Gramian W_S** — Integrates the NTK over the training trajectory to identify which output directions were "trained" (signal) vs. untouched (reservoir/noise).
  *Replication cost*: "2-4h H100" (needs NTK computation at L19 for 500 samples; NTK is n×n with n=500 at a single layer, feasible but requires forward passes + Jacobian-vector products).
  *We'd plausibly run this*: yes — the core eigendecomposition can be approximated cheaply via random projections, and the signal-channel rank at L19 would directly explain our AUROC ceiling.

- **Per-parameter SNR gate (µ²_k > σ²_k/(b-1))** — Binary mask that updates a parameter only when its squared mean gradient exceeds its variance scaled by 1/(b-1). Separates population-relevant signal from batch-specific noise.
  *Replication cost*: "10min CPU" (only need to compute mean and variance of per-feature activations across the 500 cached samples, then apply the threshold).
  *We'd plausibly run this*: yes — directly applicable to our 500×1536 activation matrix as a feature-selection criterion.

- **Self-influence via leave-one-out test-transfer operator G** — For each training point, compute its contribution to the generalization gap by treating it as a one-point test set against the remaining batch.
  *Replication cost*: "30min CPU" (for our 500-sample setup, this is 500 leave-one-out comparisons on cached features; no forward passes needed if we compute on the already-extracted L19 activations).
  *We'd plausibly run this*: yes — self-influence per problem could be a novel correctness predictor.

- **Drift-diffusion separation (Theorem 4.1)** — Decomposes SGD updates into linear drift (signal) and √t diffusion (noise), showing that signal accumulates at rate T while noise grows at √(ηT/b).
  *Replication cost*: "Would need training checkpoints — not applicable to frozen model analysis."
  *We'd plausibly run this*: no — requires access to training dynamics, not inference-time activations.

- **Population-risk objective (no validation data)** ��� Exact unbiased estimator of population risk from a single training batch, derived from exchangeability + the test-transfer operator.
  *Replication cost*: "1-2h H100" (needs gradient computation per sample, but the principle could be approximated from cached activations).
  *We'd plausibly run this*: yes (in a simplified form applied to our probe training, not full LLM training) — could validate whether our logistic regression probe on L19 features suffers from memorization.

## Approaches & framings

- **Output-space dynamics framing:** Working entirely in output space (stacked predictions) rather than parameter space. Relevant because our DoM direction is defined in hidden-state space (a single layer's output), which is an intermediate output space — the paper's theory could apply per-layer rather than only at the final logit layer.

- **Signal-channel rank as generalization capacity:** The dimensionality of range(W_S) determines how many independent directions of generalization the model supports. This reframes our "why does a single direction (DoM) work so well?" question as: the signal channel at L19 may simply have low effective rank, making a 1-D projection surprisingly competitive.

- **Noise-in-signal-channel as the only failure mode:** After reservoir invisibility and drift-diffusion separation, the only source of test error is noise that leaked into the signal channel. This reframes our selective-prediction problem: abstaining on problems where the signal-channel noise is high (rather than where DoM is low) might be more principled.

## Datasets & benchmarks

- **Modular arithmetic (a·b⁻¹ mod 97)** — 25% train split, synthetic, 9409 samples total. Applicable? no — irrelevant to MATH-500 / hidden-state pipeline.
- **PINN transport equation** — synthetic noisy IC. Applicable? no — PDE domain.
- **UltraFeedback (DPO fine-tuning)** — standard preference dataset with 30% swapped labels. Applicable? partially — they fine-tune Qwen2.5-0.5B-Instruct which is in our model family; the noisy-preference resistance metric could be relevant if we ever do preference-based training.

## Implementation details worth capturing

- SNR preconditioner is one extra parameter-sized state vector storing streaming variance EMA of squared gradient deviations; gate multiplies standard Adam moment update.
- Leave-one-out coefficient α = 1 for fresh-batch boundary, α = b/(n-b) for finite-dataset boundary.
- The gate has a "soft" form (Section F.4) for continuous weighting rather than binary masking.
- Qwen2.5-0.5B-Instruct fine-tuning with DPO used as experimental testbed.
- Code likely not yet released (preprint, Stanford, no repository link in paper).

## Replicable intermediates

- **SNR feature selection on cached L19 activations:** Compute per-feature (across 500 samples) mean and variance for correct vs incorrect groups. The µ²/σ² ratio per feature dimension is trivially computable from our cached NPZs and would tell us whether the SNR criterion selects the same dimensions as our PCA-based approach. Uses: `pathway11_h100/prefill_gated_compute/` cached activations.
- **Signal-channel rank estimate:** Compute the eigenvalues of our 500×500 sample covariance (Gram matrix) of L19 prefill activations. Count how many eigenvalues exceed a noise floor (e.g., Marchenko-Pastur upper edge). Compare this number to the effective feature dimensionality of our cov-spectrum probe (20). Uses: `pathway11_h100/cov_spectrum/` cached features.
- **Self-influence approximation:** For each of the 500 problems, compute a leave-one-out cosine similarity of its L19 activation against the remaining 499 — high self-influence (unusual activation) may predict incorrectness. Uses: cached L19 activations from `pathway11_h100/prefill_gated_compute/`.

## Cross-paper signals

- 2604.19740 — already in graph (status: PARTIALLY CONFIRMED). The "sharpness dimension" concept intersects with this paper's signal-channel spectral schedule; sharpness dimension = effective rank of the signal channel.
- 2509.12886 — already in graph (status: REPLICATED). Their hidden-representation difficulty estimation is essentially measuring projection onto signal-channel vs reservoir directions, but they don't have this theoretical framing.
- 2402.03744 — already in graph (status: TO TEST). INSIDE's eigenvalue-decomposition-based hallucination detection is a special case of signal-channel rank analysis at inference time.

## Proposed FutureExperiments

### Tier 1 — Cheap local (5min–2hr, CPU only, ROI 5–10)

```yaml
- id: P11-FE01172A
  pathway: P11
  description: "SNR feature selection on L19 prefill activations: compute per-dimension µ²/σ² ratio (correct vs incorrect mean separation over within-group variance) and compare selected dimensions against PCA top-k"
  rationale: "The paper's per-parameter gate (µ²_k > σ²_k/(b-1)) provides a theoretically motivated feature-selection criterion. If the dimensions it selects match our PCA-derived top components, it validates that our cov-spectrum probe is already capturing the signal channel. If they differ, the SNR-selected features may yield a better probe."
  trigger: "2605.01172 Corollary 6.3 — per-parameter SNR gate selects generalizing dimensions"
  status: READY
  priority: HIGH
  roi: 7
  cost: "15min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.01172"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE01172B
  pathway: P11
  description: "Estimate signal-channel rank at L19 via Gram matrix eigenspectrum: compute 500×500 Gram matrix of L19 prefill activations, identify eigenvalue knee (MP upper edge or elbow), compare rank to cov-spectrum probe dimensionality (20) and full-1536 ceiling (0.7847)"
  rationale: "If the signal-channel effective rank at L19 is ~20, this explains both why our 20-feature cov-spectrum probe nearly matches the full 1536-dim ceiling AND why no probe engineering can exceed ~0.79. The AUROC ceiling is a property of the representation, not the classifier."
  trigger: "2605.01172 Definition 3.1 — signal channel rank determines test-visible information capacity"
  status: READY
  priority: HIGH
  roi: 8
  cost: "10min CPU"
  depends-on: [F-2, F-10]
  would-update: [F-2]
  triggered-by: ["2605.01172"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE01172C
  pathway: P11
  description: "Self-influence correctness predictor: for each of 500 MATH-500 problems, compute leave-one-out influence (LOO cosine distance of L19 activation vs centroid of remaining 499). Test whether self-influence score predicts correctness (AUROC vs DoM's 0.7731)"
  rationale: "The paper's Theorem F.6 shows that averaging self-influences recovers the generalization gap. Per-sample self-influence should correlate with whether the model 'knows' the answer (correct) or is 'memorizing/guessing' (incorrect). This would be a label-free correctness signal addressing H-12."
  trigger: "2605.01172 Section 6 — self-influence as population-risk estimator"
  status: READY
  priority: HIGH
  roi: 7
  cost: "20min CPU"
  depends-on: [F-2, F-8]
  would-update: [F-8]
  triggered-by: ["2605.01172"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE01172D
  pathway: P11
  description: "Project DoM direction onto Gram eigenvectors to quantify what fraction of DoM variance lives in signal vs reservoir: decompose DoM into components along top-k (signal) vs tail (reservoir) eigenvectors of the activation Gram matrix"
  rationale: "If DoM lives entirely in the signal channel (top eigenvectors), it validates the paper's prediction that only signal-channel directions carry test information. If DoM has substantial reservoir components, the theory predicts those components are noise — we should strip them for a better probe."
  trigger: "2605.01172 Proposition 3.2 — reservoir directions are test-invisible"
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "10min CPU"
  depends-on: [F-2, F-10]
  would-update: [F-2, F-10]
  triggered-by: ["2605.01172"]
  blocked-by: null
  blocked-by-experiment: []
```

### Tier 2 — H100 required (hours–days, high ROI only)

```yaml
- id: P11-FE01172E
  pathway: P11
  description: "Approximate layer-19 NTK eigenspectrum via Jacobian-vector products: compute top-50 NTK eigenvalues at L19 using Lanczos iteration on cached model, compare signal-channel rank to effective probe dimensionality"
  rationale: "The definitive test of whether the signal-channel theory explains our AUROC ceiling requires the actual NTK eigenspectrum at L19, not just the activation covariance proxy. If signal-channel rank matches our observed ceiling, the paper provides the theoretical explanation for F-2."
  trigger: "2605.01172 Theorem 5.1 + Definition 3.1 — NTK eigenmodes determine what is test-predictable"
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "4h H100"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.01172"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-775: The AUROC ceiling (0.7928) for correctness prediction from L19 activations equals the signal-channel rank at layer 19
**Priority:** HIGH
**Motivated by:** 2605.01172 + F-2
**Test:** Compute Gram matrix eigenspectrum of L19 prefill activations (500×500 matrix); identify signal-channel rank via MP edge or elbow method; compare to effective dimensionality of best probe (20 features → 0.7928). If they match, the ceiling is a representational property.
**Requires:** CPU only, cached L19 activations
**Would change:** On confirm: F-2's ceiling becomes theoretically explained, shifts focus from probe engineering to representation engineering. On reject: signal-channel theory doesn't apply at single-layer level.
**Blocks:** nothing

### H-776: Leave-one-out self-influence computed from L19 activations achieves AUROC ≥ 0.75 for correctness prediction without supervised labels
**Priority:** HIGH
**Motivated by:** 2605.01172 Section 6 + H-12 + F-8
**Test:** For each of 500 problems, compute LOO influence (cosine distance to leave-one-out centroid, or Mahalanobis distance to LOO distribution). Compute AUROC for correct/incorrect classification. Target: match or exceed DoM (0.7731) without any labels.
**Requires:** CPU only, cached L19 activations
**Would change:** On confirm: label-free selective prediction is possible, resolving H-12. On reject: supervised probing remains necessary for deployment.
**Blocks:** H-12

## Proposed PAPER_INDEX.md classification

## 2605.01172 — A Theory of Generalization in Deep Learning (Litman, Guo, 2025)

**Relevance:** Provides a non-asymptotic generalization theory based on NTK signal-channel / reservoir decomposition. The signal channel (directions where training dissipated loss) is exactly the subspace visible at test. Directly relevant to understanding why our DoM direction predicts correctness (it may live in the signal channel), why the AUROC ceiling exists (signal-channel rank), and why PH/topology adds nothing (reservoir geometry is provably noise-like). The population-risk objective offers a potential label-free alternative to supervised DoM.

**Key claim we tested:** Signal-channel rank determines the effective dimensionality of test-predictable information; reservoir directions are unconditionally test-invisible.

**Our result:** **TO TEST** — The theoretical framework elegantly explains F-2's ceiling and F-10's null, but requires verification that the output-space theory applies to intermediate-layer activations (L19) rather than only final logits. Tier 1 experiments (FE01172A–D) can validate the activation-level predictions cheaply.

**Related experiments:** P11-FE01172A, P11-FE01172B, P11-FE01172C, P11-FE01172D, P11-FE01172E, H-775, H-776

**Status:** TO TEST

## New claims

None — this brief only quotes existing headline numbers (AUROC 0.7731, 0.7928, 0.7847) and does not introduce new quantitative results.

## Sources consulted

- ~/topo-confidence/CLAUDE.md (headline numbers section) — default context
- ~/topo-confidence/FINDINGS.md (grep for F-N headers) — default context
- ~/topo-confidence/HYPOTHESES.md (grep for H-N headers + Next ID) — default context
- ~/topo-confidence/PAPER_INDEX.md (grep for status flags) — default context
- research-graph `query.py paper 2605.01172` — prior admission note
- research-graph `query.py future P11` — next FE ID lookup
- research-graph `query.py paper 2604.19740` (via PAPER_INDEX grep) — cross-paper check
- research-graph `query.py paper 2509.12886` (via PAPER_INDEX grep) — cross-paper check
- research-graph `query.py paper 2402.03744` (via PAPER_INDEX grep) — cross-paper check
