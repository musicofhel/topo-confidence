# Result brief — P11-FE719 (Leave-One-Subject-Out CV on F-2)

First Phase 1 cheap-win. Tests whether F-2 (prefill L19 DoM AUROC 0.7731) is a
correctness signal or a topic detector under MATH-500 subject-stratified
leakage. Threshold per FE719 spec: mean LOCO AUROC < 0.65 ⇒ F-2 refuted.

## FE

```yaml
fe_id: P11-FE719
status: COMPLETED
outcome: "Mean LOCO AUROC 0.7427 (worst 0.6032 on Number Theory, best 0.9000 on Geometry, 7 folds). F-2 is NOT refuted (0.7427 ≥ 0.65 threshold). ~3pp drop from the 0.7731 random-OOF headline confirms a small subject-leakage component, but the bulk of the signal is correctness."
result_json: pathway11_h100/loco_subject/results.json
result_json_keys:
  - auroc_mean
  - auroc_worst
  - auroc_best
  - per_subject
  - n_folds_scored
  - in_sample_full_auroc
  - f2_refuted
regen_cmd: "python pathway11_h100/loco_subject/recompute_fe719.py"
```

## Result JSON verification

`pathway11_h100/loco_subject/results.json` exists; declared keys are
top-level. Regen recomputes all numbers from
`pathway11_h100/prefill_inversion/cache/m15b_prefill.npz` + the cached
HuggingFaceH4/MATH-500 test split — both inputs are deterministic.

## EXPERIMENT_LOG entry

## EXP-43: P11-FE719 — Leave-One-Subject-Out CV on F-2 prefill L19 DoM
**Date:** 2026-05-01
**Status:** COMPLETE
**Motivated by:** Probe-non-diagnosticity critique formalised in 2603.18280
§2 (probe results) — random-split CV is non-diagnostic, null controls reach
100% accuracy. F-2's 0.7731 AUROC was reported under 5-fold *random* CV;
subject-stratified leakage is plausible because MATH-500 base accuracy varies
by subject (Algebra 124 problems vs Counting & Probability 38). LOCO-CV is
the diagnostic test.
**Hypothesis:** If F-2 is largely a subject/topic detector, mean LOCO AUROC
drops below 0.65 (refutation threshold per FE719 spec). If F-2 is a
correctness signal, LOCO AUROC stays close to random-OOF.
**What we actually tested:** Loaded the L19 prefill cache
(`pathway11_h100/prefill_inversion/cache/m15b_prefill.npz`, 500×1536 fp16)
and the MATH-500 `subject` column. For each of 7 subjects, computed DoM =
mean(prefill[correct ∧ ~held]) − mean(prefill[~correct ∧ ~held]) on the
training fold (~430 problems), then scored AUROC of `prefill_held @ DoM`
vs `correct_held` on the held-out subject.
**Key result:** mean LOCO AUROC 0.7427, worst 0.6032 (Number Theory, n=62),
best 0.9000 (Geometry, n=41). Per-subject: Algebra 0.6982, Counting &
Probability 0.7278, Intermediate Algebra 0.7702, Prealgebra 0.7919,
Precalculus 0.7074. In-sample full-data DoM AUROC 0.7834 (sanity anchor —
no held-out, not directly comparable to the 0.7731 5-fold OOF figure). All
3 new claims PASS in `validate_claims.py` and REGEN_PASS via
`recompute_fe719.py`.
**Verdict:** F-2 NOT REFUTED. The ~3pp drop from 0.7731 (random-OOF) to
0.7427 (LOCO mean) confirms a small subject-leakage component, but 0.7427
is well above the 0.65 refutation threshold and well above chance.
**Changed our understanding of:** F-2 generalises across subjects with a
modest but real topic component; the strongest fold (Geometry 0.90) is
likely small-n variance (n=41, ~50/50 correct/incorrect), and the weakest
fold (Number Theory 0.60) suggests Number Theory problems use prefill
geometry slightly differently from the rest of MATH-500 — a direction
worth a follow-up FE.
**Files:** `pathway11_h100/loco_subject/recompute_fe719.py` (recompute
script, deterministic), `pathway11_h100/loco_subject/results.json` (output).
**Depends on:** EXP-037 (the 0.7731 5-fold OOF baseline).
**Enables:** Reusable LOCO machinery for FE299 (topic-stratified AUROC),
FE448 (length partial-correlation), and the rest of the Phase 1 sanity
battery.

## STATE.md last-experiment update

EXP-43 (P11-FE719 LOCO-CV by MATH-500 subject — F-2 NOT refuted at 0.65
threshold; mean LOCO AUROC 0.7427, worst 0.6032 on Number Theory, best
0.9000 on Geometry, 7 folds). First end-to-end run of `promote_result.py`
on a real result. F-2's strength holds; the ~3pp drop vs random-OOF
quantifies a small subject-leakage component, not a topic-detector
artifact.

## FINDINGS.md updates

### F-2: Prefill L19 DoM is a strong correctness predictor, *better than final-token*

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E43]
counterargument: "Mean LOCO AUROC 0.7427 (FE719) is ~3pp below the 0.7731 random-OOF figure, indicating a small subject-leakage component. Worst fold (Number Theory) drops to 0.6032 — the signal is real but topic-modulated."
overturned_by: "Would be overturned by a finding where mean LOCO AUROC drops below 0.65 (refutation threshold per FE719 spec, derived from 2603.18280)."
```

**Claim.** On Qwen-2.5-1.5B-Instruct, a 5-fold OOF logistic probe fit on
mean-pooled L19 activations at position 0 (last prompt token, pre-generation)
achieves **AUROC 0.7731** for K=1-correctness. Final-token DoM AUROC is
**0.7186**. Under leave-one-subject-out CV across the 7 MATH-500 subjects
(EXP-43), mean AUROC is **0.7427** — ~3pp below the random-OOF figure but
well above the 0.65 refutation threshold. The 7B shows the same pattern with
a larger gap (~0.876 vs ~0.77).

**Strength:** STRONG (2 models same family, clean methodology, survives
LOCO-CV).

**Evidence:** EXP-037
(`pathway11_h100/prefill_gated_compute/results.json` fields
`prefill_DoM_auroc_oof` = 0.7731, `final_token_DoM_auroc_oof` = 0.7186).
EXP-43 (`pathway11_h100/loco_subject/results.json` field `auroc_mean` =
0.7427, `auroc_worst` = 0.6032, `n_folds_scored` = 7).

**Controls passed:**
- Length-residualization: cos(L19 DoM, length direction) ≈ −0.09 at every
  layer; length-residualized probe AUROC matches raw within 0.002 (i.e., not
  a length artifact — in contrast to the ABC-44 pipeline).
- 5-fold stratified OOF (seed 9999) — not a test/train leak.
- **Leave-One-Subject-Out CV (EXP-43, FE719)** — mean AUROC 0.7427 across 7
  MATH-500 subjects, worst 0.6032 (Number Theory). Subject-stratified
  leakage is small (~3pp), not the dominant signal.

**Strongest counterargument:** The 0.6032 Number-Theory fold is markedly
weaker than the rest. If a follow-up FE shows that the DoM direction varies
substantially across subjects (per-subject DoM cosine matrix), the "single
correctness direction" framing weakens.

## New claims

- loco-auroc-mean = 0.7427 (FE719 LOCO mean AUROC, 1.5B prefill L19 DoM, 7 MATH-500 subjects)
- loco-auroc-worst = 0.6032 (FE719 LOCO worst-case AUROC, Number Theory held-out)
- loco-n-folds = 7 (FE719 LOCO folds scored)

## Sources

- PLAN_cheap_wins.md §"Phase 1: Sub-1h CPU sanity battery"
- 2603.18280 (probe-non-diagnosticity critique — random-split CV insufficient)
- pathway11_h100/loco_subject/results.json (this experiment's output)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (input cache)
- HuggingFaceH4/MATH-500 (subject labels)
