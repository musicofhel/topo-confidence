# Triage brief — 2012.13255 — Intrinsic Dimensionality Explains the Effectiveness of Language Model Fine-Tuning

Source: https://arxiv.org/abs/2012.13255
Triaged: 2026-04-29

## Refutations

1. **Refutes F-2 ("one direction is enough" implication).** Aghajanyan et al. find d₉₀ ≈ 207–8030 across BERT/RoBERTa × MRPC/QQP, and explicitly argue this is an *upper bound* on a continuous-relaxation of sparsification — i.e. tasks fundamentally require ≥hundreds of trainable directions to reach 90% of full performance. F-2 reports a single L19 prefill DoM achieves AUROC 0.7731 (1024-tok). If tasks have d₉₀ ≈ 200, then a 1-direction probe should be far from the achievable ceiling: a multi-direction (k=10, 50, 200) random-projection probe of the same L19 prefill activations should monotonically beat 0.77 by a wide margin, with diminishing returns near k≈d₉₀. **Falsifiable test:** project P11 cached L19 prefill activations into random k-dim subspaces (FastFood, k ∈ {1, 10, 50, 200, 1000}), train a logistic correctness classifier, plot AUROC(k). If AUROC plateaus at k=1, F-2's "one-direction" framing is genuinely correct and Aghajanyan's framework over-predicts task complexity for *correctness* (a meta-task on top of MATH-500). If AUROC climbs to ~0.85 by k=200, F-2's headline is leaving signal on the table and the SAID prediction wins.

2. **Refutes F-9 (CoE-60 redundant with single-layer L19 DoM).** Aghajanyan's central methodological finding is **SAID > DID across all four model × task cells of Table 1** (e.g., RoBERTa-Large MRPC: 207 SAID vs 322 DID; BERT-Base MRPC: 1608 vs 1861). Layer-wise scaling λᵢ measurably reduces required intrinsic dimension because "individual layers specialize separately." F-9 claims multi-layer Chain-of-Embedding features are redundant with a *single* L19 DoM, which is the structural inverse of SAID's premise. **Falsifiable test:** train a SAID-style structure-aware probe over Qwen-1.5B Stage-2 cached activations (L0…L27) with d_total = single-layer-DoM dimension + 28 layer-scaling parameters λᵢ. If the SAID-probe AUROC > 0.7731 + 0.02 (significant lift), F-9's redundancy claim is wrong: layer structure carries non-redundant correctness signal.

3. **Refutes F-1 (breathing universality across scales).** Aghajanyan §5.2 + Figure 3 demonstrate a *strong inverse correlation* between parameter count and d₉₀ across BERT/RoBERTa/BART/Electra/Albert/XLNet/T5/XLM-R: bigger models compress tasks into smaller subspaces. F-1 frames dimensional breathing as universal across Qwen-1.5B and 7B (and across architectures). Universality of breathing *amplitude* (PR ratios, peak height, asymmetric collapse) at fixed labels would be inconsistent with the predicted scale-monotone compression: if 7B has lower intrinsic d for "MATH-500 correctness," its participation ratio at L19 prefill should be systematically *lower* than 1.5B's, not just rescaled. **Falsifiable test:** compute Aghajanyan-style d₉₀ on Qwen-1.5B *and* Qwen-7B for the same MATH-500 correctness probe (random subspace + logistic classifier sweep). If d₉₀(7B) ≈ d₉₀(1.5B), F-1's universality claim survives a strong external prediction. If d₉₀(7B) << d₉₀(1.5B), the "universality" framing is masking scale-dependent compression that the PR-magnitude metric is too coarse to see.

## Direct connections to F-N / H-N

- **F-1 (breathing universal across architectures/scales).** Headline anchor: 1.5B vs 7B PR shapes presented as analogous. Aghajanyan's parameter-count-vs-d₉₀ inverse relation predicts scale-dependent compression, providing an *external* numerical prediction we can test against cached PR temporal curves. See refutation #3.
- **F-2 (Prefill L19 DoM AUROC 0.7731, OOF 5-fold).** Aghajanyan's d₉₀ ≈ 207 for RoBERTa-Large MRPC sets a quantitative scale for "how many task-relevant directions exist in pretrained representations." Using this to bound the multi-direction probing experiment described in refutation #1.
- **F-9 (CoE-60 redundant with single-layer DoM).** Direct contradiction with SAID > DID. Headline anchor: 0.046 cosine of prefill vs final-token DoM (F-3) plus the redundancy framing of F-9.
- **H-9 (breathing amplitude predicts problem difficulty).** Aghajanyan's Figure 2 shows Yelp Polarity has lower d₉₀ across all checkpoints than ANLI — explicit prediction that "easier task → smaller intrinsic d." H-9 is the *intra-task* (per-problem) version of this *inter-task* claim. Replicating their cross-task d₉₀ ordering on MATH-500 difficulty levels (1–5 metadata) would corroborate the underlying mechanism.
- **H-10 (breathing changes after fine-tuning).** Aghajanyan §5.1 is the strongest theoretical scaffolding we have for this hypothesis: pretraining monotonically reduces d₉₀ over 200k updates without ever seeing downstream data. Fine-tuning should reshape the breathing curve in *the intrinsic subspace* (where d₉₀ lives) but leave noise directions invariant.
- **H-4 (breathing ↔ Sharpness Dimension / EoS).** Both H-4 and Aghajanyan share the framing of "low-dim subspace governs trainability." Provides an additional cross-validation reference if H-4 is pursued.
- **H-16 (Marchenko-Pastur bias correction preserves breathing magnitude).** If the *true* intrinsic d for MATH-500 correctness is ≈ hundreds (Aghajanyan-style), then most of the residual stream variance is noise. MP correction *should* be the right tool, and its preservation of breathing amplitude is a key consistency test.

## Methodologies extracted

- **DID (Direct Intrinsic Dimension) via FastFood transform** — Reparameterize θ^D = θ₀^D + θ^d · M with M = HGΠHB (Hadamard × Gaussian-diag × permutation × ±1-diag), train only θ^d ∈ R^d via SGD, find smallest d achieving 90% of full-finetune performance. Compute in O(D log d).
  *Replication cost:* 1h CPU per (model, task, d) cell with cached L19 features, but *we'd adapt it for probing not fine-tuning* — random projection of activations into k-dim subspace then logistic classifier.
  *We'd plausibly run this:* yes — reuses cached P11 NPZs, directly tests F-2's "one direction" claim, low cost.

- **SAID (Structure-Aware Intrinsic Dimension)** — Same as DID but reserves m parameters as per-layer scaling λᵢ, so θ^d = [θ^(d-m), λ]. Empirically lowers d₉₀ across all tested models.
  *Replication cost:* 2h CPU on cached multi-layer activations.
  *We'd plausibly run this:* yes — this is the direct test that refutes F-9 (see refutation #2).

- **d₉₀ binary search across pretraining checkpoints** — Save model every 10k updates, compute d₉₀ per checkpoint per task, plot trajectory. Threshold = 0.9 × full-finetune metric of *that checkpoint* (not final).
  *Replication cost:* requires saving training checkpoints; not directly applicable to our static Qwen models.
  *We'd plausibly run this:* no — we don't pretrain models; H-10's fine-tuning version is the closest applicable variant.

- **Cross-architecture d₉₀ on a fixed benchmark (MRPC)** — Compute d₉₀ for 8 model families × multiple sizes, fit log-log trend.
  *Replication cost:* infeasible to retrain all sizes; could be approximated by computing d₉₀ for activation-probing on Qwen-1.5B vs 7B vs Phi-3 vs Llama on MATH-500.
  *We'd plausibly run this:* yes — directly tests refutation #3 against F-1 universality.

- **(0,S)-compression generalization bound: L₀(f) ≤ L̂₀(f) + O(√(d/m))** — Theoretical lift from Arora et al. 2018 (1802.05296), applies because intrinsic-subspace fine-tuning is losslessly described by (random seed, pretrained weights, θ^d).
  *Replication cost:* derivation only, no compute.
  *We'd plausibly run this:* no — but useful to cite if F-2/F-8 are written up as bounds rather than empirical AUROCs.

## Approaches & framings

- **Intrinsic dimension as MDL of the task within the pretrained framework.** Each NLP task is a θ^d-vector + random seed + fixed θ₀^D. Reframes "what is a task" as a compression problem. Relevant to our F-2/F-7 framing: a "correctness probe" can be reinterpreted as the minimal-description vector of the meta-task "predict whether MATH-500 K=1 will be correct" within the Qwen residual-stream framework.
- **Pretraining as implicit task-compression.** Pretraining never sees downstream tasks but monotonically lowers their d₉₀. Provides theoretical motivation for H-10 (fine-tuning should *further* compress task-specific subspaces) and H-8 (DoM stable across checkpoints — should hold *modulo* the intrinsic subspace).
- **SAID/DID as continuous relaxation of sparsification.** If M is constrained binary, the method recovers lottery-ticket pruning; relaxed to FastFood it gives intrinsic dimension. Frames our DoM probing as a low-rank (rank-1) special case of a much larger family of intrinsic-subspace probes.
- **Compression-based generalization bounds independent of D.** Generalization scales with √(d/m), not √(D/m). Justifies why 91/91 validation passes despite sparse experimental coverage relative to model parameters.

## Datasets & benchmarks

- **MRPC** — ~3700 training pairs, paraphrase classification. Microsoft Research, freely available via GLUE/HuggingFace. Applicable? **no** — text classification, not generation; no MATH-500 overlap. CITED ONLY value for any breathing-on-classification cross-domain probe.
- **QQP (Quora Question Pairs)** — ~363k pairs. Applicable? **no** — same reason.
- **SST-2, MNLI, Yelp Polarity, ANLI** — GLUE/SuperGLUE-adjacent classification. Applicable? **no** for MATH-500 directly, but **yes** for H-9 cross-task difficulty probe (ANLI is the canonical "hard task with high d₉₀" reference Aghajanyan uses).

## Implementation details worth capturing

- FastFood transform M = HGΠHB computed via Fast Walsh-Hadamard Transform in O(D log d). Reference impl in HuggingFace Transformers fork (Aghajanyan 2020).
- Initialize θ^d = 0 so starting point is exactly the pretrained model — required for the d=0 baseline to recover the original parameterization.
- Hyperparameter search over four learning rates per (d, model, task) cell — cheap because most settings are stable.
- Binary search d ∈ [100, 4M] with multiplicative steps; "unable to compute d₉₀" recorded explicitly when full-finetune fails to exceed majority class.
- For sentence classification, the random-projection subspace also covers the *randomly-initialized classification head* parameters, so the d-count includes the head — directly analogous to including the logistic-classifier weights when probing residual-stream activations.

## Replicable intermediates

- **Aghajanyan-style d₉₀ for the prefill correctness probe.** Project cached `pathway11_h100/qwen_1_5b/stage_2_features.npz` L19 prefill activations into random k-dim subspaces via FastFood (k ∈ {1, 5, 10, 50, 200, 1000}), train OOF 5-fold logistic regression for each k, plot AUROC(k). Threshold = 0.9 × 0.7731 = 0.696. The smallest k achieving 0.696 is the empirical d₉₀ of *correctness* in the prefill subspace. Compares directly against F-2's headline. ~30min CPU.
- **SAID-style multi-layer probe on Qwen-1.5B.** All-layer (28-layer) Stage-2 activations cached at MATH-500 prompts; train probe with d = (single-layer DoM) + 28 λᵢ scaling factors, FastFood projection. Compare AUROC head-to-head against single-layer L19 DoM (0.7731). If significantly higher, F-9 redundancy claim breaks. ~1h CPU.
- **Per-difficulty d₉₀ on MATH-500.** MATH-500 has level-1…level-5 difficulty metadata. Compute d₉₀ of correctness probe within each difficulty bucket. If d₉₀(level-5) >> d₉₀(level-1), corroborates Aghajanyan's task-difficulty ordering and provides external evidence for H-9. ~1h CPU.

## Cross-paper signals

- **1804.08838** — already in graph (status: pending_triage). The original "Measuring the Intrinsic Dimension of Objective Landscapes" (Li, Farkhoor, Liu, Yosinski 2018) — direct methodological predecessor; SAID is built on top of it. If we cite 2012.13255 we should also pull through their full citation chain. Recommend coupling triage of these two papers since they share the FastFood projection apparatus.
- **2008.03156** — NOT in graph; recommend admission. Aghajanyan, Shrivastava, Gupta, Goyal, Zettlemoyer, Gupta 2020, "Better fine-tuning by reducing representational collapse" — companion paper from the same author cluster; introduces R3F/R4F regularization that may bear on H-10 (breathing changes after fine-tuning) and on the asymmetric-collapse picture (F-4).
- **1802.05296** — NOT in graph; recommend admission. Arora, Ge, Neyshabur, Zhang 2018, "Stronger generalization bounds for deep nets via a compression approach" — the underlying compression-bound machinery used by Aghajanyan. Relevant if the project ever writes generalization-bound claims for prefill-DoM selective prediction.

## Proposed FutureExperiments

```yaml
- id: P11-FE73
  pathway: P11
  description: "Aghajanyan-style d_90 for the L19 prefill correctness probe. Project cached Qwen-1.5B Stage-2 L19 prefill activations into random k-dim FastFood subspaces (k in {1, 5, 10, 50, 200, 1000}), train OOF 5-fold logistic correctness classifier per k, compute AUROC(k). Identify smallest k achieving 0.9 * 0.7731 = 0.696 (the empirical d_90 of correctness in prefill space). Compare against F-2's single-direction headline."
  rationale: "Aghajanyan claims tasks need d_90 in the hundreds. F-2 claims one direction is enough. Direct head-to-head — refutation #1 of the brief."
  trigger: "2012.13255 — strong external prediction that single-direction probes leave signal on the table."
  status: READY
  priority: HIGH
  roi: 8
  cost: "30min CPU on cached Stage 2 NPZs"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2012.13255"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE74
  pathway: P11
  description: "SAID-style structure-aware multi-layer correctness probe on Qwen-1.5B. Train probe with d_total = single-layer-DoM dimension plus 28 per-layer scaling parameters lambda_i (one per residual stream layer), use FastFood random projection over the concatenated all-layer activation vector. Compare AUROC head-to-head against single-layer L19 DoM (0.7731). Significant lift refutes F-9's redundancy claim."
  rationale: "SAID > DID in every cell of Aghajanyan Table 1 (e.g., RoBERTa-Large MRPC: 207 vs 322). F-9 says multi-layer CoE features are redundant with single-layer DoM — the structural inverse of SAID's premise. Refutation #2 of the brief."
  trigger: "2012.13255 — quantitative gap between SAID and DID across all tested model x task cells."
  status: READY
  priority: HIGH
  roi: 8
  cost: "1h CPU on cached multi-layer activations"
  depends-on: [F-9, F-2]
  would-update: [F-9]
  triggered-by: ["2012.13255"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE75
  pathway: P11
  description: "Per-difficulty d_90 on MATH-500. Use MATH-500 level-1..level-5 metadata, compute Aghajanyan-style d_90 of the L19 prefill correctness probe within each difficulty bucket. If d_90(level-5) >> d_90(level-1), corroborates the task-difficulty-vs-intrinsic-d ordering Aghajanyan documented (Yelp << ANLI) and provides external evidence for H-9."
  rationale: "Aghajanyan Figure 2 shows Yelp Polarity has lower d_90 than ANLI across all pretraining checkpoints. H-9 (breathing predicts difficulty) is the per-problem version of this. Cheap external corroboration test."
  trigger: "2012.13255 — quantitative cross-task d_90 ordering tied to difficulty."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "1h CPU on cached Stage 2 NPZs + MATH-500 level metadata"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2012.13255"]
  blocked-by: null
  blocked-by-experiment: []
```

```yaml
- id: P11-FE76
  pathway: P11
  description: "Cross-scale d_90 for MATH-500 correctness probe. Compute d_90 on Qwen-1.5B and Qwen-7B (and Phi-3 / Llama if cached) using identical random-subspace + logistic protocol. Aghajanyan predicts inverse correlation with parameter count. F-1 (breathing universality) implies stable d_90. If d_90(7B) << d_90(1.5B), F-1's universality framing masks scale-dependent compression."
  rationale: "Aghajanyan §5.2 + Figure 3 establish strong inverse param-count vs d_90 trend across 8 model families. Refutation #3 of the brief."
  trigger: "2012.13255 — predicts scale-monotone compression that F-1's PR-amplitude metric is too coarse to detect."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "2h CPU on cached cross-model activations"
  depends-on: [F-1, F-2]
  would-update: [F-1]
  triggered-by: ["2012.13255"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-91: Aghajanyan d₉₀ for the MATH-500 correctness probe is in the hundreds, and a single L19 DoM direction lies at the floor of a much larger task-relevant subspace
**Priority:** HIGH
**Motivated by:** 2012.13255 + F-2 + F-9
**Test:** Project cached Qwen-1.5B L19 prefill activations into FastFood random subspaces of dimension k ∈ {1, 5, 10, 50, 200, 1000}, train OOF 5-fold logistic correctness classifier per k, plot AUROC(k). Aghajanyan-style threshold: smallest k achieving 0.9 × 0.7731 = 0.696. P11-FE73. ~30min CPU.
**Requires:** CPU only; cached `pathway11_h100/qwen_1_5b/stage_2_features.npz` L19 prefill activations + MATH-500 K=1 correctness labels.
**Would change:** Confirms → F-2's single-direction framing is empirically at-ceiling for the linear-probe family; reframe headline as "rank-1 sufficient" rather than "DoM is the only direction." Rejects → multi-direction probes climb significantly above 0.77 by k≈200, F-2's headline understates achievable selective-prediction accuracy and invalidates F-9's redundancy framing.
**Blocks:** Nothing.

### H-92: A SAID-style structure-aware multi-layer probe beats single-layer L19 DoM by ≥ 0.02 AUROC, refuting F-9
**Priority:** HIGH
**Motivated by:** 2012.13255 + F-9 + F-3
**Test:** Train probe with d_total = single-layer-DoM dim + 28 λᵢ scaling factors over all-layer cached activations, FastFood projection. Compare AUROC against L19 DoM (0.7731). P11-FE74. ~1h CPU.
**Requires:** CPU only; multi-layer (L0…L27) cached activations.
**Would change:** Confirms → F-9's redundancy claim is wrong; layer structure carries non-redundant correctness signal; CoE-60 result needs reinterpretation. Rejects → SAID's structure-aware advantage doesn't transfer to inference-time probing in autoregressive math models.
**Blocks:** P11-FE74's interpretation; partial overlap with H-13 (head-level attribution).

### H-93: d₉₀ of the correctness probe is monotonically lower for Qwen-7B than Qwen-1.5B at fixed labels
**Priority:** MEDIUM
**Motivated by:** 2012.13255 + F-1
**Test:** Run cross-model d₉₀ protocol on Qwen-1.5B and 7B (and any cached Phi-3/Llama). P11-FE76. ~2h CPU.
**Requires:** CPU only; cached cross-model activations from P11.
**Would change:** Confirms → F-1's "universality" framing is masking scale-dependent compression and the PR-magnitude metric is too coarse; the breathing magnitude story should be re-narrated as "amplitude is universal, but task-relevant subspace shrinks with scale." Rejects → universality claim survives a strong external prediction; strengthens F-1.
**Blocks:** Nothing.

## Proposed PAPER_INDEX.md classification

## 2012.13255 — Intrinsic Dimensionality Explains the Effectiveness of Language Model Fine-Tuning (Aghajanyan, Zettlemoyer, Gupta, 2020)

**Relevance:** Foundational pre-LoRA paper introducing SAID/DID — measures intrinsic dimension d₉₀ of fine-tuning by FastFood random projection of trainable parameters into a low-dim subspace, finding d₉₀ ≈ 207 for RoBERTa-Large MRPC and a strong inverse correlation between parameter count and d₉₀ across 8 model families. Provides the conceptual scaffolding for understanding F-2 (prefill DoM AUROC 0.7731) as a *rank-1 special case* of intrinsic-subspace probing, and gives quantitative external predictions against F-1 (breathing universality across scales) and F-9 (CoE-60 redundancy with single-layer DoM). Their "easier task → lower d₉₀" finding (Yelp << ANLI across all pretraining checkpoints) is the inter-task analog of H-9 (breathing predicts per-problem difficulty).

**Key claim we tested:** Common NLP tasks have intrinsic dimension d₉₀ in the hundreds, structure-aware (SAID) projections measurably reduce d₉₀ below structure-unaware (DID) projections, and pretraining monotonically reduces d₉₀ over the average task without ever seeing downstream data.

**Our result:** **TO TEST.** None of the experiments we'd run to test this paper's predictions against F-1/F-2/F-9 have been executed. Cached P11 NPZs make the head-to-head tests cheap (30min–2h CPU each) and they would directly stress three findings simultaneously.

**Related experiments:** P11-FE73, P11-FE74, P11-FE75, P11-FE76 (proposed); H-91, H-92, H-93 (proposed).

**Status:** TO TEST

## New claims

- The brief introduces the prediction that *Aghajanyan d₉₀ for MATH-500 correctness probe ≈ hundreds*; this is a referenced external claim, not a novel project number, and does not need a `validate_claims.py` entry until P11-FE73 produces a measured d₉₀.
- If P11-FE74 lands a SAID-style probe AUROC, that AUROC will need a new `Claim` entry. Not blocking now.

## Sources consulted

- arxiv 2012.13255 full text (via `mcp__arxiv__read_paper` after `mcp__arxiv__download_paper` triggered conversion) — primary paper read.
- ar5iv HTML mirror (`https://ar5iv.labs.arxiv.org/html/2012.13255`) via WebFetch — additional cross-check on Table 1 and Figure 2/3 numbers.
- `~/topo-confidence/CLAUDE.md` §"Headline numbers" — required default context.
- `~/topo-confidence/FINDINGS.md` + `HYPOTHESES.md` headers via grep — required default context.
- `~/topo-confidence/PAPER_INDEX.md` headers — required default context.
- `python query.py paper 2012.13255` — prior admission note (foundational 2020, relevance to F-1/H-10).
- `python query.py paper 1804.08838` — confirmed in graph as pending_triage (Li et al. 2018, predecessor to SAID).
- `python query.py paper 2008.03156` and `1802.05296` — confirmed NOT in graph; recommend admission.
- `python query.py future P11` — to choose non-colliding P11-FE id (chose 73–76, well above current high ~20).
- `wc -l` and `grep -c "^### H-"` on HYPOTHESES.md — to choose non-colliding H-N number (chose 91–93, well above current count of 23).
