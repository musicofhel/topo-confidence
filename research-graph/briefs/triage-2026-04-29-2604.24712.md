# Triage brief — 2604.24712 — When Prompt Under-Specification Improves Code Correctness

Source: https://arxiv.org/abs/2604.24712
Triaged: 2026-04-29

## Refutations

1. **F-2 (Prefill L19 DoM AUROC 0.7731 from prompt alone) may be measuring
   prompt-surface features rather than problem-difficulty features.**
   *Mechanism:* The paper shows that on prompts with multi-layered structure
   (LiveCodeBench), Pass@1 changes by ~30-50 problems per model when only
   docstring vocabulary or identifier names are paraphrased — same problem,
   same gold solution, different prompt → different correctness. If prefill
   activations change as much as the answer does under such mutations, then
   the 0.7731 AUROC is partially an artefact of which surface features the
   prompt happens to contain. The 1.5B Qwen MATH-500 prompts are themselves
   relatively rich (problem + LaTeX + worked-example template + "Please
   reason step by step. Put your answer within \boxed{}"), so they sit
   closer to LCB than to HumanEval on the structure axis — meaning F-2's
   AUROC could be driven by which template tokens fire, not by the
   problem.
   *Falsifiable test:* Apply LV-style paraphrases to the 500 MATH-500
   prompts (rename variables, broaden vocabulary, swap problem-statement
   verbs). Re-extract prefill L19 activations on Qwen-2.5-1.5B. Re-fit the
   5-fold OOF DoM probe. If AUROC drops more than 0.04 (i.e. below
   ~0.733), F-2 is at least partly prompt-feature-encoded and the "prompt
   alone predicts correctness" framing in F-2 needs to be rewritten as
   "prompt-encoding-of-features predicts correctness conditional on a
   fixed phrasing template."

2. **F-7 (D-bucket has a distinctive *collective* geometric signature, K=1
   right / K=8 wrong) may dissolve when D-bucket prompts are mutated.**
   *Mechanism:* Table 8 of the paper shows that for 35% of LCB F→P US
   flips, a numeric/categorical bound in the constraint section
   "anchored a wrong algorithm" — removing the bound let the model fall
   back to a correct general algorithm (Figure 1: Ci ≤ 10⁹ activated
   star-graph diameter formula instead of two-BFS). This is the
   prompt-time analog of the D-bucket "K=1 lucks into right, K=8
   averages onto memorized-but-wrong" pathology in F-7 — same
   memorization mechanism, different time axis. If F-7 is really
   memorization-driven, applying LV/US-style mutations to D-bucket
   problems should recover them at a substantially higher rate than
   for A-bucket controls. If recovery rates are equal, F-7's
   memorization framing is wrong and the geometric signature reflects
   something else (e.g. residual-stream noise floor).
   *Falsifiable test:* Apply LV mutations to all 36 D-bucket and a
   matched 36-problem A-bucket sample. Re-sample K=8 on Qwen-2.5-1.5B.
   Count D→{A,B} migrations vs A→{D} migrations. If D→{A,B} ≥ 12
   (>33%) AND A→D ≤ 4 (<11%), F-7 is mechanistically explained as
   memorized-retrieval pathology. If migration is roughly symmetric
   across buckets, F-7's pathology is not retrieval-cue-driven.

3. **H-5 (Prefill signal encodes problem familiarity vs decomposability,
   "familiar = good signal") is directly contradicted by the paper's
   retrieval-cue mechanism.**
   *Mechanism:* The paper shows that *higher* prompt familiarity
   (domain-specific vocabulary like "currency"/"countries", recognizable
   identifiers, idiomatic constraint phrasings) systematically *hurts*
   correctness for at least 47 LCB problems by activating memorized but
   wrong solution templates. The implicit "familiar prompt → confident
   prefill → correct answer" reading of H-5 is exactly the failure mode
   the paper documents. If H-5 is right in the form currently in the
   doc, removing familiarity cues from MATH-500 prompts should reduce
   the prefill DoM signal and degrade correctness. The paper predicts
   the opposite for at least a subset.
   *Falsifiable test:* Identify a "familiar-cued" subset of MATH-500
   problems (named theorems, named geometric figures, idiomatic
   competition wording — manually annotate ~50). Apply LV
   neutralizations ("Pythagorean" → "right-triangle", "AMC"-style
   wording → generic). Compare K=1 accuracy and prefill DoM score on
   this subset before/after. If K=1 accuracy goes UP and DoM score goes
   DOWN, H-5 is refuted in its current direction — DoM is reading a
   memorization-confidence signal that is anticorrelated with problem
   correctness for cued problems.

(Three substantive refutations identified — sufficient.)

## Direct connections to F-N / H-N

- **F-7 (D-bucket pathology, prefill PR 14.49 group / 12.84 per-problem)** —
  the paper's Table 8 taxonomy (constraint-anchored wrong algorithm,
  I/O-format-primed parsing, bound-pushed buggy optimisation) provides a
  testable mechanism catalog for *what kind* of memorization D-bucket
  problems are stuck on. Currently F-7 documents the geometry of the
  pathology but is silent on cause. If the project ever ports the
  mutation methodology to MATH-500, the same taxonomy lets us classify
  each recovered D-bucket problem by which cue was driving the wrong
  K=8 majority.

- **F-2 (prefill DoM AUROC 0.7731)** — the paper's HumanEval attention
  finding (60–86% of last-layer last-token attention concentrated on
  the description) suggests that DoM at prefill is reading whatever
  region of the prompt attention has collapsed onto. If MATH-500
  prompts also concentrate attention on a single region (the problem
  text), DoM = "summary of that region" rather than "summary of the
  problem". Already partially anticipated by P11-FE21 (D-bucket
  attention entropy).

- **H-21 (D-bucket has narrower prefill attention than A-bucket)** —
  this paper is the one currently cited as motivating H-21. The
  triage adds: the paper's quantitative anchor is 60–86% on
  HumanEval description vs 30–50% maximum on any LCB region, so
  H-21's predicted entropy gap should be of comparable order
  (factor ~2×) on MATH-500 if the analogy holds.

- **F-8 (Selective prediction acc 71.6% at coverage 0.5, K=2.5 avg)** —
  selective prediction inherits any prompt-feature confound in F-2.
  If mutating prompts flips correctness for ~30 of 500 MATH-500
  problems (paper's LCB rate scaled), the routing decisions at the
  coverage-0.5 threshold are unstable to phrasing — a 71.6% headline
  could shift by several points under paraphrase.

## Methodologies extracted

- **3-class prompt mutation taxonomy (LV, US, SF).** Lexical Vagueness =
  paraphrase + identifier renaming, Under-Specification = single
  constraint deletion, Syntax/Formatting = surface noise.
  *Replication cost:* 30min CPU per benchmark using gpt-5-mini Batch
  API as the mutator, plus an LLM judge pass (Qwen2.5-Coder-32B
  greedy fp16) for compliance/naturalness scoring.
  *We'd plausibly run this:* yes — directly enables Refutations 1, 2,
  3 above. The LV class is the cheapest and most generalizable to
  MATH-500.

- **F→P / P→F transition counting.** Per-example flip analysis between
  original and mutated prompt; the F→P/P→F ratio cleanly separates
  "no signal" from "two opposing forces cancel."
  *Replication cost:* trivial (binary correctness arrays, set ops).
  *We'd plausibly run this:* yes — directly applicable to A↔D bucket
  migration analysis under prompt mutation.

- **Last-layer last-token attention distribution across labelled prompt
  regions.** Paper's Figure 3 averages across all attention heads and
  reports per-region mass. Same recipe applies to MATH-500 prefill if
  we segment the prompt into [problem-text, instruction-suffix,
  worked-example-prefix, latex-template].
  *Replication cost:* small GPU, ~30min on cached Stage 2 attention if
  saved, else ~1h re-extraction.
  *We'd plausibly run this:* yes — overlaps with P11-FE21 but at a
  finer (per-region, not just entropy) granularity.

- **Edit-distance binning of perturbations.** Quintile-bin mutated
  prompts by Levenshtein distance to original; check whether the
  drop scales with edit distance. Paper finds NO correlation on
  HumanEval LV → "memorized surface form dependence rather than
  noise sensitivity." Cleanly distinguishes structural from
  perturbation-magnitude effects.
  *Replication cost:* trivial post-hoc analysis.
  *We'd plausibly run this:* yes, as a free control on any
  mutation-on-MATH-500 experiment.

- **Test coverage as flip-validity control.** F→P solutions are
  validated by checking line+branch coverage matches P→F solutions
  (91.2% / 83.4% vs 91.9% / 84.5%), ruling out "passing only because
  the test happens to be lax." Direct correctness analog for
  MATH-500 doesn't apply (single boxed answer), but the spirit
  carries: validate F→P answers by independent verifier prompt.
  *Replication cost:* trivial.
  *We'd plausibly run this:* yes if the migration test in
  Refutation 2 produces F→P flips.

- **Δname / Δvocab decomposition (their Table 3).** Separately measure
  effect of identifier-only renaming vs full docstring paraphrase to
  attribute degradation. Δname dominates for ≤7B models — function
  signatures ARE the retrieval cue, not the prose. For MATH-500
  there are no function signatures, but the analog could be:
  Δquestion-text vs Δinstruction-template.
  *Replication cost:* 2× the basic mutation experiment.
  *We'd plausibly run this:* yes — clean attribution between
  problem-text vs prompt-template effects on prefill DoM.

## Approaches & framings

- **"Robustness is an artefact of prompt structure, not a model
  property."** This reframes H-5 (prefill encodes
  familiarity/decomposability) — robustness should be tested
  conditional on prompt-structure regime, not as a fixed model
  attribute. Implies our prefill DoM AUROC numbers (F-2) are
  template-conditional and should be reported with the prompt
  template explicit.

- **"Specification completeness ≠ prompt quality."** Counterintuitive
  framing that bears on how we'd interpret a "C_exact verifier"
  signal (P11-FE19) or any "give the model more context" intervention
  — adding context can hurt if it surfaces a memorized-wrong cue.
  Adjusts our priors when designing prompt-augmenting interventions.

- **"Retrieval-cue sensitivity is a generalization gap that scale
  alone does not resolve."** They observe Claude-Sonnet-4 and
  GPT-5-mini both lose ~9.7% under US — same as 7B opensource. Maps
  onto our F-7 "D-bucket exists across scales" intuition: if
  D-bucket is retrieval-cue pathology, it should not vanish at 7B
  (matches their data) and should not vanish at 70B either.

- **"Two opposing forces cancel at aggregate."** The mathematical
  framing — that a near-zero net effect can mask large equal-and-
  opposite flips — is a useful warning when reporting any
  intervention's net Pass@1 delta on MATH-500. We should always
  decompose into F→P / P→F at minimum.

## Datasets & benchmarks

- **HumanEval** — 164 Python tasks, public, MIT-style license,
  HuggingFace `openai/openai_humaneval`. Applicable? no — not a
  reasoning benchmark; outside our hidden-state pipeline target.

- **LiveCodeBench** — 1055 sampled problems, public, contamination-free
  by design (timestamped competition problems), HuggingFace
  `livecodebench/code_generation_lite`. Applicable? no for current
  hidden-state work (we are MATH-500-locked); yes as a future test
  bed if breathing/DoM is ever ported to code generation (would
  validate H-3 with a memorization-controlled benchmark).

- **MBPP** — mentioned, not used. Skip.

- **Replication package: github.com/Amal-AK/PromptAnalysis** — full
  mutation prompts and judge prompts. Applicable? yes — gives us a
  drop-in mutation generator we could point at MATH-500 with minor
  prompt rewrites.

## Implementation details worth capturing

- Mutation generator: **gpt-5-mini via OpenAI Batch API**, structured
  prompt per mutation type. They iterate the mutation prompt until
  judge compliance ≥ 85%.
- Judge: **Qwen2.5-Coder-32B-Instruct** in fp16 greedy, binary JSON
  output (`{"score": 0|1}`). Architecturally distinct from the
  generator to limit collusion (cites the LLM-as-judge paper).
- Compliance/naturalness as separate axes; manual sample of 100
  mutants confirms 97% / 86% agreement with the judge — suggests we
  could trust the LLM-judge protocol for our own MATH-500 mutation
  experiments.
- Open-source eval on **A100 fp16 greedy decoding (T=0)** for
  reproducibility, 20-second timeout per solution.
- Code execution in isolated processes — relevant only if we ever do
  code-gen experiments.
- Anonymous replication URL: https://anonymous.4open.science/r/PromptAnalysis/

## Replicable intermediates

- **D-bucket prompt-recovery sanity check (cheap, ~2-3h on a small
  GPU).** Take the 36 D-bucket problems from
  `pathway11_h100/prefill_gated_compute/results.json`, rename
  variables and paraphrase the question text manually (or via a
  GPT-5-mini batch call mirroring the paper's LV recipe), re-run
  Qwen-2.5-1.5B at K=8, count how many D→{A,B} migrations occur.
  This is the smallest concrete check of Refutation 2.

- **Prefill DoM AUROC stability under paraphrase (cheap, ~1h on
  cached pipeline).** Generate one LV-style paraphrase per MATH-500
  problem, re-extract L19 prefill activations, re-fit the 5-fold
  OOF probe, compare AUROC against 0.7731. Direct check of
  Refutation 1.

- **Last-layer attention region map on MATH-500 (small GPU, ~30min
  if Stage 2 saved attention; otherwise ~1h re-extraction).**
  Replicates paper's Figure 3 recipe on MATH-500 prompts segmented
  into [problem-text, instruction-suffix, worked-example,
  latex-template]. Adds a region-resolved baseline that P11-FE21
  (entropy-only) doesn't capture.

## Cross-paper signals

- Carlini et al. 2021 USENIX Security "Extracting Training Data from
  LLMs" — not arxiv-IDed in our normal channel; cited by the paper
  as the foundational memorization reference. Indirectly relevant
  to F-7-as-memorization but outside our admission scope.
- All other cited papers (HumanEval 2107.03374, LiveCodeBench
  2403.07974, DeepSeek-Coder 2401.14196, Qwen2.5-Coder 2409.12186,
  Code Llama 2308.12950, StarCoder2 2402.19173, BigCodeBench
  2406.15877, EvalPlus 2305.01210, SpecFix 2505.07270, Larbi
  2507.20439, Yang underspec 2505.13360, Sclar formatting ICLR'24,
  PromptBench 2306.04528) are SE/code-generation works without
  hidden-state instrumentation — none worth flagging for admission
  on relevance grounds.

- **none worth flagging** for admission to research-graph beyond
  what's already there.

## Proposed FutureExperiments

```yaml
- id: P11-FE71
  pathway: P11
  description: "LV-style paraphrase of MATH-500 prompts; re-extract Qwen-2.5-1.5B L19 prefill activations; re-fit 5-fold OOF DoM probe. Compare AUROC against the 0.7731 baseline. If the AUROC drop exceeds 0.04, F-2 is partly prompt-surface-encoded and its framing must be conditional on a fixed template."
  rationale: "Refutation 1 of brief 2604.24712. Paper shows code-generation correctness flips for ~5-10% of LCB problems under LV mutation alone. If the same is true on MATH-500, F-2's AUROC is template-conditional and its 'prompt alone predicts correctness' headline is a partial artefact of which surface tokens fire."
  trigger: "Akli et al. 2604.24712 finding that LV mutations (vocabulary + identifier renaming) alone produce F→P/P→F flip ratios of 0.99 on LCB across 10 models, indicating prompt-surface tokens drive a substantial share of correctness variance."
  status: READY
  priority: HIGH
  roi: 8
  cost: "1-2h H100 (re-extract prefill on paraphrased prompts) + 20min CPU (probe fit)"
  depends-on: [F-2]
  would-update: [F-2, F-8]
  triggered-by: ["2604.24712"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE72
  pathway: P11
  description: "Apply LV/US mutations to the 36 D-bucket and a matched 36-problem A-bucket sample on MATH-500. Re-sample K=8 on Qwen-2.5-1.5B. Count D→{A,B} bucket migrations and A→D migrations. If D→{A,B} ≥ 12 (>33%) and A→D ≤ 4 (<11%), F-7 is mechanistically explained as memorized-retrieval pathology a la Akli et al. Table 8."
  rationale: "Refutation 2 of brief 2604.24712. F-7 documents the geometry of D-bucket pathology but is silent on cause. The paper's Table 8 shows constraint-anchored wrong algorithms drive 35% of LCB US flips — the exact memorized-retrieval mechanism F-7 is silently consistent with. This experiment converts that silence into a falsifiable prediction."
  trigger: "Akli et al. 2604.24712 Table 8: 35% of US-induced F→P flips on LCB are 'constraint anchored wrong algorithm' — a numeric or categorical bound activates a memorized shortcut that misfits the problem; removing the cue lets the model fall back to a correct general algorithm. Direct prompt-time analog of D-bucket K=1-right / K=8-wrong (F-7)."
  status: READY
  priority: HIGH
  roi: 9
  cost: "3-4h H100 (LV mutation generation via gpt-5-mini batch + K=8 re-sampling on 72 problems) + 30min CPU"
  depends-on: [F-7]
  would-update: [F-7, F-1]
  triggered-by: ["2604.24712"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE73
  pathway: P11
  description: "Replicate Akli et al. Figure 3 attention-region map on MATH-500: segment each prefill prompt into [problem-text, instruction-suffix, worked-example-prefix, latex-template]; compute mean last-layer last-token attention mass per region averaged across heads, separately for the 36 D-bucket and 207 A-bucket problems. Direct test of whether D-bucket concentrates attention on the problem-text region in the same 60-86% range the paper reports for HumanEval."
  rationale: "Extends P11-FE21 (entropy only) to a region-resolved attention map matching the paper's exact protocol. The paper's quantitative anchor (HumanEval 60-86% on description, LCB 30-50% maximum on any region) gives a concrete prediction: D-bucket should look like HumanEval, A-bucket like LCB. If D-bucket attention is uniformly distributed across the four regions, F-7 is not an attention-concentration phenomenon."
  trigger: "Akli et al. 2604.24712 Figure 3 region-mass attention analysis showing structural-redundancy benchmarks have decentralized attention (30-50% per region) while minimal-spec benchmarks have concentrated attention (60-86% on description)."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "1h H100 (re-extract prefill attention with regional masks) + 30min CPU analysis"
  depends-on: [F-7]
  would-update: [F-7]
  triggered-by: ["2604.24712"]
  blocked-by: null
  blocked-by-experiment: ["P11-FE21"]
```

## Proposed HYPOTHESES.md additions

### H-71: Prefill L19 DoM is template-stable but content-unstable
**Priority:** HIGH
**Motivated by:** 2604.24712 + F-2
**Test:** LV-paraphrase MATH-500 prompts (variable rename + verb broadening, keeping the instruction suffix and worked-example template fixed). Re-extract Qwen-2.5-1.5B L19 prefill activations. Re-fit 5-fold OOF DoM probe. Measure (a) AUROC on paraphrased prompts vs original 0.7731, (b) cosine similarity between original and paraphrased DoM directions. Estimated time: 2h H100 + 20min CPU.
**Requires:** H100 (re-extraction with new prompts), cached MATH-500 problem text, gpt-5-mini API for paraphrase generation.
**Would change:** If AUROC drops > 0.04 OR cos(orig DoM, paraphrased DoM) < 0.7, F-2's framing flips from "prefill encodes problem difficulty" to "prefill encodes prompt-surface features that correlate with difficulty under a fixed template." If both metrics are stable, F-2 is robustified.
**Blocks:** Decision on whether F-8 selective-prediction headline (71.6%) needs a paraphrase-stability disclaimer.

### H-72: D-bucket pathology is memorized-retrieval-cue pathology
**Priority:** HIGH
**Motivated by:** 2604.24712 + F-7
**Test:** Apply LV/US mutations to the 36 D-bucket problems and a matched-difficulty 36-problem A-bucket control on MATH-500. Re-sample K=8 on Qwen-2.5-1.5B. Count D→{A,B,C} and A→{B,C,D} bucket migrations. Manually classify each F→P (D→A or D→B) flip according to the Akli et al. Table 8 taxonomy (constraint-anchored wrong algorithm / I/O-format-primed parsing / bound-pushed buggy optimisation / no causal link). Estimated time: 3h H100 + 1h manual annotation.
**Requires:** H100 for re-sampling, LV mutation generator (gpt-5-mini batch), MATH-500 K=8 outputs from `pathway11_h100/prefill_gated_compute/`.
**Would change:** If D→{A,B} migration rate > 33% AND A→D rate < 11% AND > 50% of D→{A,B} flips classify cleanly into Akli's taxonomy, F-7 gets a mechanistic explanation (memorized-retrieval pathology) and predicts D-bucket vanishes for prompts that defeat retrieval. If migration rates are symmetric across buckets, F-7 is something else (sampling noise concentration, intrinsic-dimension floor).
**Blocks:** H-21 (D-bucket attention narrower than A-bucket — H-72 provides the causal upstream test that H-21 measures the consequence of).

### H-73: Familiarity-cue removal *improves* MATH-500 K=1 accuracy on a "named-trigger" subset
**Priority:** MEDIUM
**Motivated by:** 2604.24712 + H-5
**Test:** Manually annotate ~50 MATH-500 problems where the question contains a named-theorem cue (Pythagorean, Fermat's Little Theorem, AMC-style problem patterns). Apply LV neutralization replacing named cues with generic descriptions. Compare K=1 accuracy on Qwen-2.5-1.5B before vs after. Also extract the prefill DoM score per problem before/after. Estimated time: 2h H100 + 1h manual.
**Requires:** H100, manual annotation, MATH-500 source text.
**Would change:** If K=1 accuracy on the named-cue subset goes UP under neutralization while DoM score goes DOWN, H-5's "familiarity = good signal" framing is refuted in its current direction — DoM is reading a memorization-confidence signal that anticorrelates with correctness on this subset. Refines H-5 to "familiarity = good signal *when the memorized response is correct*; otherwise inverted."
**Blocks:** Whether H-5 should be split into "familiarity-correct" and "familiarity-trapped" sub-hypotheses before any work begins on it.

## Proposed PAPER_INDEX.md classification

## 2604.24712 — When Prompt Under-Specification Improves Code Correctness (Akli et al., 2026)

**Relevance:** Empirical study showing that prompt structure (not model
size or architecture) governs correctness sensitivity to prompt
mutations. The paper's "constraint-anchored wrong algorithm" and
"identifier-anchored memorized retrieval" mechanisms (Table 7, Table
8) are the prompt-time analogs of our D-bucket pathology (F-7) at
sampling time. The paper's attention-region map (Figure 3) is the
direct precursor to P11-FE21 / P11-FE73 attention-entropy work for
distinguishing D-bucket from A-bucket geometry. Most importantly, the
paper's mutation methodology (LV + US + LLM-judge protocol) is a
ready-to-port toolkit for stress-testing whether F-2's prefill DoM
AUROC 0.7731 is template-stable or template-fragile.

**Key claim we tested:** Mutations to prompt surface features
(identifier names, vocabulary, single-constraint deletions) flip ~10%
of LCB Pass@1 outcomes per model with F→P/P→F ≈ 0.99 — meaning a
substantial fraction of "correctness" is prompt-surface-encoded
rather than problem-encoded. Our analog: the prefill L19 DoM signal
(F-2, AUROC 0.7731) and the D-bucket pathology (F-7) may be the
internal-representation reflection of this same retrieval-cue
sensitivity at sampling time.

**Our result:** **TO TEST** (H-71, H-72, H-73; P11-FE71, P11-FE72,
P11-FE73). Three predictions are now concrete: (a) prefill DoM AUROC
should drop ≥ 0.04 under LV paraphrase if F-2 is template-conditional;
(b) D-bucket should preferentially recover under LV/US mutation if
F-7 is memorized-retrieval pathology; (c) attention-region mass
on MATH-500 D-bucket should match the paper's HumanEval 60-86%
description-concentration regime (vs A-bucket distributing across
regions LCB-style). The first two are HIGH priority; the third is
MEDIUM and overlaps with the existing P11-FE21.

**Delta from their setup:** Their unit of analysis is the prompt at
sampling time (T=0 greedy); ours is the K=8 sampling distribution at
fixed prompt. Their benchmark axis is structural-richness
(HumanEval ↔ LCB); we are MATH-500 only, which sits closer to LCB
on prompt-richness. Their model axis is 10 code LLMs across three
scale tiers; we are Qwen-2.5-1.5B / 7B reasoning models. The
mechanism (memorized-retrieval cues hurting correctness) is
hypothesized to be invariant to all three deltas — testing this is
exactly H-72.

**Related experiments:** P11-FE21 (D-bucket attention entropy,
already graphed), P11-FE71 (DoM AUROC under paraphrase, NEW),
P11-FE72 (D-bucket recovery under LV/US, NEW), P11-FE73
(region-resolved attention map, NEW), H-21, H-71, H-72, H-73, F-2,
F-7, H-5.

**Status:** TO TEST (was CITED ONLY motivating H-21; upgraded to TO
TEST after deep triage identified two HIGH-priority falsifiable
predictions — H-71 against F-2 and H-72 against F-7).

## New claims

This brief introduces three new quantitative claims that need
`validate_claims.py` entries before promotion:

- **H-71 acceptance threshold:** "Prefill L19 DoM AUROC drops by ≤
  0.04 (i.e. stays ≥ 0.733) under LV paraphrase" — backed by
  P11-FE71 result JSON when run.
- **H-72 acceptance threshold:** "D→{A,B} migration rate ≥ 33% AND
  A→D rate ≤ 11% under LV mutation on Qwen-2.5-1.5B MATH-500
  sampling at K=8" — backed by P11-FE72 result JSON when run.
- **H-73 acceptance threshold:** "K=1 accuracy on the named-cue
  subset INCREASES by ≥ 2 percentage points under LV neutralization,
  AND the per-problem prefill DoM score DECREASES on average" —
  backed by P11-FE73-adjacent result JSON when run.

All three are forward-looking — they are not numbers we are
asserting in the narrative now, only thresholds the future
experiments will be evaluated against. They become live claims only
when the experiments run.

## Sources consulted

- /tmp/paper_2604.24712.txt (full PDF text via curl + pdftotext) — primary source
- ~/topo-confidence/CLAUDE.md (headlines section only)
- ~/topo-confidence/FINDINGS.md (F-2 and F-7 bodies, on-demand)
- ~/topo-confidence/HYPOTHESES.md (H-21 body + template, on-demand)
- ~/topo-confidence/PAPER_INDEX.md (existing 2604.24712 entry — was CITED ONLY motivating H-21 MEDIUM)
- research-graph `query.py paper 2604.24712` — prior_admission_note
- research-graph `query.py future P11` — existing P11-FE list (highest existing FE: 22; chose 71+ for collision safety)
- research-graph `query.py paper 2505.13360 / 2506.06971 / 2507.20439` — confirmed not in graph
