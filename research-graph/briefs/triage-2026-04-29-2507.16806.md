# Triage brief — 2507.16806 — Beyond Binary Rewards: Training LMs to Reason About Their Uncertainty

Source: https://arxiv.org/abs/2507.16806
Triaged: 2026-04-29

Paper: Damani, Puri, Slocum, Shenfeld, Choshen, Kim, Andreas (MIT + IBM, 22 Jul 2025).
Method: **RLCR** — augments standard RLVR (binary correctness reward via GRPO) with a Brier-score
calibration term so the model jointly learns to produce a verbalized confidence q ∈ [0,1] alongside
its answer. Base model is **Qwen2.5-7B** — the same family/scale as our P11 7B pipeline.
Headline results: on Math (Math500 + GSM8K + Big-Math averaged), RLVR drops ECE from 0.39 → 0.26
relative to base while RLCR goes 0.39 → 0.10, and SFT+RLCR → 0.08, all at ~73% accuracy
(matching the headline 7B 73.2% MATH-500 K=1). The "RLVR+Probe" baseline (linear probe on the
final-layer embedding of the RLVR model) gets Math AUROC = 0.65 / Brier 0.19 / ECE 0.13 — a direct
methodological neighbour of our prefill-L19-DoM probe (AUROC 0.7731 on Qwen-2.5-1.5B base, F-2).

## Refutations

1. **Refutes F-2's framing of prefill L19 DoM as a stable, model-agnostic correctness signal.** F-2
   anchors prefill L19 DoM AUROC=0.7731 on the *base* Qwen-2.5-1.5B and the related 7B base.
   RLCR shows that RL training systematically **moves the calibration-relevant signal off the
   residual stream and into the verbalized analysis CoT** — their post-RLVR linear probe on
   final-layer embeddings of Qwen-2.5-7B gets Math AUROC = 0.65 (Table 1b), well below our base-model
   prefill DoM. Mechanism: GRPO-without-KL backprop reshapes residual geometry to push correctness
   information into emitted tokens (the new gradient-receiving locus); the answer-prefill activation
   loses its monopoly on correctness. **Falsifiable test**: extract prefill L19 activations from
   Qwen-2.5-7B-RLCR (paper appendix C lists prompts and the SFT+RLCR weights are nominally
   reproducible), refit the DoM probe on MATH-500 with our 5-fold protocol, compare AUROC to the
   base-model 0.7731. If it drops below ~0.70, F-2's "prefill DoM is a strong correctness predictor"
   is a property of *base / RLHF* models, not of post-RLVR/RLCR reasoning models.

2. **Refutes F-9's "CoE-60 trajectory features are redundant with single-layer L19 DoM" once you
   condition on a uncertainty-aware CoT.** Figure 5 of the paper shows that the **analysis CoT** —
   the explicit-uncertainty-reasoning span between `<analysis>` and `</analysis>` — carries
   information beyond what a final-layer classifier can extract from a non-uncertainty CoT, and this
   gap *grows* at smaller capacity (0.5B, 1.5B). F-9's redundancy result was measured on
   non-uncertainty-conditioned CoT (P11 vanilla generation). Mechanism: trajectory features along an
   *uncertainty-bearing* CoT span access a different subspace than the answer-prefill DoM. **Test**:
   on a 1.5B RLCR generation, extract per-token L19 activations across the analysis span, fit
   CoE-60 (Pathway 9) over those tokens only, and compare AUROC to L19-DoM on the answer-prefill
   token. If CoE-60 ≥ DoM by ≥ 0.03 AUROC, F-9's redundancy is a property of vanilla CoT, not of
   reasoning more generally.

3. **Refutes H-12 (semantic-entropy probes label-free confidence) as the right cheap alternative
   to supervised DoM.** H-12 hypothesises SE-probe-style label-free signals replace prefill DoM.
   RLCR provides a *different* alternative — a label-cheap (only needs binary correctness)
   training-time procedure that yields verbalized confidences with HotpotQA AUROC 0.69 / Math AUROC
   0.67 (RLCR) and 0.78 (SFT+RLCR), beating both `RLVR+Probe` (0.55/0.65) and the 7B-classifier
   `RLVR+BCE` (0.66/0.78). The right framing for "label-cheap calibration" may be RL-with-
   proper-scoring, not SE-probes. **Test**: in a small-scale Qwen-2.5-1.5B RLCR run on MATH-500
   subset, compare RLCR verbalized AUROC against semantic-entropy probe AUROC on the same
   problems. If RLCR ≥ SE-probe by AUROC ≥ 0.05 at comparable cost, H-12's framing is the wrong
   cheap-alternative pick.

4. **Refutes F-3's "prefill and final-token DoM are orthogonal (cos=0.046)" as a model-agnostic
   geometric claim.** F-3 measures cos(prefill_DoM, final_DoM) = 0.046 on Qwen-2.5-1.5B base.
   RLCR's gradient signal — joint correctness reward + Brier on the trailing `<confidence>`
   token — explicitly couples the answer-token correctness signal to the *final*-token confidence
   logit. By the chain rule, this should drag the two DoMs toward each other across training.
   Mechanism: the same scalar p(correct) is the optimal prediction at both the answer span (where
   correctness is decided) and the confidence span (where it's verbalized), so RL gradient
   alignment pulls both directions toward the calibrated p(correct) axis. **Test**: extract the
   prefill-L19 DoM and final-layer confidence-token DoM from an RLCR-trained Qwen-2.5-7B,
   compute cos. If cos > 0.3 (an order of magnitude above 0.046), F-3 is a property of *base*
   models, not of all reasoning models — significantly weakens its use as a "two distinct
   mechanisms" argument in PERSPECTIVES.

## Direct connections to F-N / H-N

- **F-2 (prefill L19 DoM AUROC 0.7731)** vs paper's `RLVR+Probe` baseline (final-layer linear probe
  on Qwen-2.5-7B, Math AUROC = 0.65, Table 1b). Same family of method (linear probe → confidence),
  different residual-stream depth and pre/post-RL state; gives a calibrated point comparison.
- **F-8 (selective-prediction acc 71.6% at coverage 0.5, K=2.5 avg)** vs the paper's
  confidence-weighted majority vote / max-confidence test-time scaling (Fig 4a, monotone
  improvement with N). RLCR's verbalized confidence is a label-free abstention proxy that
  competes directly with prefill-DoM thresholding at the same coverage.
- **H-1 (per-position DoM steering moves MATH-500 accuracy)** vs RLCR's "use verbalized confidence
  as test-time selector" — both ask whether the model's internal/external confidence signal can be
  *acted on* to lift accuracy without retraining. RLCR shows this works via voting weights;
  H-1 conjectures it works via residual-stream intervention.
- **H-6 (CoE-60 still beats single-layer DoM at 1024-tok labels)** vs paper Fig 5
  analysis-classifier-vs-baseline gap at small scales — supports the "reasoning span carries
  information beyond a single-token probe" intuition.
- **H-12 (semantic-entropy probes label-free)** — paper offers a competing label-cheap method;
  shifts the cheap-alternative comparison set.
- **MATH-500 K=1 7B = 73.2% (CLAUDE.md headline)** ≈ paper Math accuracy 72.7% (RLCR) / 72.9%
  (RLVR) on Qwen-2.5-7B trained on Big-Math. Independent corroboration that 7B-base on MATH-500
  sits near 73% with K=1 / Pass@1 evaluation (modulo their 4096-tok max-response which is more
  generous than our 1024).
- **F-3 (cos(prefill_DoM, final_DoM) = 0.046 on 1.5B base)** vs RLCR's joint-objective coupling of
  the answer-correctness signal to the trailing confidence token — strong mechanistic prediction
  that this orthogonality breaks under RLCR training.

## Methodologies extracted

- **RLCR (joint correctness + Brier reward via GRPO)** — augment binary RLVR reward with
  −(q − 1y≡y∗)² where q is the model-emitted confidence within `<confidence>` tags.
  *Replication cost*: ~1 week H100 + working GRPO/Open-Reasoner-Zero infra + 15K Big-Math problems.
  *We'd plausibly run this*: no — too expensive and orthogonal to current pathway focus, but the
  released checkpoint (if any) is worth using as a probe target.

- **RLVR + Linear Probe baseline on final-layer embedding** — train a linear probe σ(θᵀφ(x,y)) on
  RLVR-tuned model embeddings to predict 1y≡y∗ (BCE loss).
  *Replication cost*: 30 min CPU on cached final-token activations.
  *We'd plausibly run this*: yes — exactly mirrors our DoM probe, gives a direct cross-method
  comparison anchor.

- **RLVR + BCE / Brier Classifier baseline** — fine-tune a separate Qwen-2.5-7B (BCE or MSE loss)
  to predict correctness from (question, RLVR-CoT) pairs.
  *Replication cost*: 4h H100 (smaller scale possible on 1.5B at 30 min).
  *We'd plausibly run this*: maybe — useful as a "supervised upper bound" anchor for F-2 / H-12,
  but two-model overhead is significant.

- **Confidence-weighted majority vote (CWMV)** — at test time, ensemble N samples and weight each
  vote by the model's verbalized confidence q.
  *Replication cost*: 2h CPU on cached K=8 generations + per-sample DoM scores.
  *We'd plausibly run this*: yes — applies directly to our DoM scores as an alternative to F-8's
  threshold-based abstention; cheap.

- **Analysis-CoT ensemble (resample K analysis chains for fixed answer, average q)** — fix the
  answer, sample K different uncertainty CoTs, average their confidences. Brier improves with K
  (Fig 4b).
  *Replication cost*: needs an RLCR model we don't have — could approximate by sampling K CoT
  rollouts from the base model and averaging per-step DoM scores, ~2h H100.
  *We'd plausibly run this*: maybe — interesting variance-reduction angle for F-8 selective
  prediction.

- **Brier score + ECE evaluation harness** — group confidences into 10 bins, compute
  |acc(Bm) − conf(Bm)| weighted by bin size.
  *Replication cost*: 20 min CPU, all data cached.
  *We'd plausibly run this*: yes — adds calibration metric to our existing AUROC-only reporting,
  cheap and a strict superset of current evaluation.

- **Inter-solution-confidence-sum diagnostic** — for K answer candidates, sum mean confidence per
  unique answer; should equal 1 in a well-calibrated model.
  *Replication cost*: 30 min CPU on cached K-samples.
  *We'd plausibly run this*: yes — sanity check whether DoM-derived per-sample confidence
  satisfies coherence across self-consistency samples.

- **GRPO without std-division in advantage + BNPO token-loss aggregation** — training-recipe
  details (Turtel 2025; Xiao 2025).
  *Replication cost*: only relevant if we run any RL.
  *We'd plausibly run this*: no — out of scope.

- **DeepSeek-R1 distillation for SFT warmup** — generate uncertainty-analyses with DeepSeek-R1 on
  500 base-model solutions, SFT before RL.
  *Replication cost*: API budget for 500 generations + 30 min H100 SFT.
  *We'd plausibly run this*: no — only useful inside the RLCR loop.

## Approaches & framings

- **Proper-scoring-rule decomposition of the calibration objective.** Reframes "well-calibrated
  reasoning" as `R = correctness − S(q, c)` for any bounded proper scoring rule S; theorem 1 says
  Brier (and any bounded proper rule) jointly maximises accuracy and calibration. *Intersects
  F-8 / H-1*: gives a principled objective for the abstention/selective-prediction lens we're
  pursuing in P11.

- **"Verbalized over probed" framing for label-cheap calibration.** The paper's Fig 5 result that
  analysis-CoT classifiers beat baseline classifiers at smaller scale flips the conventional
  "internal probes > verbalized confidence" intuition (Tian 2023, Kadavath 2022). *Intersects
  H-12 and the broader F-2 narrative*: prefill DoM may be an artefact of base-model geometry that
  RL training erodes in favour of CoT-encoded confidence.

- **"RL training shifts the locus of confidence representation."** The paper's RLVR-degrades-then-
  RLCR-restores pattern (ECE 0.39 → 0.26 → 0.10) implies the residual stream is not a stationary
  substrate for confidence — RL training reshapes it. *Intersects F-2, F-3, F-7*: all of those
  findings were measured on base or pre-RL models; their persistence under RL is now an open
  question.

## Datasets & benchmarks

- **MATH-500** — 500 problems, MIT/CC-style license, HF accessible. **Applicable: yes** — our
  canonical benchmark; paper reports same problem set with same K=1 protocol.
- **Big-Math** (Albalak et al., 2502.17387) — 250K curated math problems for RL training,
  filtered by LLaMA-8B solve-rate ∈ [0,70%] and numerical-answer-only, HF accessible.
  **Applicable: partially** — useful as a difficulty-graded test set even without RL training;
  the LLaMA-8B solve-rate label is a free difficulty proxy we don't currently have.
- **GSM8K** (Cobbe 2021) — ~8K grade-school math problems, HF accessible.
  **Applicable: yes** — already a candidate OOD eval for F-2 / F-8 generalisation; the paper
  reports it as part of "Math averaged" so we'd get a cross-method Brier/ECE anchor.
- **HotpotQA-Modified** (Yang 2018 + paper's modification) — 20K multi-hop QA with
  systematically-removed evidence paragraphs; license CC BY-SA 4.0. **Applicable: no** — outside
  our math-reasoning scope.
- **TriviaQA, SimpleQA, GPQA, CommonsenseQA** — used as OOD evaluations. **Applicable: no** —
  outside math-reasoning pipeline.

## Implementation details worth capturing

- Base model: **Qwen2.5-7B base** (matches our P11 7B). RL initialised directly from base, **no
  KL regularization** (following Hu 2025 / Guo 2025 / DeepSeek-R1 lineage).
- RL algorithm: **GRPO** with two specific deviations: (a) advantage normalisation drops the
  std division (Turtel 2025), (b) token-loss aggregated via BNPO (Xiao 2025, 2506.02864) using
  active-token count.
- Sampling: 32 responses per prompt, T = 0.7, effective batch size 2048.
- Max response length: 1536 (HotpotQA) / **4096 (Math)** — note this is 4× our 1024 ceiling, so
  any direct number comparison must flag truncation differences.
- Format reward: simple bonus for tag-compliant output (`<think>`, `<answer>`, `<analysis>`,
  `<confidence>`).
- ECE: 10 equal-mass bins.
- Verifier: `math-verify` library
  (https://github.com/huggingface/Math-Verify) — robust expression evaluation, used to reduce
  verifier noise in Math.
- SFT warmup: DeepSeek-R1 generates uncertainty analyses on 500 base-model solutions,
  SFT briefly, then RLCR. Gives best in-distribution calibration but **catastrophic forgetting
  on OOD accuracy** (Big-Math 50.9% → 43.8% averaged).
- Probe baseline detail: linear probe on **final-layer** embedding of the *RLVR-tuned* model
  (not the base) — important when interpreting their AUROC=0.65 number.
- No public checkpoint reference given in v1 PDF; would need to email authors or wait for a v2.
- `https://github.com/huggingface/Math-Verify` is a useful drop-in for our own verifier noise
  reduction (we currently use SymPy + heuristics in `pathway11_h100/`).

## Replicable intermediates

- **Compute Brier + ECE on the Qwen-2.5-1.5B base prefill-DoM probe over MATH-500.** Take the
  cached prefill-L19 activations + 5-fold OOF DoM scores from `pathway11_h100/`, sigmoid-calibrate
  the raw projection, compute Brier + 10-bin ECE. Compare against paper Table 1b base
  (Brier 0.40 / ECE 0.39) and RLVR (Brier 0.28 / ECE 0.26). ~30 min CPU.
- **Same for Qwen-2.5-7B base** at the equivalent prefill-DoM layer to anchor a direct
  cross-scale calibration comparison. ~30 min CPU on cached 7B activations.
- **Confidence-weighted majority vote sanity check on F-8 selective-prediction setup.** For our
  K=8 generations on MATH-500, weight each sample's vote by sigmoid(prefill-DoM score), compute
  weighted-vote accuracy at coverage 0.5; compare against the unweighted majority baseline and
  F-8's prefill-DoM-thresholded 71.6% answered accuracy. ~1 h CPU using existing K=8 outputs.
- **Inter-solution coherence diagnostic.** Sum DoM-derived per-sample confidences across unique
  answers in K=8 generations; histogram the per-question total. Flag any deviation from 1.0 as
  a coherence violation analogous to paper Fig 6b. ~30 min CPU.
- **No replication is possible for the RLCR-trained checkpoint claims** — paper has not (as of
  v1) released weights, and 7B GRPO training is out of our budget.

## Cross-paper signals

- **2410.13640** — Chain-of-Embedding (Wang ICLR 2025) — already in graph (status: PARTIALLY
  CONFIRMED / CONTRADICTED, P10). Connects via the "trajectory features beyond final layer"
  framing relevant to refutation #2 (analysis-CoT trajectory).
- **2604.18805** — Ríos-García "AI scientists produce results without reasoning scientifically"
  — already in graph (CITED ONLY, motivated H-15). The RLCR Fig 5 result that analysis-CoTs
  matter at small scale is a positive counterpoint to Ríos-García's "agents over-fit to existing
  frames" failure mode — uncertainty CoT specifically resists that.
- **2509.18116** — Adaptive Layer-wise Steering — already in graph (CITED ONLY). Tangential —
  RLCR doesn't steer per-layer, but the "where in the residual stream does confidence live"
  question is shared.

NOT in graph; recommend admission:

- **2505.05410** — Chen et al., "Reasoning models don't always say what they think" (CoT
  unfaithfulness, Anthropic 2025). Directly relevant to F-2 / F-9: if CoT is unfaithful, then
  CoT-trajectory features may not represent the actual computation — affects how we interpret
  CoE-60 redundancy.
- **2505.14489** — Yoon et al., "Reasoning models better express their confidence". Directly
  relevant to H-12 / F-8: claims slow-thinking improves verbalized calibration. Methodologically
  adjacent to our DoM-based confidence.
- **2506.18183** — Mei et al., "Reasoning about uncertainty: do reasoning models know when they
  don't know?" Directly relevant to F-8 / H-12 selective-prediction story.
- **2503.02623** — Stangel et al., "Rewarding Doubt: RL approach to confidence calibration of
  LLMs" — uses clipped log-loss reward; closest related-work prior to RLCR. Worth admitting as a
  partial-overlap reference for the proper-scoring-rule lineage.
- **2207.05221** — Kadavath et al., "Language models (mostly) know what they know" (P(true) probe).
  Major missing seed — the canonical "internal probe for correctness" paper that anchors F-2's
  intellectual lineage.
- **2304.13734** — Azaria & Mitchell, "The internal state of an LLM knows when it's lying."
  Direct ancestor of F-2 / F-8 — internal-probe-for-truthfulness foundational paper.
- **2502.17387** — Big-Math dataset paper. Worth admitting if we use Big-Math difficulty
  buckets for D-bucket (F-7) cross-validation.
- **2505.23646** — Yao et al., "Are reasoning models more prone to hallucination?" Direct
  motivation for RLCR's framing; would corroborate or contextualise our 1.5B vs 7B
  hallucination-rate observations.
- **2412.14737** — Yang et al., "On verbalized confidence scores for LLMs" — direct background
  for verbalized-vs-probe comparison relevant to H-12.

## Proposed FutureExperiments

```yaml
- id: P11-FE72
  pathway: P11
  description: "Brier+ECE re-evaluation of prefill-L19 DoM probe on Qwen-2.5-1.5B and 7B base models over MATH-500. Sigmoid-calibrate the OOF 5-fold DoM scores, compute 10-bin ECE and Brier on the same problems used for AUROC=0.7731 (1.5B). Compare against paper Table 1b base (ECE 0.39, Brier 0.40) and RLVR (ECE 0.26, Brier 0.28). Anchors F-2's calibration story alongside its discrimination (AUROC) story; if our base-model ECE matches their base ECE within 0.05, we share a calibration regime; if it's much lower, prefill-DoM is a stronger signal than they credit final-layer probes for."
  rationale: "F-2 currently has no calibration metric — only AUROC. RLCR's headline numbers are calibration metrics (Brier, ECE), and the paper's RLVR+Probe baseline is the closest methodological neighbour. Computing Brier/ECE on our existing OOF probe scores costs nothing in compute but adds a third anchor to F-2 and a direct cross-paper comparison."
  trigger: "Paper 2507.16806 reports Brier and ECE for a final-layer linear probe on RLVR-tuned Qwen-2.5-7B; we can cheaply produce the analogous numbers for our prefill-L19 DoM probe on the base model."
  status: READY
  priority: HIGH
  roi: 8
  cost: "30 min CPU on cached 1.5B + 7B prefill activations and OOF DoM scores"
  depends-on: [F-2, F-8]
  would-update: [F-2, F-8]
  triggered-by: ["2507.16806"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE73
  pathway: P11
  description: "Confidence-weighted majority vote (CWMV) on F-8's selective-prediction K=8 setup. For each MATH-500 problem in our cached K=8 1.5B and 7B generations, compute per-sample sigmoid(prefill-L19 DoM score) as confidence q_i, then implement (a) max-confidence selection (pick the answer with the highest q), (b) confidence-weighted majority vote (vote weight = q_i), and (c) un-weighted majority. Compare answered-accuracy at coverage 0.5 against F-8's threshold-based 71.6%. Also compute the inter-solution-coherence diagnostic from paper Fig 6b: sum mean q across unique answers per question; flag deviations from 1.0."
  rationale: "F-8 currently uses prefill DoM as a per-sample correctness signal but stops there — the paper shows verbalized confidences combine usefully with majority voting at test time. Replicating CWMV with our DoM-derived confidence directly tests whether the F-8 signal generalises from abstention to test-time scaling, and whether DoM-derived per-sample confidences satisfy coherence across self-consistency samples (a sharper test than AUROC)."
  trigger: "Paper 2507.16806 Fig 4a shows CWMV outperforming both vanilla majority and max-confidence at every N; same paper Fig 6b introduces the confidence-coherence diagnostic. Both are cheap to apply to our existing K=8 cache."
  status: READY
  priority: HIGH
  roi: 8
  cost: "1 h CPU on cached K=8 generations + per-sample DoM scores"
  depends-on: [F-8, F-2]
  would-update: [F-8]
  triggered-by: ["2507.16806"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-72: RL-with-verifiable-rewards training degrades the prefill-L19 DoM correctness signal in proportion to ECE worsening
**Priority:** MEDIUM
**Motivated by:** 2507.16806 + F-2
**Test:** Take any publicly-available Qwen-2.5-7B-RLVR or Qwen-2.5-7B-RLCR checkpoint
(e.g. from Open-Reasoner-Zero / DeepSeek-R1-distilled releases that reproduce the RLVR
pipeline). Extract prefill-L19 activations on MATH-500. Re-fit the DoM probe with the
same 5-fold OOF protocol used for F-2. Predict: AUROC drops from 0.7731 (base) to ≤ 0.70
(post-RL) in lockstep with ECE worsening from ~0.39 to ~0.26. Estimated time: 4 h H100
to extract activations on a borrowed checkpoint + 30 min CPU to refit the probe.
**Requires:** H100 pod or rented checkpoint, MATH-500 cache (already have), DoM extraction
script (already have).
**Would change:** On confirm, F-2 must be retitled to "prefill L19 DoM is a strong
correctness predictor *on base / RLHF models* but degrades under RLVR" — restricts the
generality of the headline finding. On reject, F-2 holds across post-RL models too,
strengthening the persistence story.
**Blocks:** H-13 (head-level attribution depends on which model state we attribute), H-1
(steering interventions may need different vectors for base vs post-RL).

### H-73: Confidence-weighted majority vote with prefill-DoM-derived confidence beats threshold-based abstention at matched coverage on MATH-500
**Priority:** HIGH
**Motivated by:** 2507.16806 + F-8 + EXP-040
**Test:** On our K=8 MATH-500 cache (Qwen-2.5-1.5B and 7B), compute per-sample
sigmoid-calibrated prefill-L19 DoM as q_i. Compare three selection rules at coverage 0.5:
(a) F-8's current threshold abstention (drop low-DoM-mean problems), (b) max-confidence
(pick highest-q sample per problem and accept if max-q > τ), (c) confidence-weighted
majority vote (weighted vote per problem, accept if winning weight > τ). Predict: CWMV
matches or beats (a) by ≥ 1 pt answered accuracy at coverage 0.5. Estimated time: 1 h CPU.
**Requires:** Existing K=8 cache + per-sample DoM scores (already have).
**Would change:** On confirm, F-8 should be reframed around CWMV, not threshold-abstention,
giving a strictly cheaper test-time scaling story. On reject, threshold-abstention is the
better DoM consumption pattern, which itself is a useful negative result against the
paper's CWMV-superiority claim — possibly indicating DoM is poorly self-consistent across
samples.
**Blocks:** nothing.

## Proposed PAPER_INDEX.md classification

## 2507.16806 — Beyond Binary Rewards: Training LMs to Reason About Their Uncertainty (Damani et al., 2025)

**Relevance:** Direct methodological neighbour of P11's prefill-L19 DoM probe and F-8
selective-prediction work, on the **same Qwen-2.5-7B base model** evaluated on **MATH-500
+ GSM8K + Big-Math**. Introduces RLCR — a GRPO-with-Brier-reward training objective —
and reports calibration metrics (ECE, Brier) for four confidence-extraction baselines
including a final-layer linear probe (RLVR+Probe, Math AUROC 0.65). The paper's
"RL degrades base-model calibration unless explicitly trained against" finding has direct
implications for F-2's and F-3's persistence under post-RL models. Cited as the closest
work for the proper-scoring-rule framing of selective prediction (H-1, H-12).

**Key claim we tested:** That a single linear probe on a Qwen-2.5-7B model's hidden
states delivers Math correctness AUROC ≈ 0.65 with ECE ≈ 0.13 / Brier ≈ 0.19 — i.e. the
"hidden-state probe is a weak calibration tool" baseline they compare RLCR against.

**Our result:** **PARTIALLY CONFIRMED for the verbalized-confidence story; PENDING for
the probe-degrades-with-RL claim.** Our prefill-L19 DoM probe on Qwen-2.5-1.5B *base*
gets AUROC = 0.7731 — well above their post-RLVR final-layer probe AUROC = 0.65. This is
consistent with "post-RL probes lose calibration"; we cannot yet test whether *our* probe
specifically degrades after RLCR/RLVR (H-72 is the planned test, FE-72/FE-73 are
calibration-anchor experiments using only existing caches).

**Related experiments:** P11-FE72 (Brier/ECE re-eval of base-model DoM), P11-FE73 (CWMV
on K=8 cache), H-72 (post-RL probe degradation), H-73 (CWMV vs threshold abstention).

**Status:** PARTIALLY CONFIRMED.

## New claims

- Paper Table 1b base-model ECE = 0.39 / Brier = 0.40 / AUROC = 0.56 on Math (averaged
  Math-500 + GSM8K + Big-Math), Qwen-2.5-7B base. Cited as reference number when adding
  Brier/ECE to F-2; will need a `validate_claims.py` entry once FE-72 produces our
  comparable number.
- Paper Table 1b RLVR ECE = 0.26 / Brier = 0.28 on Math; RLCR ECE = 0.10 / Brier = 0.17;
  SFT+RLCR ECE = 0.08 / Brier = 0.14. Cited as comparison anchors only; no direct
  `validate_claims.py` entry needed unless we replicate.
- Paper Table 1b RLVR+Probe Math AUROC = 0.65 — the explicit "linear probe on RLVR-tuned
  Qwen-2.5-7B final-layer embeddings" baseline that F-2 should be cited against.
- Paper Math accuracy 72.7% (RLCR) on MATH-500 + GSM8K + Big-Math averaged — independent
  corroboration of CLAUDE.md headline 7B MATH-500 K=1 = 73.2% (no `validate_claims.py`
  entry; numbers are not literally identical and live in different docs).

## Sources consulted

- /tmp/arxiv_2507_16806/paper.txt (decompressed PDF) — full paper read for methodology,
  results tables, and references.
- ~/topo-confidence/CLAUDE.md (Headline numbers section) — required default-context anchor.
- Bash grep of FINDINGS.md / HYPOTHESES.md — F-N / H-N one-liners, default-context anchor.
- Bash grep of PAPER_INDEX.md — existing status flags, default-context anchor.
- `python query.py paper 2507.16806` — prior admission note.
- `python query.py future P11` — current P11 FE inventory (highest extant: P11-FE22; FE-72
  picked at +50 to avoid concurrent-brief collisions).
- `python query.py paper <id>` for 12 cited arxiv IDs — all not-yet-in-graph; recommended
  admissions logged in Cross-paper signals.
- No on-demand reads of F-N / H-N body sections were performed (Refutations were written
  off the one-liner index alone).
