# Triage brief — 2509.24202 — Can Large Language Models Express Uncertainty Like Human?

Source: https://arxiv.org/abs/2509.24202
Triaged: 2026-04-29

Authors: Linwei Tao, Yi-Fan Yeh, Bo Kai, Minjing Dong, Tao Huang, Tom A. Lamb, Jialin Yu, Philip H.S. Torr, Chang Xu (Sydney / CityU HK / SJTU / Oxford), 2025.

## Refutations

1. **Black-box LC+ prompting could match prefill-DoM AUROC, undermining F-2's mechanistic framing.**
   The paper reports LC+ AUROC up to **0.8083** (gpt-5-mini, SimpleQA) and **0.8179** (claude-sonnet-4, SimpleQA) using *only* a hedging-instruction prompt + a DistilRoBERTa mapper that converts hedge tokens to scores — no hidden states, no probes, no logits. F-2's headline claim is that prefill L19 DoM is a *strong* correctness predictor at AUROC 0.7731 on Qwen-2.5-1.5B / MATH-500, framed as evidence of an internal geometric correctness signal. Mechanism of refutation: if a label-free black-box prompt (LC+) reaches ≥0.77 AUROC on the same MATH-500 / Qwen-2.5-1.5B set, then F-2's number reflects something already verbalizable, not a deep latent geometric finding — it would shrink F-2 from "mechanistic finding" to "we recovered a signal the model already exposes." Falsifiable: re-run our 500 cached MATH-500 generations with the LC+ prompt (Appendix C.3 of this paper), apply the released mapper, score AUROC. ~30min CPU on existing NPZs + 1 hr regen for fresh LC+ prompted answers.

2. **SU-distilled SFT could beat F-8's selective-prediction at matched coverage.**
   Table 4: LC (SFT) on Qwen3-8B reaches **AUROC 0.7331 / ECE 0.2837** on NQ-Open, beating semantic-uncertainty itself (0.7252 / 0.3878). F-8 anchors selective prediction at 71.6% accuracy at 50% coverage on Qwen-2.5-1.5B / MATH-500 via prefill DoM. Mechanism of refutation: if SU→SFT distilled LC works on math reasoning (not just QA), then the right pipeline is "compute SU once → distill into the model itself" rather than "extract prefill L19 DoM and route." That would put F-8's specific *prefill-DoM-as-router* mechanism on the wrong tier of the abstraction stack: the model can be taught to verbalize its own uncertainty more cheaply than we can probe it. Falsifiable: K=10 sampling on MATH-500 (we have data for K=2.5 from EXP-040), compute Farquhar-style SU + DeBERTa entailment per question, run SFT recipe (LoRA r=32 α=32 dropout=0.05, 3 epochs) on Qwen-2.5-1.5B, measure selective acc at 50% coverage on held-out subset. ~1 day H100.

3. **Reasoning-effort improves verbalized AUROC by ≥10 pp — challenges H-22's framing.**
   Their Table 4 shows GPT-5 VNC AUROC jumping 0.7227 → 0.8286 (+0.106) when reasoning is enabled vs minimal. H-22 hypothesizes Abstract-CoT produces compressed/different breathing. The implicit assumption behind H-22 is that reasoning is *content* the model verbalizes; this paper provides evidence that reasoning effort directly modulates *uncertainty expression* quality — i.e. reasoning may be improving the linguistic signal, not just the answer. Mechanism of refutation: if the prefill DoM AUROC on Qwen-2.5-1.5B *also* moves with reasoning effort (e.g., extended thinking tokens), then F-2 is partly a "reasoning-budget × verbalizability" artifact rather than a fixed geometric direction. Falsifiable: extract prefill activations on MATH-500 with `<think>` budgets at 0 / 256 / 1024 tokens; measure DoM AUROC per budget. If AUROC tracks budget, F-2's direction is not stable across compute regimes.

4. **Hedging-rich generations may invalidate the 1024-tok label re-extraction.**
   The paper documents that LLMs reliably emit hedges like "probably", "I'm not sure", "I believe" when prompted with LC+. Our P11 H100 1024-tok pipeline grades correctness from final answer extraction. If the LC+ regime makes models hedge more, final-answer extraction (and therefore the labels feeding F-2 / F-8 AUROCs) becomes prompt-fragile. Mechanism of refutation: if our prefill-DoM AUROC drops materially when the answering prompt is replaced by their LC+ prompt — even on the same questions — then F-2's signal is conditional on the un-hedged greedy regime and not a robust correctness predictor under deployment-realistic prompting. Falsifiable: re-run extraction stage with LC+ prompt vs vanilla prompt on the same 500 problems, recompute AUROC, compare.

## Direct connections to F-N / H-N

- **F-2 (Prefill L19 DoM AUROC 0.7731)** — direct competitor. LC+ on QA reaches 0.66–0.83 AUROC across models without any hidden states (Tables 2 & 3). Our 0.7731 on Qwen-2.5-1.5B / MATH-500 is in-band of theirs but on a harder task. Forces a direct head-to-head.

- **F-8 (Selective-prediction acc 71.6% at coverage 0.5)** — their LC (SFT) framework offers an alternative *training-time* path to selective prediction (vs our *probe-time* path). Table 4 NQ-Open AUROC 0.7331 beats SU baseline.

- **H-12 (Semantic-entropy probes label-free for refusal)** — paper *uses* SEPs (Kossen 2406.15927) as supervision signal, not as the deployed mechanism. This re-frames H-12: the cheap path may not be "deploy SEP at inference" but "distill SEP into the model via SFT once." Our F-8 71.6% number is the comparator either way.

- **H-1 (Per-position DoM steering)** — orthogonal but contrastive. They steer via prompts and SFT in the *output* distribution; we steer via residual-stream additions. Their Ji et al. (2503.14477) reference — "calibrating verbal uncertainty as a *linear feature*" — is the bridge.

Numerical anchors: AUROC 0.7731 (F-2, prefill 1024-tok), AUROC 0.7186 (final-token 1.5B), 71.6% selective-prediction acc at coverage 0.5 (F-8), cos(prefill_DoM, final_DoM) 0.046 (F-3).

## Methodologies extracted

- **DistilRoBERTa hedge-to-confidence mapper** — small encoder + linear + sigmoid trained on either (a) LLM-generated discrete labels {0, 0.25, 0.5, 0.75, 1} or (b) human-annotated continuous scores. MSE 50.7 vs LLM-judging baseline 183–385.
  *Replication cost*: ~30min on a CPU; weights would need to be downloaded from their anonymous repo.
  *We'd plausibly run this*: yes — it's the cheapest possible black-box correctness predictor; running it on our cached MATH-500 generations is the minimum-effort refutation test for F-2.

- **LC+ hedging-instruction prompt** (Appendix C.3 of the paper) — a vanilla QA prompt augmented with explicit "hedge when uncertain" instructions.
  *Replication cost*: 1hr regenerating 500 MATH-500 answers with the LC+ prompt at K=1.
  *We'd plausibly run this*: yes — required for refutation #1.

- **SU→SFT distillation framework** — for each question, sample 10 generations, compute Farquhar-style semantic uncertainty (DeBERTa entailment), discretize to 5 levels, retrieve LLM-generated hedging sentences at that level, build (question, hedged-answer) pairs, LoRA-SFT (r=32, α=32, dropout=0.05, 3 epochs, 8-bit base, 2× RTX 3090).
  *Replication cost*: ~1 day H100 for Qwen-2.5-1.5B; we already have K≈2.5 sampling, would need K=10 regeneration.
  *We'd plausibly run this*: yes if refutation #2 looks promising — directly relevant to F-8.

- **Reasoning-effort ablation** — vary GPT-5 reasoning budget between minimal (0 thinking tokens) and moderate (~1000 thinking tokens) and measure ECE / AUROC.
  *Replication cost*: 30min H100 with Qwen-2.5-1.5B at varying thinking budgets.
  *We'd plausibly run this*: yes — bears on H-22 and refutation #3.

- **AUROC-Incl / ECE-Excl evaluation protocol** — treat explicit abstentions ("I don't know") as confidence=0 for AUROC, exclude them from ECE. Different from our current evaluation which has no abstention class.
  *Replication cost*: trivial, post-hoc on existing NPZs.
  *We'd plausibly run this*: yes — gives us a fair comparison axis to their numbers.

- **Annotation-validation protocol** — 5 annotators per item, reject worker if score on 5 expert-validated items deviates >2σ in 3+ cases, then 1σ neighborhood filter around per-level mean. 90% acceptance rate, 50,000 → 12,762 valid annotations.
  *Replication cost*: not applicable — they release the dataset.
  *We'd plausibly run this*: no — we'd consume their dataset, not rebuild it.

## Approaches & framings

- **Linguistic Confidence (LC) as a deployment-realistic uncertainty axis.** Hedging language ("probably", "might") is what users naturally see and act on; numerical confidence and semantic-entropy probes are deployment-impractical. This re-frames H-12 from "label-free probe replaces supervised DoM" to "label-free probe is a *training signal*, not an inference-time deployable." Our F-8 selective-prediction story is currently inference-time — this paper's framing argues the inference cost should be amortized into training.

- **Hedge mapping as a quantitative bridge between linguistic and probabilistic uncertainty.** Their MSE 50.7 mapper says hedges *can* be cheaply reduced to scalars without LLM-as-judge. This is an enabling primitive: any correctness prediction work that wants to compare to "what the model would just say" now has a cheap evaluator.

- **SU-as-supervision rather than SU-as-prediction.** Distillation re-frames SU's role. Adjacent to H-12. If true, the right move for our pipeline isn't "deploy SEP" but "use SEP once to bootstrap an SFT dataset."

## Datasets & benchmarks

- **Linguistic-Uncertainty-Dataset** (introduced) — 12,762 valid hedging-expression annotations across 5 confidence levels (988 moderate / 428 low / 131 lowest / 64 high / 11 completely uncertain), released anonymously at https://anonymous.4open.science/r/Linguistic-Uncertainty-Dataset-051E. Applicable? **partially** — the *mapper* is directly applicable to any of our cached generations; the dataset itself isn't math-specific.

- **SimpleQA** (Wei et al., 2411.04368) — short-form factuality. ~20% accuracy on gpt-5-mini. License: CC, HF accessible. Applicable? **no** — orthogonal to MATH-500's reasoning regime, but the LC+ pipeline they validate on it transfers.

- **NQ-Open** (Lee et al., 1906.00300) — open-domain QA. Applicable? **no** for our domain, **yes** as a sanity check that mapper works outside their training distribution.

- **PopQA** (Mallen et al., 2212.10511) — they use a 1,000-q subset. Applicable? **no** for math reasoning.

## Implementation details worth capturing

- LoRA: rank=32, α=32, dropout=0.05, 3 epochs, 8-bit base load, 2× RTX 3090.
- Mapper architecture: DistilRoBERTa encoder + 1 linear + sigmoid; MSE loss.
- SU implementation: 10 samples per question + DeBERTa-v3 entailment for clustering (Kossen 2024a / Farquhar 2024).
- 5-level discretization rule for SU→hedge-level mapping in their Table 7 (Appendix).
- 200 SimpleQA questions held out as supervision-target generation set; excluded from eval.
- Per-question generation: 4 LLMs (GPT-5, Grok-3, Claude-Sonnet-4, Gemini-2.5-Pro) × 10 hedges × 5 levels = 200 sentences/question; uniformly sample 10,000 for benchmark.
- Code + dataset: anonymous.4open.science repo (link in abstract).

## Replicable intermediates

- **Apply LC+ prompt + their mapper to cached `pathway11_h100/prefill_gated_compute/results.json` generations** — if the cached JSON contains the prompt + raw response per question, we may not even need regeneration, just mapper-scoring of the existing answers. Need to verify response format. Cached artifact: `pathway11_h100/prefill_gated_compute/results.json`. Predicted result: AUROC 0.5–0.65 *because* our generations weren't LC+-prompted; this is the *vanilla LC* condition (their AUROC 0.47–0.50 in Table 3) — useful as the floor.
- **Compute mapper-scored LC AUROC on existing 1024-tok responses** as a baseline that would establish whether *any* hedging signal exists in the unprompted generations. Cached artifact: same `results.json`.
- **Cross-check the SimpleQA → MATH-500 transferability of the mapper** by hand-spot-checking ~20 MATH-500 responses for hedging tokens. Cached artifact: `pathway11_h100/prefill_gated_compute/results.json`.

## Cross-paper signals

- **2406.15927** (Kossen — Semantic Entropy Probes / SEPs) — already in graph (status: graphed). Direct: paper uses SEPs as supervision signal for their SFT framework.
- **2205.14334** (Lin et al. — Teaching models to express uncertainty in words) — already in graph (status: rejected; relevance preserved as H-12 motivator). Direct: foundational verbalized confidence work, this paper extends it linguistically.
- **2503.14477** (Ji et al. — *Calibrating verbal uncertainty as a linear feature to reduce hallucinations*) — **NOT in graph; recommend admission**. Critical: they treat verbal uncertainty as a *linear feature in residual stream* — this is the closest thing in the literature to our DoM framework applied to LC. Should triage immediately.
- **2306.13063** (Xiong et al. — Can LLMs express their uncertainty? VNC) — **NOT in graph; recommend admission**. The VNC baseline this paper compares against; foundational for verbalized numerical confidence.
- **2407.15814** (Belem et al. — Perceptions of linguistic uncertainty by language models and humans) — **NOT in graph; recommend admission**. Demonstrates hedge→numeric mapping is sensitive to LLM priors — directly bears on the validity of any LC pipeline we adopt.
- **2410.04315** (Wang et al. — Calibrating expressions of certainty) — **NOT in graph; recommend admission**. Proposes per-hedge uncertainty distributions; pushes back on the "treat hedge as scalar" simplification.
- **2405.16908** (Yona et al. — Can LLMs faithfully express their intrinsic uncertainty in words?) — **NOT in graph; recommend admission**. The paper this work directly extends and contradicts (Yona claims LC fails; this paper claims LC+ works).
- **2411.04368** (Wei et al. — SimpleQA) — **NOT in graph; recommend admission only if we plan to test on SimpleQA**. Otherwise low priority.

## Proposed FutureExperiments

```yaml
- id: P11-FE85
  pathway: P11
  description: "Apply LC+ prompt + DistilRoBERTa hedge-mapper from 2509.24202 to Qwen-2.5-1.5B on MATH-500 K=1 generations; compute AUROC against existing correctness labels and compare directly to F-2 prefill-DoM AUROC 0.7731."
  rationale: "Direct refutation test for F-2's mechanistic framing. If a black-box prompt + 80M-param hedge mapper reaches ≥0.77 AUROC on the same data, F-2 is recovering a signal the model already exposes, not a deep geometric finding."
  trigger: "Paper 2509.24202 reports LC+ AUROC 0.66-0.83 on QA without any hidden states; our F-2 sits at 0.7731 on math reasoning and is in-band."
  status: READY
  priority: HIGH
  roi: 9
  cost: "1hr regen MATH-500 with LC+ prompt + 30min CPU mapper scoring"
  depends-on: [F-2, F-8]
  would-update: [F-2]
  triggered-by: ["2509.24202"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE86
  pathway: P11
  description: "Replicate the SU-distilled SFT framework (Section 6 of 2509.24202) on Qwen-2.5-1.5B with MATH-500 supervision: K=10 sampling, Farquhar-SU per question, discretize to 5 levels, build hedged-answer training pairs via GPT-5, LoRA r=32 α=32 3 epochs. Measure selective-prediction accuracy at coverage 0.5 vs F-8 baseline 71.6%."
  rationale: "Refutation #2 — if SU→SFT distillation outperforms prefill-DoM-based selective prediction at matched coverage, the right pipeline is amortizing SU into training, not deploying probes at inference. Re-tiers F-8."
  trigger: "Paper's LC (SFT) reaches AUROC 0.7331 on NQ-Open, beating SU itself; question is whether it generalizes to MATH-500."
  status: READY
  priority: HIGH
  roi: 8
  cost: "1 day H100 (K=10 regen + LoRA SFT + eval)"
  depends-on: [F-8, F-2]
  would-update: [F-8]
  triggered-by: ["2509.24202"]
  blocked-by: null
  blocked-by-experiment: [P11-FE85]

- id: P11-FE87
  pathway: P11
  description: "Reasoning-effort ablation on Qwen-2.5-1.5B: re-extract prefill L19 DoM AUROC on MATH-500 at thinking-token budgets {0, 256, 1024}. Test whether F-2's AUROC tracks the budget the same way the paper's VNC AUROC moves +10pp on GPT-5."
  rationale: "Refutation #3 — if AUROC moves with reasoning budget, F-2's direction is not stable across compute regimes. Bears on H-22's reasoning-vs-verbalization split."
  trigger: "Paper's Table 4 shows VNC AUROC 0.7227 → 0.8286 with reasoning toggle on GPT-5."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "30min H100 (3 budget settings × 500 problems extraction)"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2509.24202"]
  blocked-by: null
  blocked-by-experiment: []

- id: P11-FE88
  pathway: P11
  description: "LC+ vs vanilla-prompt robustness check. Re-extract the full prefill L19 stack on MATH-500 under the LC+ hedging-instruction prompt (Appendix C.3 of 2509.24202) instead of our current vanilla prompt. Recompute prefill DoM AUROC. Quantify how much F-2's signal is conditional on the un-hedged regime."
  rationale: "Refutation #4 — F-2's AUROC may be a property of greedy non-hedging generations; under deployment-realistic LC+ prompting it could collapse, indicating prompt fragility."
  trigger: "Paper shows LC+ prompt materially changes uncertainty expression on every model tested (Table 3)."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "1hr H100 + 30min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2509.24202"]
  blocked-by: null
  blocked-by-experiment: []
```

## Proposed HYPOTHESES.md additions

### H-72: LC+ prompt + DistilRoBERTa hedge mapper matches prefill-DoM AUROC on MATH-500
**Priority:** HIGH
**Motivated by:** 2509.24202 + F-2 (prefill L19 DoM AUROC 0.7731)
**Test:** Re-prompt 500 MATH-500 problems on Qwen-2.5-1.5B with LC+ prompt (Appendix C.3 of 2509.24202), apply released DistilRoBERTa mapper to convert hedge phrases to confidence scores, compute AUROC vs ground-truth correctness. Compare against F-2's 0.7731.
**Requires:** 1.5B model on H100 for re-prompted generation (~1hr), DistilRoBERTa mapper weights from anonymous repo, existing correctness labels for MATH-500.
**Would change:** If LC+ mapper-AUROC ≥0.77, F-2's framing as a *mechanistic* finding shrinks — the model's surface output already exposes the signal. F-2 becomes a calibration finding rather than a geometry finding. If LC+ AUROC ≤0.65, F-2's hidden-state signal genuinely exceeds what verbalization captures, strengthening the mechanistic claim.
**Blocks:** H-73 (no point distilling SU into SFT if simple LC+ prompting suffices).

### H-73: SU-distilled SFT beats prefill-DoM selective prediction on MATH-500 at matched coverage
**Priority:** HIGH
**Motivated by:** 2509.24202 (Table 4: LC SFT AUROC 0.7331 NQ-Open beats SU baseline 0.7252) + F-8 (selective acc 71.6% at coverage 0.5)
**Test:** K=10 sampling on MATH-500 with Qwen-2.5-1.5B → Farquhar-SU per question via DeBERTa entailment → discretize to 5 hedge levels → retrieve GPT-5-generated hedge sentences → LoRA SFT (r=32 α=32 dropout=0.05 3 epochs). Evaluate on held-out 100 problems: selective accuracy at coverage 0.5 vs F-8's 71.6%.
**Requires:** ~1 day H100, DeBERTa-v3 for entailment (small), GPT-5 API access for hedge sentence generation (~$50), held-out subset of MATH-500.
**Would change:** If SFT > 71.6% selective acc, the optimal F-8 pipeline becomes "amortize SU into training once" rather than "probe DoM at inference" — re-tiers F-8 as a feasibility result rather than a final answer. If SFT ≤ 71.6%, prefill-DoM probing remains the better tool; F-8 is robust.
**Blocks:** nothing.

### H-74: Prefill DoM AUROC tracks reasoning budget on Qwen-2.5-1.5B
**Priority:** MEDIUM
**Motivated by:** 2509.24202 (VNC AUROC moves +10pp with GPT-5 reasoning toggle) + F-2
**Test:** Extract prefill L19 activations on MATH-500 with `<think>` budgets {0, 256, 1024} tokens. Recompute DoM AUROC at each budget. Slope > 5pp would mark F-2 as budget-conditional.
**Requires:** 30min H100, existing extraction harness.
**Would change:** Confirm → F-2's "stable correctness signal" claim weakens; the prefill direction is a function of reasoning compute. Reject → strengthens F-2 as compute-invariant.
**Blocks:** nothing.

## Proposed PAPER_INDEX.md classification

## 2509.24202 — Can Large Language Models Express Uncertainty Like Human? (Tao et al., 2025)

**Relevance:** Direct competitor to F-2 (prefill DoM AUROC 0.7731) and F-8 (selective prediction 71.6%) on the *black-box* axis. The paper claims that with the right prompt (LC+) and a DistilRoBERTa hedge-to-confidence mapper, LLMs reach AUROC 0.66–0.83 on QA tasks without any hidden-state access. Their SU-distilled SFT framework achieves AUROC 0.7331 on NQ-Open, beating semantic-uncertainty itself. Both directly probe whether prefill-DoM probing is the *best* path or only a *sufficient* one. Also delivers a publicly-released hedging dataset (12,762 annotations) and a small mapper that is the cheapest possible black-box correctness predictor — useful as a baseline regardless of replication outcome.

**Key claim we tested:** LC+ prompting + lightweight hedge mapper achieves competitive calibration and discriminability vs semantic-uncertainty and verbalized numerical confidence, *without* hidden-state probing. SU-distilled LoRA SFT further improves both. In our terms: a prompt change + 80M-param sigmoid head can replace prefill-DoM extraction.

**Our result:** **TO TEST.** P11-FE85 (LC+ mapper on MATH-500 / 1.5B) and P11-FE86 (SU→SFT replication) are the two refutation-grade experiments. P11-FE87 (reasoning-effort ablation) and P11-FE88 (LC+ prompt fragility) are auxiliary. Until P11-FE85 runs, we cannot know whether F-2's AUROC 0.7731 is mechanistic or recoverable from surface output alone.

**Related experiments:** P11-FE85, P11-FE86, P11-FE87, P11-FE88, H-72, H-73, H-74; F-2, F-8; H-12.

**Status:** TO TEST

## New claims

This brief introduces several quantitative claims that will need `validate_claims.py` entries upon promotion *only if* the corresponding refutation experiments are run and the headline numbers move. Pre-experiment, the claims live as forward-pointing predictions, not asserted findings:

- LC+ AUROC 0.7591–0.8083 on SimpleQA (gpt-5-mini, Table 2 of 2509.24202) — paper-reported, would only become a validate_claims entry if cited in narrative docs.
- LC (SFT) AUROC 0.7331 / ECE 0.2837 on NQ-Open (Qwen3-8B, Table 4) — same.
- Mapper MSE 50.68 (human-anno trained) (Table 1) — same.

No claims need new validate_claims entries until the proposed FE results are committed.

## Sources consulted

- /tmp/paper2509/full_unwrap.md — full text of 2509.24202 extracted via mcp__arxiv__download_paper + mcp__arxiv__read_paper (chunked via Read).
- ~/topo-confidence/CLAUDE.md (headlines section only) — F-2/F-8 numerical anchors.
- ~/topo-confidence/FINDINGS.md, ~/topo-confidence/HYPOTHESES.md (one-liner grep only) — F-N/H-N IDs.
- ~/topo-confidence/PAPER_INDEX.md (status flag grep) — overlap check.
- research-graph `query.py paper <id>` for: 2509.24202, 2406.15927, 2503.14477, 2306.13063, 2205.14334, 2407.15814, 2410.04315, 2405.16908, 2411.04368, 2212.10511, 1906.00300, 2207.05221, 2002.07650, 1610.02136, 2506.07461 — graph-membership check for cited papers.
- research-graph `query.py future P11` — to pick the next P11-FE id (highest existing was P11-FE22; chose P11-FE85+ for collision-safety).
- HYPOTHESES.md tail — confirmed highest H-N is H-22; chose H-72+ for collision-safety.

No F-N body files were read on demand — refutations were drafted from the headlines table alone, then connections section anchored with the specific F-2/F-8 AUROC and selective-prediction numbers cached in CLAUDE.md.
