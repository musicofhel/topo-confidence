# Result brief — P11-FE881 (per-problem cov spectrum on PC1-residualized L19 clouds)

FE291 follow-up #2, paired with FE880 (PH-after-PC1) and FE882
(decomposition triangle). FE880 (EXP-56) showed PH features add
*negative* signal after PC1 removal (real 0.6884 vs matched-cov
null 0.7628, gap −0.074). The natural follow-up: if Gaussianizing
the residualized covariance retains the signal, the *eigenvalue
spectrum* of that covariance should expose it as a feature vector.

**Result.** Top-K log-eigvals of the per-problem L19 covariance
(after PC1 residualization), fed to 5-fold OOF logistic, swept over
K ∈ {5, 10, 20, 50, 100}. **Best K=20: OOF AUROC = 0.7928** (cv5
0.7920 ± 0.027). This **beats supervised DoM 0.7679 by +2.49 pp**
and **beats the 2-feat (PC1, PC9) directional probe 0.7856 by
+0.7 pp** — meaning per-problem second-order structure (the *shape*
of the residualized covariance spectrum) carries correctness
information beyond *any* single direction or pair of directions in
the 1536-d activation space.

F-2 reframes: the L19 correctness signal is part directional (PC1
mean shift, PC9 trim) and part *spectral* (per-problem residual
eigenvalue decay rate). The cov-spectrum probe is the strongest
correctness probe at this layer at any tier — single-feature,
two-feature, full-1536-d L2-reg (FE882: 0.7847), or PH (FE880:
0.6884). Single-feature baselines on individual log-eigvals:
log_eigval_3 alone hits 0.7488, log_eigval_2 0.6925, log_eigval_1
0.5223 — the signal is distributed across rank 2–20, not
concentrated in any single eigenvalue.

## FE

```yaml
fe_id: P11-FE881
status: COMPLETED
outcome: "On 500 PC1-residualized L19 trajectory clouds (PC1 from FE291, var_share 0.1474), per-problem covariance eigenvalues extracted via SVD, log-transformed and fed to 5-fold OOF StratifiedKFold logistic regression (standardized, no penalty tuning). K-grid {5, 10, 20, 50, 100}: best K=20, OOF AUROC = 0.7928 (cv5 0.7920 ± 0.027). Beats supervised DoM 0.7679 by +2.49 pp, 2-feat [PC1, PC9] directional probe 0.7856 by +0.7 pp, and FE880 PC1-resid PH null 0.7628 by +3.0 pp. Single-feature baselines: log_eigval_1 0.5223, log_eigval_2 0.6925, log_eigval_3 0.7488 — signal is distributed across rank 2-20. F-2 reframes: L19 correctness signal is partly directional (PC1 mean shift, PC9 trim, ceiling at 0.7856) and partly spectral (per-problem residual eigenvalue decay rate, lifts to 0.7928, NOT capturable by any linear directional probe — see FE882). Cloud sizes: T_min=123, T_median=521, T_max=1024."
result_json: pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json
result_json_keys:
  - best_auroc_oof
  - best_top_k
  - classifiers
  - pc1_var_share
  - cloud_T_median
  - anchors
regen_cmd: "python pathway11_h100/cov_spectrum/recompute_pc1_resid_cov_spectrum.py"
```

## Result JSON verification

`pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json`
exists; declared keys (`best_auroc_oof`, `best_top_k`, `classifiers`,
`pc1_var_share`, `cloud_T_median`, `anchors`) are present. Regen
reads `pathway11_h100/prefill_inversion/cache/m15b_prefill.npz` for
PC1 (sub-second SVD) and the 500 cached pathway8 trajectory NPZs at
`pathway8_layerwise/data/math500/problem_*.npz` for per-problem
clouds. **Cold disk runtime ~12 min wall**; **cached eigval-cache
runtime ~2 s wall** (sidecar `eigval_cache.npz` at 406 KB stores
the (500, 100) eigval matrix + correct/T_i vectors after first
extraction; subsequent regens skip cloud reload and run only the
LR sweep). Deterministic seed=42.

## EXPERIMENT_LOG entry

## EXP-57: P11-FE881 — Per-problem cov spectrum (top-K log-eigvals) on PC1-residualized L19 clouds
**Date:** 2026-05-02
**Status:** COMPLETE
**Motivated by:** EXP-56 (FE880) showed PH features on PC1-residualized
clouds carry NEGATIVE signal (real 0.6884 vs matched-cov null 0.7628,
gap −0.074). If Gaussianizing the residualized covariance retains the
signal, the eigenvalue spectrum of that covariance should expose it
as a feature vector — bypassing the PH descriptor family entirely.
**Hypothesis pre-registered:** F-2 strengthens spectrally if top-K
log-eigvals of the PC1-residualized per-problem cov give OOF AUROC
≥ 0.78 (above the supervised DoM 0.7679 ceiling at this layer). F-2
narrows directionally if cov-spectrum AUROC ≤ DoM, indicating the
signal is fully captured by direction.
**What we actually tested:** for each of 500 MATH-500 problems, load
the L19 trajectory cloud from `pathway8_layerwise/data/math500/problem_*.npz`,
project out the FE291 PC1 direction, compute the centered SVD on the
residualized cloud, take the top K_MAX=100 squared singular values
(eigenvalues of the per-problem cov), log-transform with a small
floor. Stack into (500, 100) eigval matrix, cache to
`pathway11_h100/cov_spectrum/eigval_cache.npz`. For each K ∈
{5, 10, 20, 50, 100}, run 5-fold StratifiedKFold OOF logistic
regression on the top-K log-eigvals (standardized per-fold, no
penalty tuning). Also evaluate single-feature OOF AUROC for
log_eigval_1, _2, _3 alone.
**Key results:**
  | Probe                        | OOF AUROC | cv5 mean ± std | n_features |
  |------------------------------|-----------|----------------|------------|
  | top-5 log-eigvals            | 0.7632    | 0.7627 ± 0.024 | 5          |
  | top-10 log-eigvals           | 0.7925    | 0.7935 ± 0.012 | 10         |
  | **top-20 log-eigvals**       | **0.7928** | 0.7920 ± 0.027 | 20         |
  | top-50 log-eigvals           | 0.7925    | 0.7936 ± 0.029 | 50         |
  | top-100 log-eigvals          | 0.7886    | 0.7914 ± 0.021 | 100        |
  | log_eigval_1 alone           | 0.5223    | 0.5335 ± 0.048 | 1          |
  | log_eigval_2 alone           | 0.6925    | 0.6926 ± 0.039 | 1          |
  | log_eigval_3 alone           | 0.7488    | 0.7507 ± 0.022 | 1          |

  Anchors (cross-experiment, for context):
  - F-2 supervised DoM (FE110 EXP-53 matched protocol): 0.7679
  - FE291 best 2-feat (PC1, PC9): 0.7856
  - FE880 PC1-resid PH real: 0.6884
  - FE880 PC1-resid matched-cov Gaussian null PH: 0.7628
  - FE882 full 1536-d L2-reg max (C=0.001): 0.7847

  Cloud-size distribution: T_min=123 (worst-case truncated trajectory),
  T_median=521, T_max=1024 (full prefill+CoT). Correctness rate 0.486
  (243/500 correct on Qwen-2.5-1.5B-Instruct K=1 MATH-500).
**Verdict:** F-2's signal is part directional (PC1 mean shift + PC9
trim, capping at 0.7856 with the 2-feat probe) and part **spectral**
(per-problem residualized eigenvalue decay rate, lifting to 0.7928
with top-20 log-eigvals). The +0.7 pp lift over the directional
ceiling is genuinely orthogonal to direction (FE882 confirms via
full 1536-d L2-reg ceiling at 0.7847 ≈ 2-feat 0.7856 — under-
regularization is not the explanation).
**Changed our understanding of:**
  (1) F-2 strengthens spectrally. The cov-spectrum probe (top-20
      log-eigvals on residualized clouds) is the strongest correctness
      probe at L19 at any tier — beats single-feature, 2-feature,
      full-1536-d L2-reg, and PH probes. The +2.49 pp over supervised
      DoM is the strongest lift over the F-2 baseline observed.
  (2) F-2 factors explicitly into 3 pieces: (a) PC1 mean shift
      (1-d AUROC 0.7679 supervised, 0.7458 unsupervised), (b) PC9
      trim (raises 1-d to 2-d 0.7856, the directional ceiling),
      (c) per-problem residual eigenvalue decay rate (raises 2-d to
      spectral 0.7928, NOT capturable by any linear direction probe
      — see FE882 EXP-58).
  (3) F-10 sharpens further. PH features fail on PC1-residualized
      clouds (FE880 gap −0.074), but the *covariance spectrum* of
      the same clouds carries the signal at 0.7928. The post-PC1
      correctness signal is purely spectral, not topological.
**Open follow-ups suggested by this result:**
  (a) Per-problem effective rank / participation ratio / stable rank
      as a *single* scalar feature — does any standard scalar of
      the residualized cov match the top-20 0.7928? If yes, the
      narrative collapses to a named geometric property (much cleaner
      than "20 log-eigvals").
  (b) Generalize PC1 → top-K residualization (PC1..PC9 covers the
      directional ceiling). If residualizing PC1..PC9 keeps cov-spectrum
      AUROC at 0.79, the spectral signal lives in PC10+ low-eigenvalue
      tail. If it collapses, F-2 is fully captured by directional+spectral.
  (c) Causal test: rank-truncate the PC1-residualized covariance
      per-problem (project tokens to top-K eigval directions and
      back) before continuation; measure correctness drop. Tests
      whether the spectral signal is causally load-bearing or
      observational-only.
**Files:** `pathway11_h100/cov_spectrum/recompute_pc1_resid_cov_spectrum.py`
(regen, ~12min cold / ~2s cached), `pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json`
(output), `pathway11_h100/cov_spectrum/eigval_cache.npz` (sidecar
cache, 406 KB), `pathway11_h100/prefill_inversion/cache/m15b_prefill.npz`
(FE291 PC1 source), `pathway8_layerwise/data/math500/problem_*.npz`
(per-problem L19 clouds).
**Depends on:** EXP-55 (FE291 PC1 grounding); EXP-56 (FE880 PH
fails on residualized clouds — motivates spectral probe); F-2
supervised DoM 0.7679 baseline; F-10 covariance pathway framing.
**Enables:** EXP-58 (FE882 decomposition triangle: full 1536-d
L2-reg ceiling, disambiguates spectral vs under-regularized
directional); causal interventions (rank-truncate residualized cov,
2D ablation along (PC1, PC9) plane); single-scalar effective-rank
probe collapse.

## STATE.md last-experiment update

EXP-57 (P11-FE881 per-problem cov spectrum on PC1-residualized L19
clouds). Top-20 log-eigvals of the per-problem L19 covariance after
PC1 residualization, 5-fold OOF logistic AUROC = **0.7928** (cv5
0.7920 ± 0.027). **Beats supervised DoM 0.7679 by +2.49 pp** and
beats the 2-feat (PC1, PC9) directional probe 0.7856 by +0.7 pp.
F-2 reframes: L19 correctness signal is part directional (PC1
mean shift, PC9 trim, capping at 0.7856) and part spectral
(per-problem residual eigenvalue decay rate, lifting to 0.7928 —
NOT capturable by any linear direction probe, see FE882 EXP-58).
The cov-spectrum probe is now the strongest correctness probe at
L19 at any tier (single-feature, 2-feat, full-1536-d L2-reg, PH).
Single-feature baselines: log_eigval_3 alone 0.7488, log_eigval_2
0.6925, log_eigval_1 0.5223 — signal distributed across rank 2-20.

## FINDINGS.md updates

### F-2: A single L19 prefill direction predicts correctness on Qwen-2.5-1.5B at AUROC 0.7731

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E57]
counterargument: "EXP-57 shows F-2's signal at L19 is part directional and part *spectral*. The 2-feat (PC1, PC9) directional probe caps at 0.7856 (FE291); FE882 confirms the full 1536-d L2-reg ceiling is 0.7847 ≈ 2-feat (under-regularization is not the explanation). But the top-20 log-eigvals of the per-problem PC1-residualized covariance lifts to 0.7928 — +0.7 pp above the directional ceiling, +2.49 pp above supervised DoM. F-2 is no longer 'a single direction'; it factors into (a) PC1 mean shift (1-d 0.7679 supervised), (b) PC9 trim (lifts to 2-d 0.7856), (c) per-problem residual eigenvalue decay rate (lifts to spectral 0.7928, NOT capturable by any linear direction probe properly regularized or not). The original 'single direction' framing applies to (a)+(b) — pieces that account for ~85% of the supervised AUROC budget — but the residual ~15% is genuinely second-order, not directional."
```

**Claim.** F-2 was originally framed as a single L19 prefill
direction at AUROC 0.7731 (supervised 1536-d logistic, FE110/EXP-001).
EXP-55 reframed the direction as unsupervised-identifiable (PC1
cosine 0.922 with DoM, 85% DoM-energy in PC1). **EXP-57 shows the
direction framing is incomplete**: the cov-spectrum probe (top-20
log-eigvals on per-problem PC1-residualized clouds) reaches OOF
AUROC 0.7928 — **above** the directional ceiling. F-2 now factors
into three pieces: (a) PC1 mean shift, ≈0.92·DoM, single-feature
0.7679 supervised; (b) PC9 trim, lifts 1-d to 2-d 0.7856 (the
directional ceiling — FE882/EXP-58 confirms full 1536-d L2-reg max
0.7847 ≈ 2-feat); (c) per-problem residual eigenvalue decay rate,
lifts 2-d to spectral 0.7928. The (a)+(b) directional component
accounts for ~85% of the AUROC budget; the (c) spectral component
is the residual ~15% and is NOT capturable by any linear direction
probe. The cov-spectrum probe is the strongest correctness probe
at L19 at any tier.

**Strength:** STRONG (now triangulated by supervised DoM, unsupervised
PC1, CAST class-mean PC1, 2-feat directional ceiling, full 1536-d
L2-reg ceiling, AND per-problem cov-spectrum lift — the directional
agreement is within 0.022 AUROC at cosine ≥ 0.92, and the spectral
piece is reproducible across K ∈ [10, 50] within ±0.001 AUROC).

**Evidence:** EXP-001 (supervised 1536-d probe AUROC 0.7731);
EXP-53 / FE110 (DoM mass-mean replication 0.7711); EXP-55 / FE291
(unsupervised PCA-PC1 AUROC 0.7458, cos with DoM 0.9216, 85%
DoM-energy in PC1); **EXP-57** /
`pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json`
(top-20 log-eigvals on PC1-residualized clouds OOF AUROC 0.7928 —
beats DoM by 2.5 pp and 2-feat directional ceiling by 0.7 pp).

**Controls passed:**
- Supervised vs mass-mean DoM (FE110 EXP-53): 0.7711 ≈ 0.7731.
- Mean-shift on Song-Zhong residualized clouds (FE115): residual
  DoM AUROC 0.8016 (+0.029 over raw F-2).
- Unsupervised PCA-PC1 (EXP-55, FE291): 0.7458 single-feature OOF,
  cos 0.922 with DoM, 85% DoM-energy in PC1.
- CAST class-mean PCA-PC1 (EXP-55, FE291): 0.7458, numerically same
  direction as unsupervised PC1.
- 2-feat (PC1, PC9) directional ceiling (FE291): 0.7856.
- **Per-problem cov spectrum (EXP-57, FE881)**: top-20 log-eigvals
  on PC1-residualized clouds OOF AUROC 0.7928 — strongest probe at
  L19. cv5 0.7920 ± 0.027. Stable across K ∈ [10, 50] (range
  0.7925–0.7928).

**Strongest counterargument:** EXP-57 strengthens F-2 *correlationally*
(adds a +0.7 pp spectral lift over the directional ceiling) but does
not address causality. The cov-spectrum probe is supervised — beating
the directional ceiling supervised-vs-supervised says "there's
information you can't capture with linear directions on raw
activations," not "the model uses this spectral information." The
load-bearing causal test is rank-truncating the PC1-residualized
covariance per-problem (proposed FE881 follow-up (c)) and observing
correctness drop. Without that, F-2's spectral piece is observational-
only.

**Would be overturned by:** (a) Causal noising/ablation at L19 PC1
preserving accuracy (FE283/FE214/FE269); (b) cov-spectrum probe
AUROC dropping below the 2-feat 0.7856 on a held-out 5×5 nested
CV — would say the +0.7 pp is fold-noise; (c) cov-spectrum AUROC
dropping below DoM on a non-MATH-500 benchmark via cached
activations; (d) cross-checkpoint PC1 rotation > cos 0.5 across HF
Qwen 1.5B checkpoints (FE700).

### F-10: Topology summary statistics on residual streams are not distinguishable from a Gaussian null

```yaml
strength: STRONG
status: ACTIVE
add_evidence: [P11-E57]
counterargument: "EXP-57 closes a loop opened by EXP-56 (FE880 PH-fails-on-residualized-clouds). FE880 showed real PH < matched-cov Gaussian null on PC1-residualized clouds (gap −0.074). FE881 (EXP-57) shows the SAME residualized covariance, processed through its top-20 log-eigvals, gives OOF AUROC 0.7928 — strongest L19 probe yet. F-10 sharpens to: 'PH summaries integrate over the eigenvalue spectrum and lose the signal that lives in the spectrum's *shape* — Gaussianizing preserves the spectrum, PH does not.' The covariance pathway carries the post-PC1 correctness signal *spectrally*, not topologically."
```

**Claim.** F-10's narrow form (PH = matched-cov Gaussian null) now
has a quantified positive companion: the post-PC1 correctness signal
lives in the eigenvalue spectrum of the per-problem residualized
covariance. **EXP-57 / FE881** shows top-20 log-eigvals on
PC1-residualized clouds give OOF AUROC 0.7928 (strongest L19 probe
at any tier). The same cloud's PH features fail (EXP-56/FE880
gap −0.074). F-10 sharpens: the covariance pathway carries the signal
*spectrally* — PH descriptors integrate over the eigenvalue
distribution and lose the shape-of-decay information that the
spectral probe captures.

**Strength:** STRONG (PH-null gap holds under raw, zigzag-residualized,
and PC1-residualized clouds, with the post-PC1 spectral pathway now
quantified at 0.7928 — three-way triangulation of the topology-vs-
covariance question).

**Evidence:** EXP-026 (raw 5-feature PH gap −0.003); EXP-52 (FE321
zigzag); EXP-54 (FE116 residualized); EXP-55 (PCA-PC1 = DoM); EXP-56
(FE880 PC1-residualized PH gap −0.074); **EXP-57** /
`pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json`
(top-20 log-eigvals on PC1-residualized clouds OOF AUROC 0.7928 —
the post-PC1 correctness signal is purely spectral, not topological).

**Controls passed:**
- Diagonal vs full covariance null (D1 fix in EXP-026 v2).
- Drop H0_n_features rank-tie (D2 fix).
- Zigzag descriptor pair on 28-layer trajectory (FE321).
- Song-Zhong residualization (FE116).
- PCA-PC1 = supervised DoM (EXP-55, FE291).
- PC1-residualization: PH gap −0.074 (EXP-56, FE880).
- **Per-problem cov spectrum (EXP-57, FE881)**: top-20 log-eigvals
  on PC1-residualized clouds OOF AUROC 0.7928 — quantifies the
  post-PC1 signal that PH integrates over.

**Strongest counterargument:** A higher-order PH instrument computed
on the same PC1-residualized clouds (zigzag, FTS-PH, persistence
landscapes) showing real ≥ null + 0.05 would say *some* topology
captures the spectral signal. None attempted yet. A single-feature
H1_max_lifetime probe on PC1-residualized clouds (the only PH
feature where real beats null in EXP-56) reaching AUROC ≥ 0.65
would also suggest residual topology. Both proposed as
EXP-56/EXP-57 follow-ups.

**Would be overturned by:** Higher-order PH (zigzag/FTS-PH/landscapes)
on PC1-residualized clouds with real ≥ null + 0.05 — would falsify
the sharpened "spectrum, not topology" form.

## New claims

(none — pca-pc1resid-cov-spectrum-top20-auroc already in
validate_claims.py from prior session, value 0.7928, threshold
0.005, regen wired)

## Sources

- pathway11_h100/cov_spectrum/recompute_pc1_resid_cov_spectrum.py (this experiment's regen)
- pathway11_h100/cov_spectrum/pc1_resid_cov_spectrum_results.json (output)
- pathway11_h100/cov_spectrum/eigval_cache.npz (sidecar cache, 406 KB, (500,100) eigval matrix + correct/T_i)
- pathway11_h100/prefill_inversion/cache/m15b_prefill.npz (FE291 PC1 source)
- pathway8_layerwise/data/math500/problem_*.npz (per-problem L19 trajectory clouds)
- briefs/result-2026-05-02-P11-FE291.md (FE291 PC1 grounding, DoM = PC1)
- briefs/result-2026-05-02-P11-FE880.md (PH-fails-on-residualized motivates spectral probe)
- handoff/2026-05-02-pca-followups-session2.md (post-EXP-55 plan: per-problem cov spectrum)
- handoff/2026-05-02-pca-followups-session3.md (cov-spectrum landed; opens triangle question)
- handoff/2026-05-02-pca-followups-session4.md (paired-promote narrative for FE880/881/882)
