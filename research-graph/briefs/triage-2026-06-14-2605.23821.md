# Triage brief — 2605.23821 — Hierarchical Concept Geometry in Language Models Emerges from Word Co-occurrence

Source: https://arxiv.org/abs/2605.23821
Triaged: 2026-06-14

Authors: Andres Nava, Matthieu Wyart (Johns Hopkins / EPFL), 2026. Theory + empirics, no released code yet.

## Refutations

This is a *deflationary* paper: it argues that hierarchical/orthogonal concept geometry in representation spaces is **not** evidence of a function-specific mechanism — it is the generic spectral shadow of pairwise co-occurrence statistics ("Such organization may be useful for function, but is not driven by it"). That thesis is a direct deflationary threat to our claim that the L19 prefill DoM/PC1 axis is a *correctness* computation rather than an inherited embedding-spectrum artifact.

1. **Refutes F-2 (single L19 prefill direction predicts correctness, DoM≈PC1, cos(DoM,PC1)=0.9216, AUROC 0.7731).**
   *Mechanism:* The paper proves that the leading eigenvectors of a representation Gram matrix are dictated by the top eigenmodes of the normalized co-occurrence kernel M\* (Eq. 1), with the **first ("scaling") mode varying only with depth/frequency, not content** (Theorem 2.1). If our prefill PC1 (which ≈ DoM at cos 0.9216) is actually the leading scaling mode of the prompt-token co-occurrence kernel — i.e. it tracks problem-token frequency/length anisotropy rather than a correctness axis — then the 0.7731 AUROC is riding on inherited lexical statistics, not a "correctness direction." This dovetails with the already-REFUTED `geometry-portable-signal` premise: it would explain *why* the geometry adds no portable signal over free length+logprob baselines.
   *Falsifiable by:* build the token co-occurrence Gram for the 500 MATH prompts, fit f(d)=αe^(−βd), compute top-k eigenspace alignment g(k)=‖UₖᵀVₖ‖²_F/k (Eq. 10) between the co-occurrence Gram and our cached L19 prefill activation Gram. High alignment of PC1 with the scaling mode ⇒ correctness axis is partly an inherited artifact. Also: whiten the L19 prefill activations (Σ^(−1/2)(v−μ), the paper's Park-style processing) and re-measure DoM AUROC — if it collapses, the signal was global anisotropy.

2. **Refutes F-3 (prefill and final DoM are orthogonal "structurally, not positionally", cos=0.046).**
   *Mechanism:* The paper reproduces Park et al.'s "orthogonal innovation" pattern (cos(ℓ̄_w − ℓ̄_p, ℓ̄_p) ≈ 0) from a *noiseless co-occurrence model with no functional mechanism*, and notes degenerate eigenspaces make individual directions "identifiable only up to rotation." Near-orthogonality of two directions drawn from a whitened high-dim embedding with co-occurrence-induced degeneracy is therefore *expected by construction*, not evidence of two distinct structural computations. Our cos=0.046 may simply be the generic null for whitened embedding directions.
   *Falsifiable by:* compute the distribution of cos between random direction pairs in our whitened L19 activation space (and in the co-occurrence-predicted Gram). If 0.046 sits inside that null band, the "structural orthogonality" interpretation weakens to "generic high-dim near-orthogonality."

3. **Refutes F-7 (D-bucket has a distinctive *collective* (not per-problem) geometric signature).**
   *Mechanism:* The paper's central result is that *collective* hierarchical geometric signatures (block structure of the Gram matrix, coarse-to-fine splitting) emerge purely from how token co-occurrence decays with semantic distance. Harder vs easier MATH problems use systematically different vocabulary (more symbols, rarer tokens), so the D-bucket "collective geometric signature" could be the hierarchical-splitting / co-occurrence-spectrum shadow of difficulty-correlated token statistics, not a correctness-geometry phenomenon.
   *Falsifiable by:* regress the D-bucket Gram block structure against the co-occurrence-kernel-predicted Gram built from each problem's tokens; if the alignment area (Eq. 11) is high, F-7's signature is lexical-statistical, not geometric-of-correctness.

## Direct connections to F-N / H-N

- **F-2** — the paper's whitening + Gram-spectrum machinery is the natural null model for our headline cos(DoM,PC1)=0.9216 and AUROC 0.7731: CAST PC1 ≈ supervised DoM is exactly the "leading eigenvector dictated by co-occurrence" prediction.
- **F-3** — cos(prefill_DoM, final_DoM)=0.046 is the quantity their degenerate-eigenspace / orthogonal-innovation analysis predicts as generic, not special.
- **F-8 / cov-spectrum probe (FE881, AUROC 0.7928; FE882 full-1536-d ceiling 0.7847)** — the cov-spectrum top-20 log-eigvals probe is *literally* a spectral readout of the activation Gram; the paper says that spectrum is structured by co-occurrence. Connection: how much of the 0.7928 is co-occurrence-spectrum-explained vs correctness-specific?
- **F-10 (topology summary stats indistinguishable from a Gaussian null)** — same deflationary spirit: geometry that looks meaningful is null once you model the generating statistics. This paper is a co-occurrence-based cousin of F-10's "it's just the null" verdict.

## Methodologies extracted

- **Normalized co-occurrence matrix M\* + eigendecomposition (Eq. 1)** — PMI-like matrix Mᵢⱼ\* = (Pᵢⱼ−PᵢPⱼ)/√… ≈ log(Pᵢⱼ/PᵢPⱼ); embeddings = top-d eigenvectors scaled by √eigenvalue (Eq. 2).
  *Replication cost*: 20min CPU (co-occurrence from our own 500 prompts).
  *We'd plausibly run this*: yes — gives a concrete null model for our PC1/DoM axis.
- **Top-k eigenspace (Grassmann) alignment g(k)=‖UₖᵀVₖ‖²_F / k (Eq. 10) + alignment-area statistic (Eq. 11)** — degeneracy-robust subspace comparison between two Gram matrices, with a shuffled-label baseline.
  *Replication cost*: 5min CPU on cached Gram matrices.
  *We'd plausibly run this*: yes — drop-in robust replacement for our cos(DoM, X) single-vector comparisons (F-3) and a clean way to compare prefill vs final eigenspaces.
- **Whitening of representations before spectral analysis (ṽ = Σ^(−1/2)(v−μ), Park et al.)** — centers+whitens to remove global anisotropy prior to Gram/PC analysis.
  *Replication cost*: 10min CPU.
  *We'd plausibly run this*: yes — direct test of whether DoM AUROC is anisotropy-driven.
- **Haar-wavelet multi-resolution basis on a tree (scaling modes φ_ℓ, wavelet modes ψ_{u,r})** — analytic basis that block-diagonalizes a hierarchy-symmetric Gram matrix; explains coarse-to-fine eigenvalue ordering (Cauchy interlacing across subtree heights).
  *Replication cost*: theory; only applicable if we impose a tree on MATH problems (e.g. topic taxonomy).
  *We'd plausibly run this*: no — our problem set has no clean WordNet-style hierarchy.
- **Exponential decay-kernel fit f(d)=α·e^(−βd) (Eq. 9; organism fit α=1.967, β=1.235)** — parametric co-occurrence kernel as a function of semantic-graph distance.
  *Replication cost*: 15min CPU.
  *We'd plausibly run this*: yes (as the generative null for the Gram matrices above).
- **Concept-vector orthogonality diagnostic cos(ℓ̄_w − ℓ̄_p, ℓ̄_p)** — Park et al.'s parent/child-innovation orthogonality test, estimated on 70% of descendant tokens.
  *Replication cost*: 30min CPU (needs a concept/parent labeling we don't have for MATH).
  *We'd plausibly run this*: no — no parent/child concept structure in our correctness setting.

## Approaches & framings

- **"Geometry emerges from pairwise statistics, not from function"** — reframes any geometric correctness/concept signal as a candidate co-occurrence/embedding-spectrum artifact until a null model excludes it. Directly intersects F-2/F-3/F-7 and reinforces the REFUTED `geometry-portable-signal` premise — gives it a *mechanistic* (not just empirical) explanation.
- **Coarse-to-fine spectral organization** — the rank order of eigenvectors itself carries meaning (scaling mode first, then progressively finer splits). Suggests we should look at *which* PC index our correctness signal lives in (we already know PC1 + PC9, FE291 2-feat AUROC 0.7856) and ask whether that index structure is co-occurrence-predicted.
- **Subspace-level rather than vector-level comparison** — because degeneracy makes single eigenvectors rotation-arbitrary, robust claims must be about eigenspaces. A methodological caution for all our single-direction (DoM) claims.

## Datasets & benchmarks

- **WordNet noun hypernym graph** — standard lexical database, fully accessible (`nltk.corpus.wordnet`). Applicable? no — our pipeline has no hypernym/taxonomy structure to map onto MATH problems.
- **word2vec embeddings (constructed from M\*, d=2048)** — derivable from any corpus. Applicable? indirectly — only as the null-model construction, not as a benchmark.
- **Gemma 2B unembeddings** — open weights (HF, gated-by-license but accessible). Applicable? no for MATH correctness, but yes as a *second model* if we ever test cross-architecture co-occurrence-vs-correctness.

## Implementation details worth capturing

- Vocabulary filtered to (i) WordNet noun lemma, (ii) matching Gemma token, (iii) no polysemy — to get unambiguous word–sense.
- WordNet DAG → arborescence by max-depth parent selection, then contract paths through ineligible nodes (Appendix C.6).
- Perfect binary subtrees of depth L=3 sampled uniformly via DP counts (L=2 in appendix); ≤5,000 trees per root.
- d=2048 chosen to match Gemma's representation dim for apples-to-apples spectral comparison.
- Whitening uses **full-vocabulary** mean μ and covariance Σ (not per-subtree).
- Shuffled-label baseline = globally permute (word, vector) pairs — the controlling null for every alignment claim.
- Negative eigenvalues of indefinite M\* truncated to zero; decay law preserved on the positive spectrum (Appendix D).
- Code "will be released" — not yet available.

## Replicable intermediates

- **Whitening test of DoM AUROC**: load cached L19 prefill NPZ (500×1536, `pathway11_h100/prefill_gated_compute/`), compute Σ^(−1/2)(v−μ) over the 500 prompts, recompute DoM and OOF 5-fold AUROC. Cross-checks F-2's 0.7731 against the anisotropy-artifact hypothesis. (~10min CPU.)
- **Eigenspace alignment prefill vs final**: compute g(k) (Eq. 10) between cached prefill and final L19 activation Gram matrices as a degeneracy-robust version of cos=0.046 (F-3). Artifacts: same NPZ + `scratch/pathway10_temporal_and_verifier_results.json` directions. (~5min CPU.)
- **Co-occurrence Gram from the 500 MATH prompts**: build token co-occurrence on our own prompt corpus, fit f(d)=αe^(−βd) over token-pair distances, and align its top-k eigenspace against our activation Gram. (~30min CPU.)

## Cross-paper signals

- 2311.03658 — already in graph (status: graphed). Park & Veitch "linear representation hypothesis / geometry of LLMs" — this paper's whitening procedure and orthogonality framing come directly from it.
- 2602.15029 — already in graph (status: graphed). Karkada, Nava, Wyart "Symmetry in language statistics shapes the geometry of model representations" — same author group, the continuous-attribute companion to this hierarchical-attribute paper.
- 2406.01506 — NOT in graph; recommend admission. Park, Choe, Jiang, Veitch "Geometry of categorical and hierarchical concepts in LLMs" (ICLR 2025) — the functional account this paper is explicitly deflating; high relevance to our DoM-as-concept-direction framing.
- 2604.07886 — NOT in graph; recommend admission. Sakata et al. "Linear representations of hierarchical concepts in LMs" (2026) — linear-probe recoverability of hypernym structure; relevant to whether our DoM probe is reading lexical hierarchy.
- 2405.07987 — already in graph (status: TO TEST). Platonic Representation Hypothesis — same "convergent geometry from statistics" lineage; this paper supplies a concrete co-occurrence mechanism.

## Proposed FutureExperiments

### Tier 1 — Cheap local

```yaml
- id: P11-FE23821
  pathway: P11
  description: "Whiten cached L19 prefill activations (Σ^-1/2 (v-μ) over the 500 MATH prompts, Park/Nava-Wyart processing) and recompute the DoM direction and OOF 5-fold AUROC; compare against the unwhitened 0.7731 to test whether the correctness axis is global-anisotropy/co-occurrence artifact vs content-specific."
  rationale: "Nava & Wyart 2605.23821 argue leading representation eigenmodes are dictated by co-occurrence statistics and global anisotropy, not function. If whitening collapses DoM AUROC toward chance, F-2's 'correctness direction' is largely an inherited spectral artifact; if it survives, the signal is content-specific and the deflation fails."
  trigger: "Paper claims hierarchical/orthogonal concept geometry emerges from co-occurrence spectrum with no functional mechanism."
  status: READY
  priority: HIGH
  roi: 8
  cost: "20min CPU"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.23821"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

```yaml
- id: P11-FE23822
  pathway: P11
  description: "Replace the single-vector cos(prefill_DoM, final_DoM)=0.046 (F-3) with the degeneracy-robust top-k eigenspace alignment g(k)=||Uk^T Vk||_F^2 / k (Eq. 10) between cached prefill and final L19 activation Gram matrices, with a shuffled-label null. Determine whether 0.046 is inside the generic whitened-high-dim near-orthogonality null."
  rationale: "The paper shows near-orthogonality of representation directions is expected from degenerate co-occurrence eigenspaces, not a sign of distinct computations. A robust subspace test plus a random-pair null distribution checks whether F-3's 'structural orthogonality' is special or generic."
  trigger: "Paper reproduces Park et al. orthogonal-innovation pattern from a noiseless co-occurrence model and warns single eigenvectors are rotation-arbitrary under degeneracy."
  status: READY
  priority: HIGH
  roi: 7
  cost: "15min CPU"
  depends-on: [F-3]
  would-update: [F-3]
  triggered-by: ["2605.23821"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

```yaml
- id: P11-FE23823
  pathway: P11
  description: "Build a token co-occurrence Gram matrix from the 500 MATH-500 prompts, fit the exponential kernel f(d)=alpha*e^(-beta*d) (Eq. 9), and compute the top-k eigenspace alignment / alignment-area (Eqs. 10-11) between this co-occurrence Gram and the cached L19 prefill activation Gram. Quantify how much of PC1 (cos(DoM,PC1)=0.9216) is co-occurrence-spectrum-explained."
  rationale: "Direct test of the paper's core mechanism on our data: if the leading prefill eigenspace aligns with the co-occurrence-kernel eigenspace far above the shuffled baseline, then the DoM/PC1 correctness axis is substantially an inherited lexical-statistics artifact, sharpening the already-REFUTED geometry-portable-signal verdict with a mechanism."
  trigger: "Paper proves leading Gram eigenvectors are set by the co-occurrence decay kernel; provides g(k) and alignment-area as the comparison statistics."
  status: READY
  priority: HIGH
  roi: 8
  cost: "40min CPU"
  depends-on: [F-2, F-7]
  would-update: [F-2, F-7]
  triggered-by: ["2605.23821"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

```yaml
- id: P11-FE23824
  pathway: P11
  description: "Test whether D-bucket difficulty correlates with co-occurrence-kernel-predicted Gram block structure: build per-problem token co-occurrence Grams, fit the decay kernel, and regress the D-bucket collective geometric signature (F-7) against alignment area (Eq. 11). Checks whether F-7 is correctness-geometry or difficulty-correlated lexical statistics."
  rationale: "Harder MATH problems use rarer/more-symbolic vocabulary; the paper says collective hierarchical Gram structure emerges from such token-distance co-occurrence. If alignment area tracks D-bucket, F-7's 'distinctive collective geometric signature' reduces to a lexical-statistics shadow."
  trigger: "Paper attributes collective Gram block structure entirely to co-occurrence decay with semantic distance."
  status: READY
  priority: MEDIUM
  roi: 6
  cost: "45min CPU"
  depends-on: [F-7]
  would-update: [F-7]
  triggered-by: ["2605.23821"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

### Tier 2 — H100 required

```yaml
- id: P11-FE23825
  pathway: P11
  description: "Cross-model replication: extract L19-equivalent prefill activations for a second architecture (e.g. Gemma 2B) on MATH-500, whiten per the paper, and test whether a co-occurrence-kernel Gram null explains the leading correctness eigenspace there as it does (or doesn't) in Qwen-2.5-1.5B. Separates architecture-general co-occurrence artifact from Qwen-specific correctness geometry."
  rationale: "The paper shows the co-occurrence signature transfers from word2vec to Gemma unembeddings. If the same co-occurrence null explains the prefill correctness eigenspace across two architectures, the DoM correctness axis is an architecture-general lexical artifact, strongly generalizing the F-2 deflation; if Qwen's axis resists the null where Gemma's doesn't, the signal is model-specific and functional."
  trigger: "Paper demonstrates the co-occurrence spectral signature extends from word2vec to Gemma 2B unembeddings."
  status: READY
  priority: MEDIUM
  roi: 7
  cost: "1 H100 day"
  depends-on: [F-2]
  would-update: [F-2]
  triggered-by: ["2605.23821"]
  blocked-by: null
  blocked-by-experiment: []
  relies-on: []
```

## Proposed HYPOTHESES.md additions

### H-23821: The L19 prefill DoM/PC1 correctness axis is substantially an inherited co-occurrence-spectrum artifact
**Priority:** HIGH
**Motivated by:** 2605.23821 + F-2 (cos(DoM,PC1)=0.9216, AUROC 0.7731) + REFUTED premise geometry-portable-signal
**Test:** Whiten cached L19 prefill activations (Σ^-1/2(v-μ)) and recompute DoM AUROC (P11-FE23821); build a co-occurrence-kernel Gram from the 500 prompts and measure top-k eigenspace alignment with the activation Gram (P11-FE23823). ~1hr CPU total.
**Requires:** CPU, cached `pathway11_h100/prefill_gated_compute/` NPZ; nltk/gensim for co-occurrence; no new forward passes.
**Would change:** On confirm (whitening collapses AUROC and/or high co-occurrence alignment), F-2 is re-framed as a lexical-statistics artifact and the geometry-portable-signal refutation gains a mechanism. On reject (AUROC survives whitening, low alignment), DoM is content-specific and this paper's deflation does not reach our correctness setting.
**Blocks:** nothing

### H-23822: Prefill/final DoM orthogonality (cos=0.046) is generic whitened-high-dim near-orthogonality, not a distinct structural computation
**Priority:** MEDIUM
**Motivated by:** 2605.23821 + F-3 (cos=0.046, "structural not positional")
**Test:** Compute the null distribution of cos between random direction pairs in the whitened L19 activation space and the degeneracy-robust eigenspace alignment g(k) between prefill and final Grams (P11-FE23822). ~15min CPU.
**Requires:** CPU, cached prefill+final L19 activations and direction vectors.
**Would change:** On confirm (0.046 inside the null), F-3's "structural orthogonality" weakens to a generic geometric fact. On reject (0.046 far below null), the orthogonality is special and survives the co-occurrence deflation.
**Blocks:** nothing

## Proposed PAPER_INDEX.md classification

## 2605.23821 — Hierarchical Concept Geometry in Language Models Emerges from Word Co-occurrence (Nava & Wyart, 2026)

**Relevance:** A distributional theory proving that hierarchical/orthogonal concept geometry in word2vec and Gemma 2B unembeddings is the spectral shadow of co-occurrence statistics (M\* eigendecomposition + Haar-wavelet block structure), not a hierarchy-specific functional mechanism. It supplies a concrete, testable *null model* for our claim that the L19 prefill DoM/PC1 axis is a correctness computation: the same machinery (whitening, Gram-spectrum, top-k eigenspace alignment) can be turned on our cached activations to ask whether the 0.7731 AUROC and cos(DoM,PC1)=0.9216 are inherited lexical-statistics artifacts. Deflationary in the same spirit as our F-10 and the REFUTED geometry-portable-signal premise.

**Key claim we tested:** Leading eigenvectors of a representation Gram matrix are dictated by the co-occurrence decay kernel, so "meaningful" geometric directions can arise without any functional mechanism — our terms: PC1/DoM correctness axis may be a co-occurrence-spectrum artifact, not a correctness direction.

**Our result:** **TO TEST** Three Tier-1 CPU experiments (P11-FE23821 whitening test of DoM AUROC; P11-FE23822 eigenspace-alignment null for prefill/final orthogonality; P11-FE23823 co-occurrence-Gram alignment vs PC1) can adjudicate within ~1hr on cached NPZs. No result yet.

**Related experiments:** P11-FE23821, P11-FE23822, P11-FE23823, P11-FE23824, P11-FE23825; H-23821, H-23822; FE291 (PC1≈DoM), FE881 (cov-spectrum 0.7928).

**Status:** TO TEST

## New claims

None. The brief introduces no quantitative claims of its own; it quotes existing headline numbers (0.7731, 0.046, 0.9216, 0.7928, 0.7847) and the paper's own figures (α=1.967, β=1.235 organism fit) which would only enter validate_claims.py as `kind="external"` if cited in a narrative doc after an experiment runs.

## Sources consulted

- /home/musicofhel/.claude/.../mcp-arxiv-read_paper-1781458311359.txt — full converted text of 2605.23821 (theory §3, empirics §4–5, references).
- ~/topo-confidence/CLAUDE.md (Headline numbers section only) — numerical anchors.
- ~/topo-confidence/FINDINGS.md + HYPOTHESES.md (grep of F-/H- one-liners only) — F-N/H-N IDs.
- ~/topo-confidence/PAPER_INDEX.md (grep of headers/status only) — existing status flags.
- research-graph `query.py paper` for 2605.23821, 2406.01506, 2311.03658, 2604.07886, 2602.15029, 2505.18235, 2601.18617 — prior admission note + cross-paper graph membership.
- research-graph `premises.py list` — premise vocabulary (geometry-portable-signal REFUTED).
