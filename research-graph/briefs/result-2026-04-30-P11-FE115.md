# Result brief — P11-FE115 (smoke-test only — DO NOT PROMOTE)

This is a synthetic brief used to exercise `promote_result.py --dry-run`. It
references a real result JSON (prefill_gated_compute/results.json) but the
"result" described below is fabricated for parser-validation purposes only.

## FE

```yaml
fe_id: P11-FE115
status: COMPLETED
outcome: "Song-Zhong pos/ctx decomp on L19: cos(prefill_DoM_resid, final_DoM_resid) = 0.31 (jumped from 0.046 raw); prefill-resid AUROC 0.62 vs final-resid AUROC 0.71. F-3 'orthogonality' framing was largely a positional artifact."
result_json: pathway11_h100/prefill_gated_compute/results.json
result_json_keys:
  - prefill_DoM_auroc_oof
  - final_token_DoM_auroc_oof
  - headline
regen_cmd: ""
```

## Result JSON verification

JSON path exists; declared keys are top-level. (No regen command for this
synthetic test — the values cited above are illustrative, not recomputed.)

## EXPERIMENT_LOG entry

## EXP-43: P11 cheap-wins FE115 — Song-Zhong pos/ctx decomp on L19
**Date:** 2026-04-30 (smoke-test entry — DO NOT MERGE)
**Status:** COMPLETE
**Motivated by:** PLAN_cheap_wins.md Phase 2 — direct refutation test for F-3.
Song-Zhong (2310.04861) decomposes hidden states into μ + pos_t + ctx_c + resid;
if our prefill/final orthogonality is positional, subtracting pos_t should
collapse it.
**Hypothesis:** cos(prefill_DoM_resid, final_DoM_resid) jumps from 0.046 to
>0.30 after removing the positional component.
**What we actually tested:** Computed mean per-position activation across all
1024-tok MATH-500 trajectories on L19; subtracted pos_t from each token's
hidden state; refit DoM probes on residuals.
**Key result:** cos(prefill_DoM_resid, final_DoM_resid) = 0.31 (vs 0.046
raw); prefill AUROC drops 0.7731 → 0.62 on resid; final-token AUROC drops
0.7186 → 0.71 (less affected). The orthogonality finding (F-3) is largely
positional.
**Verdict:** REJECTED — F-3 weakened.
**Changed our understanding of:** "Two circuits" framing was over-claimed;
much of the prefill/final geometric distinction is positional encoding, not
distinct computation.
**Files:** `pathway11_h100/prefill_gated_compute/results.json` (anchor only —
this is a synthetic smoke-test).
**Depends on:** EXP-037.
**Enables:** Re-evaluation of F-2 vs final-token results in light of position.

## STATE.md last-experiment update

EXP-43 (smoke-test for promote_result.py — Song-Zhong decomp on L19, REJECTED
at α=0.30 threshold, F-3 demoted to WEAKENED). First end-to-end run of the
new internal-experiment promotion pipeline.

## FINDINGS.md updates

### F-3: Prefill and final-token DoM directions are partly orthogonal *but the gap is largely positional*

```yaml
strength: PRELIMINARY
status: WEAKENED
add_evidence: [P11-E43]
counterargument: "Song-Zhong decomposition on L19 (EXP-43) shows cos(prefill_DoM_resid, final_DoM_resid) = 0.31 — the raw 0.046 was a positional artifact. The 'two circuits' framing is no longer supported."
overturned_by: "Already weakened. Would be fully overturned by a setting where post-resid cos > 0.7."
```

**Claim.** On Qwen-2.5-1.5B, the cosine between prefill DoM and final-token
DoM is 0.046 *on raw activations* but rises to 0.31 once the
position-component is removed (Song-Zhong decomposition). The orthogonality
finding is a positional artifact, not a structural-circuit claim.

**Strength:** PRELIMINARY (1 model, single decomposition method).

**Evidence:** EXP-037 (raw cos = 0.046), EXP-43 (post-resid cos = 0.31).

**Controls passed:**
- Position component computed across all 1024-tok trajectories (not a small-n
  artifact).
- Residual probe AUROC drop is consistent across folds.

**Strongest counterargument:** Song-Zhong's mean-pos subtraction is one of
several decomposition choices; alternative methods (e.g. PCA-based) may give
different residual cosines.

## New claims

(none — this smoke-test does not introduce new quantitative claims.)

## Sources

- PLAN_cheap_wins.md §"Phase 2"
- 2310.04861 (Song-Zhong, "Uncovering hidden geometry in Transformers via
  disentangling position and context")
- pathway11_h100/prefill_gated_compute/results.json (anchor)
